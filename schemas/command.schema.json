{
  "$defs": {
    "CommandName": {
      "description": "Phase 1 command names supported by the Rust contract.",
      "enum": [
        "ping",
        "version",
        "probeMediaRuntime"
      ],
      "type": "string"
    },
    "CommandPayload": {
      "description": "Phase 1 command payloads.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Payload accepted by the Phase 1 `ping` command.",
          "properties": {
            "kind": {
              "const": "ping",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Payload accepted by the Phase 1 `version` command.",
          "properties": {
            "kind": {
              "const": "version",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Payload accepted by the Phase 1 non-editing runtime probe command.",
          "properties": {
            "kind": {
              "const": "probeMediaRuntime",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Rust-owned command envelope accepted by the Electron binding boundary.",
  "oneOf": [
    {
      "properties": {
        "command": {
          "const": "ping"
        },
        "payload": {
          "properties": {
            "kind": {
              "const": "ping"
            }
          },
          "required": [
            "kind"
          ]
        }
      }
    },
    {
      "properties": {
        "command": {
          "const": "version"
        },
        "payload": {
          "properties": {
            "kind": {
              "const": "version"
            }
          },
          "required": [
            "kind"
          ]
        }
      }
    },
    {
      "properties": {
        "command": {
          "const": "probeMediaRuntime"
        },
        "payload": {
          "properties": {
            "kind": {
              "const": "probeMediaRuntime"
            }
          },
          "required": [
            "kind"
          ]
        }
      }
    }
  ],
  "properties": {
    "command": {
      "$ref": "#/$defs/CommandName"
    },
    "payload": {
      "$ref": "#/$defs/CommandPayload"
    },
    "requestId": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "command",
    "payload"
  ],
  "title": "CommandEnvelope",
  "type": "object"
}
