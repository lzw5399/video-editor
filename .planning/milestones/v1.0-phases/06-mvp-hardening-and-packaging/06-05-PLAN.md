---
phase: "06-mvp-hardening-and-packaging"
plan: "05"
type: execute
wave: 5
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
autonomous: true
requirements:
  - TEST-06
  - TEST-07
files_modified:
  - docs/release-ffmpeg-manifest.md
  - docs/third-party-notices.md
  - docs/mvp-known-limits.md
  - scripts/phase6-release-guards.sh
  - package.json
  - justfile
summary: "Document known limits, license posture, and post-MVP backlog"
must_haves:
  truths:
    - "D-14: MVP known limits, external FFmpeg dependency, packaging/signing limitations, unsupported advanced semantics, and post-MVP backlog are documented."
    - "D-15: Public root gates include suitable Phase 06 checks and expose slower packaging gates explicitly."
    - "D-07/D-10: Release docs do not represent local Homebrew or PATH FFmpeg as a redistributable bundled build."
  artifacts:
    - path: "docs/release-ffmpeg-manifest.md"
      provides: "external FFmpeg runtime/license posture"
    - path: "docs/third-party-notices.md"
      provides: "third-party notices and FFmpeg external-runtime note"
    - path: "docs/mvp-known-limits.md"
      provides: "known limits and post-MVP backlog"
    - path: "scripts/phase6-release-guards.sh"
      provides: "release/source/root gate checks"
  key_links:
    - from: "package.json"
      to: "scripts/phase6-release-guards.sh"
      via: "test:phase6-release-gates"
    - from: "justfile"
      to: "pnpm run test:phase6-runtime"
      via: "root just test gate"
---

<objective>
Close Phase 06 release readiness with documentation, release guards, and public root gates.

Purpose: make the MVP packaging/runtime posture explicit, prevent accidental bundled-FFmpeg claims, and expose the correct Phase 06 verification commands to developers and CI.
Output: release FFmpeg manifest, third-party notices, MVP known-limits/backlog document, Phase 06 release guard script, and root pnpm/just gate wiring.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-mvp-hardening-and-packaging/06-CONTEXT.md
@.planning/phases/06-mvp-hardening-and-packaging/06-RESEARCH.md
@.planning/phases/06-mvp-hardening-and-packaging/06-VALIDATION.md
@docs/runtime-boundaries.md
@scripts/phase5-source-guards.sh
@package.json
@justfile
@apps/desktop-electron/package.json
</context>

## Artifacts this plan produces

New file paths:
- `docs/release-ffmpeg-manifest.md`
- `docs/third-party-notices.md`
- `docs/mvp-known-limits.md`
- `scripts/phase6-release-guards.sh`

Named scripts/commands:
- `pnpm run test:phase6-packaging`
- `pnpm run test:phase6-runtime`
- `pnpm run test:phase6-release-gates`
- `pnpm run test:phase6`
- `just test-phase6-packaging`
- `just test`

Named source assertions:
- docs contain `FFmpeg is external/user-provided for the MVP`
- docs contain `VE_FFMPEG_PATH` and `VE_FFPROBE_PATH`
- docs contain `No FFmpeg binary is bundled by Phase 6`
- docs contain `Homebrew --enable-gpl is development/test only`
- package config does not contain a bundled FFmpeg `extraResources` entry

<tasks>

<task id="06-05-01" type="auto">
  <name>1. Write release docs for external FFmpeg posture, notices, known limits, and backlog</name>
  <files>docs/release-ffmpeg-manifest.md, docs/third-party-notices.md, docs/mvp-known-limits.md</files>
  <read_first>
    - `.planning/phases/06-mvp-hardening-and-packaging/06-CONTEXT.md`
    - `.planning/phases/06-mvp-hardening-and-packaging/06-RESEARCH.md`
    - `docs/runtime-boundaries.md`
    - `README.md`
    - `README.zh-CN.md`
  </read_first>
  <action>
    Create `docs/release-ffmpeg-manifest.md` stating exactly that `FFmpeg is external/user-provided for the MVP`, runtime discovery uses `VE_FFMPEG_PATH`, `VE_FFPROBE_PATH`, or `PATH`, `No FFmpeg binary is bundled by Phase 6`, and local `Homebrew --enable-gpl is development/test only` per D-06, D-07, and D-10. Include a concise checklist for any later bundled FFmpeg work: actual binary source, configure line, LGPL/GPL/nonfree review, source-offer obligations, third-party notices, and packaged resource resolver tests; do not imply those artifacts exist in Phase 06. Create `docs/third-party-notices.md` covering the app license, Electron/React/Rust dependency posture, and the external FFmpeg notice without claiming redistribution. Create `docs/mvp-known-limits.md` per D-14 covering external FFmpeg dependency, unsigned/local package status when no signing config is present, unsupported advanced editor semantics already scheduled in Phases 7-13, and post-MVP compatibility/mobile/server backlog. Keep desktop product copy references Simplified Chinese per D-16.
  </action>
  <verify>
    <automated>test -f docs/release-ffmpeg-manifest.md && test -f docs/third-party-notices.md && test -f docs/mvp-known-limits.md</automated>
    <automated>rg -n "FFmpeg is external/user-provided for the MVP|VE_FFMPEG_PATH|VE_FFPROBE_PATH|No FFmpeg binary is bundled by Phase 6|Homebrew --enable-gpl is development/test only" docs/release-ffmpeg-manifest.md</automated>
    <automated>rg -n "signing|notarization|Phases 7-13|Jianying|CapCut|Kaipai|mobile|server" docs/mvp-known-limits.md</automated>
  </verify>
  <acceptance_criteria>
    All three docs exist. `release-ffmpeg-manifest.md` contains the exact external-runtime strings listed above and does not claim bundled FFmpeg redistribution. `mvp-known-limits.md` names packaging/signing limitations, external FFmpeg, Phases 7-13 advanced semantics, Jianying/CapCut/Kaipai compatibility backlog, and mobile/server backlog. `third-party-notices.md` distinguishes the project MIT license from external user-provided FFmpeg.
  </acceptance_criteria>
  <done>Release docs state the external FFmpeg posture, notices, known MVP limits, and post-MVP backlog with exact guardable strings.</done>
</task>

<task id="06-05-02" type="auto">
  <name>2. Add Phase 06 release guards and public root gates</name>
  <files>scripts/phase6-release-guards.sh, package.json, justfile</files>
  <read_first>
    - `scripts/phase5-source-guards.sh`
    - `package.json`
    - `justfile`
    - `apps/desktop-electron/package.json`
    - `apps/desktop-electron/electron-builder.yml`
  </read_first>
  <action>
    Create `scripts/phase6-release-guards.sh` with `set -euo pipefail`. The guard must verify the release docs contain the exact external-runtime strings from Task 1; verify `apps/desktop-electron/package.json` exposes `package:dir`, `test:packaged-smoke`, `test:runtime-diagnostics`, `test:real-workflow`, `test:packaged-real-workflow`, and `test:packaged`; verify root `package.json` exposes `test:phase6-packaging`, `test:phase6-runtime`, `test:phase6-release-gates`, and `test:phase6`; call `scripts/phase5-source-guards.sh`; verify generated contracts have no drift; and reject any Phase 06 package config that contains a bundled FFmpeg/ffprobe resource entry. Use `rg` pipelines that filter comments when counting forbidden tokens; do not use bare unfiltered `grep -c` gates. Add root scripts per D-15: `test:phase6-packaging` runs the slower packaged gate explicitly, `test:phase6-runtime` runs runtime capability plus diagnostics plus no-mock workflow gates, `test:phase6-release-gates` runs the guard script, and `test:phase6` runs the suitable non-packaging Phase 06 gates. Chain `test:phase6` into root `pnpm run test` after Phase 5 gates and before contract drift. Update `justfile` so `just test` runs `pnpm run test:phase6`, and add `just test-phase6-packaging` as the explicit slower packaged command. Do not chain packaged installer/signing/notarization work into root gates.
  </action>
  <verify>
    <automated>pnpm run test:phase6-release-gates</automated>
    <automated>pnpm run test:phase6-runtime</automated>
    <automated>pnpm run test</automated>
    <automated>/Users/zhiwen/.cargo/bin/just test</automated>
  </verify>
  <acceptance_criteria>
    `pnpm run test:phase6-release-gates` passes and fails if docs imply bundled FFmpeg or package config adds FFmpeg resources. `pnpm run test` includes `test:phase6` but does not have to include the slower `test:phase6-packaging`. `/Users/zhiwen/.cargo/bin/just test` includes `pnpm run test:phase6`. `just test-phase6-packaging` exists for explicit packaged release verification. Root gates preserve existing Phase 4/5 guards and contract drift checks.
  </acceptance_criteria>
  <done>Phase 06 release guards pass, root `pnpm run test` and `just test` include suitable Phase 06 checks, and slower packaged verification is exposed explicitly.</done>
</task>

</tasks>

<verification>
Run `pnpm run test:phase6-release-gates`, `pnpm run test:phase6-runtime`, `pnpm run test`, and `/Users/zhiwen/.cargo/bin/just test`. Run `pnpm run test:phase6-packaging` before release verification or `$gsd-verify-work`; it remains explicit because packaged app smoke is slower than ordinary root gates.
</verification>

<success_criteria>
- TEST-06 has public dev/no-mock runtime gates.
- TEST-07 has public packaged smoke/workflow gates.
- D-07, D-10, D-14, D-15, and D-16 are documented and enforced by source/release guards.
- Release docs clearly state external FFmpeg posture, known MVP limits, and post-MVP backlog without implementing deferred scope.
</success_criteria>

<must_haves>
Truths:
- Developer can identify the exact Phase 06 commands for runtime, packaging, and release documentation gates.
- Release docs state FFmpeg is external/user-provided and not bundled in Phase 06.
- Known MVP limits and post-MVP backlog are documented without changing editor semantics.

Artifacts:
- `docs/release-ffmpeg-manifest.md` exists.
- `docs/third-party-notices.md` exists.
- `docs/mvp-known-limits.md` exists.
- `scripts/phase6-release-guards.sh` exists and is executable.

Key links:
- Root `package.json` `test` includes `test:phase6`.
- Root `justfile` `test` includes `pnpm run test:phase6`.
- `scripts/phase6-release-guards.sh` calls `scripts/phase5-source-guards.sh`.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release docs -> developer/release operator | Documentation affects packaging and distribution decisions. |
| package config -> distributed artifact | Build config controls whether binaries/resources are shipped. |
| root gates -> CI/local verification | Public commands decide what is considered phase-complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Repudiation | release docs | mitigate | Guard exact external-runtime strings and known-limit coverage so release posture is auditable. |
| T-06-14 | Tampering | `electron-builder.yml` resources | mitigate | Guard rejects FFmpeg/ffprobe resource entries unless a later plan adds manifest/notices/resource resolver tests together. |
| T-06-15 | Information Disclosure | docs/log examples | mitigate | Docs describe posture and env vars without publishing local absolute paths or raw configure dumps as required user copy. |
| T-06-16 | Denial of Service | root gates | mitigate | Chain suitable Phase 06 runtime/release checks into root gates and expose slower packaged gate explicitly to avoid making ordinary tests impractically long. |
| T-06-SC | Tampering | package/source guards | mitigate | `scripts/phase6-release-guards.sh` calls Phase 5 source guards and generated contract drift checks. |
</threat_model>

## Source Coverage Audit

| Source Item | Coverage |
|-------------|----------|
| GOAL: Verify full workflow in dev and packaged builds | Plan 06-04 verifies behavior; this plan publishes gates and docs for release readiness. |
| REQ TEST-06 | Covered by root `test:phase6-runtime` and docs/gates traceability. |
| REQ TEST-07 | Covered by explicit `test:phase6-packaging` and `just test-phase6-packaging`. |
| RESEARCH: release docs, FFmpeg legal posture, source guards, root gates | Covered by Tasks 1 and 2. |
| CONTEXT D-07, D-10, D-14, D-15, D-16 | Implemented here. |
| CONTEXT D-01 through D-06 and D-08 through D-13 | Implemented by Plans 06-01 through 06-04; this plan's guards preserve their outcomes. |
| Deferred ideas | Signing/notarization, bundled FFmpeg, adapters, mobile/server, and advanced semantics are documented as limits/backlog only, not implemented. |

<output>
Create `.planning/phases/06-mvp-hardening-and-packaging/06-05-SUMMARY.md` when done.
</output>
