---
phase: 17.1-interaction-session-and-template-import-main-chain-hardening
plan: "04"
type: execute
wave: 4
depends_on:
  - 17.1-02
  - 17.1-03
files_modified:
  - apps/desktop-electron/src/renderer/App.tsx
  - apps/desktop-electron/src/renderer/workspace/Timeline.tsx
  - apps/desktop-electron/src/renderer/workspace/Inspector.tsx
  - apps/desktop-electron/tests/interaction-timeline-keyframe.spec.ts
  - crates/bindings_node/src/project_session_service.rs
  - crates/bindings_node/tests/project_interaction_session.rs
autonomous: true
requirements:
  - UI-11
  - P0-UI-05
  - P0-EDIT-01
  - P0-GPU-02
  - SCHED-01
  - SCHED-02
  - ANIM-03
  - PRODFX-02
requirements_addressed:
  - UI-11
  - P0-UI-05
  - P0-EDIT-01
  - P0-GPU-02
  - SCHED-01
  - SCHED-02
  - ANIM-03
  - PRODFX-02
must_haves:
  truths:
    - "Timeline segment move, cross-track move, edge trim, and any visible transition/fade handles stream Rust provisional results and commit once; unsupported transition/fade controls are hidden or product-gated with tests."
    - "Playhead scrub uses coalesced navigation/seek with generation rejection and no undo/save."
    - "Keyframe marker/value editing preserves integer segment-relative time and does not require exact playhead deletion when a keyframe is focused."
    - "Decision coverage: D-18 D-19 D-21 D-29."
---

<objective>
Move timeline, playhead scrub, and keyframe high-frequency interactions onto Rust-owned provisional/navigation paths.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-interaction-session-and-template-import-main-chain-hardening/17.1-CONTEXT.md
@.planning/phases/17.1-interaction-session-and-template-import-main-chain-hardening/17.1-RESEARCH.md
@apps/desktop-electron/src/renderer/workspace/Timeline.tsx
@apps/desktop-electron/src/renderer/App.tsx
@crates/bindings_node/src/project_session_service.rs
@crates/engine_core/src/frame_state.rs
</context>

## Artifacts This Phase Produces

- `beginTimelineSegmentInteraction`
- `beginPlayheadScrubInteraction`
- `beginKeyframeInteraction`
- `ProjectInteractionKind::TimelineMoveTrim`
- `ProjectInteractionKind::PlayheadScrub`
- `ProjectInteractionKind::KeyframeEdit`
- `apps/desktop-electron/tests/interaction-timeline-keyframe.spec.ts`

## Decision Coverage

- D-18: Move timeline move, cross-track move, trim, multi-select, transition/fade handles where exposed, and snapping preview to Rust provisional semantics.
- D-19: Move playhead drag, ruler scrub, timecode scrub, frame repeat, wheel/trackpad scrub, and rapid navigation seeks to a coalesced navigation path.
- D-21: Add keyframe marker/value interaction semantics and nearest/focused keyframe behavior.
- D-29: Require usable trim/keyframe hit targets.
- PRODFX-02: Treat transition/fade handles as high-frequency only when typed transition/fade semantics are production-backed; otherwise gate them.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add product tests for timeline drag/trim/scrub/keyframe</name>
  <files>apps/desktop-electron/tests/interaction-timeline-keyframe.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/tests/product-user-journey.spec.ts`
    - `apps/desktop-electron/tests/helpers/userJourney.ts`
    - `apps/desktop-electron/src/renderer/workspace/Timeline.tsx`
  </read_first>
  <action>Add Playwright/Electron coverage for segment drag move, cross-track move where legal, left/right trim, playhead drag scrub, keyframe marker/value edit, and transition/fade handles if visible. Assert that live preview/selection responds during drag, but revision/save/undo change only at commit for draft mutations and never for scrub navigation. If transition/fade handles are not production-backed, assert they are hidden or product-gated rather than inert.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/interaction-timeline-keyframe.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Test fails if timeline move/trim uses only local ghost until pointer-up.
    - Test fails if scrub creates undo or saves project JSON.
    - Test fails if keyframe time is stored as floating point or absolute draft time instead of segment-relative microseconds.
  </acceptance_criteria>
  <done>Timeline/keyframe/scrub acceptance is product-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire timeline move/trim/cross-track through interaction sessions</name>
  <files>apps/desktop-electron/src/renderer/workspace/Timeline.tsx, apps/desktop-electron/src/renderer/App.tsx, crates/bindings_node/src/project_session_service.rs</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/Timeline.tsx` `TimelineSegmentBlock` drag code.
    - Existing move/trim project-session intents in `apps/desktop-electron/src/renderer/App.tsx`.
    - Rust move/trim validation and snapping/main-track behavior in `draft_commands`.
  </read_first>
  <action>Replace pointer-up-only timeline commit with begin/update/commit/cancel interaction calls. UI may pass pointer delta and target-track hint; Rust must calculate legal start/trim/cross-track result, snapping/magnet preview, affected ranges, and provisional timeline view model. Commit calls the canonical move/trim command once. Any visible transition/fade handle must use a typed Rust interaction session and canonical commit path; otherwise it must be hidden or disabled behind a product unavailable gate with source/E2E coverage.</action>
  <verify>
    <automated>cargo test -p bindings_node project_interaction_session --locked</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/interaction-timeline-keyframe.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Cross-track legality is enforced in Rust during update and commit.
    - Live snap/magnet preview comes from Rust provisional response.
    - One drag creates one undo item.
    - Trim handles have at least 16px effective hit target in CSS/source assertion or screenshot check.
    - Visible transition/fade handles either use interaction sessions or are proven hidden/product-gated.
  </acceptance_criteria>
  <done>Timeline drag/trim semantics are live and Rust-owned.</done>
</task>

<task type="auto">
  <name>Task 3: Add coalesced playhead scrub/navigation path</name>
  <files>apps/desktop-electron/src/renderer/App.tsx, crates/bindings_node/src/project_session_service.rs</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/App.tsx` `handleSeekPlayhead`, realtime preview host seek, and audio seek handling.
    - `crates/task_runtime/src/config.rs` `ScrubSeek` coalescing policy.
    - Realtime preview generation/freshness code.
  </read_first>
  <action>Implement a navigation interaction for playhead scrub/timecode drag/frame repeat that coalesces obsolete targets, carries target microseconds and generation, seeks preview/audio through the scheduler, and rejects stale frames. It must not mutate draft, save, or push undo.</action>
  <verify>
    <automated>cargo test -p bindings_node project_interaction_session --locked</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/interaction-timeline-keyframe.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Rapid scrub produces final accepted target and coalesces obsolete intermediate seeks.
    - Preview/audio seek uses generation/freshness checks.
    - No canonical revision changes during scrub.
  </acceptance_criteria>
  <done>Scrub feels live without blocking or polluting history.</done>
</task>

<task type="auto">
  <name>Task 4: Add keyframe marker/value interaction semantics</name>
  <files>apps/desktop-electron/src/renderer/workspace/Timeline.tsx, apps/desktop-electron/src/renderer/workspace/Inspector.tsx, crates/bindings_node/src/project_session_service.rs</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/Inspector.tsx` `AnimationInspectorTab`.
    - `crates/engine_core/src/frame_state.rs` keyframe evaluation.
    - `crates/draft_model/src/validation.rs` keyframe validation.
  </read_first>
  <action>Add interaction support for dragging keyframe markers and editing keyed property values through inspector sliders. Preserve segment-relative integer microsecond times, validate duplicate property/time collisions, and support focused/nearest keyframe removal so deletion does not require exact playhead equality when the UI has a focused marker.</action>
  <verify>
    <automated>cargo test -p bindings_node project_interaction_session --locked</automated>
    <automated>cargo test -p engine_core keyframe --locked</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/interaction-timeline-keyframe.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Keyframe marker drag rejects duplicate property/time conflicts.
    - Keyframe edit affects engine/render graph sampled state.
    - Focused keyframe deletion works even when playhead is near but not exactly equal, if the UI selected that keyframe marker.
  </acceptance_criteria>
  <done>Keyframe interaction is usable and preserves Rust-owned animation semantics.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-17.1-10 | Tampering | Timeline target hints | mitigate | UI target-track hints are advisory; Rust validates all legality. |
| T-17.1-11 | Denial of Service | Scrub flood | mitigate | Scrub uses coalesced scheduler generation and one in-flight seek cap. |
| T-17.1-12 | Tampering | Keyframe times | mitigate | Rust validates integer segment-relative microseconds and duplicate keys. |
</threat_model>

<verification>
Run:
- `cargo test -p bindings_node project_interaction_session --locked`
- `cargo test -p engine_core keyframe --locked`
- `pnpm --filter @video-editor/desktop exec playwright test tests/interaction-timeline-keyframe.spec.ts --reporter=line`
</verification>

<success_criteria>
Plan 04 is complete when timeline, scrub, and keyframe interactions use Rust-owned live/provisional paths and product tests reject commit-only ghost behavior.
</success_criteria>

<output>
Create `.planning/phases/17.1-interaction-session-and-template-import-main-chain-hardening/17.1-04-SUMMARY.md` when done.
</output>
