---
phase: "15.1-p0-basic-editing-chain-repair"
plan: "03"
type: execute
wave: 2
depends_on:
  - "15.1-01"
files_modified:
  - "assets/fonts"
  - "crates/draft_model/src/timeline.rs"
  - "crates/draft_model/tests/schema_exports.rs"
  - "crates/media_runtime/src/capabilities.rs"
  - "crates/ffmpeg_compiler/src"
  - "crates/realtime_preview_runtime/src"
  - "apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx"
  - "apps/desktop-electron/src/generated"
autonomous: true
requirements:
  - P0-EDIT-03
user_setup: []
---

<objective>
Add a bundled, license-tracked font registry so text preview and export resolve the same stable `fontRef` instead of host fallback fonts.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.1-03-01: Add font registry contract and validation tests</name>
  <files>crates/draft_model/src/timeline.rs, crates/draft_model/tests/schema_exports.rs, assets/fonts</files>
  <action>Add tests for bundled font manifest entries: stable `fontRef`, family, style/weight, relative file path, license metadata, file existence, and CJK glyph coverage. Select an open-source Chinese-capable font and include license material in repo assets.</action>
  <verify>
    <automated>cargo test -p draft_model font_registry -- --nocapture</automated>
  </verify>
  <done>Font registry tests fail until bundled assets and metadata exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-03-02: Resolve text preview/export fonts through registry</name>
  <files>crates/media_runtime/src/capabilities.rs, crates/ffmpeg_compiler/src, crates/realtime_preview_runtime/src</files>
  <action>Replace host-only text font readiness for baseline text with registry resolution. FFmpeg/ASS export and realtime preview diagnostics must use the same `fontRef` and fail explicitly when the registry entry is missing, unlicensed, unreadable, or lacks needed glyph coverage.</action>
  <verify>
    <automated>cargo test -p media_runtime runtime_capability -- --nocapture</automated>
    <automated>cargo test -p ffmpeg_compiler text -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime text_parity -- --nocapture</automated>
  </verify>
  <done>Text preview/export no longer depend on implicit host fonts for baseline text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-03-03: Default desktop text creation to bundled fontRef</name>
  <files>apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx, apps/desktop-electron/src/generated, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Expose the bundled font as the default text option and ensure added text segments carry the stable `fontRef`. Keep broad font picker polish for Phase 15.2 if needed.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "bundled font"</automated>
  </verify>
  <done>New text segments use the bundled font registry path by default.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p draft_model font_registry -- --nocapture</automated>
<automated>cargo test -p media_runtime runtime_capability -- --nocapture</automated>
<automated>cargo test -p ffmpeg_compiler text -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime text_parity -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop build</automated>
</verification>

<success_criteria>
Baseline text segments use a bundled, license-tracked font in both preview and export, with explicit validation failures for missing or incompatible font assets.
</success_criteria>

<output>
Create `.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-03-SUMMARY.md` when done.
</output>
