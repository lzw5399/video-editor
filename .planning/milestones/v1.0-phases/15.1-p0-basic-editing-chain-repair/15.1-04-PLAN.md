---
phase: "15.1-p0-basic-editing-chain-repair"
plan: "04"
type: execute
wave: 3
depends_on:
  - "15.1-01"
files_modified:
  - "crates/draft_model/src/timeline.rs"
  - "crates/draft_commands/src/canvas.rs"
  - "crates/draft_commands/src/timeline.rs"
  - "crates/draft_commands/tests/canvas_adaptation.rs"
  - "crates/bindings_node/src/lib.rs"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - P0-EDIT-04
user_setup: []
---

<objective>
Make empty drafts adopt the first video/image material canvas profile in Rust-owned semantics and avoid default stretch distortion.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.1-04-01: Add canvas adaptation tests</name>
  <files>crates/draft_commands/tests/canvas_adaptation.rs, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Add RED tests for portrait, square, and landscape first material adoption. Assert a manual canvas edit prevents later auto changes. Assert default inserted visual fit is not stretch for aspect-mismatched material.</action>
  <verify>
    <automated>cargo test -p draft_commands canvas_adaptation -- --nocapture</automated>
  </verify>
  <done>Tests describe the first-material behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-04-02: Implement Rust-owned canvas adoption policy</name>
  <files>crates/draft_model/src/timeline.rs, crates/draft_commands/src/canvas.rs, crates/draft_commands/src/timeline.rs, crates/bindings_node/src/lib.rs</files>
  <action>Add a backward-compatible manual/auto canvas policy and helper that derives canvas width, height, aspect, and frame rate from the first video/image material metadata. Invoke it from the accepted import/add-to-timeline command path, not renderer-only code. Preserve manual canvas changes.</action>
  <verify>
    <automated>cargo test -p draft_commands canvas_adaptation -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
  </verify>
  <done>Canvas adaptation is deterministic, undoable where appropriate, and generated to bindings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-04-03: Surface auto canvas behavior in desktop flow</name>
  <files>apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Update the renderer caller path to use the Rust-owned response. The UI may display the resulting canvas readout, but it must not compute the canonical canvas adaptation itself.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "auto canvas"</automated>
  </verify>
  <done>Adding the first vertical material makes the preview canvas vertical without renderer-owned canvas math.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p draft_commands canvas_adaptation -- --nocapture</automated>
<automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "auto canvas"</automated>
</verification>

<success_criteria>
Empty drafts adopt the first visual material profile unless the user manually changed canvas settings, and inserted material defaults avoid stretch distortion.
</success_criteria>

<output>
Create `.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-04-SUMMARY.md` when done.
</output>
