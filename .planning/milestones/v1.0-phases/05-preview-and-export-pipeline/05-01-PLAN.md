---
phase: "05-preview-and-export-pipeline"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "crates/engine_core/Cargo.toml"
  - "crates/engine_core/src/lib.rs"
  - "crates/engine_core/src/normalize.rs"
  - "crates/engine_core/src/frame_state.rs"
  - "crates/engine_core/src/text_layout.rs"
  - "crates/engine_core/tests/normalization.rs"
  - "crates/engine_core/tests/frame_state_snapshots.rs"
autonomous: true
requirements:
  - TEXT-03
  - EXP-02
  - TEST-03
must_haves:
  truths:
    - "Every preview/export caller can normalize a Draft before reading track/segment semantics."
    - "Frame state is resolved at integer microsecond timeline positions and rational frame rates."
    - "Text layout has a pinned deterministic profile that preview and export both consume."
  artifacts:
    - path: "crates/engine_core/src/normalize.rs"
      provides: "NormalizedDraft, NormalizedTrack, NormalizedSegment, and render-ready diagnostics"
    - path: "crates/engine_core/src/frame_state.rs"
      provides: "FrameState and RenderRangeState evaluation over normalized draft semantics"
    - path: "crates/engine_core/src/text_layout.rs"
      provides: "TextLayoutProfile and deterministic resolved text overlays"
    - path: "crates/engine_core/tests/normalization.rs"
      provides: "Normalization, stacking, mute, material status, and timerange tests"
    - path: "crates/engine_core/tests/frame_state_snapshots.rs"
      provides: "Frame-state and text-layout snapshot tests"
  key_links:
    - from: "crates/engine_core/src/lib.rs"
      to: "crates/engine_core/src/normalize.rs"
      via: "public exports"
      pattern: "pub use normalize::"
    - from: "crates/engine_core/src/frame_state.rs"
      to: "crates/draft_model"
      via: "Microseconds and RationalFrameRate"
      pattern: "Microseconds|RationalFrameRate"
    - from: "crates/engine_core/src/text_layout.rs"
      to: "crates/draft_model::TextSegment"
      via: "resolved deterministic text overlays"
      pattern: "TextSegment"
---

<objective>
Implement the Rust-owned semantic engine path for Phase 5.

Purpose: Preview and export must consume one normalized draft and frame-state model before render graph or FFmpeg work begins.
Output: `engine_core` normalization, frame-state evaluation, deterministic text-layout profile, and executable TEST-03/TEXT-03 gates.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-preview-and-export-pipeline/05-CONTEXT.md
@.planning/phases/05-preview-and-export-pipeline/05-RESEARCH.md
@.planning/phases/05-preview-and-export-pipeline/05-PATTERNS.md
@.planning/phases/05-preview-and-export-pipeline/05-VALIDATION.md
@crates/engine_core/src/lib.rs
@crates/draft_model/src/lib.rs
@crates/draft_model/src/time.rs
@crates/draft_model/src/timeline.rs
@crates/draft_model/src/material.rs
@crates/draft_commands/src/timeline.rs
</context>

<decision_map>
D-01 Shared Semantic Path: `engine_core` normalizes draft semantics before preview/export.
D-05 Text Layout Determinism: text segments use pinned deterministic layout settings for preview/export parity.
D-07 Testing And Gates: engine tests cover normalization, time mapping, stacking, text layout, and frame-state snapshots.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-01-01: Normalize draft semantics for preview/export</name>
  <files>crates/engine_core/Cargo.toml, crates/engine_core/src/lib.rs, crates/engine_core/src/normalize.rs, crates/engine_core/tests/normalization.rs</files>
  <behavior>
    - Test 1: available video, image, audio, and text segments normalize into sorted track/segment structures using Jianying terms only.
    - Test 2: muted tracks and unavailable materials are represented as classified non-renderable diagnostics without mutating the input Draft.
    - Test 3: sourceTimerange to targetTimerange mapping uses checked integer microsecond arithmetic and rejects overflow or out-of-bounds material ranges.
    - Test 4: visual track stacking follows the existing Rust timeline order and does not use renderer-derived ordering.
  </behavior>
  <action>Add only local and already-approved dependencies to `crates/engine_core/Cargo.toml`: `draft_model`, `serde` with derive if needed for snapshots, `serde_json` for tests, and `thiserror` only if classified errors need derives. Create `normalize.rs` with `EngineProfile`, `NormalizedDraft`, `NormalizedTrack`, `NormalizedSegment`, `NormalizedMaterialRef`, `EngineError`, and `EngineErrorKind`. Implement `normalize_draft(draft: &Draft, profile: &EngineProfile) -> Result<NormalizedDraft, EngineError>` per D-01: validate through `draft_model`, preserve `.veproj/project.json` as the input source, keep filesystem/Electron/FFmpeg/process code out of `engine_core`, use Jianying terms `draft/material/track/segment/sourceTimerange/targetTimerange/keyframe/filter/transition`, and expose public exports from `lib.rs`. Create failing tests first in `normalization.rs`, then implement until they pass.</action>
  <verify>
    <automated>cargo test -p engine_core normalization -- --nocapture</automated>
    <automated>pnpm run test:phase3-source-guards</automated>
  </verify>
  <done>TEST-03 normalization coverage passes and `engine_core` exposes a pure Rust normalized draft API without runtime/UI dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-01-02: Resolve frame state and render ranges from normalized drafts</name>
  <files>crates/engine_core/src/lib.rs, crates/engine_core/src/frame_state.rs, crates/engine_core/tests/frame_state_snapshots.rs</files>
  <behavior>
    - Test 1: resolving at a Microseconds value returns active visual layers, audio segments, and text overlays whose target ranges cover that value.
    - Test 2: source position is computed from sourceTimerange.start plus timeline offset using integer microseconds.
    - Test 3: rational frame-rate sampling maps frame indices to microsecond positions without persisted floating-point fields.
    - Test 4: snapshot output is stable across repeated runs with deterministic ordering.
  </behavior>
  <action>Create `frame_state.rs` with `FrameState`, `FrameVisualLayer`, `FrameAudioSegment`, `RenderRange`, `RenderRangeState`, and `resolve_frame_state(normalized: &NormalizedDraft, at: Microseconds) -> Result<FrameState, EngineError>`. Add `resolve_render_range(normalized, target_timerange)` for preview segment/export callers. Implement D-01 and EXP-02 by deriving frame/range data only from `NormalizedDraft`; do not read files, construct render graphs, construct FFmpeg jobs, or ask the renderer for layer lists. Use `RationalFrameRate` from `draft_model` and integer microseconds/frame indices for all timing. Create JSON-like snapshot assertions in tests using stable sorted vectors, not a new snapshot crate.</action>
  <verify>
    <automated>cargo test -p engine_core frame_state -- --nocapture</automated>
    <automated>cargo test -p engine_core -- --nocapture</automated>
  </verify>
  <done>Frame state and render range evaluation are deterministic and covered by TEST-03 snapshots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-01-03: Pin deterministic text layout semantics</name>
  <files>crates/engine_core/src/lib.rs, crates/engine_core/src/text_layout.rs, crates/engine_core/tests/frame_state_snapshots.rs</files>
  <behavior>
    - Test 1: a TextSegment resolves into a deterministic overlay with pinned font family, fallback candidate identity, alignment, safe-area, wrapping policy, and integer dimensions.
    - Test 2: missing or incomplete text layout profile data returns a classified EngineErrorKind instead of silently changing layout.
    - Test 3: frame-state snapshots include text overlay ordering and layout profile values used later by render graph and FFmpeg compiler.
  </behavior>
  <action>Create `text_layout.rs` with `TextLayoutProfile`, `TextFontPolicy`, `ResolvedTextOverlay`, and helper APIs consumed by `frame_state.rs`. Implement D-05 using the resolved research decision: `TextLayoutProfile::mvp_default()` pins `PingFang SC` with fallback candidates `VE_TEXT_FONT_PATH`, `/System/Library/Fonts/PingFang.ttc`, `/System/Library/Fonts/Supplemental/Arial Unicode.ttf`, `/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc`, and `/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf`; availability probing remains outside `engine_core`, and unresolved font policy returns a classified engine error. Do not add advanced text bubbles, proprietary text effects, animated keyframes, renderer canvas text, or filesystem font discovery in this crate. Extend frame-state snapshots so preview/export parity can assert the same resolved text overlay inputs later.</action>
  <verify>
    <automated>cargo test -p engine_core text_layout -- --nocapture</automated>
    <automated>cargo test -p engine_core -- --nocapture</automated>
  </verify>
  <done>TEXT-03 has deterministic Rust-owned text-layout inputs ready for render graph and FFmpeg compiler consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft input to engine_core | Untrusted serialized draft data becomes normalized semantic state. |
| engine_core to render_graph | Pure semantic output crosses into renderer-neutral planning. |
| text layout profile to text overlay | Caller-provided font/layout settings become deterministic render input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `normalize_draft` | mitigate | Validate through `draft_model`, reject invalid sourceTimerange/targetTimerange and material status with classified EngineErrorKind, and never mutate the input Draft. |
| T-05-02 | Denial of Service | integer time mapping | mitigate | Use checked integer arithmetic and reject overflow/out-of-bounds ranges before downstream render planning. |
| T-05-03 | Information Disclosure | text profile | accept | Font candidate identity is local render configuration and is not persisted to `.veproj/project.json`; no secrets or external accounts are introduced. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new external package installs; only local crates and already-approved workspace dependencies may be wired. |
</threat_model>

<verification>
<automated>cargo test -p engine_core -- --nocapture</automated>
<automated>pnpm run test:phase3-source-guards</automated>
</verification>

<success_criteria>
- TEST-03 passes for normalization, time mapping, track stacking, text layout, and frame-state snapshots.
- TEXT-03 has pinned deterministic text-layout inputs without filesystem/runtime work in `engine_core`.
- EXP-02 foundation is present: preview and export can consume one normalized draft and frame-state API.
- `draft_model` and `draft_commands` remain pure, and `.veproj/project.json` remains the canonical semantic source.
</success_criteria>

<output>
Create `.planning/phases/05-preview-and-export-pipeline/05-01-SUMMARY.md` when done.
</output>
