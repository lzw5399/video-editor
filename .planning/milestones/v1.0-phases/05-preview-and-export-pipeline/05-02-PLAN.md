---
phase: "05-preview-and-export-pipeline"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - "crates/render_graph/Cargo.toml"
  - "crates/render_graph/src/lib.rs"
  - "crates/render_graph/src/graph.rs"
  - "crates/render_graph/src/profile.rs"
  - "crates/render_graph/tests/render_graph_snapshots.rs"
autonomous: true
requirements:
  - PREV-02
  - PREV-03
  - EXP-01
  - EXP-02
  - TEST-04
must_haves:
  truths:
    - "Preview frame, preview segment, and MP4 export can be represented as render output profiles over the same renderer-neutral graph."
    - "Render graph construction consumes NormalizedDraft and RenderRangeState produced by 05-01."
    - "Render graph snapshots are stable and contain no FFmpeg command syntax or process execution."
  artifacts:
    - path: "crates/render_graph/src/graph.rs"
      provides: "RenderGraph, material references, video layers, audio mixes, text overlays, filters, and transitions"
    - path: "crates/render_graph/src/profile.rs"
      provides: "PreviewFrame, PreviewSegment, and ExportMp4 output profiles"
    - path: "crates/render_graph/tests/render_graph_snapshots.rs"
      provides: "Renderer-neutral graph/profile snapshots for TEST-04"
  key_links:
    - from: "crates/render_graph/src/lib.rs"
      to: "crates/engine_core"
      via: "NormalizedDraft and RenderRangeState"
      pattern: "engine_core"
    - from: "crates/render_graph/src/profile.rs"
      to: "crates/draft_model"
      via: "Microseconds and RationalFrameRate profile fields"
      pattern: "RationalFrameRate|Microseconds"
---

<objective>
Implement the typed renderer-neutral render graph layer.

Purpose: Preview and export must share one graph representation before FFmpeg compilation, with editing semantics already resolved by `engine_core`.
Output: `render_graph` graph/profile types and TEST-04 snapshots.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-preview-and-export-pipeline/05-CONTEXT.md
@.planning/phases/05-preview-and-export-pipeline/05-RESEARCH.md
@.planning/phases/05-preview-and-export-pipeline/05-PATTERNS.md
@.planning/phases/05-preview-and-export-pipeline/05-VALIDATION.md
@.planning/phases/05-preview-and-export-pipeline/05-01-SUMMARY.md
@crates/render_graph/src/lib.rs
@crates/engine_core/src/lib.rs
@crates/draft_model/src/time.rs
@crates/draft_model/src/timeline.rs
</context>

<decision_map>
D-01 Shared Semantic Path: render graph consumes 05-01 normalized frame/range state.
D-02 Render Graph And FFmpeg Compiler: `render_graph` owns typed renderer-neutral intents and does not execute FFmpeg.
D-07 Testing And Gates: render graph outputs have stable snapshot tests.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-02-01: Define typed render graph intents from engine state</name>
  <files>crates/render_graph/Cargo.toml, crates/render_graph/src/lib.rs, crates/render_graph/src/graph.rs, crates/render_graph/tests/render_graph_snapshots.rs</files>
  <behavior>
    - Test 1: a normalized render range builds a stable RenderGraph with material-backed visual layers, audio mixes, and text overlays.
    - Test 2: filter and transition intents are represented as renderer-neutral supported/degraded data, not as FFmpeg syntax.
    - Test 3: renderer-neutral snapshots keep deterministic track/segment/material/text ordering.
  </behavior>
  <action>Add only local/approved dependencies to `crates/render_graph/Cargo.toml`: `draft_model`, `engine_core`, `serde` with derive, and `serde_json` for tests if needed. Implement `graph.rs` with `RenderGraph`, `RenderMaterial`, `RenderVideoLayer`, `RenderAudioMix`, `RenderTextOverlay`, `RenderFilterIntent`, `RenderTransitionIntent`, and classified `RenderGraphError`. Implement a public builder from `NormalizedDraft` and `RenderRangeState` per D-02 and EXP-02; do not execute FFmpeg, decide editing behavior, read files, construct shell strings, or accept renderer layer lists. Create failing snapshot tests first, then implement until the focused render graph test passes.</action>
  <verify>
    <automated>cargo test -p render_graph render_graph -- --nocapture</automated>
  </verify>
  <done>TEST-04 render graph snapshots prove a renderer-neutral graph derived from 05-01 engine state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-02-02: Add preview and export output profiles over one graph</name>
  <files>crates/render_graph/src/lib.rs, crates/render_graph/src/profile.rs, crates/render_graph/tests/render_graph_snapshots.rs</files>
  <behavior>
    - Test 1: PreviewFrame, PreviewSegment, and ExportMp4 profiles share the same graph shape and differ only by output profile/range metadata.
    - Test 2: output profiles use rational frame rates, integer target ranges, deterministic dimensions, and stable preset IDs.
    - Test 3: unsupported profile settings return classified RenderGraphError values.
  </behavior>
  <action>Implement `profile.rs` with `RenderOutputProfile::PreviewFrame`, `RenderOutputProfile::PreviewSegment`, and `RenderOutputProfile::ExportMp4`, including output dimensions, rational frame rate, target timerange, still-frame format, MP4 preset metadata, and validation hints consumed by `ffmpeg_compiler`. Keep output profiles as render intent data only; do not add FFmpeg arguments, cache keys, file writes, process execution, renderer state, or `.veproj/project.json` fields. Extend snapshots so the same graph can be asserted under all three output profiles.</action>
  <verify>
    <automated>cargo test -p render_graph output_profiles -- --nocapture</automated>
    <automated>cargo test -p render_graph -- --nocapture</automated>
  </verify>
  <done>PREV-02, PREV-03, EXP-01, and EXP-02 have a shared typed graph/profile contract ready for FFmpeg compilation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine_core to render_graph | Normalized semantic state crosses into renderer-neutral planning. |
| render_graph to ffmpeg_compiler | Typed graph/profile data crosses into FFmpeg job compilation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | graph builder | mitigate | Build only from 05-01 NormalizedDraft/RenderRangeState and reject unsupported profile/intent values with classified errors. |
| T-05-05 | Elevation of Privilege | render_graph crate | mitigate | Keep process execution, filesystem writes, Electron, and desktop runtime dependencies out of `render_graph`; snapshots assert graph data only. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new external package installs; only local crates and already-approved workspace dependencies may be wired. |
</threat_model>

<verification>
<automated>cargo test -p render_graph -- --nocapture</automated>
</verification>

<success_criteria>
- TEST-04 render graph snapshots pass.
- Render graph consumes 05-01 engine outputs and does not duplicate timeline semantics.
- Preview frame, preview segment, and export MP4 are represented as output profiles over one graph.
- No renderer, FFmpeg process, cache, or project persistence code appears in `render_graph`.
</success_criteria>

<output>
Create `.planning/phases/05-preview-and-export-pipeline/05-02-SUMMARY.md` when done.
</output>
