---
phase: "05-preview-and-export-pipeline"
plan: "09"
type: execute
wave: 8
depends_on:
  - "05-04"
  - "05-08"
files_modified:
  - "crates/testkit/Cargo.toml"
  - "crates/testkit/src/lib.rs"
  - "crates/testkit/src/render_compare.rs"
  - "crates/testkit/tests/preview_export_parity.rs"
  - "scripts/phase5-source-guards.sh"
  - "package.json"
  - "justfile"
autonomous: true
requirements:
  - TEXT-03
  - PREV-01
  - PREV-02
  - PREV-03
  - PREV-04
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - TEST-03
  - TEST-04
  - TEST-05
must_haves:
  truths:
    - "Preview frame and exported frame parity is tested through one normalized draft, render graph, FFmpeg compiler, and runtime path."
    - "Root public gates include Phase 5 render core, source guards, preview/export UI tests, contract drift, and full `just test` coverage."
    - "Source guards prevent renderer-owned FFmpeg, render graph, cache, export script, validation, and process behavior."
  artifacts:
    - path: "crates/testkit/src/render_compare.rs"
      provides: "Frame metadata and pixel tolerance helpers"
    - path: "crates/testkit/tests/preview_export_parity.rs"
      provides: "TEST-05 golden parity gate"
    - path: "scripts/phase5-source-guards.sh"
      provides: "Final Phase 5 architecture/source guard"
    - path: "package.json"
      provides: "Phase 5 root scripts and test chaining"
    - path: "justfile"
      provides: "Phase 5 public `just test` gate chaining"
  key_links:
    - from: "crates/testkit/tests/preview_export_parity.rs"
      to: "preview_service + export command path"
      via: "shared render graph parity"
      pattern: "preview_export_parity"
    - from: "package.json"
      to: "scripts/phase5-source-guards.sh"
      via: "test:phase5-source-guards"
      pattern: "test:phase5-source-guards"
---

<objective>
Close Phase 5 with parity tests, final source guards, and public gates.

Purpose: The phase is complete only when preview/export parity and the renderer/Rust ownership boundary are executable checks in the normal test path.
Output: TEST-05 parity helpers/tests, final source guards, root scripts, and `just test` chaining.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-preview-and-export-pipeline/05-CONTEXT.md
@.planning/phases/05-preview-and-export-pipeline/05-RESEARCH.md
@.planning/phases/05-preview-and-export-pipeline/05-PATTERNS.md
@.planning/phases/05-preview-and-export-pipeline/05-VALIDATION.md
@.planning/phases/05-preview-and-export-pipeline/05-04-SUMMARY.md
@.planning/phases/05-preview-and-export-pipeline/05-08-SUMMARY.md
@crates/testkit/src/lib.rs
@crates/testkit/tests/render_smoke.rs
@scripts/phase5-source-guards.sh
@package.json
@justfile
</context>

<decision_map>
D-01 Shared Semantic Path: parity tests use one normalized draft/frame-state path.
D-02 Render Graph And FFmpeg Compiler: parity tests use the same graph/compiler outputs for preview and export.
D-03 Preview Service And Cache: parity tests request preview artifacts through `preview_service`.
D-04 Export Runtime And Job Behavior: parity tests use runtime export and validation output.
D-05 Text Layout Determinism: parity tests include text overlays with documented tolerance.
D-06 Desktop UI Integration: final source guards block renderer-owned preview/export behavior.
D-07 Testing And Gates: final Phase 5 gates run from root `pnpm run test` and `just test`.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-09-01: Add preview/export parity helpers and golden test</name>
  <files>crates/testkit/Cargo.toml, crates/testkit/src/lib.rs, crates/testkit/src/render_compare.rs, crates/testkit/tests/preview_export_parity.rs</files>
  <behavior>
    - Test 1: a generated golden draft with video, audio, and text renders a preview frame and matching exported frame through the same normalized graph/compiler path.
    - Test 2: parity comparison checks exact width/height, expected frame index, timestamp within one output frame duration, mean absolute RGB delta <= 8.0, and 99th percentile RGB delta <= 24.
    - Test 3: missing FFmpeg filters, encoders, or fonts return classified setup errors with remediation text rather than silent skips.
  </behavior>
  <action>Add `render_compare.rs` with metadata and pixel-tolerance helpers using existing FFmpeg/testkit temporary directory patterns. Add `preview_export_parity.rs` that builds or loads a golden draft with video/audio/text segments, requests a preview frame through `preview_service`, runs export through the shared compiled path, extracts the comparable frame, and asserts the resolved tolerance from 05-RESEARCH.md: exact dimensions, expected frame index, timestamp within one output frame duration, mean absolute RGB delta <= 8.0, and 99th percentile RGB delta <= 24. Use capability probes and classified setup errors for missing FFmpeg filters/encoders/fonts; do not require cross-host byte-perfect equality.</action>
  <verify>
    <automated>cargo test -p testkit preview_export_parity -- --nocapture</automated>
  </verify>
  <done>TEST-05 proves preview/export parity through the shared Rust path and documented tolerance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-09-02: Add final Phase 5 source guards and public gates</name>
  <files>scripts/phase5-source-guards.sh, package.json, justfile</files>
  <behavior>
    - Test 1: source guards reject renderer FFmpeg/render graph/cache/export script/process/output validation ownership.
    - Test 2: source guards reject derived preview/export/cache/script fields in draft schema and fixtures.
    - Test 3: `pnpm run test` and `just test` include Phase 5 render core, source guards, desktop preview/export tests, and contract drift.
  </behavior>
  <action>Finalize `scripts/phase5-source-guards.sh` using the Phase 4 guard style and grep gate hygiene: avoid bare `grep -c` gates on unfiltered files, filter comments when counting forbidden tokens, and keep generated headers allowed. Guard renderer files against `ffmpeg`, `ffprobe`, `filter_complex`, `FfmpegJob`, render graph construction, preview cache key/invalidation ownership, export script generation, direct process APIs, and output validation logic. Guard schema/fixtures against derived render graph, FFmpeg script, preview cache, thumbnail/waveform/proxy, and export fields. Add root scripts `test:phase5-render-core` and `test:phase5-source-guards`; chain Phase 5 render core, guards, preview/export workspace tests, and contract drift into root `test` and `just test` after Phase 4 gates.</action>
  <verify>
    <automated>pnpm run test:phase5-render-core</automated>
    <automated>pnpm run test:phase5-source-guards</automated>
    <automated>pnpm run test</automated>
    <automated>just test</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <done>Final public gates cover TEXT-03, PREV-01..04, EXP-01..04, TEST-03..05, renderer ownership, and contract drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testkit to runtime tools | Golden parity tests invoke local FFmpeg/ffprobe through runtime boundaries. |
| renderer to Rust boundary | Source guards enforce command-only preview/export UI ownership. |
| draft fixtures/schema to derived artifacts | Persisted semantics are checked for derived preview/export/cache fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | parity test | mitigate | Compare preview/export frames through one compiled path with documented tolerance and classified capability failures. |
| T-05-19 | Elevation of Privilege | renderer source | mitigate | Source guards reject renderer-owned FFmpeg, render graph, cache, export script, validation, and process behavior. |
| T-05-20 | Tampering | draft schema/fixtures | mitigate | Source guards reject derived preview/export/cache/script fields in canonical draft artifacts. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new external package installs; final source guards and lockfile discipline preserve the approved dependency set. |
</threat_model>

<verification>
<automated>cargo test -p testkit preview_export_parity -- --nocapture</automated>
<automated>pnpm run test:phase5-render-core</automated>
<automated>pnpm run test:phase5-source-guards</automated>
<automated>pnpm run test</automated>
<automated>just test</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- TEST-05 passes with documented preview/export parity tolerance.
- Root public gates include Phase 5 render core, source guards, desktop preview/export coverage, generated contract drift, and full test execution.
- Renderer code cannot own FFmpeg, render graph, cache, export script, validation, or process behavior.
- All Phase 5 requirements are covered by automated verification.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 5 | Shared render graph path for preview frames, preview cache, and MP4 export | 05-01, 05-02, 05-03, 05-04, 05-07, 05-09 | COVERED | Core semantics, graph/compiler, preview, export runtime, and parity are planned.
REQ | TEXT-03 | Pinned deterministic text layout for preview/export parity | 05-01, 05-03, 05-09 | COVERED | Engine text profile, ASS/compiler snapshots, parity gate.
REQ | PREV-01 | Preview current draft in center player | 05-04, 05-05, 05-06 | COVERED | Preview service, binding commands, Chinese UI.
REQ | PREV-02 | Seek/scrub deterministic preview frame | 05-02, 05-04, 05-05, 05-06 | COVERED | frame profile, requestPreviewFrame command, UI.
REQ | PREV-03 | Short cached preview segment | 05-02, 05-04, 05-05, 05-06 | COVERED | PreviewSegment profile, cache service, binding/UI.
REQ | PREV-04 | Cache invalidates affected ranges only | 05-04, 05-05, 05-09 | COVERED | preview_service range invalidation and source guards.
REQ | EXP-01 | H.264 MP4 export presets | 05-02, 05-03, 05-07, 05-08 | COVERED | Export profile, FFmpeg compiler, runtime, UI.
REQ | EXP-02 | Preview/export same path | 05-01, 05-02, 05-03, 05-04, 05-08, 05-09 | COVERED | Shared path and parity gate.
REQ | EXP-03 | Export progress/cancel/logs/errors | 05-07, 05-08 | COVERED | media_runtime job API and binding/UI status.
REQ | EXP-04 | Export output validation | 05-07, 05-08 | COVERED | ffprobe output validation and UI report.
REQ | TEST-03 | Engine tests | 05-01, 05-09 | COVERED | normalization/frame/text snapshots and root gate.
REQ | TEST-04 | Render graph/compiler snapshots | 05-02, 05-03, 05-09 | COVERED | graph/job/filter/ASS snapshots and root gate.
REQ | TEST-05 | Preview/export frame parity | 05-09 | COVERED | testkit parity gate.
RESEARCH | Resolved deterministic font default/fallback | 05-01, 05-03, 05-09 | COVERED | TextLayoutProfile plus compiler capability classification.
RESEARCH | Resolved job-state ownership | 05-04, 05-07, 05-08 | COVERED | preview_service, media_runtime, thin binding registry.
RESEARCH | Resolved preview/export tolerance | 05-09 | COVERED | exact dimensions/time plus RGB tolerance.
RESEARCH | Renderer source guards | 05-05, 05-06, 05-09 | COVERED | phase5-source-guards and root scripts.
CONTEXT | D-01 | Shared semantic path | 05-01, 05-02, 05-08, 05-09 | COVERED | Cited in decision maps and tasks.
CONTEXT | D-02 | Render graph and FFmpeg compiler | 05-02, 05-03, 05-08, 05-09 | COVERED | Cited in decision maps and tasks.
CONTEXT | D-03 | Preview service and cache | 05-04, 05-05, 05-06, 05-09 | COVERED | Cited in decision maps and tasks.
CONTEXT | D-04 | Export runtime and job behavior | 05-07, 05-08, 05-09 | COVERED | Cited in decision maps and tasks.
CONTEXT | D-05 | Text layout determinism | 05-01, 05-03, 05-09 | COVERED | Cited in decision maps and tasks.
CONTEXT | D-06 | Desktop UI integration | 05-05, 05-06, 05-08, 05-09 | COVERED | Cited in decision maps and tasks.
CONTEXT | D-07 | Testing and gates | 05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08, 05-09 | COVERED | Every plan has automated gates.
</source_audit>

<output>
Create `.planning/phases/05-preview-and-export-pipeline/05-09-SUMMARY.md` when done.
</output>
