---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04F"
type: execute
wave: 12
depends_on:
  - "15.2-04E"
files_modified:
  - "apps/desktop-electron/tests/helpers/foregroundProductApp.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/src/main/index.ts"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "crates/realtime_preview_runtime/src/platform/macos.rs"
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - TEST-E2E-01
  - TEST-E2E-02
  - NO-FALLBACK-01
autonomous: true
user_setup: []
---

<objective>
Close the remaining macOS foreground/window-server blocker by making product
E2E prove that the packaged Electron app owns a foreground, window-server-visible
preview window before WGPU acquire, then repair the production native surface
ownership path until visible render-graph GPU playback presents or fails closed
with a precise non-product-environment blocker.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-04F-01: Add foreground and window-server gates to the packaged product harness</name>
  <files>apps/desktop-electron/tests/helpers/foregroundProductApp.ts, apps/desktop-electron/tests/helpers/userJourney.ts, apps/desktop-electron/tests/product-user-journey.spec.ts</files>
  <action>Extend the macOS foreground/CDP harness with explicit foreground probes before playback: System Events process state, current frontmost app, bundle id, process pid, and CoreGraphics window-server entries for the packaged app process. The harness must record whether the app has accessibility-visible windows, whether CGWindow reports onscreen windows with bounds/layer/alpha, and whether activation attempts actually make Video Editor frontmost. Product playback must not continue silently when the harness cannot prove a foreground product window; failure output must include the probes.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
    <automated>pnpm --filter @video-editor/desktop package:dir</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>Foreground failure is separated from compositor failure and includes System Events plus CGWindow evidence; no screenshot-only or fallback success is accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04F-02: Correct macOS preview bounds semantics and native drawable diagnostics</name>
  <files>crates/realtime_preview_runtime/src/platform/macos.rs, apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/tests/helpers/userJourney.ts</files>
  <action>Add native diagnostics before WGPU acquire that identify the parent and child window numbers, window levels, key/main state, occlusion state, active app state, layer hierarchy, and drawable size. Correct the macOS geometry contract so DOM/CSS host rect values are treated as AppKit points for window/view frames; use `scaleFactorMillis` only for `CAMetalLayer.contentsScale` and backing-pixel `drawableSize`. If the harness sees CGWindow onscreen but AppKit reports occlusion-invisible, the error must say so explicitly. Diagnostics are evidence only; they cannot mark `renderGraphGpuComposited` or increment product-presented frame counts.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>Occluded WGPU acquire failures explain whether the native surface is missing, not attached, not in a window-server-visible window, or blocked by foreground activation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04F-03: Repair the packaged macOS main-window foreground and in-window surface path</name>
  <files>apps/desktop-electron/src/main/index.ts, apps/desktop-electron/src/main/realtimePreviewHost.ts, crates/realtime_preview_runtime/src/platform/macos.rs</files>
  <action>Apply one production-shaped foreground repair: make the packaged BrowserWindow show/focus through a ready-to-show foreground path, keep the app activation policy regular, remove or adjust any test-only window-level behavior that prevents normal activation, and recreate or resize the WGPU surface only after the product window is foreground-visible. Prefer a corrected in-window `NSView`/`CAMetalLayer` attachment under the Electron parent view over a separate child `NSWindow`, unless diagnostics prove the subview path cannot be made drawable. Do not introduce offscreen, DOM, native-video, screenshot, or CPU fallback success.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop build:native</automated>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
    <automated>pnpm --filter @video-editor/desktop package:dir</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>The packaged product app can produce a non-occluded WGPU surface texture in the preview window, or the remaining blocker is recorded as a precise macOS foreground/window-server environment issue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04F-04: Re-prove visible render-graph GPU playback or keep downstream plans blocked</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, .planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04F-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Run the normal product user journey: import repo video, add to timeline, press play, observe advancing target time, and assert `renderGraphGpuComposited`, presented frame count, non-occluded WGPU acquire, and visible preview-region pixel change. If this still fails, record the narrowed blocker and leave 15.2-05/06 unreleased. If it passes, unblock 15.2-05.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
    <automated>git diff --check -- . ':!reference'</automated>
  </verify>
  <done>The summary records either proven real visible render-graph GPU playback or an honest fail-closed blocker with 15.2-05/06 still blocked.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop build:native</automated>
<automated>pnpm --filter @video-editor/desktop build:electron</automated>
<automated>pnpm --filter @video-editor/desktop package:dir</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The packaged product journey either proves real visible render-graph GPU
playback through a foreground, window-server-visible macOS WGPU surface, or it
fails closed with enough foreground/AppKit/CoreGraphics evidence to identify the
remaining non-product-environment blocker. Downstream product matrix work stays
blocked until real compositor playback is proven.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04F-SUMMARY.md` when done.
</output>
