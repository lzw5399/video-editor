---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "03"
type: execute
wave: 3
depends_on:
  - "15.2-02"
files_modified:
  - "crates/realtime_preview_runtime/src/frame_provider.rs"
  - "crates/realtime_preview_runtime/src/media_io_adapter.rs"
  - "crates/realtime_preview_runtime/src/software_video_provider.rs"
  - "crates/media_runtime_desktop/src/**/*.rs"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "crates/realtime_preview_runtime/tests/video_frame_provider.rs"
  - "crates/realtime_preview_runtime/tests/media_io_handoff.rs"
autonomous: true
requirements:
  - P0-GPU-01
  - P0-GPU-02
  - P0-GPU-04
  - NO-FALLBACK-01
user_setup: []
---

<objective>
Feed imported material frames and audio state into realtime preview without
per-frame FFmpeg artifact generation or fallback success.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-03-01: Add product material-frame provider tests</name>
  <files>crates/realtime_preview_runtime/tests/video_frame_provider.rs, crates/realtime_preview_runtime/tests/media_io_handoff.rs</files>
  <action>Add tests that open repository-owned video/image fixtures through the production media IO path and provide frame/texture input to the compositor by material ID and integer microsecond source time. Missing frames, unsupported codecs, and unavailable hardware paths must return unavailable diagnostics, not FFmpeg artifact fallback success.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime video_frame_provider -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
  </verify>
  <done>Tests fail unless real imported materials can feed the compositor path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-03-02: Connect media IO frames to compositor inputs</name>
  <files>crates/realtime_preview_runtime/src/frame_provider.rs, crates/realtime_preview_runtime/src/media_io_adapter.rs, crates/realtime_preview_runtime/src/software_video_provider.rs, crates/media_runtime_desktop/src/**/*.rs</files>
  <action>Implement a frame provider path for imported video/image materials that can supply compositor-ready frames or texture handles at requested timeline source times. Preserve Rust-owned ownership and fail closed when required native media capabilities are unavailable.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime video_frame_provider -- --nocapture</automated>
    <automated>cargo test -p media_runtime_desktop capabilities -- --nocapture</automated>
  </verify>
  <done>Imported media feeds the realtime compositor without per-frame FFmpeg artifact generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-03-03: Carry audio preview state alongside compositor playback</name>
  <files>crates/realtime_preview_runtime/src/media_io_adapter.rs, crates/bindings_node/src/realtime_preview_service.rs, apps/desktop-electron/tests/product-user-journey.spec.ts</files>
  <action>Ensure baseline audio playback/mix state is synchronized with the realtime preview timeline and is tested as part of the product workflow. Audio fallback diagnostics must not mark the preview workflow as successful.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
  </verify>
  <done>Baseline audio state participates in the preview workflow and fails closed when unavailable.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime video_frame_provider -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
<automated>cargo test -p media_runtime_desktop capabilities -- --nocapture</automated>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Realtime preview can obtain compositor inputs from real imported materials and
does not use FFmpeg artifacts or fallback paths as product frame evidence.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-03-SUMMARY.md` when done.
</output>

