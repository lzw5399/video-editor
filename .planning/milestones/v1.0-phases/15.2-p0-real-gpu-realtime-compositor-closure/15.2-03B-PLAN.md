---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "03B"
type: execute
wave: 5
depends_on:
  - "15.2-03"
  - "15.2-03A"
files_modified:
  - "crates/media_runtime/src/texture.rs"
  - "crates/media_runtime_desktop/src/platform/macos.rs"
  - "crates/media_runtime_desktop/src/lib.rs"
  - "crates/realtime_preview_runtime/src/frame_provider.rs"
  - "crates/realtime_preview_runtime/src/media_io_adapter.rs"
  - "crates/realtime_preview_runtime/src/gpu/compositor.rs"
  - "crates/realtime_preview_runtime/src/gpu/texture_cache.rs"
  - "crates/realtime_preview_runtime/tests/media_io_handoff.rs"
  - "crates/realtime_preview_runtime/tests/video_frame_provider.rs"
requirements:
  - P0-GPU-01
  - P0-GPU-02
  - P0-GPU-04
  - NO-FALLBACK-01
autonomous: true
user_setup: []
---

<objective>
Replace the current opaque decoded-texture handle gap with a ref-counted native
texture import path that lets the WGPU compositor sample real decoded material
frames for product preview.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-03B-01: Add RED tests for importing decoded textures into the compositor</name>
  <files>crates/realtime_preview_runtime/tests/media_io_handoff.rs, crates/realtime_preview_runtime/tests/video_frame_provider.rs</files>
  <action>Add tests that route an imported video material through media IO into the compositor and require the frame provider to resolve the handle into compositor-sampleable GPU texture data. A handle ID with no registered native texture lease must fail closed and must not fall back to CPU/FFmpeg/offscreen/native-video evidence.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime video_frame_provider -- --nocapture</automated>
  </verify>
  <done>The tests fail because current descriptors expose metadata only and the compositor rejects external texture handles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-03B-02: Add native texture lease registry and import contract</name>
  <files>crates/media_runtime/src/texture.rs, crates/media_runtime_desktop/src/platform/macos.rs, crates/media_runtime_desktop/src/lib.rs, crates/realtime_preview_runtime/src/frame_provider.rs, crates/realtime_preview_runtime/src/media_io_adapter.rs</files>
  <action>Introduce a Rust-owned lease registry that ties texture handles to live decoded native texture resources, owner sessions, playback generations, pixel format, dimensions, color metadata, and device identity. On macOS, keep the CoreVideo/Metal decode texture lifetime valid for compositor import. Unregistered, stale, incompatible, CPU, platform-opaque, or artifact handles must remain unavailable diagnostics.</action>
  <verify>
    <automated>cargo test -p media_runtime_desktop texture -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
  </verify>
  <done>Media IO hands the realtime runtime live, generation-checked decoded texture leases instead of metadata-only opaque IDs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-03B-03: Sample imported decoded textures in the WGPU compositor</name>
  <files>crates/realtime_preview_runtime/src/gpu/compositor.rs, crates/realtime_preview_runtime/src/gpu/texture_cache.rs, crates/realtime_preview_runtime/src/gpu/pipelines.rs</files>
  <action>Teach the compositor to import and sample supported decoded texture leases on the same WGPU device used for presentation. Implement the required pixel-format shader path for supported decoded frames, including NV12 where exposed by the desktop media runtime. Reject incompatible devices or formats as product-unavailable diagnostics rather than copying through CPU or FFmpeg.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime gpu_subset -- --nocapture</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>Imported video material frames are sampleable by the render-graph GPU compositor without fallback evidence.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p media_runtime_desktop texture -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime video_frame_provider -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime gpu_subset -- --nocapture</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Imported video material frames reach the WGPU compositor as live decoded GPU
texture leases and cannot be substituted by CPU decode probes, FFmpeg artifacts,
native-video presentation, mock output, or offscreen readback.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-03B-SUMMARY.md` when done.
</output>

