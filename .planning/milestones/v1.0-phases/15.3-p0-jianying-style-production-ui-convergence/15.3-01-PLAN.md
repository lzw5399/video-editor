---
phase: "15.3-p0-jianying-style-production-ui-convergence"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/src/renderer/viewModel.ts"
  - "apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx"
  - "apps/desktop-electron/src/renderer/styles.css"
  - "apps/desktop-electron/src/main/index.ts"
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/tests/project-entry.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - P0-UI-02
  - P0-UI-03A
  - P0-UI-01
user_setup: []
---

<objective>
Add the production project entry state so normal users create or open a project
before material import, while preserving explicit test/dev entry points.
</objective>

<threat_model>
- T-15.3-01A: A blank launch can still bypass project entry and import materials into an implicit `/tmp` draft. Mitigation: production-default import controls are absent until `workspace.projectState.kind === "open"`.
- T-15.3-01B: Project-open failures can expose raw local paths or core diagnostics. Mitigation: default errors use product-safe Chinese copy; raw diagnostics stay behind developer diagnostics.
- T-15.3-01C: Renderer creates project semantics locally. Mitigation: new/open/save intents route through existing command/platform bridges and accepted Rust responses.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 15.3-01-01: Introduce project entry view model and shell</name>
  <files>App.tsx, viewModel.ts, WorkspaceShell.tsx, styles.css</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/App.tsx`
    - `apps/desktop-electron/src/renderer/viewModel.ts`
    - `apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx`
    - `.planning/phases/15.3-p0-jianying-style-production-ui-convergence/15.3-UI-SPEC.md`
  </read_first>
  <action>Add a product entry state with `新建项目` and `打开项目`. The editor workspace renders only after a project is created/opened or an explicit test fixture opens it. Import controls, timeline, preview, and inspector are not visible in the entry state. The entry state uses dense production styling, not a marketing hero.</action>
  <acceptance_criteria>
    - `App.tsx` contains an explicit project entry/open state separate from `workspace`.
    - Default launch shows buttons named `新建项目` and `打开项目`.
    - Default launch has zero `导入素材` buttons until a project is open.
    - `workspace.spec.ts` or `project-entry.spec.ts` asserts entry-before-import behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/project-entry.spec.ts --reporter=line</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
  </verify>
  <done>Normal product launch starts at project entry and cannot import material before project creation/open.</done>
</task>

<task type="auto">
  <name>Task 15.3-01-02: Wire create/open project actions through product-safe platform paths</name>
  <files>App.tsx, main/index.ts, preload/index.ts, project-entry.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/main/index.ts`
    - `apps/desktop-electron/src/preload/index.ts`
    - `apps/desktop-electron/src/renderer/commandHelpers.ts`
    - `apps/desktop-electron/tests/helpers/userJourney.ts`
  </read_first>
  <action>Add product create/open project affordances using existing `saveProjectBundle`/`openProjectBundle` command envelopes and platform dialogs/test env hooks where needed. Tests may provide deterministic project bundle paths without exposing manual path inputs in production UI.</action>
  <acceptance_criteria>
    - `新建项目` opens the editor with a saved project bundle path or deterministic test project path.
    - `打开项目` opens an existing `.veproj` through command envelope handling.
    - No production textbox labeled `草稿包路径` exists unless developer diagnostics are enabled.
    - Opening an invalid project shows product-safe Chinese error copy without raw stack traces.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/project-entry.spec.ts --reporter=line</automated>
  </verify>
  <done>Create/open project flows are normal user flows rather than debug path-entry flows.</done>
</task>

<task type="auto">
  <name>Task 15.3-01-03: Update product E2E launch helpers</name>
  <files>helpers/userJourney.ts, workspace.spec.ts, product-user-journey.spec.ts, real-workflow.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/tests/helpers/userJourney.ts`
    - `apps/desktop-electron/tests/workspace.spec.ts`
    - `apps/desktop-electron/tests/product-user-journey.spec.ts`
    - `docs/product-e2e-acceptance-policy.md`
  </read_first>
  <action>Make product helpers create/open a project before import. Preserve explicit fixture/dev helpers for tests that intentionally start inside an already-open workspace, and name those helpers accordingly.</action>
  <acceptance_criteria>
    - Product user journey tests import materials only after the helper has opened/created a project.
    - Test helper names distinguish product entry from fixture workspace bootstrap.
    - No product E2E helper relies on `VIDEO_EDITOR_TEST_SHOW_DEVELOPER_DIAGNOSTICS=1` for normal workflows.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/real-workflow.spec.ts --reporter=line</automated>
  </verify>
  <done>Product E2E starts from the same entry path a normal user sees.</done>
</task>

</tasks>

<verification>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/project-entry.spec.ts --reporter=line</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The product default flow starts from project entry, and the workspace import/edit
surface only appears after a project has been created or opened.
</success_criteria>

## Artifacts this phase produces

- `ProjectEntryState` renderer display state.
- Project entry UI in `App.tsx` or a local component.
- `tests/project-entry.spec.ts`.
- Product E2E helper for create/open project before import.

<output>
Create `15.3-01-SUMMARY.md` when done.
</output>
