---
phase: 01-foundation-and-golden-harness
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - crates/bindings_node/Cargo.toml
  - crates/bindings_node/build.rs
  - crates/bindings_node/src/lib.rs
  - crates/bindings_node/tests/binding_smoke.rs
autonomous: true
requirements:
  - FOUND-02
must_haves:
  truths:
    - "Node-API exposes only `ping`, `version`, and `execute_command` per D-05."
    - "All binding calls return the standardized `ok/error/events` envelope per D-08."
    - "Unsupported command names return structured errors instead of panics or ad hoc strings."
  artifacts:
    - path: "crates/bindings_node/src/lib.rs"
      provides: "napi-rs binding functions."
      exports: ["ping", "version", "execute_command"]
    - path: "crates/bindings_node/build.rs"
      provides: "napi-rs build integration."
    - path: "crates/bindings_node/tests/binding_smoke.rs"
      provides: "Rust-level binding smoke tests."
  key_links:
    - from: "crates/bindings_node/src/lib.rs"
      to: "crates/draft_model/src/lib.rs"
      via: "Rust contract imports"
      pattern: "CommandEnvelope|CommandResultEnvelope|CommandError"
---

<objective>
Implement the Rust Node-API binding crate for the Phase 1 command boundary.

Purpose: Electron needs a narrow native boundary that calls Rust-owned contracts without introducing editor semantics in JavaScript.
Output: napi-rs binding crate with `ping`, `version`, `execute_command`, and binding smoke tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-golden-harness/01-CONTEXT.md
@.planning/phases/01-foundation-and-golden-harness/01-RESEARCH.md
@.planning/phases/01-foundation-and-golden-harness/01-VALIDATION.md
@.planning/phases/01-foundation-and-golden-harness/01-PATTERNS.md
@.planning/phases/01-foundation-and-golden-harness/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates the Node-API binding subset of Phase 1 artifacts: `bindings_node` build metadata, the native functions `ping`, `version`, and `execute_command`, and tests proving those functions preserve the Rust-owned envelope shape.

<tasks>

<task type="auto">
  <name>Task 01-W2-01: Configure napi-rs binding crate</name>
  <files>crates/bindings_node/Cargo.toml, crates/bindings_node/build.rs</files>
  <action>Configure `bindings_node` for napi-rs per D-04 and D-05 using researched versions `napi`, `napi-derive`, and `napi-build`. Add a standard `build.rs` that calls the napi build setup. Depend on `draft_model` for all command/result contract types. Do not add custom `node-gyp`, handwritten ABI loader logic, Electron imports, or JavaScript-owned contract definitions.</action>
  <verify>
    <automated>cargo check -p bindings_node --locked</automated>
  </verify>
  <done>`bindings_node` builds as a Rust Node-API crate and depends on `draft_model` for contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-W2-02: Expose ping, version, and execute_command envelopes</name>
  <files>crates/bindings_node/src/lib.rs, crates/bindings_node/tests/binding_smoke.rs</files>
  <behavior>
    - `ping` returns an `ok: true` envelope with `PingResponse` data and empty events.
    - `version` returns an `ok: true` envelope with `VersionResponse` data and empty events.
    - `execute_command` supports Phase 1 ping/version commands and returns `UnsupportedCommand` for any editing command.
  </behavior>
  <action>Implement napi-rs functions named exactly `ping`, `version`, and `execute_command` per D-05 and D-08. Direct functions and `execute_command` must return the same `CommandResultEnvelope` shape. Unsupported command names must return `ok: false`, `error.kind = UnsupportedCommand`, and deterministic empty `events`. Do not add split, trim, move, delete, material import, draft persistence, preview, export, or FFmpeg command execution behavior in this binding plan. Keep JavaScript/TypeScript generation out of this plan; Plan 01-07 owns generated schema and TS files per D-06.</action>
  <verify>
    <automated>cargo test -p bindings_node -- --nocapture</automated>
    <automated>grep -R "Split\\|Trim\\|Move\\|Delete\\|ImportMaterial\\|Export" crates/bindings_node/src crates/bindings_node/tests && exit 1 || true</automated>
  </verify>
  <done>Binding tests prove `ping`, `version`, and `execute_command` return standardized Rust-owned envelopes without real editing commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JavaScript -> Node-API addon | Electron/Node callers invoke native Rust functions. |
| command envelope -> Rust binding | Potentially malformed command payloads enter Rust-owned validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of privilege | `bindings_node` exported functions | mitigate | Export only `ping`, `version`, and `execute_command` per D-05. |
| T-01-02 | Tampering | `execute_command` payload handling | mitigate | Route through `draft_model` contracts and return structured `UnsupportedCommand` errors for non-Phase-1 commands. |
| T-01-03 | Denial of service | native error handling | mitigate | Return bounded, deterministic envelope errors; do not panic on unsupported commands. |
| T-01-SC | Tampering | cargo dependency additions | mitigate | Use only approved napi-rs crates from 01-RESEARCH.md Package Legitimacy Audit. |
</threat_model>

<verification>
Run `cargo check -p bindings_node --locked`, `cargo test -p bindings_node -- --nocapture`, and the forbidden editing-command grep gate.
</verification>

<success_criteria>
- FOUND-02 has a Rust Node-API binding surface over Rust-owned contracts.
- D-05 and D-08 are implemented exactly.
- D-06 remains preserved because contracts come from `draft_model`; generated outputs are assigned to Plan 01-07.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-golden-harness/01-04-SUMMARY.md` when done.
</output>
