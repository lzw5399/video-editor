---
phase: 01-foundation-and-golden-harness
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - crates/draft_model/Cargo.toml
  - crates/draft_model/src/lib.rs
  - crates/draft_model/tests/contract.rs
  - crates/draft_commands/Cargo.toml
  - crates/draft_commands/src/lib.rs
  - crates/engine_core/Cargo.toml
  - crates/engine_core/src/lib.rs
  - crates/render_graph/Cargo.toml
  - crates/render_graph/src/lib.rs
  - crates/ffmpeg_compiler/Cargo.toml
  - crates/ffmpeg_compiler/src/lib.rs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - TEST-01
must_haves:
  truths:
    - "Pure Rust semantic crates compile without Electron, FFmpeg process, filesystem, or platform trait dependencies per D-17."
    - "Rust owns the Phase 1 command/result envelope contracts per D-05, D-06, D-07, and D-08."
    - "Unknown command envelope fields are rejected by Rust tests before TypeScript or Electron uses the contract."
  artifacts:
    - path: "crates/draft_model/src/lib.rs"
      provides: "Rust-owned command contract types."
      exports: ["CommandEnvelope", "CommandName", "CommandPayload", "CommandResultEnvelope", "CommandError", "CommandErrorKind", "CommandEvent", "PingResponse", "VersionResponse"]
    - path: "crates/draft_model/tests/contract.rs"
      provides: "Serde contract tests for ok/error/events envelopes and unknown-field rejection."
    - path: "crates/draft_commands/src/lib.rs"
      provides: "Compile-safe command crate shell with semantic boundary notes."
    - path: "crates/engine_core/src/lib.rs"
      provides: "Compile-safe engine crate shell with semantic boundary notes."
  key_links:
    - from: "crates/draft_model/src/lib.rs"
      to: "crates/bindings_node/src/lib.rs"
      via: "types imported by later binding plan"
      pattern: "CommandEnvelope|CommandResultEnvelope"
    - from: "crates/draft_model/tests/contract.rs"
      to: "crates/draft_model/src/lib.rs"
      via: "serde validation of boundary types"
      pattern: "deny_unknown_fields|serde_json"
---

<objective>
Create the pure Rust semantic crate shells and the Rust-owned Phase 1 command contract.

Purpose: The editor core must start with Rust-owned vocabulary and contracts before Electron or runtime integrations are wired on top.
Output: Compile-safe semantic crates plus `draft_model` command envelope types and contract tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-golden-harness/01-CONTEXT.md
@.planning/phases/01-foundation-and-golden-harness/01-RESEARCH.md
@.planning/phases/01-foundation-and-golden-harness/01-VALIDATION.md
@.planning/phases/01-foundation-and-golden-harness/01-PATTERNS.md
@.planning/phases/01-foundation-and-golden-harness/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates the pure semantic crate subset of Phase 1 artifacts: `draft_model`, `draft_commands`, `engine_core`, `render_graph`, and `ffmpeg_compiler`. It also creates the Phase 1 Rust contract symbols `CommandEnvelope`, `CommandName`, `CommandPayload`, `CommandResultEnvelope`, `CommandError`, `CommandErrorKind`, `CommandEvent`, `PingResponse`, and `VersionResponse`.

<tasks>

<task type="auto">
  <name>Task 01-W1-01: Create compile-safe pure crate shells</name>
  <files>crates/draft_model/Cargo.toml, crates/draft_commands/Cargo.toml, crates/draft_commands/src/lib.rs, crates/engine_core/Cargo.toml, crates/engine_core/src/lib.rs, crates/render_graph/Cargo.toml, crates/render_graph/src/lib.rs, crates/ffmpeg_compiler/Cargo.toml, crates/ffmpeg_compiler/src/lib.rs</files>
  <action>Create compile-safe crate shells for the pure semantic crates per D-03, D-17, and the architecture responsibility map. `draft_commands`, `engine_core`, `render_graph`, and `ffmpeg_compiler` should contain module docs or boundary notes that name their future responsibility using Jianying concepts, but no real timeline editing, render graph, or FFmpeg compilation behavior. Do not import Electron, `std::process`, `which`, filesystem platform traits, or FFmpeg runtime traits in `draft_model`, `draft_commands`, or `engine_core`. Do not create `adapter_jianying`, mobile/server FFI crates, or a generic `platform` crate per D-20 and deferred scope.</action>
  <verify>
    <automated>cargo check -p draft_commands -p engine_core -p render_graph -p ffmpeg_compiler --locked</automated>
    <automated>grep -R "std::process\\|which::\\|FfmpegExecutor\\|PlatformFileSystem\\|PreviewRenderer" crates/draft_model crates/draft_commands crates/engine_core && exit 1 || true</automated>
  </verify>
  <done>Pure semantic crate shells compile and remain free of platform/runtime dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-W1-02: Define Rust-owned command envelope contracts</name>
  <files>crates/draft_model/Cargo.toml, crates/draft_model/src/lib.rs, crates/draft_model/tests/contract.rs</files>
  <behavior>
    - Valid `ping` and `version` command envelopes deserialize with Jianying-aligned Rust type names.
    - `CommandResultEnvelope` serializes `ok`, `data`, `error`, and `events`.
    - Unknown top-level fields fail deserialization.
  </behavior>
  <action>Implement Phase 1 command contracts in `draft_model` per D-05, D-06, D-07, and D-08. Add dependencies for `serde`, `serde_json`, `schemars`, and `ts-rs` using versions from 01-RESEARCH.md. Define `CommandEnvelope`, `CommandName`, `CommandPayload`, `CommandResultEnvelope`, `CommandError`, `CommandErrorKind`, `CommandEvent`, `PingResponse`, and `VersionResponse`. Use `serde(rename_all = "camelCase", deny_unknown_fields)` for boundary structs. Derive `Serialize`, `Deserialize`, `JsonSchema`, and `TS` wherever generated schema/type output will need it. Use stable English Jianying concept naming and do not introduce `Asset` or `Clip` aliases. Keep real editing command names out of scope; Phase 1 supports ping/version-style commands and standardized unsupported-command errors only.</action>
  <verify>
    <automated>cargo test -p draft_model contract -- --nocapture</automated>
    <automated>cargo check -p draft_model --locked</automated>
  </verify>
  <done>`draft_model` owns the Phase 1 command/result envelope and tests prove unknown command fields are rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command JSON -> Rust model | Untrusted command payloads deserialize into Rust-owned boundary types. |
| semantic crates -> service/runtime crates | Pure editor semantics must not depend on platform execution boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `CommandEnvelope` | mitigate | Apply serde `deny_unknown_fields` and negative tests before generated schema/types are added. |
| T-01-02 | Information disclosure | contract vocabulary | mitigate | Use stable Jianying-aligned concept names per D-07; avoid alternate aliases that leak conflicting semantics. |
| T-01-03 | Elevation of privilege | pure semantic crates | mitigate | Keep platform traits and process execution out of `draft_model`, `draft_commands`, and `engine_core` per D-17. |
| T-01-SC | Tampering | cargo dependency additions | mitigate | Use only 01-RESEARCH.md approved crates: `serde`, `serde_json`, `schemars`, and `ts-rs`. |
</threat_model>

<verification>
Run `cargo test -p draft_model contract -- --nocapture`, `cargo check -p draft_model --locked`, and the pure-crate dependency grep gate.
</verification>

<success_criteria>
- FOUND-01 has compile-safe pure crate shells.
- FOUND-02 has Rust-owned command contracts ready for Node-API bindings.
- TEST-01 has an initial Rust model validation gate for unknown-field rejection.
- D-05 through D-08 and D-17 are implemented or protected by explicit tests.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-golden-harness/01-02-SUMMARY.md` when done.
</output>
