---
phase: 01-foundation-and-golden-harness
plan: 07
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-05
files_modified:
  - crates/testkit/Cargo.toml
  - crates/testkit/src/lib.rs
  - crates/testkit/tests/render_smoke.rs
  - fixtures/media-generated/.gitkeep
  - goldens/README.md
autonomous: true
requirements:
  - FOUND-04
must_haves:
  truths:
    - "Tiny media is generated at test time with FFmpeg lavfi and no binary media is committed per D-14."
    - "Render smoke fails when FFmpeg or ffprobe are missing per D-15."
    - "Render smoke asserts output file existence and ffprobe metadata only per D-16."
    - "Golden fixture structure exists before later rendering work depends on it per D-13."
  artifacts:
    - path: "crates/testkit/src/lib.rs"
      provides: "Tiny media and metadata smoke helpers."
      exports: ["generate_tiny_lavfi_media", "run_tiny_render_smoke", "probe_media_metadata", "assert_tiny_smoke_metadata"]
    - path: "crates/testkit/tests/render_smoke.rs"
      provides: "Required lavfi render smoke."
    - path: "fixtures/media-generated/.gitkeep"
      provides: "Generated-media directory marker without binary media."
    - path: "goldens/README.md"
      provides: "Golden harness scope documentation."
  key_links:
    - from: "crates/testkit/tests/render_smoke.rs"
      to: "crates/media_runtime"
      via: "runtime discovery before FFmpeg execution"
      pattern: "discover_runtime_config|FfmpegExecutor|ffprobe"
    - from: "crates/testkit/src/lib.rs"
      to: "fixtures/media-generated"
      via: "generated media directory convention"
      pattern: "media-generated"
---

<objective>
Add the required tiny FFmpeg/ffprobe render smoke harness.

Purpose: Later preview and export work needs a deterministic media gate before real draft/render semantics exist.
Output: `testkit` lavfi generation helpers, ffprobe metadata assertions, generated-media directory marker, and golden scope docs.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-golden-harness/01-CONTEXT.md
@.planning/phases/01-foundation-and-golden-harness/01-RESEARCH.md
@.planning/phases/01-foundation-and-golden-harness/01-VALIDATION.md
@.planning/phases/01-foundation-and-golden-harness/01-PATTERNS.md
@.planning/phases/01-foundation-and-golden-harness/01-03-SUMMARY.md
@.planning/phases/01-foundation-and-golden-harness/01-05-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates the render-smoke subset of Phase 1 artifacts: generated-media directory marker, `goldens/README.md`, `generate_tiny_lavfi_media`, `run_tiny_render_smoke`, `probe_media_metadata`, `SmokeMetadata`, `assert_tiny_smoke_metadata`, and `crates/testkit/tests/render_smoke.rs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-W4-01: Generate tiny lavfi media during tests</name>
  <files>crates/testkit/Cargo.toml, crates/testkit/src/lib.rs, fixtures/media-generated/.gitkeep</files>
  <behavior>
    - Test media is generated in a temporary directory using FFmpeg `testsrc2` video and `sine` audio.
    - No MP4/MOV/WAV/AAC/PNG binary media is committed.
    - Missing FFmpeg or ffprobe causes the test helper to return a failing error.
  </behavior>
  <action>Implement generated-media helpers per D-13, D-14, and D-15. Use approved `tempfile` and existing runtime crates. Generate media inside a temp directory with FFmpeg lavfi inputs: `testsrc2` at 160x90, 10 fps, about one second, and `sine` audio around one second. Encode a temporary MP4 output for smoke validation. Use `media_runtime` discovery and `media_runtime_desktop` process execution; do not commit binary media, skip tests when FFmpeg is missing, use shell-concatenated FFmpeg strings, or add full draft/render goldens.</action>
  <verify>
    <automated>cargo test -p testkit generate_tiny -- --nocapture</automated>
    <automated>find fixtures goldens -type f \\( -name '*.mp4' -o -name '*.mov' -o -name '*.wav' -o -name '*.aac' -o -name '*.png' \\) | grep . && exit 1 || true</automated>
  </verify>
  <done>`testkit` generates temporary lavfi media and the repository contains no committed binary media fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-W4-02: Assert ffprobe metadata for the render smoke output</name>
  <files>crates/testkit/src/lib.rs, crates/testkit/tests/render_smoke.rs, goldens/README.md</files>
  <behavior>
    - Render smoke asserts output file existence.
    - Render smoke asserts approximate one-second duration, 160x90 resolution, 10 fps, and video/audio stream presence.
    - Render smoke does not use pixel or hash comparison.
  </behavior>
  <action>Add `probe_media_metadata`, `SmokeMetadata`, and `assert_tiny_smoke_metadata` helpers plus `crates/testkit/tests/render_smoke.rs` per D-15 and D-16. Use ffprobe JSON output to assert file existence, approximate duration, fps, resolution, and stream presence. Add `goldens/README.md` explaining that Phase 1 creates harness structure only and that full draft/render goldens are assigned to later phases. Do not add pixel comparison, image hashes, binary golden baselines, export presets, or preview cache behavior.</action>
  <verify>
    <automated>cargo test -p testkit render_smoke -- --nocapture</automated>
    <automated>grep -R "hash\\|pixel" crates/testkit/tests/render_smoke.rs goldens/README.md && exit 1 || true</automated>
  </verify>
  <done>Render smoke validates generated MP4 metadata through ffprobe and fails if FFmpeg/ffprobe are unavailable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testkit -> FFmpeg/ffprobe process | Test harness launches local binaries with generated lavfi inputs. |
| ffprobe JSON -> Rust assertions | External process metadata is parsed into Rust smoke assertions. |
| temp media -> repository fixtures | Generated media must not become committed source artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering/Elevation of privilege | render smoke FFmpeg invocation | mitigate | Use runtime executor argument arrays; no shell-concatenated commands or UI-authored FFmpeg strings. |
| T-01-02 | Denial of service | FFmpeg/ffprobe output | mitigate | Reuse bounded runtime error summaries from Plan 01-05. |
| T-01-03 | Tampering | binary media fixtures | mitigate | Verify no committed binary media under `fixtures` or `goldens`. |
| T-01-04 | Spoofing | FFmpeg/ffprobe candidates | mitigate | Use Plan 01-05 discovery and version probes before smoke execution. |
| T-01-SC | Tampering | cargo dependency additions | mitigate | Use only 01-RESEARCH.md approved `tempfile` and existing workspace dependencies. |
</threat_model>

<verification>
Run `cargo test -p testkit render_smoke -- --nocapture`, the binary-fixture find gate, and the pixel/hash grep gate.
</verification>

<success_criteria>
- FOUND-04 has a deterministic generated-media render smoke.
- D-13 through D-16 are implemented exactly.
- Missing FFmpeg/ffprobe fails rather than skips the Phase 1 render smoke.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-golden-harness/01-07-SUMMARY.md` when done.
</output>
