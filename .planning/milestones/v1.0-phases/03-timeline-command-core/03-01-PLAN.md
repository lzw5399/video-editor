---
phase: 03-timeline-command-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/draft_commands/Cargo.toml
  - crates/draft_commands/src/lib.rs
  - crates/draft_commands/src/error.rs
  - crates/draft_commands/src/selection.rs
  - crates/draft_commands/src/timeline.rs
  - crates/draft_commands/tests/timeline_model.rs
  - crates/draft_model/src/lib.rs
  - crates/draft_model/tests/schema_exports.rs
  - schemas/command.schema.json
  - apps/desktop-electron/src/generated/CommandEnvelope.ts
  - apps/desktop-electron/src/generated/CommandResultEnvelope.ts
autonomous: true
requirements:
  - TIME-01
  - TIME-02
  - TIME-06
  - TIME-07
must_haves:
  truths:
    - "The MVP root timeline remains `Draft.tracks` and supports video, audio, and text tracks without introducing multi-sequence semantics per D-01 and TIME-01."
    - "Track order is semantic: later visual tracks stack above earlier visual tracks, audio tracks mix in order, and `Track.muted` plus `Track.locked` stay persisted draft state per D-02 and TIME-07."
    - "Single-track segment overlap, zero-duration ranges, overflowed ranges, incompatible track/material pairs, locked-track mutation, and material-duration overrun are rejected before commit per D-03, D-10, D-11, D-13, and TIME-06."
    - "`draft_commands` is a pure semantic crate depending only on `draft_model` and contains no Electron, filesystem, FFmpeg, preview, render, or platform imports per D-05."
  artifacts:
    - path: crates/draft_commands/src/error.rs
      provides: "TimelineCommandError structured rejection variants"
    - path: crates/draft_commands/src/selection.rs
      provides: "TimelineSelection helpers keyed by SegmentId and TrackId"
    - path: crates/draft_commands/src/timeline.rs
      provides: "checked timerange math, track/material compatibility, no-overlap, stacking, and locked-track guards"
    - path: crates/draft_commands/tests/timeline_model.rs
      provides: "timeline_tracks, track_rules, and timerange_rules tests"
    - path: crates/draft_model/src/lib.rs
      provides: "Phase 3 base command state, selection, snapping, and response contracts"
  key_links:
    - from: crates/draft_commands/src/timeline.rs
      to: crates/draft_model/src/validation.rs
      via: "command-level validation followed by validate_draft"
      pattern: "validate_draft"
    - from: crates/draft_model/tests/schema_exports.rs
      to: schemas/command.schema.json
      via: "Rust-generated command schema and TypeScript contracts"
      pattern: "TimelineCommandResponse|CommandState|TimelineSelection"
---

<objective>
Create the Phase 3 timeline model and validation foundation: pure `draft_commands` dependency wiring, session/selection command contracts, overlap and stacking rules, checked timerange helpers, locked-track rejection, track/material compatibility, and atomic validation helpers.

Purpose: Later edit commands need one unambiguous Rust-owned foundation so the UI cannot become the timeline engine and invalid edits cannot mutate the draft partially. This plan implements TIME-01, TIME-02 foundation behavior, TIME-06 foundation behavior, and TIME-07 while honoring D-01 through D-13.

Output: `draft_commands` foundation modules, generated base command contracts, and focused Rust tests for timeline tracks, track rules, and timerange rules.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-timeline-command-core/03-CONTEXT.md
@.planning/phases/03-timeline-command-core/03-RESEARCH.md
@.planning/phases/03-timeline-command-core/03-VALIDATION.md
@.planning/phases/03-timeline-command-core/03-PATTERNS.md
@.planning/phases/02-draft-and-material-system/02-01-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-04-SUMMARY.md
@docs/runtime-boundaries.md
@crates/draft_model/src/lib.rs
@crates/draft_model/src/timeline.rs
@crates/draft_model/src/validation.rs
@crates/draft_model/tests/schema_exports.rs
@crates/draft_commands/Cargo.toml
@crates/draft_commands/src/lib.rs
</context>

## Artifacts this phase produces

- Files: `crates/draft_commands/Cargo.toml`, `crates/draft_commands/src/error.rs`, `crates/draft_commands/src/selection.rs`, `crates/draft_commands/src/timeline.rs`, `crates/draft_commands/tests/timeline_model.rs`.
- Contract symbols: `TimelineSelection`, `CommandState`, `CommandHistorySnapshot`, `TimelineCommandResponse`, `SnappingSettings`.
- Command foundation symbols: `TimelineCommandError`, `TimelineCommandErrorKind`, `checked_source_end`, `checked_target_end`, `target_ranges_overlap`, `validate_timeline_rules`, `validate_segment_material_bounds`, `validate_track_material_compatibility`, `validate_track_unlocked`, `visual_track_stack_order`, `audio_track_mix_order`, `main_video_track_id`.
- Generated paths: `schemas/command.schema.json`, `apps/desktop-electron/src/generated/CommandEnvelope.ts`, `apps/desktop-electron/src/generated/CommandResultEnvelope.ts`.
- Tests: `timeline_tracks`, `track_rules`, `timerange_rules`.

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 3 | Rust-owned timeline editing semantics and command/undo behavior | 03-01 to 03-04 | COVERED | Foundation starts here; command/edit/history/text/audio coverage follows in later plans. |
| REQ | TIME-01 | Root timeline supports video/audio/text tracks | 03-01 | COVERED | Uses root `Draft.tracks` per D-01. |
| REQ | TIME-02 | Add material segments with explicit source/target ranges | 03-01, 03-02 | COVERED | 03-01 creates validation foundation; 03-02 implements command. |
| REQ | TIME-06 | Invalid edits rejected atomically | 03-01, 03-02, 03-03, 03-04 | COVERED | 03-01 creates validation primitives and rejection errors. |
| REQ | TIME-07 | Track stacking/z-index and mute state represented | 03-01 | COVERED | Track order, mute, and lock rules are tested here. |
| RESEARCH | Pure semantic `draft_commands` with clone/validate/commit | 03-01 | COVERED | No platform/runtime imports; uses `validate_draft`. |
| CONTEXT | D-01 through D-13 | 03-01 | COVERED | Cited in task actions and acceptance criteria. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define base timeline command contracts and pure crate wiring</name>
  <read_first>
    @.planning/phases/03-timeline-command-core/03-CONTEXT.md
    @.planning/phases/03-timeline-command-core/03-RESEARCH.md
    @.planning/phases/03-timeline-command-core/03-PATTERNS.md
    @docs/runtime-boundaries.md
    @crates/draft_commands/Cargo.toml
    @crates/draft_commands/src/lib.rs
    @crates/draft_model/src/lib.rs
    @crates/draft_model/tests/schema_exports.rs
  </read_first>
  <files>crates/draft_commands/Cargo.toml, crates/draft_commands/src/lib.rs, crates/draft_commands/src/selection.rs, crates/draft_model/src/lib.rs, crates/draft_model/tests/schema_exports.rs, schemas/command.schema.json, apps/desktop-electron/src/generated/CommandEnvelope.ts, apps/desktop-electron/src/generated/CommandResultEnvelope.ts</files>
  <behavior>
    - `draft_commands` compiles as a pure semantic crate with `draft_model = { path = "../draft_model" }` and no platform/runtime dependencies.
    - `TimelineSelection` selects by `SegmentId` and `TrackId`, not renderer indices, per D-04 and D-06.
    - `CommandState` and `CommandHistorySnapshot` are generated Rust-owned session contracts and are not persisted draft fields per D-18, D-19, and D-20.
    - `TimelineCommandResponse` carries updated `Draft`, updated `CommandState`, `TimelineSelection`, and command events per D-09.
  </behavior>
  <action>
    Follow the explicit 03-PATTERNS.md analogs for this task: use the pure dependency pattern from `crates/bindings_node/Cargo.toml`, the command envelope/result contract pattern from `crates/draft_model/src/lib.rs`, and the generated export/drift pattern from `crates/draft_model/tests/schema_exports.rs`. Add the `draft_model` local dependency to `crates/draft_commands/Cargo.toml` and expose modules from `crates/draft_commands/src/lib.rs` for `error`, `selection`, and `timeline`. Add Rust-owned base command contracts in `crates/draft_model/src/lib.rs`: `TimelineSelection`, `CommandHistorySnapshot`, `CommandState`, `SnappingSettings`, and `TimelineCommandResponse`. Use `serde(rename_all = "camelCase", deny_unknown_fields)`, `JsonSchema`, and `TS` derives consistent with existing command contracts. Keep `CommandState` session-only and do not add it to `Draft`, `.veproj/project.json`, or `crates/draft_model/src/draft.rs` per D-19. Export all new contract types through `schema_exports.rs` and regenerate command schema/TypeScript from Rust, not by hand, per D-06, D-09, and D-27.
  </action>
  <verify>
    <automated>VE_UPDATE_GENERATED_CONTRACTS=1 cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>cargo check -p draft_commands --locked</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    `draft_commands` has exactly the local semantic dependency it needs: `draft_model`. Generated command contracts include `TimelineSelection`, `CommandState`, `CommandHistorySnapshot`, `SnappingSettings`, and `TimelineCommandResponse`. `Draft` has no persisted command history field. No TypeScript contract is hand-written.
  </acceptance_criteria>
  <done>Base contract artifacts compile, generated contract drift is clean, and the pure command crate boundary is intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement track, stacking, overlap, and timerange validation rules</name>
  <read_first>
    @.planning/phases/03-timeline-command-core/03-CONTEXT.md
    @.planning/phases/03-timeline-command-core/03-VALIDATION.md
    @.planning/phases/03-timeline-command-core/03-PATTERNS.md
    @crates/draft_model/src/timeline.rs
    @crates/draft_model/src/material.rs
    @crates/draft_model/src/time.rs
    @crates/draft_model/src/validation.rs
    @crates/draft_commands/src/lib.rs
  </read_first>
  <files>crates/draft_commands/src/error.rs, crates/draft_commands/src/timeline.rs, crates/draft_commands/tests/timeline_model.rs</files>
  <behavior>
    - `timeline_tracks` proves a root `Draft.tracks` fixture with video, audio, and text tracks is accepted without multi-sequence state per D-01 and TIME-01.
    - `track_rules` proves same-track overlaps and locked-track mutations are rejected, visual stack order is track order, audio mix order is track order, and track mute state remains persisted per D-02, D-03, and TIME-07.
    - `timerange_rules` proves checked integer microsecond math rejects zero-duration, overflowed source/target ranges, source ranges beyond known material duration, and incompatible track/material pairs per D-10 through D-13.
  </behavior>
  <action>
    Follow the explicit 03-PATTERNS.md analogs for this task: use `crates/draft_model/src/validation.rs` for tagged structured error/display style, `crates/draft_model/src/timeline.rs` for timeline model traversal, and `crates/draft_model/src/material.rs` for rollback/validation discipline generalized to clone/validate/return. Create `TimelineCommandError` and `TimelineCommandErrorKind` in `crates/draft_commands/src/error.rs` with structured variants for `TrackNotFound`, `SegmentNotFound`, `MaterialNotFound`, `LockedTrack`, `OverlappingSegment`, `IncompatibleTrackMaterialKind`, `SourceRangeExceedsMaterialDuration`, `TimerangeOverflow`, `ZeroDuration`, `InvalidSplitPoint`, `HistoryEmpty`, and `UnsupportedCommand`. Implement validation helpers in `crates/draft_commands/src/timeline.rs` that inspect only `Draft`, `Track`, `Segment`, `Material`, `SourceTimerange`, and `TargetTimerange`. Use `Microseconds::get()` plus `checked_add` for every range end calculation; never add naked `u64` timeranges without overflow checks. Enforce no overlap among segments on the same track, allow simultaneous overlay/text/audio only on separate tracks, reject mutating commands on locked tracks, validate material duration when present, and reject incompatible track/material kind pairs. Keep persisted concepts Jianying-aligned per D-04 and do not introduce `Asset`, `Clip`, or UI-only index vocabulary.
  </action>
  <verify>
    <automated>cargo test -p draft_commands timeline_tracks -- --nocapture</automated>
    <automated>cargo test -p draft_commands track_rules -- --nocapture</automated>
    <automated>cargo test -p draft_commands timerange_rules -- --nocapture</automated>
    <automated>bash -lc 'set -euo pipefail; ! rg -n "FfmpegExecutor|PlatformFileSystem|PreviewRenderer|ffmpeg|ffprobe|std::fs|std::process|napi|electron" crates/draft_commands/src crates/draft_commands/tests'</automated>
  </verify>
  <acceptance_criteria>
    Tests prove root `Draft.tracks` is the only MVP sequence. Same-track overlap, locked-track mutation, incompatible material/track kinds, zero-duration ranges, overflowed ranges, and known material-duration overruns are rejected with structured `TimelineCommandError` values. `draft_commands` contains no platform, filesystem, FFmpeg, preview, render, Node, or Electron imports.
  </acceptance_criteria>
  <done>Timeline foundation rules pass all focused tests and preserve pure semantic crate boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Command payload -> `draft_model` contracts | Untrusted renderer-originated JSON becomes typed Rust command/session contracts. |
| `draft_commands` validation -> draft mutation | Command helpers decide whether a timeline edit can proceed before any draft/state commit. |
| Draft material metadata -> source range validation | Existing material duration metadata constrains source ranges without probing files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Command payload contracts | mitigate | Use strict serde contracts with `deny_unknown_fields`, generated schema/TS, and command/payload pairing before execution. |
| T-03-02 | Tampering/DoS | SourceTimerange and TargetTimerange math | mitigate | Centralize checked integer microsecond end calculations and reject overflow, zero duration, and material-duration overrun. |
| T-03-03 | Tampering | Track mutation rules | mitigate | Reject locked-track mutation, incompatible track/material pairs, and same-track overlap before commit. |
| T-03-04 | Repudiation/Tampering | Rejected command history mutation | mitigate | This foundation defines errors and validation used before later plans push history; rejection tests compare draft/state unchanged. |
| T-03-05 | Elevation of Privilege/Tampering | Renderer bypass of Rust semantics | mitigate | Contracts and later routes expose Rust-owned timeline semantics only; no renderer direct `Draft.tracks` mutation path is introduced. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned. Stop for human legitimacy verification if a new dependency is proposed. |
</threat_model>

<verification>
<automated>cargo test -p draft_commands timeline_tracks -- --nocapture</automated>
<automated>cargo test -p draft_commands track_rules -- --nocapture</automated>
<automated>cargo test -p draft_commands timerange_rules -- --nocapture</automated>
<automated>cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- TIME-01, TIME-06 foundation behavior, and TIME-07 are test-covered in Rust.
- TIME-02 has validated source/target/material-range foundation ready for add/move/split/trim commands.
- `draft_commands` remains pure and owns semantic validation rules.
- Generated command contracts expose base session/selection/response types from Rust.
</success_criteria>

<output>
Create `.planning/phases/03-timeline-command-core/03-01-SUMMARY.md` when done.
</output>
