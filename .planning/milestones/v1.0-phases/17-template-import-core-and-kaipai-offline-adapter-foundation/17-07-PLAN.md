---
phase: 17-template-import-core-and-kaipai-offline-adapter-foundation
plan: "07"
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - crates/ffmpeg_compiler/src/lib.rs
  - crates/ffmpeg_compiler/tests/transform_snapshots.rs
  - crates/testkit/tests/preview_export_parity.rs
autonomous: true
requirements:
  - PRODFX-05
  - NO-FALLBACK-01
requirements_addressed:
  - PRODFX-05
  - NO-FALLBACK-01
must_haves:
  truths:
    - "Static center-anchor rotation has preview/export parity before rotated imports are classified as supported, per D-26, D-28."
    - "Rotation parity is implemented generically in export/compiler semantics, not as a Kaipai placement hack."
    - "Preview/export evidence checks visual rotation/layer ordering, not just non-empty output."
  artifacts:
    - path: "crates/ffmpeg_compiler/src/lib.rs"
      provides: "Generic static rotation export support"
    - path: "crates/ffmpeg_compiler/tests/transform_snapshots.rs"
      provides: "Rotation filtergraph snapshots"
    - path: "crates/testkit/tests/preview_export_parity.rs"
      provides: "Preview/export rotation parity evidence"
  key_links:
    - from: "crates/ffmpeg_compiler/src/lib.rs"
      to: "crates/realtime_preview_runtime/src/gpu/compositor.rs"
      via: "shared center-anchor transform semantics"
      pattern: "rotation"
---

<objective>
Close the generic static export rotation parity gap before imported rotated overlays are accepted as supported.

Purpose: Phase 17 fixtures include transform-heavy templates; if preview rotates but export does not, product evidence is false.
Output: generic FFmpeg compiler rotation support and parity tests for static center-anchor transforms.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-RESEARCH.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-PATTERNS.md
@crates/draft_model/src/timeline.rs
@crates/ffmpeg_compiler/tests/transform_snapshots.rs
@crates/testkit/tests/preview_export_parity.rs
@docs/no-product-fallback-policy.md
</context>

## Artifacts This Phase Produces

- Generic export behavior: center-anchor static rotation in the FFmpeg compiler
- Tests: transform snapshots and preview/export parity assertions for rotation

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generic rotation export parity tests</name>
  <files>crates/ffmpeg_compiler/tests/transform_snapshots.rs, crates/testkit/tests/preview_export_parity.rs</files>
  <read_first>
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-RESEARCH.md` Open Question 1 and Pitfall 4.
    - `crates/draft_model/src/timeline.rs` `SegmentTransform`, `SegmentAnchor`, and `SegmentRotation`.
    - Existing transform snapshot tests in `crates/ffmpeg_compiler/tests/transform_snapshots.rs`.
  </read_first>
  <behavior>
    - Test a static rotated image/video overlay compiles without unsupported-rotation diagnostics.
    - Test the filtergraph applies rotation around the generic center anchor and preserves overlay layer order.
    - Test preview/export parity samples pixels that would fail if export ignored rotation.
  </behavior>
  <action>Add failing tests for generic static rotation export parity. The tests must use canonical `SegmentTransform` and `SegmentAnchor` values and must not mention Kaipai provider fields. Include a regression assertion that export diagnostics no longer classify supported static rotation as unsupported.</action>
  <verify>
    <automated>cargo test -p ffmpeg_compiler transform -- --nocapture && cargo test -p testkit preview_export_parity -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Rotation tests fail on the current unsupported export behavior and pass after Task 2.
    - Test evidence catches ignored rotation, wrong anchor, and wrong layer ordering.
    - Test names and fixtures use generic transform terminology only.
  </acceptance_criteria>
  <done>Static rotation parity has executable regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement generic static rotation in export compiler</name>
  <files>crates/ffmpeg_compiler/src/lib.rs</files>
  <read_first>
    - `crates/ffmpeg_compiler/src/lib.rs` transform/filtergraph generation code.
    - `crates/realtime_preview_runtime/src/gpu/compositor.rs` rotation math for conceptual parity.
    - `crates/draft_model/src/canvas.rs` normalized/pixel coordinate helpers.
  </read_first>
  <action>Implement generic static rotation export support for supported image/video overlays using canonical center-anchor semantics. Update compiler diagnostics so supported static rotation is not reported as unsupported, while unsupported animated rotation or complex transform curves remain classified explicitly. Keep the implementation in generic FFmpeg compiler code and avoid adapter/provider special cases.</action>
  <verify>
    <automated>cargo test -p ffmpeg_compiler transform -- --nocapture && cargo test -p testkit preview_export_parity -- --nocapture && pnpm run test:phase17-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - Static rotation export passes transform snapshots and parity tests.
    - Unsupported animated/complex rotation cases still produce explicit diagnostics.
    - No Kaipai/provider code enters `ffmpeg_compiler`.
  </acceptance_criteria>
  <done>Generic static rotation export parity is closed for the Phase 17 supported subset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical draft transform -> export filtergraph | Canonical edit semantics become FFmpeg execution intent. |
| preview parity evidence -> product acceptance | Test evidence determines whether imported rotated content is supported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-25 | Tampering | Export transform compiler | mitigate | Snapshot and parity tests verify rotation angle, center anchor, and layer order. |
| T-17-26 | Repudiation | Rotation support classification | mitigate | Compiler diagnostics distinguish supported static rotation from unsupported animated/complex cases. |
| T-17-27 | Tampering | Provider-specific workaround | mitigate | Source guards and tests require generic draft transform semantics only. |
| T-17-28 | Information Disclosure | Export artifacts | accept | Test artifacts stay in temp/test-results paths and contain generated fixture media only. |
</threat_model>

<verification>
Run:
- `cargo test -p ffmpeg_compiler transform -- --nocapture`
- `cargo test -p testkit preview_export_parity -- --nocapture`
- `pnpm run test:phase17-source-guards`
</verification>

<success_criteria>
The plan is complete when supported static rotation is rendered consistently in preview/export without provider-specific hacks or fallback evidence.
</success_criteria>

<output>
Create `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-07-SUMMARY.md` when done.
</output>
