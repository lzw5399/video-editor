---
phase: 17-template-import-core-and-kaipai-offline-adapter-foundation
plan: "04"
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - Cargo.toml
  - crates/adapter_kaipai/Cargo.toml
  - crates/adapter_kaipai/src/lib.rs
  - crates/adapter_kaipai/src/error.rs
  - crates/adapter_kaipai/src/formula_bundle.rs
  - crates/adapter_kaipai/tests/formula_bundle_contract.rs
  - crates/adapter_kaipai/tests/fixtures.rs
  - crates/adapter_kaipai/tests/schema_exports.rs
  - fixtures/kaipai/positive/sanitized-formula-bundle.json
  - fixtures/kaipai/positive/sanitized-formula-with-direct-materials.json
  - fixtures/kaipai/negative/unknown-top-level-field.json
  - fixtures/kaipai/negative/unsafe-formula-evidence.json
  - schemas/kaipai-formula-bundle.schema.json
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - NO-FALLBACK-02
requirements_addressed:
  - COMP-01
  - COMP-02
  - NO-FALLBACK-02
must_haves:
  truths:
    - "Kaipai exists only as an offline external adapter crate, per D-01, D-04, D-05, D-08, D-09, D-10, D-35."
    - "Offline bundle validation rejects unsafe provider evidence and preserves provenance only for reports, per D-06, D-16, D-32."
    - "Old branch assets are inspected as references, not merged as a compatibility layer."
  artifacts:
    - path: "crates/adapter_kaipai/src/formula_bundle.rs"
      provides: "KaipaiFormulaBundle strict offline input contract"
    - path: "schemas/kaipai-formula-bundle.schema.json"
      provides: "Generated offline bundle schema"
    - path: "fixtures/kaipai/positive/sanitized-formula-bundle.json"
      provides: "Sanitized source fixture"
  key_links:
    - from: "crates/adapter_kaipai/src/formula_bundle.rs"
      to: "crates/draft_import/src/adaptation_report.rs"
      via: "adapter diagnostics become provider-neutral AdaptationReport items"
      pattern: "AdaptationReport"
---

<objective>
Port the offline Kaipai input contract and sanitized fixture foundation into current main.

Purpose: Phase 17 needs a real offline adapter input boundary before mapping resources or draft semantics, but the old integration layer must not be merged into current product architecture.
Output: `adapter_kaipai` crate with strict bundle parsing/validation, sanitized fixtures, schema export, and focused tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-RESEARCH.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-PATTERNS.md
@KAIPAI_FORMULA_ADAPTER_RESEARCH.md
Reference only via git:
- `origin/work/kaipai-adapter-poc:crates/adapter_kaipai/src/formula_bundle.rs`
- `origin/work/kaipai-adapter-poc:crates/adapter_kaipai/tests/fixtures.rs`
- `origin/work/kaipai-adapter-poc:schemas/kaipai-formula-bundle.schema.json`
</context>

## Artifacts This Phase Produces

- Crate: `crates/adapter_kaipai`
- Rust structs/enums/functions: `KaipaiFormulaBundle`, `FormulaBundleSchemaVersion`, `FormulaBundleKind`, `FormulaProvenance`, `FormulaResourceRef`, `DirectMaterialRef`, `SafeAreaEvidence`, `AdapterKaipaiError`, `KaipaiFormulaBundle::from_json_str`, `KaipaiFormulaBundle::validate`
- Schema/fixtures: `schemas/kaipai-formula-bundle.schema.json`, sanitized positive/negative fixture corpus

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create current-main adapter crate and fixture tests</name>
  <files>Cargo.toml, crates/adapter_kaipai/Cargo.toml, crates/adapter_kaipai/src/lib.rs, crates/adapter_kaipai/src/error.rs, crates/adapter_kaipai/tests/formula_bundle_contract.rs, crates/adapter_kaipai/tests/fixtures.rs, fixtures/kaipai/positive/sanitized-formula-bundle.json, fixtures/kaipai/positive/sanitized-formula-with-direct-materials.json, fixtures/kaipai/negative/unknown-top-level-field.json, fixtures/kaipai/negative/unsafe-formula-evidence.json</files>
  <read_first>
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md` decisions D-08, D-09, D-10, D-34, D-35.
    - `Cargo.toml` workspace member pattern.
    - Reference only: `git show origin/work/kaipai-adapter-poc:crates/adapter_kaipai/Cargo.toml`.
    - Reference only: `git ls-tree -r --name-only origin/work/kaipai-adapter-poc -- fixtures/kaipai`.
  </read_first>
  <behavior>
    - Test fixture JSON parsing accepts sanitized positive samples.
    - Test unknown fields and unsafe evidence are rejected.
    - Test fixture scanner rejects credential-like keys and remote/signed URL strings.
  </behavior>
  <action>Create `adapter_kaipai` as a workspace member using current workspace package policy. Copy only the useful sanitized fixture ideas and contract shape from the old branch by inspection; rewrite the crate files in current-main style. The crate must depend on `draft_import` for reporting boundaries, not on core/render crates for provider formula interpretation.</action>
  <verify>
    <automated>cargo test -p adapter_kaipai formula_bundle -- --nocapture && pnpm run test:phase17-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - `adapter_kaipai` compiles as an external adapter crate.
    - Positive sanitized fixtures parse through the new crate.
    - Negative fixtures reject unknown top-level fields and unsafe evidence.
    - No old branch integration file is merged wholesale.
  </acceptance_criteria>
  <done>Offline adapter crate and fixture tests exist on current main.</done>
</task>

<task type="auto">
  <name>Task 2: Implement strict KaipaiFormulaBundle parsing and schema export</name>
  <files>crates/adapter_kaipai/src/formula_bundle.rs, crates/adapter_kaipai/tests/schema_exports.rs, schemas/kaipai-formula-bundle.schema.json</files>
  <read_first>
    - `crates/draft_import/src/adaptation_report.rs` from Plan 17-01.
    - Reference only: `git show origin/work/kaipai-adapter-poc:crates/adapter_kaipai/src/formula_bundle.rs`.
    - Reference only: `git show origin/work/kaipai-adapter-poc:crates/adapter_kaipai/tests/schema_exports.rs`.
  </read_first>
  <action>Implement strict `KaipaiFormulaBundle` parsing with serde `deny_unknown_fields`, explicit schema version/kind/provenance/source media/direct materials/resources/formula evidence fields, and validation that rejects unsafe provider evidence. Generate `schemas/kaipai-formula-bundle.schema.json` from Rust. Keep `safe_area` and provider IDs as adapter input/provenance only; do not expose them as canonical render semantics.</action>
  <verify>
    <automated>cargo test -p adapter_kaipai formula_bundle -- --nocapture && cargo test -p adapter_kaipai schema_exports -- --nocapture && pnpm run test:phase17-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - Strict parser accepts sanitized fixture families and rejects unsafe or unknown input.
    - Schema is generated from Rust and committed.
    - Provider IDs, raw formula JSON, recognizer output, and safe-area evidence remain adapter-only fields.
    - `pnpm run test:phase17-source-guards` passes.
  </acceptance_criteria>
  <done>Kaipai offline bundle contract is strict, tested, and schema-backed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline JSON fixture -> `adapter_kaipai` | Untrusted provider data enters the repo-owned adapter boundary. |
| adapter provenance -> report | External IDs and evidence may be shown to users or tests as diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | Tampering | Bundle parser | mitigate | Strict serde contracts and schema tests reject unknown and unsafe input. |
| T-17-14 | Information Disclosure | Fixture corpus | mitigate | Fixture scanner rejects credentials, account IDs, cookies, signed URL shapes, and remote provider evidence. |
| T-17-15 | Tampering | Old branch reuse | mitigate | Read old branch through `git show` only; current-main crate is rewritten and source guards block legacy integration leakage. |
| T-17-16 | Repudiation | Native effects in input | mitigate | Parser preserves evidence for report classification; mapper plan must classify native effects as `needsNativeEffect`. |
</threat_model>

<verification>
Run:
- `cargo test -p adapter_kaipai formula_bundle -- --nocapture`
- `cargo test -p adapter_kaipai schema_exports -- --nocapture`
- `pnpm run test:phase17-source-guards`
</verification>

<success_criteria>
The plan is complete when sanitized offline Kaipai bundles can be parsed/validated and no old integration behavior enters current-main product architecture.
</success_criteria>

<output>
Create `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-04-SUMMARY.md` when done.
</output>
