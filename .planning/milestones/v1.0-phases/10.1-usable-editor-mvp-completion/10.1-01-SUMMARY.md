---
phase: "10.1-usable-editor-mvp-completion"
plan: "01"
subsystem: "desktop-import"
status: complete
tags:
  - electron
  - import
  - command-boundary
key-files:
  - "apps/desktop-electron/src/main/index.ts"
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx"
  - "apps/desktop-electron/tests/electron-smoke.spec.ts"
  - "apps/desktop-electron/tests/workspace.spec.ts"
---

# Summary

Implemented system file chooser import for desktop materials.

## Changes

- Added `platform:openMaterialFiles` in Electron main with sender validation and video/audio/image filters.
- Exposed `window.videoEditorPlatform.openMaterialFiles()` through preload, separate from `window.videoEditorCore`.
- Updated `导入素材` to open the system chooser and then call existing `importMaterial` for each selected file.
- Kept `导入路径` as a secondary fallback for no-mock automation and advanced local testing.
- Added a default `/tmp/video-editor-workspace.veproj` bundle path for blank startup to avoid writing `project.json` into the app directory.

## Verification

- `pnpm --filter @video-editor/desktop build`
- `pnpm --filter @video-editor/desktop exec playwright test tests/electron-smoke.spec.ts`
- `pnpm --filter @video-editor/desktop test:workspace`
- `pnpm run test:phase4-source-guards`
- `pnpm run test:phase5-source-guards`
- `pnpm run test:contracts`

## Deviations

- `copySegment` remains deferred because no Rust/generated command exists. Renderer-side copy was not implemented.

## Self-Check

PASSED

