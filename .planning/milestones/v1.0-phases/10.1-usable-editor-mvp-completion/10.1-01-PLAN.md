---
phase: "10.1-usable-editor-mvp-completion"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/desktop-electron/src/main/index.ts"
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - MVPEDIT-01
  - MVPEDIT-02
  - MVPEDIT-08
user_setup: []
---

<objective>
Replace manual material path entry as the primary UX with an Electron main/preload system file chooser that imports selected video/audio/image files through `importMaterial`.
</objective>

<context>
@AGENTS.md
@.planning/phases/10.1-usable-editor-mvp-completion/10.1-CONTEXT.md
@.planning/phases/10.1-usable-editor-mvp-completion/10.1-RESEARCH.md
@apps/desktop-electron/src/main/index.ts
@apps/desktop-electron/src/preload/index.ts
@apps/desktop-electron/src/renderer/App.tsx
@apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10.1-01-01: Add narrow platform file chooser bridge</name>
  <files>apps/desktop-electron/src/main/index.ts, apps/desktop-electron/src/preload/index.ts, apps/desktop-electron/tests/electron-smoke.spec.ts</files>
  <action>Add `dialog:openMaterialFiles` in main using Electron `dialog.showOpenDialog` with allowed sender validation, multi-selection, and filters for supported video/audio/image extensions. Expose it in preload as `window.videoEditorPlatform.openMaterialFiles()`. Do not add it to `videoEditorCore`.</action>
  <acceptance_criteria>Main/preload expose only a narrow file picker API, renderer still has no `ipcRenderer`, and existing `videoEditorCore` key shape stays unchanged.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test --grep "typed preload bridge"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 10.1-01-02: Import selected files through Rust command</name>
  <files>apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Change `导入素材` to open the platform file chooser and call existing `importMaterial` for each returned path. Preserve command result application and material diagnostics from Rust responses. Keep manual path controls only as secondary/debug fallback if still needed by existing real-workflow tests.</action>
  <acceptance_criteria>Clicking `导入素材` can import a selected fixture video without typing a path; material list shows available metadata.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test:workspace -g "导入素材|material import"</automated>
  </verify>
</task>

</tasks>

<verification>
<automated>pnpm --filter @video-editor/desktop build</automated>
<automated>pnpm --filter @video-editor/desktop test:workspace -g "导入素材|material import"</automated>
</verification>

<success_criteria>
- `导入素材` uses system chooser path through main/preload.
- Renderer calls `importMaterial`; it does not access Node/Electron or mutate draft material state directly.
- Existing material list displays imported file status and metadata.
</success_criteria>

<output>
Create `.planning/phases/10.1-usable-editor-mvp-completion/10.1-01-SUMMARY.md` when done.
</output>

