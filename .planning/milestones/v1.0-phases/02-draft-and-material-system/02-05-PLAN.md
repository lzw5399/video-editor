---
phase: 02-draft-and-material-system
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - crates/draft_model/src/lib.rs
  - crates/draft_model/tests/schema_exports.rs
  - crates/bindings_node/src/lib.rs
  - crates/bindings_node/tests/binding_smoke.rs
  - schemas/command.schema.json
  - apps/desktop-electron/src/generated/CommandEnvelope.ts
  - apps/desktop-electron/src/generated/CommandResultEnvelope.ts
  - apps/desktop-electron/src/renderer/App.tsx
  - apps/desktop-electron/tests/electron-smoke.spec.ts
autonomous: true
requirements:
  - MAT-01
  - MAT-02
  - MAT-03
  - MAT-04
must_haves:
  truths:
    - "Electron can request material import/list/missing-material behavior through generated Rust-owned command contracts per D-10, D-18, and D-19."
    - "Binding command handlers call the binding-facing material service from Plan 02-04 and do not construct ffprobe/FFmpeg process commands in Electron per D-12."
    - "The Electron smoke surface displays imported material metadata through generated contracts per MAT-03 without adding rich material-bin UI or timeline editing."
    - "Missing-material diagnostics are returned through standardized command envelopes and preserve draft entries per D-14, D-15, D-16, and MAT-04."
  artifacts:
    - path: crates/draft_model/src/lib.rs
      provides: "Phase 2 material command payload/result contract variants"
    - path: crates/bindings_node/src/lib.rs
      provides: "execute_command routing to material_service"
    - path: crates/bindings_node/tests/binding_smoke.rs
      provides: "binding smoke for import/list/missing material commands"
    - path: apps/desktop-electron/src/renderer/App.tsx
      provides: "smoke-level material metadata display"
    - path: apps/desktop-electron/tests/electron-smoke.spec.ts
      provides: "Playwright smoke asserting material metadata crosses the bridge"
  key_links:
    - from: crates/bindings_node/src/lib.rs
      to: crates/bindings_node/src/material_service.rs
      via: "importMaterial/listMaterials/listMissingMaterials command handlers"
      pattern: "importMaterial|listMaterials|listMissingMaterials"
    - from: apps/desktop-electron/src/renderer/App.tsx
      to: apps/desktop-electron/src/generated/CommandEnvelope.ts
      via: "generated command contract import"
      pattern: "CommandEnvelope"
---

<objective>
Expose Phase 2 material commands through the Rust-owned binding contract and prove metadata reaches the Electron smoke surface.

Purpose: Connect the tested material service to the existing `execute_command` bridge while keeping Electron as a smoke consumer only. This implements MAT-03 and reinforces MAT-01, MAT-02, and MAT-04 while honoring D-10, D-11, D-12, D-16, D-18, and D-19.

Output: Generated material command contracts, binding route handlers, binding smoke tests, and a smoke-level Electron material metadata row.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-draft-and-material-system/02-CONTEXT.md
@.planning/phases/02-draft-and-material-system/02-RESEARCH.md
@.planning/phases/02-draft-and-material-system/02-VALIDATION.md
@.planning/phases/02-draft-and-material-system/02-PATTERNS.md
@.planning/phases/02-draft-and-material-system/02-04-SUMMARY.md
@crates/draft_model/src/lib.rs
@crates/draft_model/tests/schema_exports.rs
@crates/bindings_node/src/lib.rs
@crates/bindings_node/tests/binding_smoke.rs
@apps/desktop-electron/src/renderer/App.tsx
@apps/desktop-electron/tests/electron-smoke.spec.ts
</context>

## Artifacts this phase produces

- `importMaterial`, `listMaterials`, and `listMissingMaterials` command payload/result variants or equivalent Rust-owned command names.
- Updated `schemas/command.schema.json`, `CommandEnvelope.ts`, and `CommandResultEnvelope.ts` generated from Rust.
- `bindings_node` route handlers that delegate to the Plan 02-04 material service.
- Electron smoke material row showing display name, material kind, duration microseconds, dimensions or audio detail, and missing/probe status.

<tasks>

<task type="auto">
  <name>Task 1: Add material command contracts and binding routes</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
    @.planning/phases/02-draft-and-material-system/02-PATTERNS.md
    @crates/draft_model/src/lib.rs
    @crates/draft_model/tests/schema_exports.rs
    @crates/bindings_node/src/lib.rs
    @crates/bindings_node/tests/binding_smoke.rs
  </read_first>
  <files>crates/draft_model/src/lib.rs, crates/draft_model/tests/schema_exports.rs, crates/bindings_node/src/lib.rs, crates/bindings_node/tests/binding_smoke.rs, schemas/command.schema.json, apps/desktop-electron/src/generated/CommandEnvelope.ts, apps/desktop-electron/src/generated/CommandResultEnvelope.ts</files>
  <action>
    Add generated command payload/result types for `importMaterial`, `listMaterials`, and `listMissingMaterials` or equivalent Rust-owned material operations. Keep command payloads strict and generated through `schema_exports.rs`; do not hand-write TypeScript contracts per D-18. Route `execute_command` in `bindings_node` through the Plan 02-04 material service. The binding may accept bundle paths, material paths/URIs, and serialized `Draft` payloads, but must not decide draft semantics, call ffprobe directly, or mutate `project.json` outside the service plus `project_store` persistence path. Map material-service, project-store, and media-runtime errors into structured `CommandResultEnvelope` errors with stable `CommandErrorKind` names such as `InvalidProject`, `ProjectIoFailed`, `MaterialProbeFailed`, or `MissingMaterial`. Update binding smoke tests for import/list/missing-material paths and ensure unsupported command behavior remains stable.
  </action>
  <verify>
    <automated>VE_UPDATE_GENERATED_CONTRACTS=1 cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>cargo test -p bindings_node -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    Generated command schema/TS include Phase 2 material commands. `bindings_node` tests prove import/list/missing-material commands travel through `execute_command` and return standardized ok/error/events envelopes. No binding route calls ffprobe directly or writes `.veproj/project.json` outside the service plus project-store persistence path.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add Electron material metadata smoke display</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
    @.planning/phases/02-draft-and-material-system/02-PATTERNS.md
    @apps/desktop-electron/src/renderer/App.tsx
    @apps/desktop-electron/tests/electron-smoke.spec.ts
    @apps/desktop-electron/src/generated/CommandEnvelope.ts
    @apps/desktop-electron/src/generated/Draft.ts
  </read_first>
  <files>apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/tests/electron-smoke.spec.ts</files>
  <action>
    Extend the Electron smoke surface to render material rows from generated `Material` metadata returned by the Rust command path, including display name, material kind, duration microseconds, dimensions or audio stream detail, and missing/probe status. Keep the UI smoke limited to proving material metadata is visible through the bridge per D-19; do not add timeline editing, rich relink UI, preview playback, render graph behavior, export behavior, or direct project JSON mutation. Update Playwright smoke to assert the material row displays metadata from generated contracts and that `ffmpeg`/`ffprobe` process strings remain absent from renderer source.
  </action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test</automated>
    <automated>grep -R "ffmpeg\|ffprobe" apps/desktop-electron/src && exit 1 || true</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    Electron smoke displays material metadata in the material bin through generated contracts. Source grep proves renderer/main UI code does not construct FFmpeg/ffprobe commands or mutate `.veproj/project.json` directly.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Electron/main binding -> Rust command contract | Renderer-originated command payloads request native material operations through generated contracts. |
| binding route -> material_service | Binding delegates import/list/missing-material behavior to the tested Rust service. |
| Rust binding -> Electron renderer | Generated material metadata crosses into smoke UI display. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | Elevation of Privilege | Renderer-native bridge | mitigate | Renderer uses generated preload command API only; no renderer FFmpeg/process/filesystem access. |
| T-02-20 | Tampering | Material command payloads | mitigate | Strict Rust command payloads and generated schema/TS prevent hand-written IPC drift. |
| T-02-21 | Tampering | Binding route responsibility | mitigate | Route through material_service and reject direct ffprobe/project JSON mutation in Electron and route code. |
| T-02-22 | Information Disclosure | Material metadata display | mitigate | Smoke UI displays only normalized metadata/status returned by Rust, not raw ffprobe JSON or process logs. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned in this binding/Electron smoke pass. Stop for human legitimacy verification if a new dependency is proposed. |
</threat_model>

<verification>
Run:

<automated>cargo test -p bindings_node -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop test</automated>
<automated>grep -R "ffmpeg\|ffprobe" apps/desktop-electron/src && exit 1 || true</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- MAT-03 is satisfied by smoke-level material metadata display through generated contracts.
- MAT-01, MAT-02, and MAT-04 are exposed through standardized Rust-owned command envelopes.
- D-12 is preserved because Electron code does not construct FFmpeg/ffprobe process commands.
- D-19 is preserved because Electron remains a smoke surface, while Rust tests remain the key acceptance proof.
</success_criteria>

<output>
Create `.planning/phases/02-draft-and-material-system/02-05-SUMMARY.md` when done.
</output>
