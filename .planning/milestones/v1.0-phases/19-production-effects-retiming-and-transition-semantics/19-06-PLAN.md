---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "06"
type: execute
wave: 4
depends_on:
  - "19-04"
files_modified:
  - crates/draft_model/src/effects.rs
  - crates/draft_model/src/timeline.rs
  - crates/draft_commands/src/transition.rs
  - crates/draft_commands/src/timeline.rs
  - crates/draft_commands/src/error.rs
  - crates/draft_commands/src/lib.rs
  - crates/draft_commands/tests/transition_commands.rs
autonomous: true
requirements:
  - PRODFX-02
must_haves:
  truths:
    - "Transitions are first-class adjacent/overlap relationships, not renderer-created segment-local deltas."
    - "Transition commands validate adjacency, overlap, trim, split, move, snapping, main-track magnet, and undo/redo."
    - "Transition edits fail atomically and produce exactly one undo snapshot when committed."
  prohibitions:
    - "Renderer transition duration or overlap validation is forbidden."
    - "Private provider transition IDs cannot become internal transition semantics."
  artifacts:
    - path: "crates/draft_commands/src/transition.rs"
      provides: "Undoable transition relationship commands"
    - path: "crates/draft_commands/tests/transition_commands.rs"
      provides: "Transition command and validation coverage"
  key_links:
    - from: "crates/draft_commands/src/transition.rs"
      to: "crates/draft_commands/src/timeline.rs"
      via: "execute_timeline_edit routes transition payloads through Rust validation"
      pattern: "AddTransition|UpdateTransitionDuration|RemoveTransition"
---

<objective>
Implement first-class transition model and command semantics.

Purpose: Make the PRODFX-02 edit contract real before graph, preview, compiler, or UI layers consume transitions.
Output: Transition relationship types, undoable commands, validation, and green command tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/draft_model/src/timeline.rs
@crates/draft_commands/src/timeline.rs
@crates/draft_commands/tests/transition_commands.rs
</context>

## Artifacts this phase produces

- `TrackTransition` or equivalent adjacency relationship type
- `draft_commands::transition::{add_transition, update_transition_duration, remove_transition, validate_transition_relationships}`
- `TimelineEditPayload::{AddTransition, UpdateTransitionDuration, RemoveTransition}`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add transition relationship model</name>
  <read_first>@crates/draft_model/src/effects.rs @crates/draft_model/src/timeline.rs @crates/draft_commands/tests/transition_commands.rs</read_first>
  <files>crates/draft_model/src/effects.rs, crates/draft_model/src/timeline.rs, crates/draft_commands/tests/transition_commands.rs</files>
  <behavior>
    - Transition relationship references adjacent segment IDs, typed transition kind, duration, and parameters.
    - Segment-local legacy transition forms are migrated or rejected as external/degraded report semantics.
    - Transition duration uses integer microseconds and rejects impossible overlap windows.
  </behavior>
  <action>Add a transition relationship model in `draft_model` that references adjacent segment IDs and typed transition kind/duration/parameters. Update Wave 0 transition tests to target the relationship model instead of string-only segment-local transitions. Keep external provider transition names out of first-party transition kinds.</action>
  <acceptance_criteria>Transition model tests prove typed relationship shape, integer duration, and external-reference classification.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands transition_commands -- --nocapture</automated>
  </verify>
  <done>Transition relationships exist as Rust draft semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transition commands and validation</name>
  <read_first>@crates/draft_commands/src/timeline.rs @crates/draft_commands/src/error.rs @crates/draft_commands/tests/transition_commands.rs</read_first>
  <files>crates/draft_commands/src/transition.rs, crates/draft_commands/src/timeline.rs, crates/draft_commands/src/error.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/tests/transition_commands.rs</files>
  <behavior>
    - Adding a dissolve transition requires adjacent compatible visual segments on the same timeline relationship.
    - Updating duration validates available overlap and affected trims without renderer decisions.
    - Split, trim, move, delete, snapping, and main-track magnet either preserve valid transitions or reject/clean invalid relationships atomically.
  </behavior>
  <action>Create `draft_commands/src/transition.rs` and route add/update/remove payloads through `execute_timeline_edit`. Extend timeline validation so segment edits cannot leave dangling or impossible transition windows. Convert Wave 0 transition tests to green and cover undo/redo with one snapshot per committed transition command.</action>
  <acceptance_criteria>Transition command tests prove adjacency validation, duration bounds, atomic invalid edits, undo/redo, and interaction with trim/split/move rules.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands transition_commands -- --nocapture</automated>
  </verify>
  <done>Transitions are Rust-owned adjacent relationships with undoable commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/API to transition command | Transition duration and boundary requests are untrusted until Rust validates adjacency. |
| Draft command validation | Segment edits may invalidate relationship windows and must fail atomically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Tampering | `draft_commands::transition` | mitigate | Validate segment ownership, adjacency, track locks, duration bounds, and overlap windows in Rust. |
| T-19-14 | Repudiation | transition command history | mitigate | Command tests require one undo snapshot per committed transition edit and no mutation on failure. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run `cargo test -p draft_commands transition_commands -- --nocapture`.
</verification>

<success_criteria>
PRODFX-02 command semantics are complete when transitions are first-class Rust relationships with validation, undo/redo, and atomic failure behavior.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-06-SUMMARY.md` when done.
</output>
