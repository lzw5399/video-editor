---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "14"
type: execute
wave: 12
depends_on:
  - "19-13"
files_modified:
  - crates/adapter_kaipai/src/mapper.rs
  - crates/testkit/tests/production_effects_preview.rs
  - crates/testkit/tests/production_effects_exports.rs
  - crates/testkit/tests/template_import_preview.rs
  - crates/testkit/tests/template_import_exports.rs
  - apps/desktop-electron/tests/template-import.spec.ts
  - apps/desktop-electron/tests/production-effects.spec.ts
  - scripts/phase19-source-guards.sh
autonomous: true
requirements:
  - PRODFX-01
  - PRODFX-02
  - PRODFX-03
  - PRODFX-04
  - PRODFX-05
must_haves:
  truths:
    - "Kaipai/Jianying-like fixtures map supported external concepts onto first-party retime/effect/transition semantics and report unsupported/private concepts explicitly."
    - "Template preview/export parity is proven through real render graph GPU preview and compiler/export evidence with no fallback success."
    - "Performance budgets for production editing fixtures are executable and tied to existing scheduler/preview/export telemetry."
  prohibitions:
    - "No private provider/native ID can persist into `.veproj/project.json` as internal render semantics."
    - "No proprietary parity claim can replace first-party support/degraded/unsupported reports."
  artifacts:
    - path: "crates/testkit/tests/production_effects_preview.rs"
      provides: "Production effects preview parity fixtures"
    - path: "crates/testkit/tests/production_effects_exports.rs"
      provides: "Production effects export parity fixtures"
    - path: "crates/adapter_kaipai/src/mapper.rs"
      provides: "Adapter mapping to first-party semantics plus compatibility reports"
  key_links:
    - from: "crates/adapter_kaipai/src/mapper.rs"
      to: "crates/draft_model/src/effects.rs"
      via: "Supported external concepts map to first-party typed semantics"
      pattern: "TransitionKind|EffectKind|RetimeMode"
    - from: "crates/testkit/tests/production_effects_preview.rs"
      to: "crates/testkit/tests/production_effects_exports.rs"
      via: "Same fixture family proves preview/export parity"
      pattern: "production.*effects"
---

<objective>
Add template-fidelity gates for Kaipai/Jianying-like production retime, transition, effect, mask, and blend scenarios.

Purpose: Make PRODFX-05 true without chasing byte-for-byte proprietary effect parity.
Output: Adapter mapping updates, production effect preview/export fixtures, desktop template import coverage, and provider-ID guards.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@docs/no-product-fallback-policy.md
@docs/product-e2e-acceptance-policy.md
@crates/adapter_kaipai/src/mapper.rs
@crates/testkit/tests/template_import_preview.rs
@crates/testkit/tests/template_import_exports.rs
@apps/desktop-electron/tests/template-import.spec.ts
</context>

## Artifacts this phase produces

- Adapter mapping for supported dissolve/retime/basic effect concepts into first-party typed semantics
- Compatibility report expectations for private/native effect IDs as needs-native/dropped/degraded report rows
- `production_effects_preview.rs` and `production_effects_exports.rs` parity fixture cases
- Desktop template import product E2E assertions for Phase 19 reports and no fallback evidence

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map supported template concepts to first-party semantics</name>
  <read_first>@crates/adapter_kaipai/src/mapper.rs @crates/draft_model/src/effects.rs @crates/testkit/tests/template_import_preview.rs @crates/testkit/tests/template_import_exports.rs</read_first>
  <files>crates/adapter_kaipai/src/mapper.rs, crates/testkit/tests/template_import_preview.rs, crates/testkit/tests/template_import_exports.rs</files>
  <behavior>
    - Supported external dissolve/speed/basic effect concepts map to first-party typed semantics.
    - Private/native provider effect IDs stay in compatibility report items and are omitted or degraded from canonical draft semantics.
    - Serialized `.veproj/project.json` does not leak provider IDs, raw formula, render URLs, or external runtime evidence.
  </behavior>
  <action>Update the Kaipai mapper so supported fade/dissolve, retime speed, and supported first-party effect concepts produce Phase 19 typed semantics. Keep private beauty/effect/filter IDs as adaptation report rows with needs-native/dropped/degraded classifications. Extend template import tests so provider-native rows remain report-only and canonical project JSON stays provider/runtime-reference free.</action>
  <acceptance_criteria>Adapter tests prove supported concepts import into first-party semantics and private IDs never become supported render semantics.</acceptance_criteria>
  <verify>
    <automated>cargo test -p adapter_kaipai offline_mapper -- --nocapture && cargo test -p testkit template_import_preview template_import_exports -- --nocapture</automated>
  </verify>
  <done>Adapter mapping honors first-party semantics and compatibility-report boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add production effects preview export fixtures</name>
  <read_first>@crates/testkit/tests/production_effects_preview.rs @crates/testkit/tests/production_effects_exports.rs @crates/testkit/tests/realtime_preview_parity.rs @crates/testkit/tests/preview_export_parity.rs</read_first>
  <files>crates/testkit/tests/production_effects_preview.rs, crates/testkit/tests/production_effects_exports.rs</files>
  <behavior>
    - Complex fixture combines retime, dissolve, blur/basic color, mask/blend, text/audio, and template adaptation reports.
    - Preview/export parity uses real render graph GPU preview facts and compiler/export evidence.
    - Performance budgets fail when preview preparation/export compilation regresses beyond existing scheduler/testkit budget helpers.
  </behavior>
  <action>Add deterministic production fixture cases in testkit for retime plus transition plus effects plus masks/blends. Reuse repository-owned media and existing scheduler/preview/export telemetry helpers for budgets. Assert no fallback/mock/artifact/CPU/DOM evidence and compare preview/export support states and output metadata for the supported subset.</action>
  <acceptance_criteria>Production effects testkit fixtures pass and fail closed when fallback evidence, provider-native semantic leakage, or unsupported-as-supported reports appear.</acceptance_criteria>
  <verify>
    <automated>cargo test -p testkit production_effects -- --nocapture</automated>
  </verify>
  <done>Template-fidelity parity and performance fixtures cover Phase 19 semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add desktop template import product coverage and guards</name>
  <read_first>@apps/desktop-electron/tests/template-import.spec.ts @apps/desktop-electron/tests/production-effects.spec.ts @scripts/phase19-source-guards.sh</read_first>
  <files>apps/desktop-electron/tests/template-import.spec.ts, apps/desktop-electron/tests/production-effects.spec.ts, scripts/phase19-source-guards.sh</files>
  <behavior>
    - Desktop template import displays bounded compatibility report data for Phase 19 supported/degraded/unsupported items.
    - Product E2E verifies preview/export evidence for supported imported production effects and no fallback success.
    - Source guards reject provider-native IDs in internal semantic code and default UI.
  </behavior>
  <action>Extend desktop template import and production effects E2E so normal user flows import the fixture, inspect compatibility report rows, preview through native render-graph GPU evidence, and export the supported subset. Extend Phase 19 guards for provider-native ID misuse, report-only classifications, and no default UI leak of raw provider IDs.</action>
  <acceptance_criteria>Desktop tests pass and fail if supported/degraded/unsupported report states are wrong, if provider IDs leak into UI/project semantics, or if fallback evidence is counted as success.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/template-import.spec.ts tests/production-effects.spec.ts --reporter=line --workers=1 && bash scripts/phase19-source-guards.sh --ui</automated>
  </verify>
  <done>Phase 19 template workflows have desktop product evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External template bundle to adapter | Untrusted provider data is parsed and mapped into first-party semantics or reports. |
| Adapter report to desktop UI | Report rows are displayed without raw provider payload leakage. |
| Template fixture to preview/export | Imported semantics drive product preview/export evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-31 | Tampering | `adapter_kaipai::mapper` | mitigate | Validate external fields and map only supported concepts to typed first-party semantics. |
| T-19-32 | Information Disclosure | compatibility reports/UI | mitigate | Display bounded report copy; block raw provider payloads, paths, signed URLs, and native IDs in default UI. |
| T-19-33 | Repudiation | template preview/export | mitigate | Testkit and desktop tests require real preview/export evidence and no-product-fallback checks. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run adapter tests, testkit production effects/template preview/export tests, desktop template/production-effects Playwright tests, and Phase 19 guards.
</verification>

<success_criteria>
PRODFX-05 is complete when complex imported fixtures prove first-party preview/export parity, compatibility reports, performance budgets, and no fallback/provider-ID semantic leakage.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-14-SUMMARY.md` when done.
</output>
