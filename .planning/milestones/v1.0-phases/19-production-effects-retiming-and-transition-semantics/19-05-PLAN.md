---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "05"
type: execute
wave: 4
depends_on:
  - "19-04"
files_modified:
  - crates/audio_engine/src/dsp_timeline.rs
  - crates/audio_engine/src/mix_intent.rs
  - crates/audio_engine/tests/dsp_timeline.rs
  - crates/testkit/src/audio_parity.rs
  - crates/testkit/tests/audio_preview_export_parity.rs
  - scripts/phase19-source-guards.sh
autonomous: true
requirements:
  - PRODFX-01
must_haves:
  truths:
    - "Retiming is represented in the audio DSP timeline and mix intent, not only video frame state or FFmpeg export."
    - "Audio follow-speed and unsupported pitch/time-stretch combinations produce typed diagnostics."
    - "Preview/export audio parity compares retime-aware source sample mapping through testkit."
  prohibitions:
    - "Audio retiming cannot be represented only as FFmpeg `atempo` export behavior."
    - "Unsupported pitch preservation cannot silently pass as supported audio parity."
  artifacts:
    - path: "crates/audio_engine/src/dsp_timeline.rs"
      provides: "Retime-aware audio DSP segment evaluation"
    - path: "crates/audio_engine/src/mix_intent.rs"
      provides: "Audio mix intent retime facts and diagnostics"
    - path: "crates/testkit/tests/audio_preview_export_parity.rs"
      provides: "Audio retime preview/export parity coverage"
  key_links:
    - from: "crates/audio_engine/src/dsp_timeline.rs"
      to: "crates/testkit/src/audio_parity.rs"
      via: "DSP mix intent is compared against render graph/export audio mixes"
      pattern: "AudioMixIntent|retime"
    - from: "scripts/phase19-source-guards.sh"
      to: "crates/audio_engine/src/dsp_timeline.rs"
      via: "retiming-audio guard requires audio graph retime representation"
      pattern: "retiming-audio|dsp_timeline"
---

<objective>
Add explicit audio graph coverage for Phase 19 retiming semantics.

Purpose: Close PRODFX-01 by ensuring speed changes are represented in `audio_engine` and testkit parity, not just in video graph/export code.
Output: Retime-aware DSP timeline/mix intent, audio parity fixtures, and staged source guard coverage.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-RESEARCH.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/audio_engine/src/dsp_timeline.rs
@crates/audio_engine/src/mix_intent.rs
@crates/testkit/src/audio_parity.rs
@crates/testkit/tests/audio_preview_export_parity.rs
</context>

## Artifacts this phase produces

- Retime-aware `DspSegment` and `AudioMixSegment` fields for source sample mapping and follow-speed diagnostics
- Audio DSP tests for constant speed, speed curve, and unsupported pitch/time-stretch diagnostics
- Testkit audio preview/export parity assertions for retimed audio mixes
- `scripts/phase19-source-guards.sh --retiming-audio`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audio DSP retime representation</name>
  <read_first>@crates/audio_engine/src/dsp_timeline.rs @crates/audio_engine/src/mix_intent.rs @crates/audio_engine/tests/dsp_timeline.rs @crates/draft_model/src/effects.rs</read_first>
  <files>crates/audio_engine/src/dsp_timeline.rs, crates/audio_engine/src/mix_intent.rs, crates/audio_engine/tests/dsp_timeline.rs</files>
  <behavior>
    - Constant speed and speed curve retime semantics change source sample mapping using integer/rational arithmetic.
    - `AudioMixIntent` carries retime/follow-speed facts needed by preview/export parity.
    - Unsupported pitch preservation or time-stretch policy emits typed diagnostics without hiding degradation.
  </behavior>
  <action>Extend `DspSegment` and `AudioMixSegment` with retime-aware source sample mapping and audio follow-speed support facts derived from the typed draft retime contract created in earlier plans. Update `evaluate_dsp_timeline` so target duration, source sample windows, gain/keyframe sample positions, and diagnostics are deterministic for constant speed and speed curves. Keep audio_engine free of FFmpeg filter construction; it owns DSP intent, not export implementation.</action>
  <acceptance_criteria>`cargo test -p audio_engine dsp_timeline -- --nocapture` proves retimed audio source samples, mix intent fields, and unsupported pitch/time-stretch diagnostics.</acceptance_criteria>
  <verify>
    <automated>cargo test -p audio_engine dsp_timeline -- --nocapture</automated>
  </verify>
  <done>Audio DSP timeline represents Phase 19 retiming semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add audio parity and staged guard coverage</name>
  <read_first>@crates/testkit/src/audio_parity.rs @crates/testkit/tests/audio_preview_export_parity.rs @scripts/phase19-source-guards.sh</read_first>
  <files>crates/testkit/src/audio_parity.rs, crates/testkit/tests/audio_preview_export_parity.rs, scripts/phase19-source-guards.sh</files>
  <behavior>
    - Testkit compares retime-aware preview audio mix intent to render graph/export audio mixes.
    - Parity diagnostics include retime/source sample mismatch and unsupported audio follow-speed categories.
    - Source guards fail if PRODFX-01 lacks audio_engine retime coverage.
  </behavior>
  <action>Extend audio parity diagnostics and fixtures so retimed audio preview/export mismatches are classified separately from gain, pan, fade, sample-rate, and source/target range mismatches. Add `--retiming-audio` guard mode requiring audio_engine retime representation, audio parity coverage, and no renderer-owned audio speed math.</action>
  <acceptance_criteria>Audio parity tests and `--retiming-audio` guards pass and fail closed when audio retime facts are missing.</acceptance_criteria>
  <verify>
    <automated>cargo test -p testkit audio_preview_export_parity -- --nocapture && bash scripts/phase19-source-guards.sh --retiming-audio</automated>
  </verify>
  <done>PRODFX-01 has audio graph and audio parity verification coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft retime semantics to audio_engine | Persisted speed changes become DSP sample mapping and diagnostics. |
| Audio preview mix to export graph | Audio mix intent is compared against render/export audio representation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10A | Tampering | `audio_engine::dsp_timeline` | mitigate | Use typed retime fields and integer/rational sample mapping; reject unsupported pitch/time-stretch as diagnostics. |
| T-19-10B | Repudiation | audio parity evidence | mitigate | Testkit reports retime-specific parity differences instead of treating audio export success as sufficient. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run `cargo test -p audio_engine dsp_timeline -- --nocapture`, `cargo test -p testkit audio_preview_export_parity -- --nocapture`, and `bash scripts/phase19-source-guards.sh --retiming-audio`.
</verification>

<success_criteria>
PRODFX-01 audio coverage is complete when audio DSP, mix intent, parity diagnostics, and staged guards all represent retimed audio semantics.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-05-SUMMARY.md` when done.
</output>
