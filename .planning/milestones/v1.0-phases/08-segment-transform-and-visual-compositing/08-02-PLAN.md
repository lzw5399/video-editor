---
phase: "08-segment-transform-and-visual-compositing"
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - "crates/engine_core/src/normalize.rs"
  - "crates/engine_core/src/frame_state.rs"
  - "crates/engine_core/tests/frame_state_snapshots.rs"
  - "crates/render_graph/src/graph.rs"
  - "crates/render_graph/tests/render_graph_snapshots.rs"
autonomous: true
requirements:
  - XFORM-01
  - XFORM-02
  - LAYER-01
  - LAYER-02
  - LAYER-03
user_setup: []
must_haves:
  truths:
    - "engine_core frame state includes transform/composition semantics for active visual layers."
    - "render_graph preserves visual intent without FFmpeg syntax."
    - "hidden segments are omitted deterministically."
  artifacts:
    - path: "crates/engine_core/src/frame_state.rs"
      provides: "transform-aware frame state"
    - path: "crates/render_graph/src/graph.rs"
      provides: "transform-aware render graph"
  key_links:
    - from: "crates/render_graph/src/graph.rs"
      to: "crates/engine_core/src/frame_state.rs"
      via: "FrameVisualLayer.visual"
      pattern: "visual"
---

<objective>
Propagate segment visual semantics through engine frame state and render graph.
</objective>

<context>
@.planning/phases/08-segment-transform-and-visual-compositing/08-CONTEXT.md
@.planning/phases/08-segment-transform-and-visual-compositing/08-RESEARCH.md
@crates/engine_core/src/normalize.rs
@crates/engine_core/src/frame_state.rs
@crates/render_graph/src/graph.rs
</context>

## Artifacts this phase produces

- `FrameVisualLayer.visual`
- `NormalizedSegment.visual`
- `RenderVideoLayer.visual`
- `RenderVisualDiagnostic`

<tasks>

<task type="auto" tdd="true">
  <name>Task 08-02-01: Add transform-aware engine frame state</name>
  <files>crates/engine_core/src/normalize.rs, crates/engine_core/src/frame_state.rs, crates/engine_core/tests/frame_state_snapshots.rs</files>
  <read_first>
    - `crates/engine_core/src/normalize.rs`
    - `crates/engine_core/src/frame_state.rs`
    - `crates/draft_model/src/timeline.rs`
  </read_first>
  <action>Copy `Segment.visual` into `NormalizedSegment`, include it in `FrameVisualLayer`, and omit visual layers when `visible=false`. Preserve track stack sort order. Add diagnostics for unsupported blend/mask and degraded background filling. Update snapshots to show transform/default visual fields.</action>
  <acceptance_criteria>
    Frame state serializes selected segment visual semantics and omits hidden segments. Diagnostics classify degraded/unsupported visual semantics.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p engine_core transform -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 08-02-02: Preserve transform intent in render graph</name>
  <files>crates/render_graph/src/graph.rs, crates/render_graph/tests/render_graph_snapshots.rs</files>
  <read_first>
    - `crates/render_graph/src/graph.rs`
    - `crates/engine_core/src/frame_state.rs`
  </read_first>
  <action>Add `visual` to `RenderVideoLayer` and diagnostics for unsupported/degraded visual modes. Keep graph output free of FFmpeg filter syntax. Ensure active visual segments still sort by stack index and segment ID.</action>
  <acceptance_criteria>
    Render graph snapshots include transform/composition intent and do not include `overlay=`, `filter_complex`, or FFmpeg-specific syntax.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p render_graph transform -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p engine_core transform -- --nocapture</automated>
<automated>cargo test -p render_graph transform -- --nocapture</automated>
</verification>

<source_audit>
REQ | LAYER-02 | deterministic composition order | 08-02 | COVERED
REQ | LAYER-03 | capability boundaries | 08-02 | COVERED
</source_audit>

<output>
Create `.planning/phases/08-segment-transform-and-visual-compositing/08-02-SUMMARY.md` when done.
</output>
