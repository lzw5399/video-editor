---
phase: "10-typed-keyframe-and-animation-system"
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - "crates/engine_core/src/normalize.rs"
  - "crates/engine_core/src/frame_state.rs"
  - "crates/engine_core/tests/frame_state_snapshots.rs"
  - "crates/engine_core/tests/normalization.rs"
  - "crates/render_graph/src/graph.rs"
  - "crates/render_graph/tests/render_graph_snapshots.rs"
  - "crates/ffmpeg_compiler/src/filters.rs"
  - "crates/ffmpeg_compiler/tests/transform_snapshots.rs"
autonomous: true
requirements:
  - ANIM-02
  - ANIM-03
user_setup: []
must_haves:
  truths:
    - "engine_core evaluates keyframes at frame time and emits resolved visual/text/audio state."
    - "render_graph preserves typed animation intent and diagnostics without FFmpeg syntax ownership."
    - "No UI-owned interpolation or naked floating-point persisted time is introduced."
  artifacts:
    - path: "crates/engine_core/src/frame_state.rs"
      provides: "frame-time keyframe evaluation"
    - path: "crates/render_graph/src/graph.rs"
      provides: "animation-aware render intent propagation"
---

<objective>
Evaluate typed keyframes through engine frame state and propagate deterministic animation intent through render graph/compiler diagnostics.
</objective>

<context>
@AGENTS.md
@.planning/phases/10-typed-keyframe-and-animation-system/10-CONTEXT.md
@.planning/phases/10-typed-keyframe-and-animation-system/10-RESEARCH.md
@crates/engine_core/src/normalize.rs
@crates/engine_core/src/frame_state.rs
@crates/render_graph/src/graph.rs
@crates/ffmpeg_compiler/src/filters.rs
</context>

## Artifacts this plan produces

- frame-time keyframe evaluator
- resolved animated visual/text/audio frame state
- animation-aware render graph snapshots and diagnostics

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-03-01: Resolve keyframes in engine_core frame state</name>
  <files>crates/engine_core/src/normalize.rs, crates/engine_core/src/frame_state.rs, crates/engine_core/tests/frame_state_snapshots.rs, crates/engine_core/tests/normalization.rs</files>
  <read_first>
    - `crates/engine_core/src/normalize.rs`
    - `crates/engine_core/src/frame_state.rs`
    - `crates/engine_core/tests/frame_state_snapshots.rs`
  </read_first>
  <action>Add deterministic keyframe evaluation helpers that group keyframes by property, use segment-relative microsecond time, and resolve supported properties onto `SegmentVisual`, text overlay inputs, and `volume_level_millis`. Use static segment values before the first keyframe and hold the last keyframe after the final keyframe. Implement `hold` and `linear` numeric interpolation with deterministic integer math; keep color interpolation hold-only unless a deterministic channel interpolation is implemented and tested. Apply easing to interpolation progress without persisting floats.</action>
  <acceptance_criteria>
    Engine tests prove animated opacity/position/scale/volume/text font size resolve at exact, between, before-first, and after-last times; invalid persisted keyframes are rejected before normalization; renderer is not involved.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p engine_core keyframe -- --nocapture</automated>
    <automated>cargo test -p engine_core frame_state_snapshots -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-03-02: Propagate animation intent through render graph and compiler diagnostics</name>
  <files>crates/render_graph/src/graph.rs, crates/render_graph/tests/render_graph_snapshots.rs, crates/ffmpeg_compiler/src/filters.rs, crates/ffmpeg_compiler/tests/transform_snapshots.rs</files>
  <read_first>
    - `crates/render_graph/src/graph.rs`
    - `crates/render_graph/tests/render_graph_snapshots.rs`
    - `crates/ffmpeg_compiler/src/filters.rs`
  </read_first>
  <action>Keep render_graph driven by engine-resolved frame state while preserving typed `keyframes` on relevant render intents for diagnostics and future compiler support. Add animation diagnostics for properties that engine can resolve but compiler cannot faithfully encode yet, especially rotation and deferred filter/sticker parameter animation. Update compiler snapshots only where Phase 10 has a supported subset; otherwise assert deterministic degraded/unsupported diagnostics instead of fake support.</action>
  <acceptance_criteria>
    Render graph snapshots show typed keyframe intent and resolved sampled frame differences. Compiler tests classify unsupported/deferred animation paths explicitly and do not add UI or FFmpeg syntax to draft semantics.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p render_graph keyframe -- --nocapture</automated>
    <automated>cargo test -p ffmpeg_compiler transform -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p engine_core keyframe -- --nocapture</automated>
<automated>cargo test -p render_graph keyframe -- --nocapture</automated>
<automated>cargo test -p ffmpeg_compiler transform -- --nocapture</automated>
</verification>

<source_audit>
REQ | ANIM-02 | deterministic interpolation/easing evaluation | 10-03 | COVERED
REQ | ANIM-03 | engine/render graph frame-time evaluation | 10-03 | COVERED
</source_audit>

<output>
Create `.planning/phases/10-typed-keyframe-and-animation-system/10-03-SUMMARY.md` when done.
</output>
