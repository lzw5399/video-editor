---
phase: "10.1-usable-editor-mvp-completion"
plan: "02"
type: execute
wave: 2
depends_on:
  - "10.1-01"
files_modified:
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/src/renderer/viewModel.ts"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "apps/desktop-electron/src/renderer/workspace/preview-inspector.css"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - MVPEDIT-05
  - MVPEDIT-09
---

<objective>
Render the returned PNG preview frame as image pixels inside the preview canvas, not just as path text.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 10.1-02-01: Add preview artifact display URL state</name>
  <files>apps/desktop-electron/src/preload/index.ts, apps/desktop-electron/src/renderer/viewModel.ts, apps/desktop-electron/src/renderer/App.tsx</files>
  <action>Provide a safe local artifact URL conversion path and store frame display URL alongside artifact path. Keep artifact path available only for diagnostics.</action>
  <acceptance_criteria>`PreviewDisplayState` can represent `frameArtifactPath` and `frameDisplayUrl`; failed/invalidated previews clear both.</acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 10.1-02-02: Render image in preview canvas</name>
  <files>apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/src/renderer/workspace/preview-inspector.css, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Render `<img aria-label=\"当前预览帧\">` in `.preview-canvas` when a frame URL exists. Keep black/empty fallback. Update tests to assert image/canvas display rather than path text inside the canvas.</action>
  <acceptance_criteria>Preview canvas shows the PNG image itself, preserves aspect ratio, and path appears only in preview artifact diagnostics.</acceptance_criteria>
</task>
</tasks>

<verification>
<automated>pnpm --filter @video-editor/desktop build</automated>
<automated>pnpm --filter @video-editor/desktop test:workspace -g "预览命令|preview"</automated>
</verification>

<output>
Create `.planning/phases/10.1-usable-editor-mvp-completion/10.1-02-SUMMARY.md` when done.
</output>

