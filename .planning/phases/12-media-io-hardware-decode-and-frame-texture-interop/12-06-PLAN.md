---
phase: "12-media-io-hardware-decode-and-frame-texture-interop"
plan: "06"
type: execute
wave: 6
depends_on:
  - "12-03"
  - "12-04"
  - "12-05"
files_modified:
  - "crates/realtime_preview_runtime/src/frame_provider.rs"
  - "crates/realtime_preview_runtime/src/media_io_adapter.rs"
  - "crates/realtime_preview_runtime/src/fallback.rs"
  - "crates/realtime_preview_runtime/tests/media_io_handoff.rs"
autonomous: true
requirements:
  - MEDIAIO-01
  - MEDIAIO-02
  - MEDIAIO-05
user_setup: []
must_haves:
  truths:
    - "MEDIAIO-01: Phase 11 preview requests decoded material frames through media IO traits rather than FFmpeg preview artifact semantics."
    - "MEDIAIO-02: preview diagnostics expose selected native/FFmpeg path, texture compatibility state, and fallback reason."
    - "MEDIAIO-05: native, CPU, FFmpeg CPU, and artifact fallback paths remain distinguishable at the preview handoff boundary."
  artifacts:
    - path: "crates/realtime_preview_runtime/src/media_io_adapter.rs"
      provides: "Phase 11 to Phase 12 decoded frame provider adapter"
    - path: "crates/realtime_preview_runtime/src/fallback.rs"
      provides: "preview-visible selected decode path and fallback diagnostics"
    - path: "crates/realtime_preview_runtime/tests/media_io_handoff.rs"
      provides: "preview handoff, stale generation, and fallback diagnostics tests"
  key_links:
    - from: "crates/realtime_preview_runtime/src/media_io_adapter.rs"
      to: "media_runtime::VideoDecoder"
      via: "material source-time decode request"
      pattern: "VideoDecodeRequest"
    - from: "crates/realtime_preview_runtime/src/media_io_adapter.rs"
      to: "media_runtime::TextureHandle"
      via: "device identity and compatibility diagnostics"
      pattern: "RuntimeDeviceId"
---

<objective>
Connect Phase 12 media IO to the Phase 11 preview runtime through a decoded-frame provider adapter that converts resolved material source-time requests into media IO decode requests and returns handle/fallback diagnostics to the compositor.

Purpose: MEDIAIO-01 and MEDIAIO-05 require Phase 11 preview to consume media IO without taking ownership of FFmpeg preview artifact semantics, render graph compilation, or timeline evaluation.
Output: Preview handoff adapter and tests for request conversion, generation rejection, and fallback diagnostics.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/notes/production-editor-architecture-decisions.md
@.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-CONTEXT.md
@.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-DESIGN.md
@.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-CONTEXT.md
@.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-RESEARCH.md
@.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-DESIGN.md
@crates/media_runtime/src/media_io.rs
@crates/media_runtime/src/decoder.rs
@crates/media_runtime/src/frame.rs
@crates/media_runtime/src/texture.rs
</context>

## Artifacts this plan produces

- `MediaIoFrameProvider`
- `PreviewMaterialDecodeRequest`
- `PreviewDecodeDiagnostic`
- `PreviewFrameStorageKind`
- stale playback generation rejection
- CPU/texture/artifact fallback diagnostics at the preview handoff boundary

<tasks>

<task type="auto" tdd="true">
  <name>Task 12-06-01: Add Phase 11 media IO handoff adapter and fallback diagnostics</name>
  <files>crates/realtime_preview_runtime/src/frame_provider.rs, crates/realtime_preview_runtime/src/media_io_adapter.rs, crates/realtime_preview_runtime/src/fallback.rs, crates/realtime_preview_runtime/tests/media_io_handoff.rs</files>
  <read_first>
    - `.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-DESIGN.md`
    - `.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-RESEARCH.md`
    - `.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-DESIGN.md`
    - `crates/media_runtime/src/media_io.rs`
    - `crates/media_runtime/src/decoder.rs`
    - `crates/media_runtime/src/frame.rs`
    - `crates/media_runtime/src/texture.rs`
  </read_first>
  <behavior>
    - Test 1: a Phase 11 preview material source-time request becomes a `VideoDecodeRequest` with material ID, source time in microseconds, playback generation, desired storage preference, and Phase 11 device identity/compatibility data.
    - Test 2: adapter returns CPU frame handles, texture handles, or artifact fallback diagnostics without normalizing drafts, resolving timeline layers, composing frames, or compiling export FFmpeg commands.
    - Test 3: stale playback generation decode results are rejected before presentation and counted in diagnostics/telemetry.
    - Test 4: CPU fallback remains valid when native texture import compatibility is not proven for the active `RuntimeDeviceId` and backend.
  </behavior>
  <action>Per MEDIAIO-01 and the Phase 12 boundary, add a `MediaIoFrameProvider` adapter in the Phase 11 preview runtime crate. The adapter converts already-resolved preview material source-time requests into media IO decode requests and returns `DecodedVideoFrame` lease metadata to the compositor. Include Phase 11 `RuntimeDeviceId`, backend, and compatibility state in requests and diagnostics so native texture handles are used only when proven compatible; otherwise return CPU or artifact fallback diagnostics. Keep timeline evaluation, render graph construction, visual composition, text layout, export semantics, audio playback, and scheduler policy outside this adapter. If the Phase 11 implementation chose a different file/module name than the design document, preserve the actual Phase 11 public API shape while creating the same adapter behavior and record the mapping in `12-06-SUMMARY.md`.</action>
  <acceptance_criteria>
    Preview-runtime tests prove request conversion, generation rejection, CPU/texture/artifact fallback diagnostics, device compatibility handling, and no ownership of render graph or timeline semantics by the media IO adapter.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime stale_generation -- --nocapture</automated>
  </verify>
  <done>Task complete when Phase 11 handoff adapter tests pass and the adapter remains a decode provider only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preview runtime -> media runtime | Resolved material source-time requests cross into decoder sessions. |
| media runtime -> preview compositor | Opaque CPU/texture handles and fallback diagnostics cross back without timeline or render graph authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | Spoofing | preview decode requests | mitigate | Include material ID, playback generation, owner session, backend, and `RuntimeDeviceId` in decode requests and diagnostics. |
| T-12-06-02 | Tampering | device compatibility | mitigate | Reject texture-ready handoff unless compatibility is proven; otherwise return CPU/artifact fallback diagnostics. |
| T-12-06-03 | Repudiation | fallback diagnostics | mitigate | Selected path and fallback reason are returned from Rust media IO and passed through preview diagnostics. |
| T-12-06-SC | Tampering | npm/pip/cargo installs | accept | This plan adds no new packages beyond those verified in 12-02B. |
</threat_model>

<verification>
<automated>cargo test -p realtime_preview_runtime media_io_handoff -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime stale_generation -- --nocapture</automated>
</verification>

<source_audit>
GOAL | Split media read/decode from FFmpeg execution and introduce preview handoff contracts | 12-06 connects resolved preview requests to media IO decode requests | COVERED
REQ | MEDIAIO-01 | Phase 11 adapter consumes media IO traits; FFmpeg preview artifact semantics remain fallback only | COVERED
REQ | MEDIAIO-02 | Adapter diagnostics expose native and FFmpeg selected path, device compatibility, and fallback reasons | COVERED
REQ | MEDIAIO-05 | Preview handoff distinguishes native, CPU, FFmpeg CPU, and artifact fallback paths | COVERED
RESEARCH | Device identity and compatibility must be present on texture handles; CPU fallback remains required until native import is proven | 12-06 request/diagnostic behavior | COVERED
CONTEXT | Phase 12 does not own render graph, timeline evaluation, composition, export semantics, audio playback, or scheduler policy | Adapter action and tests enforce decode-provider-only boundary | COVERED
</source_audit>

<success_criteria>
Phase 11 preview can request decoded material frames through media IO, receive CPU/texture/artifact fallback diagnostics with device compatibility state, reject stale generations, and keep media IO separate from timeline/render/export semantics.
</success_criteria>

<output>
Create `.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-06-SUMMARY.md` when done.
</output>
