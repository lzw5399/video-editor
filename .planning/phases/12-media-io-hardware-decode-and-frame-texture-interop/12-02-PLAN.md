---
phase: "12-media-io-hardware-decode-and-frame-texture-interop"
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - "crates/media_runtime/src/capabilities.rs"
  - "crates/media_runtime/tests/runtime_capability.rs"
  - "crates/media_runtime_desktop/src/lib.rs"
  - "crates/media_runtime_desktop/src/capabilities.rs"
  - "crates/media_runtime_desktop/src/platform/mod.rs"
  - "crates/media_runtime_desktop/src/platform/windows.rs"
  - "crates/media_runtime_desktop/src/platform/macos.rs"
  - "crates/media_runtime_desktop/tests/capabilities.rs"
autonomous: true
requirements:
  - MEDIAIO-02
  - MEDIAIO-05
user_setup: []
must_haves:
  truths:
    - "MEDIAIO-02: desktop runtime reports native Windows and macOS media IO capability domains plus FFmpeg fallback readiness."
    - "MEDIAIO-05: capability reports include structured fallback reasons while preserving existing FFmpeg probe/export/transcode behavior."
    - "First native hardware-decode acceptance target is H.264 MP4/MOV; HEVC, ProRes, AV1, and other codecs are reported as capability-supported or degraded only when proven."
  artifacts:
    - path: "crates/media_runtime/src/capabilities.rs"
      provides: "RuntimeCapabilities extension with media_io, codec, pixel format, texture interop, and fallback ladder domains"
    - path: "crates/media_runtime_desktop/src/capabilities.rs"
      provides: "desktop aggregate native plus FFmpeg capability report"
    - path: "crates/media_runtime_desktop/src/platform/windows.rs"
      provides: "cfg(windows) Windows Media Foundation/DXVA/D3D capability probe stubs"
    - path: "crates/media_runtime_desktop/src/platform/macos.rs"
      provides: "cfg(target_os = \"macos\") AVFoundation/VideoToolbox/CoreVideo/Metal capability probe stubs"
  key_links:
    - from: "crates/media_runtime_desktop/src/capabilities.rs"
      to: "crates/media_runtime/src/capabilities.rs"
      via: "desktop aggregate capability mapping"
      pattern: "RuntimeCapabilities"
    - from: "crates/media_runtime_desktop/src/platform/mod.rs"
      to: "crates/media_runtime_desktop/src/platform/windows.rs"
      via: "cfg-gated platform capability module"
      pattern: "cfg\\(windows\\)"
    - from: "crates/media_runtime_desktop/src/platform/mod.rs"
      to: "crates/media_runtime_desktop/src/platform/macos.rs"
      via: "cfg-gated platform capability module"
      pattern: "cfg\\(target_os = \"macos\"\\)"
---

<objective>
Extend desktop runtime capability reporting so Phase 12 can report native media IO domains, FFmpeg fallback readiness, codec acceptance targets, texture interop posture, and structured fallback reasons before platform decoders are implemented.

Purpose: MEDIAIO-02 and MEDIAIO-05 need capability guardrails before FFmpeg and native platform paths can safely return frame or texture handles.
Output: Runtime capability extensions, desktop aggregate reports, platform capability probes, and tests preserving existing FFmpeg capability fields.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/notes/production-editor-architecture-decisions.md
@.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-CONTEXT.md
@.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-RESEARCH.md
@.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-DESIGN.md
@crates/media_runtime/src/capabilities.rs
@crates/media_runtime_desktop/src/lib.rs
</context>

## Artifacts this plan produces

- `RuntimeCapabilities.media_io`
- `RuntimeMediaIoCapabilities`
- `PlatformMediaIoCapabilities`
- `WindowsMediaIoCapabilities`
- `MacosMediaIoCapabilities`
- `CodecCapability`
- `PixelFormatCapability`
- `TextureInteropCapability`
- `FallbackLadderCapability`
- `probe_desktop_runtime_capabilities`

<tasks>

<task type="auto" tdd="true">
  <name>Task 12-02-01: Add native and FFmpeg media IO capability reports</name>
  <files>crates/media_runtime/src/capabilities.rs, crates/media_runtime/tests/runtime_capability.rs, crates/media_runtime_desktop/src/lib.rs, crates/media_runtime_desktop/src/capabilities.rs, crates/media_runtime_desktop/src/platform/mod.rs, crates/media_runtime_desktop/src/platform/windows.rs, crates/media_runtime_desktop/src/platform/macos.rs, crates/media_runtime_desktop/tests/capabilities.rs</files>
  <read_first>
    - `crates/media_runtime/src/capabilities.rs`
    - `crates/media_runtime/tests/runtime_capability.rs`
    - `crates/media_runtime_desktop/src/lib.rs`
    - `.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-RESEARCH.md`
    - `.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-DESIGN.md`
  </read_first>
  <behavior>
    - Test 1: desktop capability report preserves existing FFmpeg/ffprobe fields and adds `mediaIo.windows`, `mediaIo.macos`, `codecs`, `pixelFormats`, `textureInterop`, and `fallbackLadder`.
    - Test 2: on non-Windows, Windows capability fields are `unavailable` with an explicit unsupported-platform reason, not a panic.
    - Test 3: on non-macOS, macOS capability fields are `unavailable` with an explicit unsupported-platform reason, not a panic.
    - Test 4: H.264 MP4/MOV is the first accepted native hardware-decode target; HEVC, ProRes, AV1, and other codecs are marked degraded/unavailable unless platform proof exists.
  </behavior>
  <action>Per MEDIAIO-02 and MEDIAIO-05, extend the current runtime capability model rather than replacing it. Add shared media IO capability types in `media_runtime` and a desktop aggregate report in `media_runtime_desktop` that combines existing FFmpeg binary/filter/encoder readiness with Windows Media Foundation/DXVA/D3D and macOS AVFoundation/VideoToolbox/CoreVideo/Metal domains. Add cfg-gated platform modules under `media_runtime_desktop`; keep the public trait surface in `media_runtime` and leave clean module boundaries for later split crates. Encode `RuntimeDeviceId`, backend, and compatibility status in texture interop capability data, and require CPU fallback until native import compatibility is proven. Preserve `probe_runtime_capabilities` behavior for existing callers and add a new desktop aggregate function for Phase 12 consumers.</action>
  <acceptance_criteria>
    Existing runtime capability tests still pass; new desktop capability tests prove native domains, fallback ladder ordering, codec acceptance/degradation status, texture interop compatibility status, and explicit unsupported-platform diagnostics without removing FFmpeg readiness fields.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p media_runtime runtime_capability -- --nocapture</automated>
    <automated>cargo test -p media_runtime_desktop capabilities -- --nocapture</automated>
  </verify>
  <done>Task complete when desktop capability reports are additive, serializable, device-aware, codec-explicit, and covered by platform-gated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform runtime -> capability report | Host OS and FFmpeg readiness becomes Rust-owned diagnostic data. |
| capability report -> later bindings | Native decode and texture interop status must not overstate compatibility before platform proof exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Spoofing | capability reports | mitigate | Report executor name, platform domain, status, backend, device compatibility, and fallback reason from Rust-owned probes, not renderer-supplied strings. |
| T-12-02-02 | Tampering | codec capability status | mitigate | Tests assert H.264 MP4/MOV first-target status and require degraded/unavailable status for unproven HEVC, ProRes, AV1, and other codecs. |
| T-12-02-03 | Repudiation | texture interop readiness | mitigate | Capability report must include `RuntimeDeviceId`, backend, compatibility state, and fallback reason so CPU fallback remains explicit until native import is proven. |
| T-12-02-SC | Tampering | npm/pip/cargo installs | accept | This plan adds no package dependencies; 12-02B performs the blocking package verification checkpoint before platform dependency additions. |
</threat_model>

<verification>
<automated>cargo test -p media_runtime runtime_capability -- --nocapture</automated>
<automated>cargo test -p media_runtime_desktop capabilities -- --nocapture</automated>
</verification>

<source_audit>
GOAL | Native media IO and hardware decode capability reporting | 12-02 establishes native and FFmpeg capability reports before decoder implementation | COVERED
REQ | MEDIAIO-02 | Windows and macOS native capability domains plus fallback reasons | COVERED
REQ | MEDIAIO-05 | FFmpeg capability fields preserved in aggregate report and fallback ladder status | COVERED
RESEARCH | Phase 11 exposes device identity through `RuntimeDeviceId`/backend/compatibility; CPU fallback remains required until native import is proven | Capability report includes device identity, backend, compatibility state, and fallback reason | COVERED
RESEARCH | H.264 MP4/MOV is first native hardware-decode target; HEVC/ProRes/AV1 are capability-reported/degraded unless proven | Codec capability tests and acceptance criteria | COVERED
CONTEXT | Shared media IO contracts stay in `media_runtime`; desktop/platform code starts under `media_runtime_desktop` cfg modules with a split seam | 12-02 file ownership and action | COVERED
</source_audit>

<success_criteria>
Desktop runtime capability reports include native Windows/macOS media IO domains, FFmpeg fallback readiness, fallback ladder diagnostics, device compatibility state, and codec-specific acceptance/degradation posture without replacing existing FFmpeg capability behavior.
</success_criteria>

<output>
Create `.planning/phases/12-media-io-hardware-decode-and-frame-texture-interop/12-02-SUMMARY.md` when done.
</output>
