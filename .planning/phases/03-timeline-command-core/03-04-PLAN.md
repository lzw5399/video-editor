---
phase: 03-timeline-command-core
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - crates/draft_model/src/lib.rs
  - crates/draft_model/src/timeline.rs
  - crates/draft_model/src/validation.rs
  - crates/draft_model/tests/schema_exports.rs
  - crates/draft_commands/src/lib.rs
  - crates/draft_commands/src/text.rs
  - crates/draft_commands/src/audio.rs
  - crates/draft_commands/tests/text_audio_commands.rs
  - crates/bindings_node/src/lib.rs
  - crates/bindings_node/tests/binding_smoke.rs
  - fixtures/draft/minimal-timeline-command.json
  - fixtures/draft/invalid-timeline-command.json
  - schemas/command.schema.json
  - schemas/draft.schema.json
  - apps/desktop-electron/src/generated/CommandEnvelope.ts
  - apps/desktop-electron/src/generated/CommandResultEnvelope.ts
  - apps/desktop-electron/src/generated/Draft.ts
  - package.json
  - justfile
autonomous: true
requirements:
  - TIME-04
  - TIME-06
  - TEXT-01
  - TEXT-02
  - AUD-01
  - AUD-02
  - TEST-02
must_haves:
  truths:
    - "Text/subtitle segments are semantic draft data with editable content, font size, color, alignment, stroke, shadow, and background, not URI-hidden external files, per D-22, D-23, TEXT-01, and TEXT-02."
    - "Audio/BGM commands add audio materials to audio tracks, set per-segment integer volume, and set `Track.muted` in Rust per D-24 and AUD-01/AUD-02."
    - "Advanced text effects, bubbles, stickers, filters, transitions, keyframes, masks, waveform generation, preview cache invalidation, and render/export behavior are explicitly excluded from Phase 3 per D-25."
    - "Undo/redo covers text edit, audio add, segment volume edit, and track mute edit, completing D-21 and TIME-04 for every MVP edit command."
    - "Command tests cover split, trim, move, delete, snapping, undo, redo, text edit, and volume edit per D-26 and TEST-02."
    - "Generated command schema and TypeScript contracts are updated from Rust and checked for drift; final gates use `just build` and `just test` per D-27."
  artifacts:
    - path: crates/draft_model/src/timeline.rs
      provides: "TextSegment, TextStyle, TextAlignment, TextStroke, TextShadow, TextBackground, and SegmentVolume persisted semantic types"
    - path: crates/draft_commands/src/text.rs
      provides: "add/edit text segment commands"
    - path: crates/draft_commands/src/audio.rs
      provides: "add audio, set segment volume, and set track mute commands"
    - path: crates/draft_commands/tests/text_audio_commands.rs
      provides: "text_commands and audio_commands coverage"
    - path: package.json
      provides: "Phase 3 source guard and command coverage gate scripts"
  key_links:
    - from: crates/draft_commands/src/text.rs
      to: crates/draft_model/src/timeline.rs
      via: "updates semantic text content/style on Segment"
      pattern: "TextSegment|TextStyle"
    - from: crates/draft_commands/src/audio.rs
      to: crates/draft_model/src/timeline.rs
      via: "updates SegmentVolume and Track.muted"
      pattern: "SegmentVolume|muted"
    - from: package.json
      to: crates/draft_commands/tests
      via: "Phase 3 test scripts call focused command tests and source guards"
      pattern: "test:phase3"
---

<objective>
Implement MVP text/audio command semantics and close Phase 3 with command coverage, generated contract drift checks, fixture classification, source guards, and full build/test gates.

Purpose: Text and audio must be first-class Rust-owned semantics before the desktop UI can expose panels or timeline controls. This plan implements TEXT-01, TEXT-02, AUD-01, AUD-02, completes TIME-04 for all MVP edits, reinforces TIME-06, and satisfies TEST-02 while honoring D-21 through D-28.

Output: Text/audio semantic types, text/audio command contracts and implementations, binding routes, generated schema/TypeScript updates, command fixtures, phase source guards, and final gates.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-timeline-command-core/03-CONTEXT.md
@.planning/phases/03-timeline-command-core/03-RESEARCH.md
@.planning/phases/03-timeline-command-core/03-VALIDATION.md
@.planning/phases/03-timeline-command-core/03-PATTERNS.md
@.planning/phases/03-timeline-command-core/03-03-SUMMARY.md
@crates/draft_model/src/lib.rs
@crates/draft_model/src/timeline.rs
@crates/draft_model/src/validation.rs
@crates/draft_model/tests/schema_exports.rs
@crates/draft_commands/src/timeline.rs
@crates/draft_commands/src/history.rs
@crates/bindings_node/src/lib.rs
@package.json
@justfile
</context>

## Artifacts this phase produces

- Persisted semantic types: `TextSegment`, `TextStyle`, `TextAlignment`, `TextStroke`, `TextShadow`, `TextBackground`, `SegmentVolume`.
- Command names: `addTextSegment`, `editTextSegment`, `addAudioSegment`, `setSegmentVolume`, `setTrackMute`.
- Payload types: `AddTextSegmentCommandPayload`, `EditTextSegmentCommandPayload`, `AddAudioSegmentCommandPayload`, `SetSegmentVolumeCommandPayload`, `SetTrackMuteCommandPayload`.
- Functions: `add_text_segment`, `edit_text_segment`, `add_audio_segment`, `set_segment_volume`, `set_track_mute`.
- Events: `textSegmentAdded`, `textSegmentEdited`, `audioSegmentAdded`, `segmentVolumeChanged`, `trackMuteChanged`, `undoCommitted`, `redoCommitted`, `timelineCommandRejected`.
- Fixtures: `fixtures/draft/minimal-timeline-command.json` and `fixtures/draft/invalid-timeline-command.json` as explicitly classified positive/negative timeline command fixtures.
- Tests: `text_commands`, `audio_commands`, `undo_redo`, `timeline_edits`, `snapping`, `invalid_edits_are_atomic`.
- Generated paths: `schemas/command.schema.json`, `schemas/draft.schema.json`, `apps/desktop-electron/src/generated/CommandEnvelope.ts`, `apps/desktop-electron/src/generated/CommandResultEnvelope.ts`, `apps/desktop-electron/src/generated/Draft.ts`.
- Scripts/gates: `test:phase3-commands`, `test:phase3-source-guards`, `test:contracts` updates in `package.json` and corresponding `just test` inclusion.

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 3 | Rust-owned timeline editing semantics and command/undo behavior before rich UI | 03-01 to 03-04 | COVERED | Plans cover model/rules, edits, history/snapping/events, text/audio, and gates. |
| REQ | TIME-01 | Draft supports root sequence with video/audio/text tracks | 03-01 | COVERED | Root `Draft.tracks` only; no multi-sequence. |
| REQ | TIME-02 | Add material segments with source/target ranges | 03-01, 03-02, 03-04 | COVERED | Generic material add in 03-02; text/audio add in 03-04. |
| REQ | TIME-03 | Select, move, split, trim, delete | 03-02 | COVERED | Exact-state Rust command tests. |
| REQ | TIME-04 | Undo/redo every committed edit | 03-03, 03-04 | COVERED | Core edits in 03-03; text/audio edits in 03-04. |
| REQ | TIME-05 | Main-track magnet/snapping in Rust core | 03-03 | COVERED | Rust snapping and magnet events. |
| REQ | TIME-06 | Invalid edits rejected atomically | 03-01 to 03-04 | COVERED | Validation foundation, command tests, history immutability, and text/audio rejections. |
| REQ | TIME-07 | Track stacking and mute state represented | 03-01, 03-04 | COVERED | Track order/locked/mute rules and `setTrackMute`. |
| REQ | TEXT-01 | Add text/subtitle segments | 03-04 | COVERED | `addTextSegment`. |
| REQ | TEXT-02 | Edit MVP text content/style | 03-04 | COVERED | `editTextSegment` covers content, font size, color, alignment, stroke, shadow, background. |
| REQ | AUD-01 | Add audio/BGM materials to audio track | 03-04 | COVERED | `addAudioSegment`. |
| REQ | AUD-02 | Adjust segment volume and track mute | 03-04 | COVERED | `setSegmentVolume` and `setTrackMute`. |
| REQ | TEST-02 | Command tests for split, trim, move, delete, snapping, undo, redo, text edit, volume edit | 03-02 to 03-04 | COVERED | Final source guards and phase test scripts require all focused tests. |
| RESEARCH | Clone/patch/validate/commit transaction | 03-01 to 03-04 | COVERED | Every command path uses validated clone commit and rejection tests. |
| RESEARCH | Generated schema/TS drift checks | 03-01 to 03-04 | COVERED | `VE_UPDATE_GENERATED_CONTRACTS=1`, schema export tests, and `git diff` gates. |
| RESEARCH | Package legitimacy audit | N/A | COVERED | No new npm/pip/cargo packages planned; no install checkpoint needed. |
| CONTEXT | D-01 through D-04 | 03-01 | COVERED | Root timeline, track order, no overlap, Jianying terms. |
| CONTEXT | D-05 through D-09 | 03-01, 03-02 | COVERED | Pure command crate, typed contracts, atomicity, caller IDs, events. |
| CONTEXT | D-10 through D-13 | 03-01, 03-02 | COVERED | Integer microseconds, source/target semantics, split/trim updates, material-duration bounds. |
| CONTEXT | D-14 through D-17 | 03-03 | COVERED | Rust snapping, threshold, first-video-track magnet, events. |
| CONTEXT | D-18 through D-21 | 03-03, 03-04 | COVERED | Session-only bounded snapshot undo/redo for all MVP edits. |
| CONTEXT | D-22 through D-25 | 03-04 | COVERED | Semantic text/audio implemented; advanced effects and rendering excluded. |
| CONTEXT | D-26 through D-28 | 03-04 | COVERED | Command coverage, generated contracts, classified fixtures. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement semantic text segment commands</name>
  <read_first>
    @.planning/phases/03-timeline-command-core/03-CONTEXT.md
    @.planning/phases/03-timeline-command-core/03-VALIDATION.md
    @.planning/phases/03-timeline-command-core/03-PATTERNS.md
    @.planning/phases/03-timeline-command-core/03-03-SUMMARY.md
    @crates/draft_model/src/timeline.rs
    @crates/draft_model/src/validation.rs
    @crates/draft_model/src/lib.rs
    @crates/draft_commands/src/timeline.rs
    @crates/draft_commands/src/history.rs
  </read_first>
  <files>crates/draft_model/src/timeline.rs, crates/draft_model/src/validation.rs, crates/draft_model/src/lib.rs, crates/draft_model/tests/schema_exports.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/src/text.rs, crates/draft_commands/tests/text_audio_commands.rs, crates/bindings_node/src/lib.rs, crates/bindings_node/tests/binding_smoke.rs, schemas/command.schema.json, schemas/draft.schema.json, apps/desktop-electron/src/generated/CommandEnvelope.ts, apps/desktop-electron/src/generated/CommandResultEnvelope.ts, apps/desktop-electron/src/generated/Draft.ts</files>
  <behavior>
    - `TextSegment` stores editable content directly in semantic draft data and is not encoded only in `Material.uri`.
    - `TextStyle` covers `font_size`, `color`, `alignment`, `stroke`, `shadow`, and `background` with deterministic JSON-compatible scalar fields.
    - `add_text_segment` creates or updates a clearly modeled internal text material/source pattern compatible with text tracks.
    - `edit_text_segment` changes only text content/style fields and preserves source/target timeranges unless the payload explicitly edits ranges through existing timeline commands.
    - Undo/redo works for add text and edit text commands.
    - Deterministic text layout and pinned-font rendering remain absent from this phase.
  </behavior>
  <action>
    Add persisted semantic text structs in `crates/draft_model/src/timeline.rs`: `TextSegment`, `TextStyle`, `TextAlignment`, `TextStroke`, `TextShadow`, and `TextBackground`. `TextSegment` must contain `content: String` and `style: TextStyle`. `TextStyle` must expose `font_size: u32`, `color: String`, `alignment: TextAlignment`, `stroke: Option<TextStroke>`, `shadow: Option<TextShadow>`, and `background: Option<TextBackground>`. Add `#[serde(default, skip_serializing_if = "Option::is_none")] pub text: Option<TextSegment>` to `Segment` with matching `ts-rs` optional/null handling so existing fixtures deserialize cleanly. Extend validation to require non-empty text content where `Segment.text` is present and to validate MVP style scalar values without enforcing Phase 5 layout/rendering. Add `AddTextSegment` and `EditTextSegment` command contracts and implement `crates/draft_commands/src/text.rs` using the same clone/validate/commit/history pattern as timeline edits. Ensure text uses a modeled internal text material/source plus `Segment.text`; do not hide text content only in a URI string per D-23. Route text commands through `bindings_node::execute_command` by delegation to `draft_commands`, then regenerate schema and TypeScript from Rust.
  </action>
  <verify>
    <automated>VE_UPDATE_GENERATED_CONTRACTS=1 cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>cargo test -p draft_commands text_commands -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>cargo test -p bindings_node timeline -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    Text commands add and edit semantic text/subtitle segments with content, font size, color, alignment, stroke, shadow, and background. Text content is visible in draft semantics and generated `Draft.ts`, not hidden only in a URI. Undo/redo covers add/edit text. No deterministic layout, pinned fonts, text effects, text bubbles, preview, or export behavior is implemented.
  </acceptance_criteria>
  <done>Text semantics, commands, routes, generated contracts, and tests are complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement semantic audio and volume commands</name>
  <read_first>
    @.planning/phases/03-timeline-command-core/03-CONTEXT.md
    @.planning/phases/03-timeline-command-core/03-VALIDATION.md
    @.planning/phases/03-timeline-command-core/03-PATTERNS.md
    @crates/draft_model/src/material.rs
    @crates/draft_model/src/timeline.rs
    @crates/draft_commands/src/timeline.rs
    @crates/draft_commands/src/history.rs
  </read_first>
  <files>crates/draft_model/src/timeline.rs, crates/draft_model/src/validation.rs, crates/draft_model/src/lib.rs, crates/draft_model/tests/schema_exports.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/src/audio.rs, crates/draft_commands/tests/text_audio_commands.rs, crates/bindings_node/src/lib.rs, crates/bindings_node/tests/binding_smoke.rs, schemas/command.schema.json, schemas/draft.schema.json, apps/desktop-electron/src/generated/CommandEnvelope.ts, apps/desktop-electron/src/generated/CommandResultEnvelope.ts, apps/desktop-electron/src/generated/Draft.ts</files>
  <behavior>
    - `SegmentVolume` uses integer millivolume semantics with default `1000`, where `0` is silent, `1000` is unity gain, and `4000` is the maximum accepted value.
    - `add_audio_segment` accepts audio/BGM materials on audio tracks and rejects incompatible video/text track targets.
    - `set_segment_volume` updates only segment volume and rejects invalid volume bounds.
    - `set_track_mute` updates `Track.muted` through Rust command semantics and rejects locked-track mutation.
    - Undo/redo works for add audio, set volume, and set track mute.
  </behavior>
  <action>
    Add `SegmentVolume` to `crates/draft_model/src/timeline.rs` with `level_millis: u32`, `Default` of `1000`, and `MAX_SEGMENT_VOLUME_MILLIS = 4000`; validation must accept `0..=4000` and reject values above `4000`. Add `#[serde(default)] pub volume: SegmentVolume` to `Segment` so existing fixtures deserialize to unity gain. Add `AddAudioSegment`, `SetSegmentVolume`, and `SetTrackMute` command contracts in `draft_model/src/lib.rs` and export them through schema/TypeScript. Implement `crates/draft_commands/src/audio.rs` so audio commands reuse Plan 03-01 track/material compatibility checks, reject locked-track mutation, update `Track.muted` through command semantics, update volume without using floating values, and push history only after successful validation. Route audio commands through `bindings_node::execute_command` by delegation to `draft_commands`. Do not add waveform generation, preview cache invalidation, mixing, render graph, or export behavior per D-24.
  </action>
  <verify>
    <automated>VE_UPDATE_GENERATED_CONTRACTS=1 cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>cargo test -p draft_commands audio_commands -- --nocapture</automated>
    <automated>cargo test -p draft_commands undo_redo -- --nocapture</automated>
    <automated>cargo test -p bindings_node timeline -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    Audio commands add audio/BGM material segments to audio tracks, adjust integer segment volume, and update track mute state through Rust semantics. Undo/redo covers add audio, volume edit, and track mute. No waveform, mixing, preview, render graph, or export behavior is introduced.
  </acceptance_criteria>
  <done>Audio semantics, commands, routes, generated contracts, and tests are complete.</done>
</task>

<task type="auto">
  <name>Task 3: Add command fixtures, source guards, and final Phase 3 gates</name>
  <read_first>
    @.planning/phases/03-timeline-command-core/03-CONTEXT.md
    @.planning/phases/03-timeline-command-core/03-VALIDATION.md
    @.planning/phases/03-timeline-command-core/03-PATTERNS.md
    @crates/draft_model/tests/schema_exports.rs
    @fixtures/draft/minimal-command.json
    @package.json
    @justfile
  </read_first>
  <files>crates/draft_model/tests/schema_exports.rs, fixtures/draft/minimal-timeline-command.json, fixtures/draft/invalid-timeline-command.json, package.json, justfile</files>
  <action>
    Add positive and negative timeline command fixtures at `fixtures/draft/minimal-timeline-command.json` and `fixtures/draft/invalid-timeline-command.json`, then update the explicit positive/negative fixture sets in `schema_exports.rs` so unclassified JSON fails tests per D-28. Add or update package scripts for `test:phase3-commands` and `test:phase3-source-guards`. `test:phase3-commands` must run focused command coverage for `timeline_edits`, `invalid_edits_are_atomic`, `snapping`, `undo_redo`, `text_commands`, and `audio_commands`. `test:phase3-source-guards` must fail on platform/runtime leakage in `draft_commands`, direct renderer timeline semantics, direct renderer mutation of `Draft.tracks`, semantic float seconds, `Asset`/`Clip` vocabulary in new timeline command contracts, command history inside `project.json` fixtures, and ungenerated contract drift. Include the Phase 3 scripts in the root `test` or `just test` path without removing prior Phase 1/2 gates. Prefer final gates from `03-VALIDATION.md`: `PATH="$HOME/.cargo/bin:$PATH" just build`, `PATH="$HOME/.cargo/bin:$PATH" just test`, and `git diff --exit-code schemas apps/desktop-electron/src/generated`.
  </action>
  <verify>
    <automated>cargo test -p draft_model schema_fixtures -- --nocapture</automated>
    <automated>pnpm run test:phase3-commands</automated>
    <automated>pnpm run test:phase3-source-guards</automated>
    <automated>pnpm run test:rust && pnpm run test:contracts && pnpm run test:bindings</automated>
    <automated>PATH="$HOME/.cargo/bin:$PATH" just build</automated>
    <automated>PATH="$HOME/.cargo/bin:$PATH" just test</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    TEST-02 coverage is executable through named package scripts and final gates. Fixture classification remains explicit. Source guards enforce pure semantic crate boundaries, Rust-owned timeline semantics, integer time, Jianying terminology, session-only command history, and generated-contract drift. `just build` and `just test` are the final phase gates when local `just` is available; if unavailable, the executor records that and runs `pnpm run build` plus `pnpm run test` as the documented fallback.
  </acceptance_criteria>
  <done>Phase 3 command coverage, source guards, fixtures, contract drift checks, and final build/test gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Text/audio command payload -> semantic draft | User-provided text, style, audio volume, and mute requests become persisted draft semantics. |
| Command tests/fixtures -> generated contracts | Fixture corpus and generated schema/TS prove command contract shape. |
| Final gates -> phase verification | Root scripts enforce security and architecture invariants before verify-work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Malformed text/audio command payloads | mitigate | Strict generated payloads, schema fixtures, and binding invalid-payload tests. |
| T-03-02 | Tampering/DoS | Text/audio source and target timeranges | mitigate | Reuse checked microsecond timerange helpers and reject zero/overflow/overrun cases. |
| T-03-03 | Tampering | Locked track, incompatible audio/text target, or mute mutation | mitigate | Reject locked-track mutation and incompatible track/material pairs before commit. |
| T-03-04 | Repudiation/Tampering | Rejected text/audio command history mutation | mitigate | Use clone/validate/commit and push history only after successful validation; tests cover rejected text/audio edits. |
| T-03-05 | Elevation of Privilege/Tampering | Renderer bypass of Rust text/audio semantics | mitigate | Binding routes generated commands to `draft_commands`; source guards fail direct renderer `Draft.tracks` mutation or timeline repair logic. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned. Stop for human legitimacy verification if a new dependency is proposed. |
</threat_model>

<verification>
<automated>cargo test -p draft_commands timeline_edits -- --nocapture</automated>
<automated>cargo test -p draft_commands invalid_edits_are_atomic -- --nocapture</automated>
<automated>cargo test -p draft_commands snapping -- --nocapture</automated>
<automated>cargo test -p draft_commands undo_redo -- --nocapture</automated>
<automated>cargo test -p draft_commands text_commands -- --nocapture</automated>
<automated>cargo test -p draft_commands audio_commands -- --nocapture</automated>
<automated>cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
<automated>cargo test -p bindings_node timeline -- --nocapture</automated>
<automated>pnpm run test:phase3-commands</automated>
<automated>pnpm run test:phase3-source-guards</automated>
<automated>PATH="$HOME/.cargo/bin:$PATH" just build</automated>
<automated>PATH="$HOME/.cargo/bin:$PATH" just test</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- TEXT-01 and TEXT-02 are satisfied by semantic text commands and generated draft/command contracts.
- AUD-01 and AUD-02 are satisfied by semantic audio add, segment volume, and track mute commands.
- TIME-04 covers every committed MVP edit, including text and audio.
- TEST-02 is satisfied by named command test coverage and source guards.
- Deferred Phase 4/5/post-MVP items remain unimplemented.
</success_criteria>

<output>
Create `.planning/phases/03-timeline-command-core/03-04-SUMMARY.md` when done.
</output>
