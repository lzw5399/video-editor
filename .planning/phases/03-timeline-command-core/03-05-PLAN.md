---
phase: 03-timeline-command-core
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - crates/draft_model/tests/schema_exports.rs
  - fixtures/draft/minimal-timeline-command.json
  - fixtures/draft/invalid-timeline-command.json
  - package.json
  - justfile
autonomous: true
requirements:
  - TIME-06
  - TEST-02
must_haves:
  truths:
    - "Command tests cover split, trim, move, delete, snapping, undo, redo, text edit, and volume edit per D-26 and TEST-02."
    - "Generated command schema and TypeScript contracts are updated from Rust and checked for drift; final gates use `just build` and `just test` per D-27."
    - "Positive and negative timeline command fixtures are explicitly classified, and unclassified JSON fixtures fail tests per D-28."
    - "Source guards enforce pure `draft_commands`, Rust-owned timeline semantics, integer semantic time, Jianying terminology, session-only command history, and generated-contract drift."
  artifacts:
    - path: fixtures/draft/minimal-timeline-command.json
      provides: "positive timeline command fixture"
    - path: fixtures/draft/invalid-timeline-command.json
      provides: "negative timeline command fixture"
    - path: crates/draft_model/tests/schema_exports.rs
      provides: "explicit command fixture classification and generated contract drift checks"
    - path: package.json
      provides: "Phase 3 command coverage and source guard scripts"
    - path: justfile
      provides: "Phase 3 scripts included in public build/test gate path"
  key_links:
    - from: crates/draft_model/tests/schema_exports.rs
      to: fixtures/draft/minimal-timeline-command.json
      via: "positive command fixture classification"
      pattern: "minimal-timeline-command\\.json"
    - from: crates/draft_model/tests/schema_exports.rs
      to: fixtures/draft/invalid-timeline-command.json
      via: "negative command fixture classification"
      pattern: "invalid-timeline-command\\.json"
    - from: package.json
      to: crates/draft_commands/tests
      via: "Phase 3 command coverage script"
      pattern: "test:phase3-commands"
    - from: package.json
      to: "source guard commands"
      via: "Phase 3 source guard script"
      pattern: "test:phase3-source-guards"
---

<objective>
Close Phase 3 with classified command fixtures, source guards, named command coverage scripts, generated-contract drift checks, and final build/test gates.

Purpose: The text/audio implementation in Plan 03-04 completes the command surface; this plan makes the phase executable and auditable by enforcing coverage, fixture discipline, architecture boundaries, and final gates. It implements TEST-02, reinforces TIME-06, and honors D-26 through D-28.

Output: Timeline command fixtures, fixture classification updates, Phase 3 package scripts, just gate inclusion, and final verification commands.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-timeline-command-core/03-CONTEXT.md
@.planning/phases/03-timeline-command-core/03-RESEARCH.md
@.planning/phases/03-timeline-command-core/03-VALIDATION.md
@.planning/phases/03-timeline-command-core/03-PATTERNS.md
@.planning/phases/03-timeline-command-core/03-04-SUMMARY.md
@crates/draft_model/tests/schema_exports.rs
@fixtures/draft/minimal-command.json
@package.json
@justfile
</context>

## Artifacts this phase produces

- Fixtures: `fixtures/draft/minimal-timeline-command.json` and `fixtures/draft/invalid-timeline-command.json` as explicitly classified positive/negative timeline command fixtures.
- Fixture classification: updated positive/negative command fixture sets in `crates/draft_model/tests/schema_exports.rs`.
- Scripts/gates: `test:phase3-commands`, `test:phase3-source-guards`, and root `test`/`just test` inclusion without removing prior Phase 1/2 gates.
- Final verification: `PATH="$HOME/.cargo/bin:$PATH" just build`, `PATH="$HOME/.cargo/bin:$PATH" just test`, and `git diff --exit-code schemas apps/desktop-electron/src/generated`.

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 3 | Rust-owned timeline editing semantics and command/undo behavior before rich UI | 03-01 to 03-05 | COVERED | This plan closes fixture, source guard, and final gate proof after command implementation. |
| REQ | TIME-01 | Draft supports root sequence with video/audio/text tracks | 03-01 | COVERED | Root `Draft.tracks` only; no multi-sequence. |
| REQ | TIME-02 | Add material segments with source/target ranges | 03-01, 03-02, 03-04 | COVERED | Generic material add in 03-02; text/audio add in 03-04. |
| REQ | TIME-03 | Select, move, split, trim, delete | 03-02 | COVERED | Exact-state Rust command tests. |
| REQ | TIME-04 | Undo/redo every committed edit | 03-03, 03-04 | COVERED | Core edits in 03-03; text/audio edits in 03-04. |
| REQ | TIME-05 | Main-track magnet/snapping in Rust core | 03-03 | COVERED | Rust snapping and magnet events. |
| REQ | TIME-06 | Invalid edits rejected atomically | 03-01 to 03-05 | COVERED | Validation foundation, command tests, history immutability, text/audio rejections, and source guards. |
| REQ | TIME-07 | Track stacking and mute state represented | 03-01, 03-04 | COVERED | Track order/locked/mute rules and `setTrackMute`. |
| REQ | TEXT-01 | Add text/subtitle segments | 03-04 | COVERED | `addTextSegment`. |
| REQ | TEXT-02 | Edit MVP text content/style | 03-04 | COVERED | `editTextSegment` covers content, font size, color, alignment, stroke, shadow, background. |
| REQ | AUD-01 | Add audio/BGM materials to audio track | 03-04 | COVERED | `addAudioSegment`. |
| REQ | AUD-02 | Adjust segment volume and track mute | 03-04 | COVERED | `setSegmentVolume` and `setTrackMute`. |
| REQ | TEST-02 | Command tests for split, trim, move, delete, snapping, undo, redo, text edit, volume edit | 03-02 to 03-05 | COVERED | Final source guards and phase test scripts require all focused tests. |
| RESEARCH | Clone/patch/validate/commit transaction | 03-01 to 03-05 | COVERED | Every command path uses validated clone commit and rejection tests; final guards enforce it. |
| RESEARCH | Generated schema/TS drift checks | 03-01 to 03-05 | COVERED | `VE_UPDATE_GENERATED_CONTRACTS=1`, schema export tests, and `git diff` gates. |
| RESEARCH | Package legitimacy audit | N/A | COVERED | No new npm/pip/cargo packages planned; no install checkpoint needed. |
| CONTEXT | D-01 through D-04 | 03-01 | COVERED | Root timeline, track order, no overlap, Jianying terms. |
| CONTEXT | D-05 through D-09 | 03-01, 03-02 | COVERED | Pure command crate, typed contracts, atomicity, caller IDs, events. |
| CONTEXT | D-10 through D-13 | 03-01, 03-02 | COVERED | Integer microseconds, source/target semantics, split/trim updates, material-duration bounds. |
| CONTEXT | D-14 through D-17 | 03-03 | COVERED | Rust snapping, threshold, first-video-track magnet, events. |
| CONTEXT | D-18 through D-21 | 03-03, 03-04 | COVERED | Session-only bounded snapshot undo/redo for all MVP edits. |
| CONTEXT | D-22 through D-25 | 03-04 | COVERED | Semantic text/audio implemented; advanced effects and rendering excluded. |
| CONTEXT | D-26 through D-28 | 03-05 | COVERED | Command coverage, generated contracts, classified fixtures, final gates. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add classified timeline command fixtures</name>
  <read_first>
    @.planning/phases/03-timeline-command-core/03-CONTEXT.md
    @.planning/phases/03-timeline-command-core/03-VALIDATION.md
    @.planning/phases/03-timeline-command-core/03-PATTERNS.md
    @.planning/phases/03-timeline-command-core/03-04-SUMMARY.md
    @crates/draft_model/tests/schema_exports.rs
    @fixtures/draft/minimal-command.json
  </read_first>
  <files>crates/draft_model/tests/schema_exports.rs, fixtures/draft/minimal-timeline-command.json, fixtures/draft/invalid-timeline-command.json</files>
  <behavior>
    - `minimal-timeline-command.json` is a positive command fixture covering a valid Phase 3 timeline command envelope.
    - `invalid-timeline-command.json` is a negative command fixture covering a Phase 3 invalid command payload or mismatch.
    - `schema_exports.rs` explicitly classifies both fixtures so unclassified `fixtures/draft/*.json` files fail tests.
    - Negative fixture behavior remains checked through serde and JSON Schema where applicable.
  </behavior>
  <action>
    Follow the explicit 03-PATTERNS.md fixture analogs: use `fixtures/draft/minimal-command.json` for positive command fixture shape and `crates/draft_model/tests/schema_exports.rs` for the positive/negative fixture classification pattern. Add `fixtures/draft/minimal-timeline-command.json` and `fixtures/draft/invalid-timeline-command.json`, then update the explicit positive/negative fixture sets in `schema_exports.rs` so unclassified JSON fails tests per D-28. The positive fixture must exercise generated Phase 3 command structure without generated media. The negative fixture must fail the same command fixture gates with an exact expected rejection where existing fixture helpers support exact errors.
  </action>
  <verify>
    <automated>cargo test -p draft_model schema_fixtures -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    Timeline command fixtures exist, are explicitly classified, and pass/fail the intended schema fixture tests. No generated media is committed.
  </acceptance_criteria>
  <done>Positive and negative timeline command fixtures are classified and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Add source guards and final Phase 3 gates</name>
  <read_first>
    @.planning/phases/03-timeline-command-core/03-CONTEXT.md
    @.planning/phases/03-timeline-command-core/03-VALIDATION.md
    @.planning/phases/03-timeline-command-core/03-PATTERNS.md
    @package.json
    @justfile
  </read_first>
  <files>package.json, justfile</files>
  <action>
    Follow the 03-PATTERNS.md shared patterns for pure semantic boundaries, generated contract drift, and fixture discipline, and preserve the existing named root gate style in `package.json` and `justfile`. Add or update package scripts for `test:phase3-commands` and `test:phase3-source-guards`. `test:phase3-commands` must run focused command coverage for `timeline_edits`, `invalid_edits_are_atomic`, `snapping`, `undo_redo`, `text_commands`, and `audio_commands`. `test:phase3-source-guards` must fail on platform/runtime leakage in `draft_commands`, direct renderer timeline semantics, direct renderer mutation of `Draft.tracks`, semantic float seconds, `Asset`/`Clip` vocabulary in new timeline command contracts, command history inside `project.json` fixtures, and ungenerated contract drift. Include the Phase 3 scripts in the root `test` or `just test` path without removing prior Phase 1/2 gates. Prefer final gates from `03-VALIDATION.md`: `PATH="$HOME/.cargo/bin:$PATH" just build`, `PATH="$HOME/.cargo/bin:$PATH" just test`, and `git diff --exit-code schemas apps/desktop-electron/src/generated`.
  </action>
  <verify>
    <automated>pnpm run test:phase3-commands</automated>
    <automated>pnpm run test:phase3-source-guards</automated>
    <automated>pnpm run test:rust && pnpm run test:contracts && pnpm run test:bindings</automated>
    <automated>PATH="$HOME/.cargo/bin:$PATH" just build</automated>
    <automated>PATH="$HOME/.cargo/bin:$PATH" just test</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    TEST-02 coverage is executable through named package scripts and final gates. Source guards enforce pure semantic crate boundaries, Rust-owned timeline semantics, integer time, Jianying terminology, session-only command history, and generated-contract drift. `just build` and `just test` are the final phase gates when local `just` is available; if unavailable, the executor records that and runs `pnpm run build` plus `pnpm run test` as the documented fallback.
  </acceptance_criteria>
  <done>Phase 3 command coverage, source guards, contract drift checks, and final build/test gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Command fixtures -> generated contracts | Fixture corpus verifies command contract shape through Rust-generated schema/TypeScript expectations. |
| Source guards -> architecture invariants | Root scripts enforce pure command boundaries and prevent renderer-owned timeline semantics. |
| Final gates -> phase verification | Build/test/drift commands define executable proof before verify-work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Malformed timeline command fixtures | mitigate | Positive/negative command fixtures are explicitly classified and validated through existing schema fixture gates. |
| T-03-02 | Tampering/DoS | Semantic float time or overflow-prone command contracts | mitigate | Source guards fail float seconds in command contracts and require generated contract drift checks. |
| T-03-03 | Tampering | Platform/runtime leakage into `draft_commands` | mitigate | Source guards fail FFmpeg, filesystem, Electron, Node, preview, render, or project-store imports in the pure command crate. |
| T-03-04 | Repudiation/Tampering | Missing required command coverage | mitigate | `test:phase3-commands` runs required focused tests for edits, snapping, undo/redo, text, and audio. |
| T-03-05 | Elevation of Privilege/Tampering | Renderer bypass of Rust timeline semantics | mitigate | Source guards fail renderer-side `Draft.tracks` mutation or timeline repair logic. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned. Stop for human legitimacy verification if a new dependency is proposed. |
</threat_model>

<verification>
<automated>cargo test -p draft_model schema_fixtures -- --nocapture</automated>
<automated>pnpm run test:phase3-commands</automated>
<automated>pnpm run test:phase3-source-guards</automated>
<automated>pnpm run test:rust && pnpm run test:contracts && pnpm run test:bindings</automated>
<automated>PATH="$HOME/.cargo/bin:$PATH" just build</automated>
<automated>PATH="$HOME/.cargo/bin:$PATH" just test</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- TEST-02 is satisfied by named command test coverage and source guards.
- Classified timeline command fixtures prove positive and negative command schema behavior.
- Final Phase 3 gates preserve prior Phase 1/2 gate coverage while adding command-core coverage.
- Generated schema and TypeScript contract drift is checked at phase close.
</success_criteria>

<output>
Create `.planning/phases/03-timeline-command-core/03-05-SUMMARY.md` when done.
</output>
