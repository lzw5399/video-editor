---
phase: 17-template-import-core-and-kaipai-offline-adapter-foundation
plan: "05"
type: execute
wave: 2
depends_on:
  - 17-02
  - 17-03
  - 17-04
  - 17-07
files_modified:
  - crates/adapter_kaipai/src/lib.rs
  - crates/adapter_kaipai/src/mapper.rs
  - crates/adapter_kaipai/tests/mapper.rs
  - fixtures/kaipai/positive/main-video.json
  - fixtures/kaipai/positive/pip-overlay.json
  - fixtures/kaipai/positive/text-sticker.json
  - fixtures/kaipai/positive/bgm-audio.json
  - fixtures/kaipai/negative/missing-resource.json
  - fixtures/kaipai/negative/native-effect.json
  - fixtures/kaipai/expected-reports/main-video.report.json
  - fixtures/kaipai/expected-reports/pip-overlay.report.json
  - fixtures/kaipai/expected-reports/text-sticker.report.json
  - fixtures/kaipai/expected-reports/bgm-audio.report.json
  - fixtures/kaipai/expected-reports/missing-resource.report.json
  - fixtures/kaipai/expected-reports/native-effect.report.json
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - PRODFX-05
requirements_addressed:
  - COMP-01
  - COMP-02
  - PRODFX-05
must_haves:
  truths:
    - "Offline Kaipai fixtures map into canonical `DraftImportPlan`, not direct draft/session mutation, per D-11, D-37."
    - "Supported subset covers canvas, main video, PIP, stickers, text, BGM/audio, simple animation, simple fades/dissolves, and native-effect diagnostics, per D-17 through D-25."
    - "Generic gaps are handled: z-order/bounds, font closure, center-anchor rotation, constant speed mapping, and report entries, per D-26 through D-30."
    - "Report snapshots cover supported main video, PIP, text sticker, BGM, missing resource, and native effect degradation, per D-33, D-39, D-46."
  artifacts:
    - path: "crates/adapter_kaipai/src/mapper.rs"
      provides: "map_kaipai_bundle_to_import_plan"
    - path: "fixtures/kaipai/expected-reports/*.report.json"
      provides: "AdaptationReport snapshots for fixture families"
  key_links:
    - from: "crates/adapter_kaipai/src/mapper.rs"
      to: "crates/draft_import/src/import_plan.rs"
      via: "emits DraftImportPlan"
      pattern: "DraftImportPlan"
    - from: "crates/adapter_kaipai/src/mapper.rs"
      to: "crates/draft_import/src/resource_localizer.rs"
      via: "localized resources before material refs"
      pattern: "localize_template_resources"
---

<objective>
Map the first offline Kaipai fixture subset into provider-neutral import plans and adaptation reports.

Purpose: users need editable approximate drafts with explicit diagnostics, while core/render/session layers stay provider-neutral.
Output: mapper function, fixture corpus, report snapshots, and tests for the supported subset.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-RESEARCH.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-PATTERNS.md
@crates/draft_import/src/import_plan.rs
@crates/draft_import/src/resource_localizer.rs
@crates/draft_model/src/canvas.rs
@crates/draft_model/src/timeline.rs
@crates/draft_model/src/font_registry.rs
@crates/ffmpeg_compiler/tests/transform_snapshots.rs
</context>

## Artifacts This Phase Produces

- Rust functions/types: `map_kaipai_bundle_to_import_plan`, `KaipaiImportOptions`, `KaipaiMappedFixture`
- Fixture families: main video, PIP overlay, text sticker, BGM/audio, missing resource, native effect degradation
- Report snapshots for each fixture family

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mapper fixture and report snapshot tests</name>
  <files>crates/adapter_kaipai/tests/mapper.rs, fixtures/kaipai/positive/main-video.json, fixtures/kaipai/positive/pip-overlay.json, fixtures/kaipai/positive/text-sticker.json, fixtures/kaipai/positive/bgm-audio.json, fixtures/kaipai/negative/missing-resource.json, fixtures/kaipai/negative/native-effect.json, fixtures/kaipai/expected-reports/main-video.report.json, fixtures/kaipai/expected-reports/pip-overlay.report.json, fixtures/kaipai/expected-reports/text-sticker.report.json, fixtures/kaipai/expected-reports/bgm-audio.report.json, fixtures/kaipai/expected-reports/missing-resource.report.json, fixtures/kaipai/expected-reports/native-effect.report.json</files>
  <read_first>
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md` decisions D-17 through D-30, D-33, D-39, D-46.
    - Reference only: `git ls-tree -r --name-only origin/work/kaipai-adapter-poc -- fixtures/kaipai`.
    - `crates/draft_import/tests/import_plan.rs` from Plan 17-03.
  </read_first>
  <behavior>
    - Main video maps canvas, source/target timeranges, crop/fit, position, scale, opacity, and speed-adjusted duration.
    - PIP and sticker fixtures map to material-backed overlay tracks with explicit z-order/bounds/fit/opacity/transform.
    - Text fixture maps content/style/stroke/shadow/layout/font fallback and records font localization gaps.
    - Audio fixture maps BGM material, gain/volume, fade-in, and fade-out.
    - Native effect and missing resource fixtures produce required report classifications.
  </behavior>
  <action>Create current-main fixture families and expected `AdaptationReport` snapshots. The snapshots must use `supported`, `approximated`, `dropped`, `missingResource`, and `needsNativeEffect` where appropriate. Include static rotation cases only after Plan 17-07 closes generic export rotation parity; keep complex animation curves and native motion presets classified as approximated or dropped with clear report entries.</action>
  <verify>
    <automated>cargo test -p adapter_kaipai offline_mapper -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before mapper implementation and pass after Task 2.
    - Expected reports include all required fixture families and classifications.
    - No expected report treats a native effect as supported.
  </acceptance_criteria>
  <done>Fixture and report expectations are executable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Kaipai-to-DraftImportPlan mapper</name>
  <files>crates/adapter_kaipai/src/lib.rs, crates/adapter_kaipai/src/mapper.rs</files>
  <read_first>
    - `crates/draft_import/src/import_plan.rs` and `crates/draft_import/src/resource_localizer.rs`.
    - `crates/draft_model/src/canvas.rs`, `crates/draft_model/src/timeline.rs`, and `crates/draft_model/src/font_registry.rs`.
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-RESEARCH.md` Pitfall 4 and rotation recommendation.
  </read_first>
  <action>Implement `map_kaipai_bundle_to_import_plan` so it parses an already validated bundle, localizes resources first, maps supported fields into canonical draft import plan types, and returns the plan plus report. Map Kaipai level to generic track ordering. Map constant speed by adjusting canonical source/target duration or emitting an approximated report item when exact mapping cannot be represented. Map simple position/scale/opacity keyframes to canonical `Keyframe`s. Use bundled/local `fontRef` when possible and produce report entries when requested fonts cannot be localized or rendered consistently. Keep raw formula interpretation inside `adapter_kaipai` only.</action>
  <verify>
    <automated>cargo test -p adapter_kaipai offline_mapper -- --nocapture && cargo test -p draft_import draft_import_plan -- --nocapture && pnpm run test:phase17-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - Mapper output validates through `draft_import::validate_import_plan`.
    - `DraftImportPlan` contains localized project-relative resources only.
    - Canvas, main video, PIP/sticker, text, audio, simple animation, simple fade/dissolve, constant speed, z-order, bounds, font fallback, and rotation cases are covered by assertions.
    - Native effects, missing resources, complex curves, and unsupported transitions are visible in reports rather than hidden as support.
  </acceptance_criteria>
  <done>Offline Kaipai adapter emits provider-neutral import plans and report snapshots for the supported subset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `adapter_kaipai` formula mapper -> `DraftImportPlan` | Provider-specific interpretation becomes canonical import intent. |
| localized resources -> material-backed segments | Files copied from templates become preview/export inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-17 | Tampering | Mapper supported subset | mitigate | Validate every mapper output through `DraftImportPlan` and `draft_model` validation. |
| T-17-18 | Repudiation | Approximation reporting | mitigate | Snapshot required report statuses for approximated/dropped/missing/native-effect cases. |
| T-17-19 | Tampering | Resource refs in mapped materials | mitigate | Consume only localizer results and fail/report missing or unsafe refs before plan application. |
| T-17-20 | Information Disclosure | Provider provenance | mitigate | Preserve external refs only in report evidence and block raw formula leakage through source guards. |
</threat_model>

<verification>
Run:
- `cargo test -p adapter_kaipai offline_mapper -- --nocapture`
- `cargo test -p draft_import draft_import_plan -- --nocapture`
- `pnpm run test:phase17-source-guards`
</verification>

<success_criteria>
The plan is complete when the supported Kaipai fixture families map to validated provider-neutral import plans and report every unsupported/degraded feature explicitly.
</success_criteria>

<output>
Create `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-05-SUMMARY.md` when done.
</output>
