---
phase: 17-template-import-core-and-kaipai-offline-adapter-foundation
plan: "10"
type: execute
wave: 4
depends_on:
  - 17-05
files_modified:
  - crates/adapter_kaipai/src/lib.rs
  - crates/adapter_kaipai/src/mapper.rs
  - crates/adapter_kaipai/tests/mapper.rs
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - PRODFX-05
requirements_addressed:
  - COMP-01
  - COMP-02
  - PRODFX-05
must_haves:
  truths:
    - "Offline Kaipai fixtures map into canonical `DraftImportPlan`, not direct draft/session mutation, per D-11, D-37."
    - "Supported subset mapping covers canvas, main video, PIP, stickers, text, BGM/audio, simple animation, simple fades/dissolves, and native-effect diagnostics, per D-17 through D-25."
    - "Main video mapping covers source/target timeranges, crop or fit/fill, position, scale, opacity, and generic transform fields, per D-18."
    - "PIP mapping uses material-backed overlay tracks and converts Kaipai `level` to generic track ordering/z-order behavior, per D-19."
    - "Basic sticker mapping treats image/video stickers as material-backed overlay segments and does not introduce a native Kaipai sticker runtime, per D-20."
    - "Text sticker mapping covers content, position, font size, color, stroke, shadow, basic layout, and font fallback through `fontRef`, per D-21."
    - "BGM/audio mapping covers audio material, volume, fade-in, and fade-out through generic audio segment semantics, per D-22."
    - "Simple animation mapping covers position, scale, and opacity keyframes while complex curves/native motion presets are approximated or reported as unsupported/degraded, per D-23."
    - "Simple transition mapping covers fade/dissolve only when generic preview/export supports it; other transitions are degraded, unsupported, or `needsNativeEffect`, per D-24."
    - "Mapper output handles generic gaps: z-order/bounds, font closure, center-anchor rotation, constant speed mapping, and report entries, per D-26 through D-30."
    - "Constant speed values such as `durationMsWithSpeed` map to canonical source/target duration or explicit degraded diagnostics, per D-27."
    - "Image/video sticker semantics make z-order, bounds, fit, opacity, and transform explicit enough for preview/export tests, per D-29."
    - "Mapper report output preserves supported main video, PIP, text sticker, BGM, missing resource, and native effect degradation classifications, per D-33, D-39, D-46."
  artifacts:
    - path: "crates/adapter_kaipai/src/mapper.rs"
      provides: "map_kaipai_bundle_to_import_plan"
    - path: "crates/adapter_kaipai/tests/mapper.rs"
      provides: "Offline mapper behavior tests using the fixture/report corpus from 17-05"
  key_links:
    - from: "crates/adapter_kaipai/src/mapper.rs"
      to: "crates/draft_import/src/import_plan.rs"
      via: "emits validated DraftImportPlan"
      pattern: "DraftImportPlan"
    - from: "crates/adapter_kaipai/src/mapper.rs"
      to: "crates/draft_import/src/resource_localizer.rs"
      via: "localizes resources before material refs enter the plan"
      pattern: "localize_template_resources"
---

<objective>
Implement the Kaipai-to-DraftImportPlan mapper against the sanitized fixture/report corpus.

Purpose: users need editable approximate drafts with explicit diagnostics, while core/render/session layers stay provider-neutral.
Output: mapper function and behavior tests proving the supported offline Kaipai subset emits provider-neutral import plans and reports.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-RESEARCH.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-PATTERNS.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-05-SUMMARY.md
@crates/draft_import/src/import_plan.rs
@crates/draft_import/src/resource_localizer.rs
@crates/draft_model/src/canvas.rs
@crates/draft_model/src/timeline.rs
@crates/draft_model/src/font_registry.rs
@crates/ffmpeg_compiler/tests/transform_snapshots.rs
</context>

## Artifacts This Phase Produces

- Rust functions/types: `map_kaipai_bundle_to_import_plan`, `KaipaiImportOptions`, `KaipaiMappedFixture`
- Mapper tests for main video, PIP overlay, text sticker, BGM/audio, missing resource, native effect degradation, static rotation, constant speed, z-order, bounds, and font fallback
- Report assertions that match the 17-05 snapshot corpus

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add offline mapper behavior tests</name>
  <files>crates/adapter_kaipai/tests/mapper.rs</files>
  <read_first>
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md` decisions D-17 through D-30, D-33, D-39, D-46.
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-05-SUMMARY.md` for fixture/report snapshot setup.
    - `crates/adapter_kaipai/tests/mapper_fixtures.rs` from Plan 17-05.
    - `crates/draft_import/tests/import_plan.rs` from Plan 17-03.
  </read_first>
  <behavior>
    - Main video maps canvas, source/target timeranges, crop/fit, position, scale, opacity, rotation, and speed-adjusted duration.
    - PIP and sticker fixtures map to material-backed overlay tracks with explicit z-order/bounds/fit/opacity/transform.
    - Text fixture maps content/style/stroke/shadow/layout/font fallback and records font localization gaps.
    - Audio fixture maps BGM material, gain/volume, fade-in, and fade-out.
    - Native effect and missing resource fixtures produce required report classifications.
  </behavior>
  <action>Create `offline_mapper` tests that call `map_kaipai_bundle_to_import_plan` on the sanitized 17-05 fixture families and validate the returned `DraftImportPlan` plus `AdaptationReport`. Assert canonical draft fields only: integer microsecond timeranges, material-backed tracks/segments, generic z-order, generic center-anchor rotation, constant-speed duration mapping, local or bundled `fontRef`, and no raw provider formula/runtime URL fields. The tests must fail before Task 2 and pass after Task 2 within this plan.</action>
  <verify>
    <automated>cargo test -p adapter_kaipai offline_mapper -- --nocapture && pnpm run test:phase17-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover all required fixture families and report classifications.
    - Tests fail on direct draft mutation, remote runtime refs, missing resource classification gaps, or native effects reported as supported.
    - Tests include static rotation only through generic center-anchor semantics closed by Plan 17-07.
  </acceptance_criteria>
  <done>Offline mapper behavior is specified by executable tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Kaipai-to-DraftImportPlan mapper</name>
  <files>crates/adapter_kaipai/src/lib.rs, crates/adapter_kaipai/src/mapper.rs</files>
  <read_first>
    - `crates/draft_import/src/import_plan.rs` and `crates/draft_import/src/resource_localizer.rs`.
    - `crates/draft_model/src/canvas.rs`, `crates/draft_model/src/timeline.rs`, and `crates/draft_model/src/font_registry.rs`.
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-RESEARCH.md` Pitfall 4 and resolved rotation recommendation.
  </read_first>
  <action>Implement `map_kaipai_bundle_to_import_plan` so it parses an already validated bundle, localizes resources first, maps supported fields into canonical draft import plan types, and returns the plan plus report. Map Kaipai level to generic track ordering. Map constant speed by adjusting canonical source/target duration or emitting an approximated report item when exact mapping cannot be represented. Map simple position/scale/opacity keyframes to canonical `Keyframe`s. Use bundled/local `fontRef` when possible and produce report entries when requested fonts cannot be localized or rendered consistently. Keep raw formula interpretation inside `adapter_kaipai` only, per D-04 through D-06 and D-41.</action>
  <verify>
    <automated>cargo test -p adapter_kaipai offline_mapper -- --nocapture && cargo test -p draft_import draft_import_plan -- --nocapture && pnpm run test:phase17-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - Mapper output validates through `draft_import::validate_import_plan`.
    - `DraftImportPlan` contains localized project-relative resources only.
    - Canvas, main video, PIP/sticker, text, audio, simple animation, simple fade/dissolve, constant speed, z-order, bounds, font fallback, and rotation cases are covered by assertions.
    - Native effects, missing resources, complex curves, and unsupported transitions are visible in reports rather than hidden as support.
  </acceptance_criteria>
  <done>Offline Kaipai adapter emits provider-neutral import plans and reports for the supported subset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `adapter_kaipai` formula mapper -> `DraftImportPlan` | Provider-specific interpretation becomes canonical import intent. |
| localized resources -> material-backed segments | Files copied from templates become preview/export inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-37 | Tampering | Mapper supported subset | mitigate | Validate every mapper output through `DraftImportPlan` and `draft_model` validation. |
| T-17-38 | Repudiation | Approximation reporting | mitigate | Assert required report statuses for approximated/dropped/missing/native-effect cases against the 17-05 snapshots. |
| T-17-39 | Tampering | Resource refs in mapped materials | mitigate | Consume only localizer results and fail/report missing or unsafe refs before plan application. |
| T-17-40 | Information Disclosure | Provider provenance | mitigate | Preserve external refs only in report evidence and block raw formula leakage through source guards. |
</threat_model>

<verification>
Run:
- `cargo test -p adapter_kaipai offline_mapper -- --nocapture`
- `cargo test -p draft_import draft_import_plan -- --nocapture`
- `pnpm run test:phase17-source-guards`
</verification>

<success_criteria>
The plan is complete when the supported Kaipai fixture families map to validated provider-neutral import plans and every unsupported/degraded feature is explicitly reported.
</success_criteria>

<output>
Create `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-10-SUMMARY.md` when done.
</output>
