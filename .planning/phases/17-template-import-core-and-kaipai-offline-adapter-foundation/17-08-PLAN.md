---
phase: 17-template-import-core-and-kaipai-offline-adapter-foundation
plan: "08"
type: execute
wave: 6
depends_on:
  - 17-06
files_modified:
  - package.json
  - crates/testkit/tests/template_import_exports.rs
  - crates/testkit/tests/template_import_preview.rs
  - scripts/phase17-source-guards.sh
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - PRODFX-05
  - NO-FALLBACK-01
requirements_addressed:
  - COMP-01
  - COMP-02
  - PRODFX-05
  - NO-FALLBACK-01
must_haves:
  truths:
    - "Imported fixture drafts preview and export through repo-owned preview/export paths without Android, live provider API, old artifacts, mock, or CPU readback success, per D-43, D-44, D-45."
    - "Each fixture export produces a non-empty MP4 with layer/text/audio evidence, per D-44."
    - "Phase 17 aggregate scripts exist and run the validation commands from 17-VALIDATION.md."
  artifacts:
    - path: "crates/testkit/tests/template_import_exports.rs"
      provides: "Export smoke/parity tests for imported fixtures"
    - path: "crates/testkit/tests/template_import_preview.rs"
      provides: "Realtime preview product-path tests for imported fixtures"
    - path: "package.json"
      provides: "test:phase17-rust, test:phase17-export-fixtures, test:phase17-preview, test:phase17"
  key_links:
    - from: "crates/testkit/tests/template_import_exports.rs"
      to: "crates/bindings_node/src/project_session_service.rs"
      via: "imports fixture through session API before export"
      pattern: "import_kaipai_formula_bundle"
    - from: "package.json"
      to: "scripts/phase17-source-guards.sh"
      via: "test:phase17 aggregate"
      pattern: "test:phase17-source-guards"
---

<objective>
Prove imported template fixtures through preview/export evidence and wire the aggregate Phase 17 gate.

Purpose: backend import is not complete until supported fixture outputs can be previewed and exported through the production architecture with no fallback success.
Output: testkit preview/export fixture tests, stronger source guards, and full Phase 17 package scripts.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-VALIDATION.md
@docs/no-product-fallback-policy.md
@docs/product-e2e-acceptance-policy.md
@crates/testkit/tests/preview_export_parity.rs
@crates/testkit/tests/render_smoke.rs
@crates/bindings_node/tests/project_session_import_kaipai.rs
@package.json
</context>

## Artifacts This Phase Produces

- Tests: `template_import_exports`, `template_import_preview`
- Package scripts: `test:phase17-rust`, `test:phase17-export-fixtures`, `test:phase17-preview`, `test:phase17`
- Source guard expansions for clean project JSON, offline-only runtime, and no fallback evidence

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add imported fixture preview and export evidence tests</name>
  <files>crates/testkit/tests/template_import_exports.rs, crates/testkit/tests/template_import_preview.rs</files>
  <read_first>
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-VALIDATION.md` rows 17-W0-07 and 17-W0-08.
    - `crates/testkit/tests/preview_export_parity.rs` and `crates/testkit/tests/render_smoke.rs`.
    - `fixtures/kaipai/expected-reports/*.report.json` from Plan 17-05.
  </read_first>
  <behavior>
    - Export tests import main video, PIP, text sticker, BGM/audio, missing-resource, and native-effect fixtures through the Rust import chain.
    - Export tests validate non-empty MP4, duration/fps/dimensions, layer order evidence, visible text evidence, and audio stream presence for audio fixtures.
    - Preview tests exercise the realtime preview product path for imported supported fixtures and reject fallback/mock/artifact/CPU/Android evidence.
  </behavior>
  <action>Create testkit integration tests that import fixture bundles through the session/API path, then run preview/export using existing runtime, render graph, and FFmpeg runtime helpers. Use generated repository-owned fixture media only. Validate adaptation reports and media outputs together so unsupported features cannot be hidden by successful file creation.</action>
  <verify>
    <automated>cargo test -p testkit template_import_exports -- --nocapture && cargo test -p testkit template_import_preview -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Export tests fail if output is empty, missing audio for audio fixture, missing visible text, wrong layer order, wrong dimensions/fps, or missing report classifications.
    - Preview tests require render-graph/realtime preview evidence and fail on fallback evidence.
    - Tests do not use Android oracle output, live provider APIs, old branch artifacts, or remote runtime URLs.
  </acceptance_criteria>
  <done>Imported fixture preview/export evidence exists and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire full Phase 17 automated gates</name>
  <files>package.json, scripts/phase17-source-guards.sh</files>
  <read_first>
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-VALIDATION.md` full-suite commands.
    - `package.json` existing phase script conventions.
    - `scripts/no-product-fallback-guards.sh`.
  </read_first>
  <action>Update package scripts so `test:phase17-rust` runs the Phase 17 Rust contract/import/adapter/session/preview/export tests, `test:phase17-export-fixtures` runs the export fixture gate, `test:phase17-preview` runs the preview gate, and `test:phase17` composes Rust, source guards, no-product-fallback, contracts, and workspace check commands required by 17-VALIDATION.md. Expand `scripts/phase17-source-guards.sh` to assert imported project JSON/report fixtures do not contain raw formula JSON as runtime semantics, remote render URLs, Android/live provider dependencies, or fallback success evidence.</action>
  <verify>
    <automated>pnpm run test:phase17-rust && pnpm run test:phase17-source-guards && pnpm run test:phase17-export-fixtures && pnpm run test:phase17-preview && pnpm run test:no-product-fallback && cargo check --workspace --locked && pnpm run test:contracts</automated>
  </verify>
  <acceptance_criteria>
    - All Phase 17 package scripts exist and run the intended commands.
    - Source guards fail injected violations for raw provider semantics, remote render runtime refs, Android/live provider dependency, and fallback success.
    - Full pre-UI gate from 17-VALIDATION.md passes.
  </acceptance_criteria>
  <done>Phase 17 backend aggregate verification is wired and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported fixture draft -> preview/export runtime | Imported canonical semantics drive user-visible media output. |
| automated evidence -> phase completion | Tests determine whether backend import can be accepted before UI work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-29 | Repudiation | Preview/export evidence | mitigate | Tests require visible/text/audio/layer media assertions plus no-fallback evidence. |
| T-17-30 | Tampering | Imported project JSON | mitigate | Source guards and tests inspect persisted `.veproj/project.json` for remote/provider runtime semantics. |
| T-17-31 | Denial of Service | Fixture export jobs | mitigate | Use bounded testkit runtime timeouts and existing bundled FFmpeg runtime checks. |
| T-17-32 | Information Disclosure | Test artifacts | mitigate | Use generated/sanitized repository-owned fixtures and scan for secret-like data. |
</threat_model>

<verification>
Run:
- `pnpm run test:phase17-rust`
- `pnpm run test:phase17-source-guards`
- `pnpm run test:phase17-export-fixtures`
- `pnpm run test:phase17-preview`
- `pnpm run test:no-product-fallback`
- `cargo check --workspace --locked`
- `pnpm run test:contracts`
</verification>

<success_criteria>
The backend foundation is complete when all imported fixture families produce valid reports and preview/export evidence through current product paths with no fallback success.
</success_criteria>

<output>
Create `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-08-SUMMARY.md` when done.
</output>
