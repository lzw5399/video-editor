---
phase: 17-template-import-core-and-kaipai-offline-adapter-foundation
plan: "06"
type: execute
wave: 5
depends_on:
  - 17-10
files_modified:
  - crates/bindings_node/Cargo.toml
  - crates/bindings_node/src/lib.rs
  - crates/bindings_node/src/project_session_service.rs
  - crates/bindings_node/tests/project_session_import_kaipai.rs
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - NO-FALLBACK-02
requirements_addressed:
  - COMP-01
  - COMP-02
  - NO-FALLBACK-02
must_haves:
  truths:
    - "Project-session layer applies validated DraftImportPlan atomically and owns revision/save behavior, per D-12, D-13, D-38."
    - "Localized imported materials and fonts are persisted into `artifact_store::resource_index` after successful session import, per D-15 and D-36."
    - "Offline import requires sessionId and expectedRevision and rejects stale session mutation."
    - "Imported `.veproj/project.json` contains canonical semantics and local resource refs only, per D-04, D-06, D-42."
  artifacts:
    - path: "crates/bindings_node/src/project_session_service.rs"
      provides: "import_kaipai_formula_bundle project-session API"
    - path: "crates/bindings_node/tests/project_session_import_kaipai.rs"
      provides: "Atomic import, stale revision, resource index persistence, clean project JSON tests"
  key_links:
    - from: "crates/bindings_node/src/project_session_service.rs"
      to: "crates/adapter_kaipai/src/mapper.rs"
      via: "calls mapper then applies DraftImportPlan"
      pattern: "map_kaipai_bundle_to_import_plan"
    - from: "crates/bindings_node/src/project_session_service.rs"
      to: "crates/project_store/src/bundle.rs"
      via: "save_project_bundle after successful application"
      pattern: "save_project_bundle"
    - from: "crates/bindings_node/src/project_session_service.rs"
      to: "crates/artifact_store/src/resource_index.rs"
      via: "persists localized imported material/font refs after successful save"
      pattern: "index_draft_resources|persist_resource_index"
---

<objective>
Expose a Rust-owned project-session API for offline Kaipai formula bundle import.

Purpose: users and UI code need one narrow command/API that applies imported template content through the same session/revision/project-store boundary as normal editing.
Output: binding service API, N-API export, and integration tests proving atomic application and stale revision rejection.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-CONTEXT.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-PATTERNS.md
@.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-10-SUMMARY.md
@.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-07-SUMMARY.md
@crates/bindings_node/src/project_session_service.rs
@crates/bindings_node/src/lib.rs
@crates/project_store/src/bundle.rs
@crates/artifact_store/src/resource_index.rs
@crates/draft_import/src/import_plan.rs
@crates/adapter_kaipai/src/mapper.rs
</context>

## Artifacts This Phase Produces

- Rust request/response structs: `ImportKaipaiFormulaBundleRequest`, `ProjectSessionTemplateImportResponse`
- Rust API/functions: `import_kaipai_formula_bundle`, `ProjectSessionRegistry::import_kaipai_formula_bundle`, `ProjectSession::apply_import_plan`
- Resource index side effect: imported localized materials/fonts persisted through `artifact_store::resource_index`
- N-API export: `importKaipaiFormulaBundle`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project-session import API tests</name>
  <files>crates/bindings_node/tests/project_session_import_kaipai.rs</files>
  <read_first>
    - `crates/bindings_node/tests/project_session.rs` for current project-session test patterns.
    - `crates/bindings_node/src/project_session_service.rs` stale revision and save behavior.
    - `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-VALIDATION.md` row 17-W0-06.
  </read_first>
  <behavior>
    - Test imports a sanitized formula bundle into an open project session and increments revision once.
    - Test stale expectedRevision is rejected without writing project JSON.
    - Test project JSON excludes raw formula JSON, provider render IDs, remote runtime URLs, and adapter-only safe-area evidence.
    - Test artifact resource index contains localized imported material/font refs with project-relative refs after successful import.
    - Test response returns view model plus AdaptationReport.
  </behavior>
  <action>Create integration tests that open/create a `.veproj`, call the new Rust import API with `sessionId`, `expectedRevision`, bundle path, and resource root, then inspect the saved project, artifact resource index, and response. Include stale revision, missing session, invalid payload, and missing/unsafe resource cases. Assert successful imports persist localized material/font refs into `artifact_store::resource_index` with project-relative refs and that failed imports do not leave partial index mutations, per D-15.</action>
  <verify>
    <automated>cargo test -p bindings_node project_session_import_kaipai -- --nocapture && pnpm run test:phase17-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before service implementation and pass after Task 2.
    - Stale revision path leaves project JSON unchanged.
    - Successful import persists canonical draft semantics and returns a report.
    - Successful import persists resource index rows for localized imported materials/fonts; failed imports do not persist partial index rows.
  </acceptance_criteria>
  <done>Project-session import behavior is specified by integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement atomic offline import service</name>
  <files>crates/bindings_node/Cargo.toml, crates/bindings_node/src/lib.rs, crates/bindings_node/src/project_session_service.rs</files>
  <read_first>
    - `crates/bindings_node/src/project_session_service.rs` `execute_project_intent`, `run_project_io_job`, and revision handling.
    - `crates/draft_import/src/import_plan.rs` and `crates/adapter_kaipai/src/mapper.rs`.
    - `.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-07-SUMMARY.md` project IO scheduler notes.
  </read_first>
  <action>Add a narrow binding/service API that parses request JSON, checks `sessionId` and `expectedRevision`, invokes the offline adapter mapper, applies the validated `DraftImportPlan` to the session draft, saves through `project_store::save_project_bundle` inside the existing project IO scheduler wrapper, persists localized imported materials/fonts into `artifact_store::resource_index` after the successful save, increments revision exactly once on success, and returns the project-session view model plus `AdaptationReport`. Expose it from `bindings_node/src/lib.rs` as `importKaipaiFormulaBundle`. Do not route this through renderer-built timeline intents or direct project JSON writes.</action>
  <verify>
    <automated>cargo test -p bindings_node project_session_import_kaipai -- --nocapture && pnpm run test:phase17-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - API rejects stale revision and invalid payloads with existing error-envelope conventions.
    - Successful import saves one canonical draft update and returns the new revision.
    - Successful import writes resource index entries for localized materials/fonts using project-relative refs.
    - `.veproj/project.json` contains local project-relative resources and no raw formula/provider runtime fields.
    - Source guards still pass.
  </acceptance_criteria>
  <done>Offline Kaipai import is available through the Rust-owned project-session boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Electron/native caller -> Rust project session | Caller requests a session-visible mutation. |
| adapter/import plan -> `.veproj/project.json` | Imported semantics become canonical project state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-21 | Tampering | Project session revision | mitigate | Require `expectedRevision` and reject stale imports before applying or saving. |
| T-17-22 | Tampering | Atomic project write and resource index update | mitigate | Apply validated plan, save through project_store, and persist resource index rows in one success path; errors leave session state and index unchanged. |
| T-17-23 | Information Disclosure | Project JSON | mitigate | Tests inspect saved JSON for raw formula/provider IDs/remote runtime refs. |
| T-17-24 | Repudiation | Import response | mitigate | Return `AdaptationReport` with the same response as the updated view model. |
</threat_model>

<verification>
Run:
- `cargo test -p bindings_node project_session_import_kaipai -- --nocapture`
- `pnpm run test:phase17-source-guards`
- `cargo check --workspace --locked`
</verification>

<success_criteria>
The plan is complete when offline template import is a Rust-owned project-session operation with revision, validation, save, and report semantics proven by tests.
</success_criteria>

<output>
Create `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-06-SUMMARY.md` when done.
</output>
