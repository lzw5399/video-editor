---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "12"
type: execute
wave: 10
depends_on:
  - "19-11"
files_modified:
  - crates/draft_model/src/interaction.rs
  - crates/draft_model/src/lib.rs
  - crates/draft_model/tests/schema_exports.rs
  - crates/editor_runtime/src/project_session_node.rs
  - crates/bindings_node/tests/project_interaction_session.rs
  - crates/bindings_node/tests/project_session.rs
  - apps/desktop-electron/src/main/nativeBinding.ts
  - apps/desktop-electron/src/preload/index.ts
  - apps/desktop-electron/src/main/index.ts
  - apps/desktop-electron/src/renderer/workspace/projectInteraction.ts
  - apps/desktop-electron/src/generated/Draft.ts
autonomous: true
requirements:
  - PRODFX-01
  - PRODFX-02
  - PRODFX-03
  - PRODFX-04
must_haves:
  truths:
    - "Desktop command surfaces expose retime, transition, effect, filter, mask, and blend edits only after Rust semantics exist."
    - "High-frequency Phase 19 manipulation uses Rust-owned project interaction sessions with coalesced updates and deterministic commit/cancel."
    - "Bindings_node remains a thin adapter over editor_runtime and does not duplicate edit semantics."
  prohibitions:
    - "Renderer code cannot mutate draft state directly or compute accepted transition/effect/retime semantics."
    - "Interaction updates cannot save, increment revision, or push undo until commit."
  artifacts:
    - path: "crates/draft_model/src/interaction.rs"
      provides: "ProjectInteractionKind variants for Phase 19 high-frequency controls"
    - path: "crates/editor_runtime/src/project_session_node.rs"
      provides: "ProjectIntent and ProjectInteractionPayload routes for Phase 19 commands"
    - path: "apps/desktop-electron/src/main/nativeBinding.ts"
      provides: "Typed desktop bridge requests for Phase 19 UI"
  key_links:
    - from: "apps/desktop-electron/src/renderer/workspace/projectInteraction.ts"
      to: "crates/editor_runtime/src/project_session_node.rs"
      via: "begin/update/commit/cancel project interaction route"
      pattern: "beginProjectInteraction|updateProjectInteraction|commitProjectInteraction"
    - from: "crates/editor_runtime/src/project_session_node.rs"
      to: "crates/draft_commands/src/effects.rs"
      via: "ProjectIntent converts UI intent into canonical Rust command"
      pattern: "ApplySegmentEffect|SetSegmentRetime|Transition"
---

<objective>
Expose Phase 19 Rust capabilities through project-session command and interaction surfaces before desktop UI controls are enabled.

Purpose: Preserve the command-only renderer boundary and high-frequency interaction guarantees for retiming, transitions, effects, masks, and blends.
Output: Project intents, interaction payloads, binding TypeScript types, and green interaction/session tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-UI-SPEC.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/editor_runtime/src/project_session_node.rs
@crates/bindings_node/src/project_session_service.rs
@apps/desktop-electron/src/main/nativeBinding.ts
@apps/desktop-electron/src/renderer/workspace/projectInteraction.ts
</context>

## Artifacts this phase produces

- `ProjectInteractionKind::{SelectedSegmentRetime, SelectedSegmentEffect, SelectedSegmentMask, SelectedTransitionDuration}` or equivalent Phase 19 variants
- `ProjectIntent::{SetSelectedSegmentRetime, ApplySelectedSegmentEffect, UpdateSelectedSegmentEffectParameter, RemoveSelectedSegmentEffect, SetSelectedSegmentMask, SetSelectedSegmentBlendMode, AddTransitionAtBoundary, UpdateSelectedTransitionDuration, RemoveSelectedTransition}`
- `ProjectInteractionPayload` variants for retime, effect/filter strength, mask handle, blend opacity, and transition duration updates
- Updated `nativeBinding.ts` and preload/main bridge typings for Phase 19 interactions

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Rust project interaction variants</name>
  <read_first>@crates/draft_model/src/interaction.rs @crates/editor_runtime/src/project_session_node.rs @crates/bindings_node/tests/project_interaction_session.rs</read_first>
  <files>crates/draft_model/src/interaction.rs, crates/draft_model/src/lib.rs, crates/editor_runtime/src/project_session_node.rs, crates/bindings_node/tests/project_interaction_session.rs</files>
  <behavior>
    - Phase 19 slider/handle drags begin a Rust-owned session, accept monotonic coalesced updates, and commit/cancel deterministically.
    - Provisional updates return revisionUnchanged and preview delta facts.
    - Commit applies one canonical Rust command and one undo/save/revision only once.
  </behavior>
  <action>Add Phase 19 interaction kind variants and corresponding `ProjectInteractionPayload` handling in `project_session_node.rs`. Convert retime handles, effect/filter sliders, mask handles, blend opacity, and transition duration samples into provisional Rust command evaluations. Extend `project_interaction_session` tests to prove monotonic sequence handling, revisionUnchanged updates, stale rejection, cancel, and one canonical commit.</action>
  <acceptance_criteria>Binding interaction tests prove high-frequency Phase 19 edits never save or push undo per update and commit exactly once.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model interaction -- --nocapture && cargo test -p bindings_node --test project_interaction_session -- --nocapture</automated>
  </verify>
  <done>Phase 19 high-frequency edits use Rust-owned interaction sessions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add project-session intents and command responses</name>
  <read_first>@crates/editor_runtime/src/project_session_node.rs @crates/bindings_node/tests/project_session.rs @crates/draft_commands/src/retiming.rs @crates/draft_commands/src/transition.rs @crates/draft_commands/src/effects.rs</read_first>
  <files>crates/editor_runtime/src/project_session_node.rs, crates/bindings_node/tests/project_session.rs</files>
  <behavior>
    - Project intents expose retime, effect/filter, mask/blend, and transition commands to desktop adapters.
    - Intent execution validates expected revision and session ownership.
    - View model/capability state returned to UI is generated by Rust, not derived from renderer draft mutation.
  </behavior>
  <action>Add explicit Phase 19 `ProjectIntent` variants in `project_session_node.rs` that call the canonical draft command modules from prior plans. Update view model response data only where necessary so UI can display capability chips, applied effect lists, selected transition facts, and retime state without reading raw draft internals. Extend project-session tests for success, invalid expected revision, unsupported capability, and no adapter-owned semantics.</action>
  <acceptance_criteria>Project-session tests pass for Phase 19 intents and prove bindings_node delegates to editor_runtime without semantic duplication.</acceptance_criteria>
  <verify>
    <automated>cargo test -p bindings_node --test project_session -- --nocapture</automated>
  </verify>
  <done>Desktop adapters have explicit Rust-owned command surfaces for Phase 19.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update TypeScript bridge contracts</name>
  <read_first>@apps/desktop-electron/src/main/nativeBinding.ts @apps/desktop-electron/src/preload/index.ts @apps/desktop-electron/src/main/index.ts @apps/desktop-electron/src/renderer/workspace/projectInteraction.ts @crates/draft_model/tests/schema_exports.rs</read_first>
  <files>crates/draft_model/tests/schema_exports.rs, apps/desktop-electron/src/generated/Draft.ts, apps/desktop-electron/src/main/nativeBinding.ts, apps/desktop-electron/src/preload/index.ts, apps/desktop-electron/src/main/index.ts, apps/desktop-electron/src/renderer/workspace/projectInteraction.ts</files>
  <behavior>
    - TypeScript bridge types match Rust-generated Phase 19 contract shapes.
    - Preload/main continue exposing explicit project-session APIs, not generic semantic envelopes.
    - Renderer interaction helper remains a thin controller over begin/update/commit/cancel.
  </behavior>
  <action>Regenerate generated Draft contracts after interaction type changes, then update manual `nativeBinding.ts` project intent and interaction payload unions. Keep preload/main IPC routing on the existing explicit project-session and project-interaction channels; do not add generic command envelopes or renderer draft mutation helpers. Update source guards to require Phase 19 interaction bridge types and block direct draft mutations.</action>
  <acceptance_criteria>TypeScript build passes with Phase 19 bridge types and `pnpm run test:contracts` has no generated drift.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model schema_exports -- --nocapture && pnpm --filter @video-editor/desktop build && pnpm run test:contracts</automated>
  </verify>
  <done>Desktop TypeScript contracts can drive Phase 19 UI without semantic ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer to Electron preload/main | UI requests cross IPC into native binding surface. |
| Node adapter to editor_runtime | JSON values become Rust project intents and interactions. |
| High-frequency interactions | Many pointer samples cross into Rust before one commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-25 | Tampering | `ProjectIntent` payloads | mitigate | Use serde `deny_unknown_fields`, expected revision checks, session ownership, and canonical command validation. |
| T-19-26 | Denial of Service | project interaction sessions | mitigate | Monotonic sequence acceptance, coalesced provisional updates, stale rejection, cancel, and one-shot commit prevent save/undo loops. |
| T-19-27 | Elevation of Privilege | renderer semantic bypass | mitigate | Source guards and tests require explicit project-session routes and block renderer draft mutation/semantic calculation. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run draft_model interaction tests, bindings_node project interaction/session tests, desktop build, and contract drift checks.
</verification>

<success_criteria>
Phase 19 Rust command surfaces are ready when the renderer can only issue typed intents/interactions and all committed semantics remain Rust-owned.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-12-SUMMARY.md` when done.
</output>
