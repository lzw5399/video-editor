---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/phase19-source-guards.sh
  - package.json
  - crates/draft_model/tests/production_effects_contracts.rs
  - crates/draft_commands/tests/retiming_commands.rs
  - crates/draft_commands/tests/transition_commands.rs
  - crates/engine_core/tests/retiming.rs
  - crates/render_graph/tests/production_effects.rs
  - crates/realtime_preview_runtime/tests/production_effects.rs
  - crates/ffmpeg_compiler/tests/production_effects.rs
  - crates/testkit/tests/production_effects_preview.rs
  - crates/testkit/tests/production_effects_exports.rs
  - apps/desktop-electron/tests/production-effects.spec.ts
  - .planning/phases/19-production-effects-retiming-and-transition-semantics/19-VALIDATION.md
autonomous: true
requirements:
  - PRODFX-01
  - PRODFX-02
  - PRODFX-03
  - PRODFX-04
  - PRODFX-05
must_haves:
  truths:
    - "Phase 19 implementation cannot start without executable source guards and package scripts covering PRODFX-01 through PRODFX-05."
    - "RED tests name the retiming, transition, capability, production effect, GPU preview/export, and template-fidelity behavior that later plans must make pass."
    - "No ORM schema push task is needed because Phase 19 touches Rust draft schemas and generated contracts, not database ORM schema files."
  prohibitions:
    - "Renderer-owned FFmpeg construction, source-time mapping, transition validation, effect evaluation, cache/fingerprint semantics, provider-native semantic IDs, fallback success evidence, and per-pointer canonical saves are blocked by source guards."
    - "Package versions and lockfiles are not changed in Wave 0."
  artifacts:
    - path: "scripts/phase19-source-guards.sh"
      provides: "Phase 19 architecture/source guard entrypoint"
    - path: "package.json"
      provides: "test:phase19-rust, test:phase19-source-guards, test:phase19-desktop, test:phase19 aggregate scripts"
    - path: "crates/draft_model/tests/production_effects_contracts.rs"
      provides: "RED tests for typed capability/retime/effect/transition contracts"
    - path: "apps/desktop-electron/tests/production-effects.spec.ts"
      provides: "RED desktop product E2E scaffold for visible Phase 19 controls"
  key_links:
    - from: "package.json"
      to: "scripts/phase19-source-guards.sh"
      via: "test:phase19-source-guards invokes the guard script"
      pattern: "test:phase19-source-guards.*phase19-source-guards"
    - from: "scripts/phase19-source-guards.sh"
      to: "apps/desktop-electron/src/renderer"
      via: "negative and required source checks for renderer command-only boundary"
      pattern: "renderer.*FFmpeg|retime|transition|fallback"
---

<objective>
Create the Wave 0 executable guard and RED-test scaffold for Phase 19 before any visible UI enablement.

Purpose: Make the known bad states fail first: renderer-owned semantics, unsupported controls as product success, fallback evidence, and high-frequency save/undo loops.
Output: Phase 19 source guard, package scripts, RED test files, and validation metadata.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-RESEARCH.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-VALIDATION.md
@docs/no-product-fallback-policy.md
@docs/product-e2e-acceptance-policy.md
@docs/refactor-and-legacy-cleanup-policy.md
</context>

## Artifacts this phase produces

- `scripts/phase19-source-guards.sh`
- `package.json` scripts `test:phase19-rust`, `test:phase19-source-guards`, `test:phase19-desktop`, `test:phase19`
- RED test files `production_effects_contracts.rs`, `retiming_commands.rs`, `transition_commands.rs`, `retiming.rs`, `production_effects.rs`, `production_effects_preview.rs`, `production_effects_exports.rs`, and `production-effects.spec.ts`

## Schema Push Detection

Scan result: no ORM schema patterns are planned for Phase 19. Rust `.veproj` draft schema and generated TypeScript contracts are in scope, but there is no Prisma/Drizzle/TypeORM/SQLx/Diesel schema push. No schema push task is needed.

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 19 source guards and package scripts</name>
  <read_first>@scripts/phase17-1-source-guards.sh @scripts/phase18-source-guards.sh @scripts/no-product-fallback-guards.sh @package.json</read_first>
  <files>scripts/phase19-source-guards.sh, package.json</files>
  <action>Create `scripts/phase19-source-guards.sh` using existing guard helper patterns. Add staged options `--wave0`, `--ui`, and default aggregate mode so early plans can run only completed-artifact checks while the final plan runs all checks. The guard must block renderer/main owned FFmpeg filter/export construction, source-to-target retiming math, transition overlap validation, effect/filter/mask/blend evaluation, dirty-range/cache/fingerprint semantics, private provider IDs in internal semantic code, DOM/CSS/artifact/mock fallback as success, and save/revision/undo loops driven by pointer samples. Add root package scripts exactly named `test:phase19-rust`, `test:phase19-source-guards`, `test:phase19-desktop`, and `test:phase19` without changing dependency versions or lockfiles.</action>
  <acceptance_criteria>`bash scripts/phase19-source-guards.sh --wave0` passes and `package.json` exposes the four Phase 19 scripts. The aggregate guard may require later artifacts, but staged mode must not fail on files intentionally created by later plans.</acceptance_criteria>
  <verify>
    <automated>pnpm run test:phase19-source-guards -- --wave0</automated>
  </verify>
  <done>Wave 0 guard and package-script entrypoints exist and are executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RED Rust semantic tests</name>
  <read_first>@crates/draft_model/tests/schema_exports.rs @crates/draft_commands/tests/visual_transform_commands.rs @crates/draft_commands/tests/keyframe_commands.rs @crates/engine_core/tests/frame_state_snapshots.rs @crates/render_graph/tests/render_graph_snapshots.rs</read_first>
  <files>crates/draft_model/tests/production_effects_contracts.rs, crates/draft_commands/tests/retiming_commands.rs, crates/draft_commands/tests/transition_commands.rs, crates/engine_core/tests/retiming.rs, crates/render_graph/tests/production_effects.rs</files>
  <behavior>
    - RED: draft model rejects stringly first-party effect/transition semantics and requires typed support states.
    - RED: retiming command tests require deterministic constant and curve speed source mapping under split/trim/move.
    - RED: transition command tests require adjacent segment validation and single undo entry on commit.
    - RED: engine and render graph tests require retime/transition/effect facts in frame state, graph intent, fingerprints, and dirty ranges.
  </behavior>
  <action>Add focused failing tests with names beginning `phase19_` for capability registry contracts, retime source mapping, transition adjacency, graph fingerprints, and dirty facts. Keep tests executable, deterministic, and tied to concrete symbols that later tasks will implement; do not leave them ignored or converted into assertions that pass against current legacy string-only semantics.</action>
  <acceptance_criteria>Each new Rust test file contains at least one `phase19_` test that fails against the current implementation for the intended missing production semantic.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; cargo test -p draft_model production_effects_contracts -- --nocapture; test $? -ne 0; cargo test -p draft_commands retiming_commands -- --nocapture; test $? -ne 0; cargo test -p draft_commands transition_commands -- --nocapture; test $? -ne 0; cargo test -p engine_core retiming -- --nocapture; test $? -ne 0; cargo test -p render_graph production_effects -- --nocapture; test $? -ne 0'</automated>
  </verify>
  <done>RED Rust test scaffold proves later semantic implementations have executable failing gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create RED preview export template and desktop tests</name>
  <read_first>@crates/realtime_preview_runtime/tests/capability_matrix.rs @crates/ffmpeg_compiler/tests/ffmpeg_job_snapshots.rs @crates/testkit/tests/template_import_preview.rs @crates/testkit/tests/template_import_exports.rs @apps/desktop-electron/tests/interaction-preview-inspector.spec.ts @apps/desktop-electron/tests/template-import.spec.ts @.planning/phases/19-production-effects-retiming-and-transition-semantics/19-VALIDATION.md</read_first>
  <files>crates/realtime_preview_runtime/tests/production_effects.rs, crates/ffmpeg_compiler/tests/production_effects.rs, crates/testkit/tests/production_effects_preview.rs, crates/testkit/tests/production_effects_exports.rs, apps/desktop-electron/tests/production-effects.spec.ts, .planning/phases/19-production-effects-retiming-and-transition-semantics/19-VALIDATION.md</files>
  <behavior>
    - RED: preview runtime reports supported GPU preview only for registry-backed first-party effects/transitions/masks/blends.
    - RED: FFmpeg compiler emits compiler-owned filtergraph output from render graph intent, never renderer strings.
    - RED: template fixtures verify preview/export parity, fallback reports, and performance evidence for production editing scenarios.
    - RED: desktop E2E performs visible speed/effect/transition interactions only after Rust capabilities exist.
  </behavior>
  <action>Add failing Phase 19 tests for realtime capability support, compiler production effects output, template preview/export parity, and product desktop workflows. Update `19-VALIDATION.md` so `nyquist_compliant` and `wave_0_complete` stay false until these tests exist and the guard/package-script checks pass; record the exact RED commands added here.</action>
  <acceptance_criteria>Each new test file contains a `phase19_` or production-effects case that currently fails for missing semantics, and `19-VALIDATION.md` lists the Wave 0 artifacts and commands.</acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; cargo test -p realtime_preview_runtime production_effects -- --nocapture; test $? -ne 0; cargo test -p ffmpeg_compiler production_effects -- --nocapture; test $? -ne 0; cargo test -p testkit production_effects -- --nocapture; test $? -ne 0; pnpm --filter @video-editor/desktop exec playwright test tests/production-effects.spec.ts --reporter=line; test $? -ne 0'</automated>
  </verify>
  <done>Wave 0 has failing preview/export/template/desktop gates that later plans must turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer to Rust project session | Untrusted UI intents and interaction samples cross into Rust-owned semantics. |
| Render graph to FFmpeg compiler | Typed render intent becomes process arguments and filter scripts. |
| Adapter reports to canonical draft | External provider references may be imported but must not become internal render semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `scripts/phase19-source-guards.sh` | mitigate | Guard forbidden renderer/main semantics and fail before implementation can pass with UI-owned editing or FFmpeg construction. |
| T-19-02 | Repudiation | Phase 19 product tests | mitigate | RED tests and no-product-fallback guards require real GPU/native preview and export evidence, not fallback artifacts. |
| T-19-03 | Denial of Service | high-frequency interaction code paths | mitigate | Source guard checks for project interaction sessions and rejects repeated canonical save/revision/undo per pointer sample. |
| T-19-SC | Tampering | npm/cargo installs | mitigate | No package installs or version changes are planned; lockfiles remain unchanged and no human package checkpoint is needed. |
</threat_model>

<verification>
Run `pnpm run test:phase19-source-guards -- --wave0` and confirm the RED commands fail for missing implementation while the Wave 0 guard/package-script checks pass.
</verification>

<success_criteria>
Wave 0 is complete when guards, scripts, RED tests, and validation notes exist before any Phase 19 visible controls are enabled.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-01-SUMMARY.md` when done.
</output>
