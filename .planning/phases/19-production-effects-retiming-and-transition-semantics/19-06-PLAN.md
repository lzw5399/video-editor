---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "06"
type: execute
wave: 5
depends_on:
  - "19-05"
files_modified:
  - crates/draft_model/src/effects.rs
  - crates/draft_commands/src/effects.rs
  - crates/draft_commands/src/timeline.rs
  - crates/draft_commands/src/error.rs
  - crates/draft_commands/src/lib.rs
  - crates/draft_commands/tests/effect_commands.rs
  - crates/render_graph/src/effects.rs
  - crates/render_graph/src/graph.rs
  - crates/render_graph/src/fingerprint.rs
  - crates/render_graph/tests/production_effects.rs
  - crates/realtime_preview_runtime/src/effects.rs
  - crates/realtime_preview_runtime/src/gpu/compositor.rs
  - crates/realtime_preview_runtime/src/gpu/pipelines.rs
  - crates/realtime_preview_runtime/src/capabilities.rs
  - crates/realtime_preview_runtime/tests/production_effects.rs
  - crates/ffmpeg_compiler/src/effects.rs
  - crates/ffmpeg_compiler/src/filters.rs
  - crates/ffmpeg_compiler/tests/production_effects.rs
autonomous: true
requirements:
  - PRODFX-03
  - PRODFX-04
must_haves:
  truths:
    - "First-party Gaussian blur, basic color adjustment, opacity adjustment/filter strength, and unsupported external effects are represented as typed Rust semantics."
    - "Supported first-party effects use production GPU preview and compiler-owned export output."
    - "Unsupported/degraded effects remain disabled or diagnostic and cannot count as product success."
  prohibitions:
    - "Renderer code cannot evaluate effect parameters, build FFmpeg filters, or promote unsupported cards as enabled."
    - "Effect implementation cannot bypass the capability registry introduced in Plan 19-02."
  artifacts:
    - path: "crates/draft_commands/src/effects.rs"
      provides: "Undoable effect/filter/adjustment commands"
    - path: "crates/realtime_preview_runtime/src/effects.rs"
      provides: "GPU preview effect pass helpers"
    - path: "crates/ffmpeg_compiler/src/effects.rs"
      provides: "Compiler-owned effect/filter export helpers"
  key_links:
    - from: "crates/draft_model/src/effects.rs"
      to: "crates/realtime_preview_runtime/src/effects.rs"
      via: "Capability-backed first-party effect intent maps to GPU pass"
      pattern: "GaussianBlur|BasicColor|EffectKind"
    - from: "crates/render_graph/src/effects.rs"
      to: "crates/ffmpeg_compiler/src/effects.rs"
      via: "Typed effect render intent maps to export filters"
      pattern: "compile.*effect|gblur|eq"
---

<objective>
Implement first-party effects, filters, and adjustments through the Rust command, render graph, GPU preview, and compiler path.

Purpose: Make PRODFX-03 and the blur/basic-effect portion of PRODFX-04 real before UI exposes effect/filter/adjustment controls.
Output: Typed effect commands, graph/fingerprint support, GPU preview passes, compiler output, and green production effect tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/draft_model/src/effects.rs
@crates/realtime_preview_runtime/src/gpu/compositor.rs
@crates/realtime_preview_runtime/src/gpu/pipelines.rs
@crates/ffmpeg_compiler/src/filters.rs
</context>

## Artifacts this phase produces

- `draft_commands::effects::{apply_segment_effect, update_segment_effect_parameter, remove_segment_effect}`
- `TimelineEditPayload::{ApplySegmentEffect, UpdateSegmentEffectParameter, RemoveSegmentEffect}`
- `realtime_preview_runtime::effects::{EffectPreviewPass, apply_phase19_effects}`
- Compiler helpers for Gaussian blur/basic color/opacity adjustment export
- Tests `effect_commands.rs` and green `production_effects` runtime/compiler assertions

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add effect/filter command semantics</name>
  <read_first>@crates/draft_commands/src/visual.rs @crates/draft_commands/src/timeline.rs @crates/draft_commands/tests/effect_commands.rs @crates/draft_model/src/effects.rs</read_first>
  <files>crates/draft_commands/src/effects.rs, crates/draft_commands/src/timeline.rs, crates/draft_commands/src/error.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/tests/effect_commands.rs, crates/draft_model/src/effects.rs</files>
  <behavior>
    - Applying, updating, enabling/disabling, and removing first-party effects are undoable Rust commands.
    - Parameter ranges are typed integer units and validated against the capability registry.
    - Unsupported external/native effects produce explicit diagnostics and cannot be applied as supported first-party effects.
  </behavior>
  <action>Create `draft_commands/src/effects.rs` for effect/filter/adjustment command semantics. Route apply/update/remove payloads through `execute_timeline_edit`, validate effect kinds and parameter ranges against `EffectCapabilityRegistry`, and push exactly one undo snapshot per committed effect change. Add `effect_commands.rs` tests for Gaussian blur, basic color adjustment, opacity adjustment strength, invalid parameters, unsupported external references, and atomic failure.</action>
  <acceptance_criteria>Effect command tests pass and prove every committed change is Rust-owned, typed, undoable, and capability-checked.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands effect_commands -- --nocapture</automated>
  </verify>
  <done>Effect/filter/adjustment edits are Rust command semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add effect graph fingerprints and GPU preview</name>
  <read_first>@crates/render_graph/src/effects.rs @crates/render_graph/src/graph.rs @crates/render_graph/src/fingerprint.rs @crates/realtime_preview_runtime/src/gpu/compositor.rs @crates/realtime_preview_runtime/src/gpu/pipelines.rs @crates/realtime_preview_runtime/tests/production_effects.rs</read_first>
  <files>crates/render_graph/src/effects.rs, crates/render_graph/src/graph.rs, crates/render_graph/src/fingerprint.rs, crates/render_graph/tests/production_effects.rs, crates/realtime_preview_runtime/src/effects.rs, crates/realtime_preview_runtime/src/gpu/compositor.rs, crates/realtime_preview_runtime/src/gpu/pipelines.rs, crates/realtime_preview_runtime/src/capabilities.rs, crates/realtime_preview_runtime/tests/production_effects.rs</files>
  <behavior>
    - Effect graph intent includes typed effect kind, parameter units, order, enable state, support, and reasons.
    - Fingerprints/dirty ranges change when effect stack or parameters change.
    - GPU preview applies supported Gaussian blur/basic color/opacity effects through the realtime compositor path.
  </behavior>
  <action>Add render graph effect intent/fingerprint support and realtime preview effect application in `realtime_preview_runtime/src/effects.rs`. Wire supported effect passes into the WGPU compositor/pipeline structure already used for production preview. Unsupported effects must remain diagnostics, not fallback overlay output. Tests must fail if effect pixels are proven only by DOM/CSS or CPU artifact paths.</action>
  <acceptance_criteria>Render graph and realtime preview production effect tests pass with support states, dirty facts, and native/GPU evidence for the supported slice.</acceptance_criteria>
  <verify>
    <automated>cargo test -p render_graph production_effects -- --nocapture && cargo test -p realtime_preview_runtime production_effects -- --nocapture</automated>
  </verify>
  <done>Supported first-party effects render through the GPU preview path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compile first-party effects for export</name>
  <read_first>@crates/ffmpeg_compiler/src/effects.rs @crates/ffmpeg_compiler/src/filters.rs @crates/ffmpeg_compiler/tests/production_effects.rs</read_first>
  <files>crates/ffmpeg_compiler/src/effects.rs, crates/ffmpeg_compiler/src/filters.rs, crates/ffmpeg_compiler/tests/production_effects.rs, scripts/phase19-source-guards.sh</files>
  <behavior>
    - Gaussian blur/basic color/opacity adjustment compile from render graph effect intent to FFmpeg filter script fragments.
    - Unsupported export paths create typed compiler diagnostics.
    - Source guards reject effect filter strings outside compiler code.
  </behavior>
  <action>Implement compiler-owned effect export helpers for the first-party supported slice using typed render graph inputs and deterministic labels. Update filter script generation and compiler tests for supported output plus unsupported/degraded diagnostics. Extend Phase 19 guards so effect completion requires command, graph, preview, compiler, and no-renderer-FFmpeg coverage.</action>
  <acceptance_criteria>Compiler tests pass for supported effect export and explicit unsupported diagnostics, and source guards block renderer/main effect evaluation or FFmpeg strings.</acceptance_criteria>
  <verify>
    <automated>cargo test -p ffmpeg_compiler production_effects -- --nocapture && bash scripts/phase19-source-guards.sh --wave0</automated>
  </verify>
  <done>First-party effects have preview/export semantics backed by the capability registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/API to effect command | User-provided effect kinds and parameter values enter Rust validation. |
| Render graph to GPU preview | Typed effect intent becomes GPU pipeline state. |
| Render graph to FFmpeg compiler | Typed effect intent becomes FFmpeg filter script fragments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-16 | Tampering | `draft_commands::effects` | mitigate | Validate effect kinds, IDs, parameter ranges, and support states against the registry before mutation. |
| T-19-17 | Tampering | `ffmpeg_compiler::effects` | mitigate | Compiler-owned filter generation avoids UI-supplied strings and uses typed numeric parameters. |
| T-19-18 | Repudiation | GPU preview evidence | mitigate | Tests require realtime/GPU compositor support and reject DOM/CSS/artifact fallback evidence. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run effect command tests, render graph tests, realtime preview tests, compiler tests, and staged source guards.
</verification>

<success_criteria>
PRODFX-03 and supported PRODFX-04 effects are complete when first-party effects are typed, undoable, GPU-previewed, export-compiled, and unsupported paths fail closed.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-06-SUMMARY.md` when done.
</output>
