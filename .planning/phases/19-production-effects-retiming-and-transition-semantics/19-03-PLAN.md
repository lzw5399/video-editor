---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "03"
type: execute
wave: 2
depends_on:
  - "19-02"
files_modified:
  - crates/draft_model/src/effects.rs
  - crates/draft_model/src/timeline.rs
  - crates/draft_model/src/lib.rs
  - crates/draft_commands/src/retiming.rs
  - crates/draft_commands/src/timeline.rs
  - crates/draft_commands/src/error.rs
  - crates/draft_commands/src/lib.rs
  - crates/draft_commands/tests/retiming_commands.rs
  - crates/engine_core/src/time_mapping.rs
  - crates/engine_core/src/frame_state.rs
  - crates/engine_core/src/normalize.rs
  - crates/engine_core/src/lib.rs
  - crates/engine_core/tests/retiming.rs
autonomous: true
requirements:
  - PRODFX-01
must_haves:
  truths:
    - "Retiming and speed curves are persisted as typed draft semantics with integer microsecond and rational speed math."
    - "Rust commands validate retiming under split, trim, move, snapping, main-track magnet, and undo/redo."
    - "engine_core evaluates source-to-target mapping for constant and curve speed without renderer-owned time math."
  prohibitions:
    - "Retiming cannot be implemented first as FFmpeg-only `setpts`, UI duration math, or adapter-specific metadata."
    - "Pointer or slider retime samples cannot save, increment revision, or push undo until commit."
  artifacts:
    - path: "crates/draft_commands/src/retiming.rs"
      provides: "Undoable retiming commands and validation"
    - path: "crates/engine_core/src/time_mapping.rs"
      provides: "Deterministic source mapping for constant and curve speed"
  key_links:
    - from: "crates/draft_commands/src/retiming.rs"
      to: "crates/engine_core/src/time_mapping.rs"
      via: "Commands persist typed retime data that engine_core evaluates"
      pattern: "SegmentRetime|RetimeMode|source.*target"
    - from: "crates/draft_commands/src/timeline.rs"
      to: "crates/draft_commands/src/retiming.rs"
      via: "execute_timeline_edit routes retime payloads through Rust command validation"
      pattern: "SetSegmentRetime"
---

<objective>
Implement production retiming semantics in the draft model, command layer, and engine source-time mapping.

Purpose: Make PRODFX-01 true before render graph, export, preview, and UI consume retiming.
Output: Typed retime fields, undoable commands, deterministic engine mapping, and green retiming RED tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/draft_model/src/effects.rs
@crates/draft_model/src/timeline.rs
@crates/draft_commands/src/timeline.rs
@crates/engine_core/src/frame_state.rs
@crates/engine_core/src/normalize.rs
</context>

## Artifacts this phase produces

- `draft_commands::retiming::{set_segment_retime, clear_segment_retime, validate_segment_retime}`
- `TimelineEditPayload::SetSegmentRetime`
- `TimelineCommandErrorKind::{InvalidRetime, UnsupportedAudioRetime}`
- `engine_core::time_mapping::{source_position_for_retime, retimed_source_range, audio_retime_diagnostic}`
- `FrameVideoLayer` and `FrameAudioSegment` retime/source mapping facts where local types require them

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retiming command semantics</name>
  <read_first>@crates/draft_commands/src/visual.rs @crates/draft_commands/src/timeline.rs @crates/draft_commands/src/error.rs @crates/draft_commands/tests/retiming_commands.rs</read_first>
  <files>crates/draft_commands/src/retiming.rs, crates/draft_commands/src/timeline.rs, crates/draft_commands/src/error.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/tests/retiming_commands.rs, crates/draft_model/src/effects.rs, crates/draft_model/src/timeline.rs</files>
  <behavior>
    - Setting constant speed and curve speed mutates only the target segment retime field and pushes one undo snapshot.
    - Invalid ratios, non-monotonic speed curve points, source-duration overflow, unsupported pitch preservation, locked tracks, and missing segments fail atomically.
    - Split, trim, move, snapping, and main-track magnet keep retime source ranges deterministic.
  </behavior>
  <action>Create `draft_commands/src/retiming.rs` following the `visual.rs` command pattern. Add timeline payload routing for `SetSegmentRetime` and `ClearSegmentRetime`, validation helpers for rational speed ratios and speed curve points, and structured error kinds for invalid retime and unsupported audio follow-speed combinations. Update the existing timeline edit commands so split/trim/move validation preserves typed retime semantics and rejects impossible source ranges without partial mutation.</action>
  <acceptance_criteria>`retiming_commands` tests prove valid retiming commits exactly once, invalid retiming leaves draft/selection/history unchanged, and split/trim/move cannot create ambiguous source mapping.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands retiming_commands -- --nocapture</automated>
  </verify>
  <done>Retiming is an undoable Rust timeline command with atomic validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evaluate retime source mapping in engine_core</name>
  <read_first>@crates/engine_core/src/frame_state.rs @crates/engine_core/src/normalize.rs @crates/engine_core/tests/retiming.rs</read_first>
  <files>crates/engine_core/src/time_mapping.rs, crates/engine_core/src/frame_state.rs, crates/engine_core/src/normalize.rs, crates/engine_core/src/lib.rs, crates/engine_core/tests/retiming.rs</files>
  <behavior>
    - `source_position_for_retime` maps target microseconds to source microseconds for constant speed and speed curves using integer/rational arithmetic.
    - Frame state video and audio segments carry retime-derived source positions consistently.
    - Unsupported audio pitch/time-stretch combinations return typed diagnostics rather than hidden success.
  </behavior>
  <action>Add `engine_core/src/time_mapping.rs` and export it. Replace the linear `source_position_at` behavior in `frame_state.rs` with the retime-aware mapping while keeping all persisted math integer/rational. Extend normalization/frame-state local structs only as needed to carry retime facts and audio follow-speed diagnostics. Do not add FFmpeg-specific concepts to `engine_core`.</action>
  <acceptance_criteria>Engine tests cover 0.5x, 1x, 2x, and multi-point speed curve mapping at segment boundaries and middle samples, plus explicit diagnostics for unsupported pitch preservation.</acceptance_criteria>
  <verify>
    <automated>cargo test -p engine_core retiming -- --nocapture</automated>
  </verify>
  <done>engine_core is the source-time authority for retimed frame state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regenerate retime contracts and staged guards</name>
  <read_first>@crates/draft_model/tests/schema_exports.rs @scripts/phase19-source-guards.sh @package.json</read_first>
  <files>crates/draft_model/src/lib.rs, crates/draft_model/tests/schema_exports.rs, schemas/draft.schema.json, apps/desktop-electron/src/generated/Draft.ts, scripts/phase19-source-guards.sh</files>
  <behavior>
    - Generated contracts include retime command and draft fields.
    - Source guards detect renderer-owned retime math and persisted floating retime values.
    - No schema/TypeScript drift remains after regeneration.
  </behavior>
  <action>Export retime contract types and command payloads through existing schema generation. Extend `scripts/phase19-source-guards.sh` staged mode so retime implementation checks require `engine_core/src/time_mapping.rs`, `draft_commands/src/retiming.rs`, and generated contract references while rejecting renderer retime math and floating persisted retime fields.</action>
  <acceptance_criteria>`pnpm run test:contracts` passes and the Phase 19 guard staged retiming checks fail if `time_mapping.rs` or command routing is removed.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model schema_exports -- --nocapture && pnpm run test:contracts && bash scripts/phase19-source-guards.sh --retiming</automated>
  </verify>
  <done>Retiming schema/contracts are generated and guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI command to draft command | Retime values from UI/API must be validated before draft mutation. |
| Draft model to engine_core | Persisted retime semantics become frame-source mapping. |
| Audio follow-speed to diagnostics | Unsupported pitch/time-stretch requests must not be hidden as success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | `draft_commands::retiming` | mitigate | Validate ratios, curve monotonicity, source bounds, locked tracks, and unsupported audio combinations before mutation. |
| T-19-08 | Repudiation | retime diagnostics | mitigate | Return typed audio retime diagnostics for unsupported pitch/time-stretch instead of treating degraded output as supported. |
| T-19-09 | Denial of Service | speed curves | mitigate | Bound curve point counts and use integer/rational arithmetic to avoid expensive per-frame floating interpolation. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run `cargo test -p draft_commands retiming_commands -- --nocapture`, `cargo test -p engine_core retiming -- --nocapture`, `cargo test -p draft_model schema_exports -- --nocapture`, `pnpm run test:contracts`, and `bash scripts/phase19-source-guards.sh --retiming`.
</verification>

<success_criteria>
PRODFX-01 semantic core is complete when retime commands, engine mapping, generated contracts, and guard checks pass without renderer-owned time math.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-03-SUMMARY.md` when done.
</output>
