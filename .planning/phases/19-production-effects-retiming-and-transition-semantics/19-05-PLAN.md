---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "05"
type: execute
wave: 4
depends_on:
  - "19-04"
files_modified:
  - crates/draft_model/src/effects.rs
  - crates/draft_model/src/timeline.rs
  - crates/draft_commands/src/transition.rs
  - crates/draft_commands/src/timeline.rs
  - crates/draft_commands/src/error.rs
  - crates/draft_commands/src/lib.rs
  - crates/draft_commands/tests/transition_commands.rs
  - crates/render_graph/src/effects.rs
  - crates/render_graph/src/graph.rs
  - crates/render_graph/src/fingerprint.rs
  - crates/render_graph/tests/production_effects.rs
  - crates/realtime_preview_runtime/src/capabilities.rs
  - crates/ffmpeg_compiler/src/effects.rs
  - crates/ffmpeg_compiler/src/filters.rs
  - crates/ffmpeg_compiler/tests/production_effects.rs
autonomous: true
requirements:
  - PRODFX-02
must_haves:
  truths:
    - "Transitions are first-class adjacent/overlap relationships, not renderer-created segment-local deltas."
    - "Transition commands validate adjacency, overlap, trim, split, move, snapping, main-track magnet, and undo/redo."
    - "Dissolve transition preview/export is supported where implemented and degraded/unsupported diagnostics are explicit elsewhere."
  prohibitions:
    - "Renderer transition duration or overlap validation is forbidden."
    - "Private provider transition IDs cannot become internal transition semantics."
  artifacts:
    - path: "crates/draft_commands/src/transition.rs"
      provides: "Undoable transition relationship commands"
    - path: "crates/render_graph/src/effects.rs"
      provides: "Transition render intent and overlap windows"
    - path: "crates/ffmpeg_compiler/src/effects.rs"
      provides: "Compiler-owned dissolve/xfade helpers"
  key_links:
    - from: "crates/draft_commands/src/transition.rs"
      to: "crates/render_graph/src/graph.rs"
      via: "Validated transition relationships become graph transition intents"
      pattern: "TransitionRelationship|RenderTransitionIntent"
    - from: "crates/render_graph/src/graph.rs"
      to: "crates/ffmpeg_compiler/src/effects.rs"
      via: "Dissolve transition intent compiles to export support"
      pattern: "Dissolve|xfade|transition"
---

<objective>
Implement first-class transition semantics with adjacent segment validation, undoable commands, preview diagnostics, and export support.

Purpose: Make PRODFX-02 production-grade before timeline transition handles become visible.
Output: Transition model/commands, graph/compiler/runtime support, and green transition RED tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/draft_model/src/timeline.rs
@crates/draft_commands/src/timeline.rs
@crates/render_graph/src/graph.rs
@crates/ffmpeg_compiler/src/filters.rs
@crates/realtime_preview_runtime/src/capabilities.rs
</context>

## Artifacts this phase produces

- `TrackTransition` or equivalent adjacency relationship type
- `draft_commands::transition::{add_transition, update_transition_duration, remove_transition, validate_transition_relationships}`
- `TimelineEditPayload::{AddTransition, UpdateTransitionDuration, RemoveTransition}`
- `RenderTransitionIntent` with from/to segment IDs and overlap window
- Compiler support for first-party dissolve transition

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add transition relationship model and commands</name>
  <read_first>@crates/draft_model/src/timeline.rs @crates/draft_commands/src/timeline.rs @crates/draft_commands/tests/transition_commands.rs</read_first>
  <files>crates/draft_model/src/effects.rs, crates/draft_model/src/timeline.rs, crates/draft_commands/src/transition.rs, crates/draft_commands/src/timeline.rs, crates/draft_commands/src/error.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/tests/transition_commands.rs</files>
  <behavior>
    - Adding a dissolve transition requires adjacent compatible visual segments on the same timeline relationship.
    - Updating duration validates available overlap and affected trims without renderer decisions.
    - Split, trim, move, delete, snapping, and main-track magnet either preserve valid transitions or reject/clean invalid relationships atomically.
  </behavior>
  <action>Add a transition relationship model in `draft_model` that references adjacent segment IDs and typed transition kind/duration/parameters. Create `draft_commands/src/transition.rs` and route add/update/remove payloads through `execute_timeline_edit`. Extend timeline validation so segment edits cannot leave dangling or impossible transition windows. Convert Wave 0 transition tests to green and cover undo/redo with one snapshot per committed transition command.</action>
  <acceptance_criteria>Transition command tests prove adjacency validation, duration bounds, atomic invalid edits, undo/redo, and interaction with trim/split/move rules.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands transition_commands -- --nocapture</automated>
  </verify>
  <done>Transitions are Rust-owned adjacent relationships with undoable commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transition graph fingerprints and preview support states</name>
  <read_first>@crates/render_graph/src/graph.rs @crates/render_graph/src/fingerprint.rs @crates/realtime_preview_runtime/src/capabilities.rs @crates/render_graph/tests/production_effects.rs</read_first>
  <files>crates/render_graph/src/effects.rs, crates/render_graph/src/graph.rs, crates/render_graph/src/fingerprint.rs, crates/render_graph/tests/production_effects.rs, crates/realtime_preview_runtime/src/capabilities.rs</files>
  <behavior>
    - Render graph transition intent includes from/to segment IDs, transition kind, duration, overlap window, support state, and reason.
    - Fingerprints/dirty ranges change when transition duration, kind, or relationship endpoints change.
    - Realtime preview reports dissolve support only when the GPU/compositor path can honor it, otherwise degraded/unsupported diagnostics are explicit.
  </behavior>
  <action>Project validated transition relationships into render graph transition intents with stable node IDs and semantic fingerprint fields. Update realtime capability classification so first-party dissolve support is explicit and unsupported transition kinds fail closed with product diagnostics. Do not mark transitions supported unless the real preview/export implementation exists in this plan.</action>
  <acceptance_criteria>Render graph and realtime tests prove transition relationship facts appear in graph snapshots, fingerprints, dirty ranges, and support reports.</acceptance_criteria>
  <verify>
    <automated>cargo test -p render_graph production_effects -- --nocapture && cargo test -p realtime_preview_runtime production_effects -- --nocapture</automated>
  </verify>
  <done>Transition graph/preview support is typed and guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compile first-party dissolve transition</name>
  <read_first>@crates/ffmpeg_compiler/src/effects.rs @crates/ffmpeg_compiler/src/filters.rs @crates/ffmpeg_compiler/tests/production_effects.rs</read_first>
  <files>crates/ffmpeg_compiler/src/effects.rs, crates/ffmpeg_compiler/src/filters.rs, crates/ffmpeg_compiler/tests/production_effects.rs, scripts/phase19-source-guards.sh</files>
  <behavior>
    - First-party dissolve transition compiles from render graph intent to compiler-owned filter script output.
    - Unsupported transition kinds emit export diagnostics and cannot pass as product success.
    - Source guards block transition filter construction outside `ffmpeg_compiler`.
  </behavior>
  <action>Implement compiler-owned dissolve transition filter generation from `RenderTransitionIntent`, including deterministic labels and overlap timing. Add compiler tests for supported dissolve and unsupported/degraded transition diagnostics. Extend the Phase 19 guard so transition completion requires command, graph, preview, compiler, and source-boundary coverage.</action>
  <acceptance_criteria>Compiler tests show dissolve export output and explicit diagnostics for unsupported transitions, with no transition FFmpeg strings outside compiler files.</acceptance_criteria>
  <verify>
    <automated>cargo test -p ffmpeg_compiler production_effects -- --nocapture && bash scripts/phase19-source-guards.sh --wave0</automated>
  </verify>
  <done>Dissolve transition has preview/export semantics or explicit diagnostics through Rust layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/API to transition command | Transition duration and boundary requests are untrusted until Rust validates adjacency. |
| Draft to render graph | Validated relationship semantics become graph overlap windows. |
| Render graph to compiler | Transition intent becomes FFmpeg transition filters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Tampering | `draft_commands::transition` | mitigate | Validate segment ownership, adjacency, track locks, duration bounds, and overlap windows in Rust. |
| T-19-14 | Repudiation | transition preview/export | mitigate | Runtime/compiler diagnostics distinguish supported dissolve from unsupported/degraded transitions. |
| T-19-15 | Tampering | FFmpeg transition output | mitigate | Generate transition filters only in `ffmpeg_compiler` from typed graph intent. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run transition command tests, render graph tests, realtime preview tests, compiler tests, and staged source guards.
</verification>

<success_criteria>
PRODFX-02 is complete when transitions are first-class Rust relationships with validation, undo/redo, graph intent, preview/export support or explicit diagnostics, and compiler-owned export output.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-05-SUMMARY.md` when done.
</output>
