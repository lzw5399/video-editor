---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "07"
type: execute
wave: 6
depends_on:
  - "19-06"
files_modified:
  - crates/draft_model/src/effects.rs
  - crates/draft_model/src/timeline.rs
  - crates/draft_commands/src/effects.rs
  - crates/draft_commands/tests/effect_commands.rs
  - crates/render_graph/src/effects.rs
  - crates/render_graph/src/graph.rs
  - crates/render_graph/src/fingerprint.rs
  - crates/render_graph/tests/production_effects.rs
  - crates/realtime_preview_runtime/src/effects.rs
  - crates/realtime_preview_runtime/src/gpu/compositor.rs
  - crates/realtime_preview_runtime/src/gpu/pipelines.rs
  - crates/realtime_preview_runtime/src/capabilities.rs
  - crates/realtime_preview_runtime/tests/production_effects.rs
  - crates/ffmpeg_compiler/src/effects.rs
  - crates/ffmpeg_compiler/src/filters.rs
  - crates/ffmpeg_compiler/tests/production_effects.rs
autonomous: true
requirements:
  - PRODFX-04
must_haves:
  truths:
    - "Masks and blend modes are typed draft semantics with capability-backed GPU preview support where implemented."
    - "Unsupported mask/blend/export combinations produce explicit degraded/unsupported diagnostics."
    - "Mask/blend changes participate in render graph fingerprints, dirty ranges, preview invalidation, and export diagnostics."
  prohibitions:
    - "Masks and blends cannot be product DOM/CSS overlays or renderer-computed alpha math."
    - "Unsupported export modes cannot be shown as successful product output."
  artifacts:
    - path: "crates/realtime_preview_runtime/src/effects.rs"
      provides: "Mask and blend GPU preview helpers"
    - path: "crates/ffmpeg_compiler/src/effects.rs"
      provides: "Mask/blend export support or explicit diagnostics"
  key_links:
    - from: "crates/draft_model/src/timeline.rs"
      to: "crates/realtime_preview_runtime/src/effects.rs"
      via: "Typed SegmentMask and SegmentBlendMode feed GPU preview"
      pattern: "SegmentMask|SegmentBlendMode"
    - from: "crates/render_graph/src/fingerprint.rs"
      to: "crates/ffmpeg_compiler/src/effects.rs"
      via: "Mask/blend semantic changes invalidate export plans"
      pattern: "mask|blend"
---

<objective>
Implement production mask and blend semantics through the typed Rust, graph, GPU preview, and export diagnostic path.

Purpose: Complete PRODFX-04 without exposing fake or fallback visual effects.
Output: Typed mask/blend commands, preview support, compiler diagnostics/support, and green production-effects tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/draft_model/src/timeline.rs
@crates/draft_commands/src/effects.rs
@crates/render_graph/src/graph.rs
@crates/realtime_preview_runtime/src/gpu/compositor.rs
@crates/ffmpeg_compiler/src/filters.rs
</context>

## Artifacts this phase produces

- Supported `SegmentMask::{Rect, Ellipse}` or equivalent typed mask variants with feather/opacity/invert integer parameters
- Supported `SegmentBlendMode::{Normal, Multiply, Screen}` or equivalent typed blend variants plus unsupported mode diagnostics
- `draft_commands::effects::{set_segment_mask, set_segment_blend_mode}`
- GPU preview mask/blend pass helpers and compiler support/diagnostics

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mask and blend command semantics</name>
  <read_first>@crates/draft_model/src/timeline.rs @crates/draft_commands/src/effects.rs @crates/draft_commands/tests/effect_commands.rs</read_first>
  <files>crates/draft_model/src/effects.rs, crates/draft_model/src/timeline.rs, crates/draft_commands/src/effects.rs, crates/draft_commands/tests/effect_commands.rs</files>
  <behavior>
    - Rect and ellipse masks validate integer normalized bounds, feather, opacity, and invert fields.
    - Normal/multiply/screen blend modes are first-party typed modes; unknown/private names become unsupported diagnostics.
    - Mask/blend updates are undoable Rust commands and fail atomically on invalid parameters or locked tracks.
  </behavior>
  <action>Extend the typed mask/blend model and effect command module. Add command helpers for setting/removing masks and setting blend mode with registry-backed support validation. Update `effect_commands.rs` to cover valid mask/blend edits, invalid parameter rejection, unsupported external modes, and one undo snapshot per committed command.</action>
  <acceptance_criteria>Effect command tests prove masks and blends are typed, validated, undoable, and not renderer-authored semantics.</acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands effect_commands -- --nocapture</automated>
  </verify>
  <done>Mask and blend edits are Rust command semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mask blend graph and GPU preview support</name>
  <read_first>@crates/render_graph/src/graph.rs @crates/render_graph/src/fingerprint.rs @crates/realtime_preview_runtime/src/effects.rs @crates/realtime_preview_runtime/src/gpu/compositor.rs @crates/realtime_preview_runtime/tests/production_effects.rs</read_first>
  <files>crates/render_graph/src/effects.rs, crates/render_graph/src/graph.rs, crates/render_graph/src/fingerprint.rs, crates/render_graph/tests/production_effects.rs, crates/realtime_preview_runtime/src/effects.rs, crates/realtime_preview_runtime/src/gpu/compositor.rs, crates/realtime_preview_runtime/src/gpu/pipelines.rs, crates/realtime_preview_runtime/src/capabilities.rs, crates/realtime_preview_runtime/tests/production_effects.rs</files>
  <behavior>
    - Render graph carries typed mask/blend intent and diagnostics.
    - Fingerprints/dirty ranges include mask geometry, feather, opacity, invert, and blend mode.
    - GPU preview applies supported masks/blends through the realtime compositor path.
  </behavior>
  <action>Propagate mask/blend fields into render graph intent, fingerprints, and diagnostics. Implement supported mask and blend preview in `realtime_preview_runtime/src/effects.rs` and the GPU compositor pipeline. Keep any UI overlay handles as future ghost/proxy surfaces only; product preview pixels must come from the native/GPU path.</action>
  <acceptance_criteria>Render graph and realtime preview tests pass and fail if mask/blend evidence is DOM/CSS/artifact-only.</acceptance_criteria>
  <verify>
    <automated>cargo test -p render_graph production_effects -- --nocapture && cargo test -p realtime_preview_runtime production_effects -- --nocapture</automated>
  </verify>
  <done>Masks and blends participate in graph invalidation and GPU preview.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mask blend export support or diagnostics</name>
  <read_first>@crates/ffmpeg_compiler/src/effects.rs @crates/ffmpeg_compiler/src/filters.rs @crates/ffmpeg_compiler/tests/production_effects.rs @scripts/phase19-source-guards.sh</read_first>
  <files>crates/ffmpeg_compiler/src/effects.rs, crates/ffmpeg_compiler/src/filters.rs, crates/ffmpeg_compiler/tests/production_effects.rs, scripts/phase19-source-guards.sh</files>
  <behavior>
    - Supported mask/blend combinations compile or are explicitly classified as degraded/unsupported by the compiler.
    - Unsupported export paths do not produce silent successful output.
    - Guards block mask/blend evaluation and fallback success outside Rust graph/preview/compiler layers.
  </behavior>
  <action>Implement compiler support for the mask/blend subset that can be correctly exported from typed graph intent. For combinations not supported by the current compiler path, emit typed degraded/unsupported diagnostics and keep product success false. Extend Phase 19 source guards for mask/blend ownership and fallback evidence.</action>
  <acceptance_criteria>Compiler tests prove supported mask/blend export where implemented and explicit diagnostics for unsupported combinations.</acceptance_criteria>
  <verify>
    <automated>cargo test -p ffmpeg_compiler production_effects -- --nocapture && bash scripts/phase19-source-guards.sh --wave0</automated>
  </verify>
  <done>Mask/blend export behavior is either implemented or explicitly classified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/API to mask/blend command | Numeric mask geometry and blend selections enter Rust validation. |
| Render graph to GPU preview | Mask alpha and blend modes become GPU compositor operations. |
| Render graph to FFmpeg compiler | Mask/blend intent becomes export support or diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-19 | Tampering | mask/blend parameters | mitigate | Validate integer normalized bounds, opacity, feather, invert, and supported blend modes in Rust. |
| T-19-20 | Repudiation | unsupported export | mitigate | Compiler emits explicit unsupported/degraded diagnostics; no fallback success. |
| T-19-21 | Tampering | GPU preview path | mitigate | Preview tests require compositor-owned mask/blend output, not renderer overlays. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run effect command tests, render graph production effects tests, realtime preview production effects tests, compiler production effects tests, and staged guards.
</verification>

<success_criteria>
PRODFX-04 is complete when masks/blends/blur/effects use production GPU preview where supported and classify unsupported export paths explicitly.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-07-SUMMARY.md` when done.
</output>
