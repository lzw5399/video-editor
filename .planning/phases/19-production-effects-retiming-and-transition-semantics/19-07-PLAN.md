---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "07"
type: execute
wave: 5
depends_on:
  - "19-06"
files_modified:
  - crates/render_graph/src/effects.rs
  - crates/render_graph/src/graph.rs
  - crates/render_graph/src/fingerprint.rs
  - crates/render_graph/tests/production_effects.rs
  - crates/realtime_preview_runtime/src/capabilities.rs
  - crates/realtime_preview_runtime/tests/production_effects.rs
  - crates/ffmpeg_compiler/src/effects.rs
  - crates/ffmpeg_compiler/src/filters.rs
  - crates/ffmpeg_compiler/tests/production_effects.rs
  - scripts/phase19-source-guards.sh
autonomous: true
requirements:
  - PRODFX-02
must_haves:
  truths:
    - "Validated transition relationships become render graph transition intents with fingerprints and dirty ranges."
    - "Dissolve transition preview/export is supported where implemented and degraded/unsupported diagnostics are explicit elsewhere."
    - "FFmpeg transition output is generated only inside `ffmpeg_compiler` from typed graph intent."
  prohibitions:
    - "Renderer, adapter, or realtime preview code cannot construct transition FFmpeg filters."
    - "Unsupported transition kinds cannot be shown as successful product output."
  artifacts:
    - path: "crates/render_graph/src/effects.rs"
      provides: "Transition render intent and overlap windows"
    - path: "crates/ffmpeg_compiler/src/effects.rs"
      provides: "Compiler-owned dissolve/xfade helpers"
  key_links:
    - from: "crates/render_graph/src/graph.rs"
      to: "crates/ffmpeg_compiler/src/effects.rs"
      via: "Dissolve transition intent compiles to export support"
      pattern: "Dissolve|xfade|transition"
    - from: "scripts/phase19-source-guards.sh"
      to: "crates/ffmpeg_compiler/src/effects.rs"
      via: "transition guard rejects filter construction outside compiler"
      pattern: "transition|ffmpeg_compiler"
---

<objective>
Propagate first-class transition relationships through render graph, preview diagnostics, compiler output, and staged guards.

Purpose: Complete PRODFX-02 beyond command semantics while keeping transition render/export ownership in Rust.
Output: Transition graph intent, preview diagnostics, compiler-owned dissolve export, and stage-specific source guards.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/render_graph/src/graph.rs
@crates/render_graph/src/fingerprint.rs
@crates/realtime_preview_runtime/src/capabilities.rs
@crates/ffmpeg_compiler/src/filters.rs
</context>

## Artifacts this phase produces

- `RenderTransitionIntent` with from/to segment IDs and overlap window
- Transition fingerprint and dirty-range fields
- Realtime support diagnostics for first-party dissolve and unsupported transitions
- Compiler support for first-party dissolve transition
- `scripts/phase19-source-guards.sh --transition`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add transition graph fingerprints and preview support states</name>
  <read_first>@crates/render_graph/src/graph.rs @crates/render_graph/src/fingerprint.rs @crates/realtime_preview_runtime/src/capabilities.rs @crates/render_graph/tests/production_effects.rs</read_first>
  <files>crates/render_graph/src/effects.rs, crates/render_graph/src/graph.rs, crates/render_graph/src/fingerprint.rs, crates/render_graph/tests/production_effects.rs, crates/realtime_preview_runtime/src/capabilities.rs, crates/realtime_preview_runtime/tests/production_effects.rs</files>
  <behavior>
    - Render graph transition intent includes from/to segment IDs, transition kind, duration, overlap window, support state, and reason.
    - Fingerprints/dirty ranges change when transition duration, kind, or relationship endpoints change.
    - Realtime preview reports dissolve support only when the GPU/compositor path can honor it, otherwise degraded/unsupported diagnostics are explicit.
  </behavior>
  <action>Project validated transition relationships into render graph transition intents with stable node IDs and semantic fingerprint fields. Update realtime capability classification so first-party dissolve support is explicit and unsupported transition kinds fail closed with product diagnostics. Do not mark transitions supported unless the real preview/export implementation exists in this plan.</action>
  <acceptance_criteria>Render graph and realtime tests prove transition relationship facts appear in graph snapshots, fingerprints, dirty ranges, and support reports.</acceptance_criteria>
  <verify>
    <automated>cargo test -p render_graph production_effects -- --nocapture && cargo test -p realtime_preview_runtime production_effects -- --nocapture</automated>
  </verify>
  <done>Transition graph/preview support is typed and guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile first-party dissolve transition and guards</name>
  <read_first>@crates/ffmpeg_compiler/src/effects.rs @crates/ffmpeg_compiler/src/filters.rs @crates/ffmpeg_compiler/tests/production_effects.rs @scripts/phase19-source-guards.sh</read_first>
  <files>crates/ffmpeg_compiler/src/effects.rs, crates/ffmpeg_compiler/src/filters.rs, crates/ffmpeg_compiler/tests/production_effects.rs, scripts/phase19-source-guards.sh</files>
  <behavior>
    - First-party dissolve transition compiles from render graph intent to compiler-owned filter script output.
    - Unsupported transition kinds emit export diagnostics and cannot pass as product success.
    - Source guards block transition filter construction outside `ffmpeg_compiler`.
  </behavior>
  <action>Implement compiler-owned dissolve transition filter generation from `RenderTransitionIntent`, including deterministic labels and overlap timing. Add compiler tests for supported dissolve and unsupported/degraded transition diagnostics. Extend the Phase 19 guard so `--transition` requires command, graph, preview, compiler, and source-boundary coverage.</action>
  <acceptance_criteria>Compiler tests show dissolve export output and explicit diagnostics for unsupported transitions, with no transition FFmpeg strings outside compiler files.</acceptance_criteria>
  <verify>
    <automated>cargo test -p ffmpeg_compiler production_effects -- --nocapture && bash scripts/phase19-source-guards.sh --transition</automated>
  </verify>
  <done>Dissolve transition has preview/export semantics or explicit diagnostics through Rust layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft to render graph | Validated relationship semantics become graph overlap windows. |
| Render graph to compiler | Transition intent becomes FFmpeg transition filters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-15 | Tampering | `ffmpeg_compiler::effects` | mitigate | Generate transition filters only in `ffmpeg_compiler` from typed graph intent. |
| T-19-16 | Repudiation | transition preview/export | mitigate | Runtime/compiler diagnostics distinguish supported dissolve from unsupported/degraded transitions. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run render graph tests, realtime preview tests, compiler tests, and `bash scripts/phase19-source-guards.sh --transition`.
</verification>

<success_criteria>
PRODFX-02 is complete when transitions have graph intent, preview/export support or explicit diagnostics, and compiler-owned export output.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-07-SUMMARY.md` when done.
</output>
