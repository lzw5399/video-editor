---
phase: 19-production-effects-retiming-and-transition-semantics
plan: "04"
type: execute
wave: 3
depends_on:
  - "19-03"
files_modified:
  - crates/render_graph/src/effects.rs
  - crates/render_graph/src/graph.rs
  - crates/render_graph/src/fingerprint.rs
  - crates/render_graph/src/incremental.rs
  - crates/render_graph/tests/production_effects.rs
  - crates/ffmpeg_compiler/src/effects.rs
  - crates/ffmpeg_compiler/src/filters.rs
  - crates/ffmpeg_compiler/src/lib.rs
  - crates/ffmpeg_compiler/tests/production_effects.rs
  - crates/realtime_preview_runtime/src/capabilities.rs
  - crates/realtime_preview_runtime/src/parity.rs
  - crates/realtime_preview_runtime/tests/production_effects.rs
  - crates/testkit/tests/production_effects_preview.rs
  - crates/testkit/tests/production_effects_exports.rs
autonomous: true
requirements:
  - PRODFX-01
must_haves:
  truths:
    - "Retiming is represented in render graph, graph fingerprints, dirty ranges, preview capability diagnostics, FFmpeg compiler output, and the follow-on audio graph plan."
    - "Preview/export parity tests use the same Rust source mapping facts created by engine_core."
    - "FFmpeg timestamp/audio filters are generated only inside ffmpeg_compiler from typed render graph intent."
  prohibitions:
    - "Renderer, adapter, or realtime preview code cannot construct retime filter strings."
    - "Cache/fingerprint dirty logic cannot ignore retime mode or speed curve changes."
  artifacts:
    - path: "crates/render_graph/src/effects.rs"
      provides: "Render graph retime intent projection"
    - path: "crates/ffmpeg_compiler/src/effects.rs"
      provides: "Compiler-owned retime filter helpers"
    - path: "crates/testkit/tests/production_effects_exports.rs"
      provides: "Retimed export parity evidence"
  key_links:
    - from: "crates/engine_core/src/time_mapping.rs"
      to: "crates/render_graph/src/graph.rs"
      via: "frame state and normalized ranges become retime render intent"
      pattern: "retime|Retime"
    - from: "crates/render_graph/src/graph.rs"
      to: "crates/ffmpeg_compiler/src/effects.rs"
      via: "typed retime intent compiles into setpts/asetpts/atempo plan fragments"
      pattern: "compile.*retime"
---

<objective>
Propagate retiming through render graph, preview diagnostics, FFmpeg compiler, cache fingerprints, and parity fixtures.

Purpose: Finish PRODFX-01 end to end so later UI can expose speed controls without owning time math.
Output: Retiming render intent, compiler support, preview/export parity tests, and dirty-range/fingerprint coverage.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-CONTEXT.md
@.planning/phases/19-production-effects-retiming-and-transition-semantics/19-PATTERNS.md
@crates/engine_core/src/time_mapping.rs
@crates/render_graph/src/graph.rs
@crates/render_graph/src/fingerprint.rs
@crates/ffmpeg_compiler/src/filters.rs
@crates/realtime_preview_runtime/src/capabilities.rs
</context>

## Artifacts this phase produces

- `RenderRetimeIntent` or equivalent typed retime field on video/audio render graph layers
- `RenderGraphNodeRole`/node IDs and semantic fingerprint fields for retime facts
- `ffmpeg_compiler::effects::{compile_video_retime_filters, compile_audio_retime_filters}`
- Testkit retime preview/export parity fixtures

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retime render graph intent and fingerprints</name>
  <read_first>@crates/render_graph/src/graph.rs @crates/render_graph/src/fingerprint.rs @crates/render_graph/src/incremental.rs @crates/render_graph/tests/production_effects.rs</read_first>
  <files>crates/render_graph/src/effects.rs, crates/render_graph/src/graph.rs, crates/render_graph/src/fingerprint.rs, crates/render_graph/src/incremental.rs, crates/render_graph/tests/production_effects.rs</files>
  <behavior>
    - Render graph video and audio layer intents carry retime mode, source mapping, speed curve, and audio follow-speed support facts.
    - Fingerprints and dirty facts change when retime ratios, curve points, or audio follow-speed policy change.
    - Unsupported/degraded retime paths emit render diagnostics instead of disappearing.
  </behavior>
  <action>Extend render graph layer structs with typed retime intent derived from engine/draft semantics. Update stable node identity or semantic fingerprint inputs so retime changes invalidate the correct video, audio, preview, export, cache, thumbnail, and waveform domains. Add render graph tests that fail if retime data is absent from snapshots, fingerprints, or dirty facts.</action>
  <acceptance_criteria>`render_graph` tests prove retime changes alter fingerprints and dirty ranges while preserving stable node IDs for unchanged segment identity.</acceptance_criteria>
  <verify>
    <automated>cargo test -p render_graph production_effects -- --nocapture</automated>
  </verify>
  <done>Render graph represents retime semantics and invalidation facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile retime export and preview diagnostics</name>
  <read_first>@crates/ffmpeg_compiler/src/filters.rs @crates/ffmpeg_compiler/tests/production_effects.rs @crates/realtime_preview_runtime/src/capabilities.rs @crates/realtime_preview_runtime/src/parity.rs</read_first>
  <files>crates/ffmpeg_compiler/src/effects.rs, crates/ffmpeg_compiler/src/filters.rs, crates/ffmpeg_compiler/src/lib.rs, crates/ffmpeg_compiler/tests/production_effects.rs, crates/realtime_preview_runtime/src/capabilities.rs, crates/realtime_preview_runtime/src/parity.rs, crates/realtime_preview_runtime/tests/production_effects.rs</files>
  <behavior>
    - Constant speed and speed curve export use compiler-owned timestamp/audio filters derived from render graph intent.
    - Audio follow-speed support uses atempo-safe chains where supported and returns typed degraded/unsupported diagnostics otherwise.
    - Realtime preview reports supported or degraded retime states from the capability registry, not fallback success.
  </behavior>
  <action>Add `ffmpeg_compiler/src/effects.rs` and export compiler-owned retime helpers. Wire `filters.rs` so video timestamp and audio timing filters are generated from typed graph intent only. Update realtime capability and parity diagnostics to include retime preview/export support and degraded audio pitch/time-stretch facts. Keep FFmpeg command/filter construction out of renderer, adapter, and runtime execution crates.</action>
  <acceptance_criteria>Compiler tests assert generated filter scripts contain retime fragments only from typed graph plans, and preview diagnostics classify unsupported audio retime combinations explicitly.</acceptance_criteria>
  <verify>
    <automated>cargo test -p ffmpeg_compiler production_effects -- --nocapture && cargo test -p realtime_preview_runtime production_effects -- --nocapture</automated>
  </verify>
  <done>Retiming compiles and previews through Rust-owned support diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove retime preview export parity in testkit</name>
  <read_first>@crates/testkit/tests/preview_export_parity.rs @crates/testkit/tests/template_import_preview.rs @crates/testkit/tests/template_import_exports.rs @crates/testkit/tests/production_effects_preview.rs @crates/testkit/tests/production_effects_exports.rs</read_first>
  <files>crates/testkit/tests/production_effects_preview.rs, crates/testkit/tests/production_effects_exports.rs, scripts/phase19-source-guards.sh</files>
  <behavior>
    - Testkit fixtures cover constant speed and speed curve preview/export parity.
    - Product evidence rejects preview artifacts, CPU readback, DOM overlays, or FFmpeg-only success.
    - Source guard staged retime mode requires graph/compiler/testkit coverage.
  </behavior>
  <action>Add deterministic production-effects testkit cases that build a retimed draft, prepare realtime preview graph evidence, compile export, and compare preview/export semantic timing. Extend the Phase 19 guard so retime completion requires testkit coverage and blocks FFmpeg retime strings outside `ffmpeg_compiler`.</action>
  <acceptance_criteria>Testkit retime cases pass and fail closed when fallback evidence is substituted for render-graph GPU preview/export parity.</acceptance_criteria>
  <verify>
    <automated>cargo test -p testkit production_effects -- --nocapture && bash scripts/phase19-source-guards.sh --retiming</automated>
  </verify>
  <done>Retiming has product-path parity evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render graph to compiler | Retime graph intent becomes FFmpeg timestamp/audio filters. |
| Preview/runtime diagnostics | Runtime support reports retime states to product UI and tests. |
| Cache/fingerprint invalidation | Retime semantic changes determine derived artifact reuse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Tampering | `ffmpeg_compiler::effects` | mitigate | Generate process/filter fragments only from typed graph intent and compiler-owned escaping/format helpers. |
| T-19-11 | Repudiation | preview/export parity | mitigate | Testkit requires real graph/preview/export facts and no-product-fallback evidence. |
| T-19-12 | Denial of Service | cache invalidation | mitigate | Dirty/fingerprint tests ensure retime changes invalidate bounded affected ranges instead of forcing hidden stale reuse. |
| T-19-SC | Tampering | dependency stack | accept | No dependency changes. |
</threat_model>

<verification>
Run render graph, compiler, realtime preview, testkit production effects tests, and `bash scripts/phase19-source-guards.sh --retiming`.
</verification>

<success_criteria>
PRODFX-01 is complete end to end when retiming survives graph, preview, compiler, fingerprint, dirty range, and parity tests.
</success_criteria>

<output>
Create `.planning/phases/19-production-effects-retiming-and-transition-semantics/19-04-SUMMARY.md` when done.
</output>
