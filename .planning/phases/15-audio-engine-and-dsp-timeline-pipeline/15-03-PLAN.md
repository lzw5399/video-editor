---
phase: 15-audio-engine-and-dsp-timeline-pipeline
plan: "03"
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/audio_output_desktop/Cargo.toml
  - crates/audio_output_desktop/src/lib.rs
  - crates/audio_output_desktop/src/cpal_output.rs
  - crates/audio_output_desktop/src/mock_output.rs
  - crates/audio_output_desktop/tests/audio_output_capabilities.rs
  - crates/audio_output_desktop/tests/native_audio.rs
autonomous: false
requirements:
  - AUDIO2-03
user_setup: []
must_haves:
  truths:
    - "AUDIO2-03: desktop preview audio output is represented by Rust traits and capability reports, with macOS targeting CoreAudio and Windows targeting WASAPI per D-10."
    - "Native output handles and buffers remain inside Rust; TypeScript receives only safe device summaries, session IDs, statuses, and diagnostics per D-11."
    - "Generic CI uses mock output/capability tests; native output proof is gated by `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1` per planner constraints."
  artifacts:
    - path: "crates/audio_output_desktop/src/lib.rs"
      provides: "Desktop output crate exports"
      exports: ["probe_desktop_audio_output_capabilities", "create_desktop_audio_output"]
    - path: "crates/audio_output_desktop/src/cpal_output.rs"
      provides: "CPAL-backed CoreAudio/WASAPI output implementation after package approval"
      exports: ["CpalAudioOutputDevice", "CpalAudioOutputStream"]
    - path: "crates/audio_output_desktop/src/mock_output.rs"
      provides: "CI-safe output implementation"
      exports: ["MockAudioOutputDevice", "MockAudioOutputStream"]
  key_links:
    - from: "crates/audio_output_desktop/src/lib.rs"
      to: "crates/audio_engine/src/output.rs"
      via: "AudioOutputDevice trait implementation"
      pattern: "AudioOutputDevice"
    - from: "crates/audio_output_desktop/tests/native_audio.rs"
      to: "VIDEO_EDITOR_TEST_NATIVE_AUDIO"
      via: "env-gated native proof"
      pattern: "VIDEO_EDITOR_TEST_NATIVE_AUDIO"
---

<objective>
Add the desktop audio output boundary behind a blocking package legitimacy checkpoint.

Purpose: AUDIO2-03 requires CoreAudio/WASAPI-capable desktop output without leaking native device handles or making native playback mandatory in generic CI. The research marks `cpal` as `[ASSUMED]`, so dependency addition must fail closed until the checkpoint approves it.
Output: Package approval gate, `audio_output_desktop` crate, CPAL-backed output implementation after approval, mock output tests, capability reports, and env-gated native proof.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-CONTEXT.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-RESEARCH.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-PATTERNS.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-VALIDATION.md
@crates/audio_engine/src/output.rs
@crates/media_runtime_desktop/src/capabilities.rs
@crates/media_runtime_desktop/tests/capabilities.rs
@Cargo.toml
</context>

## Artifacts This Phase Produces

- New workspace member path: `crates/audio_output_desktop`.
- New package file: `crates/audio_output_desktop/Cargo.toml`.
- New modules: `cpal_output.rs`, `mock_output.rs`, and `lib.rs`.
- New exports: `DesktopAudioOutputCapabilities`, `DesktopAudioOutputBackend`, `probe_desktop_audio_output_capabilities`, `create_desktop_audio_output`, `CpalAudioOutputDevice`, and `MockAudioOutputDevice`.
- New tests: `audio_output_capabilities.rs` and env-gated `native_audio.rs`.
- Package approval record in `15-03-SUMMARY.md`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 15-03-01: Verify `cpal` package legitimacy before dependency addition</name>
  <files>Cargo.toml, Cargo.lock, crates/audio_output_desktop/Cargo.toml</files>
  <read_first>
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-RESEARCH.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-CONTEXT.md
    - Cargo.toml
  </read_first>
  <action>Before adding any new Cargo dependency, verify the `cpal` `[ASSUMED]` package from the Phase 15 Package Legitimacy Audit. Run `cargo info cpal`, `cargo search cpal --limit 5`, and inspect the crates.io package page plus source repository named in `15-RESEARCH.md`. Confirm that the exact planned package is `cpal` from crates.io, version compatible with the audit, and that it maps macOS to CoreAudio and Windows to WASAPI per D-10. If the package is not approved, stop before changing `Cargo.toml` or `Cargo.lock` and record the failed checkpoint in `15-03-SUMMARY.md`. Do not add `rubato`; Phase 15 only creates a resampling seam and the research marks `rubato` as optional `[ASSUMED]`.</action>
  <verify>
    <automated>cargo info cpal && cargo search cpal --limit 5</automated>
    <human-check>Verify the crates.io package and source repository for `cpal`, then type "approved" or describe the rejection.</human-check>
  </verify>
  <acceptance_criteria>
    - `cpal` package legitimacy is explicitly approved before any `cargo add` or manifest edit.
    - If rejected, no dependency files are changed and execution stops with a clear checkpoint record.
    - `rubato` is not added by this plan.
  </acceptance_criteria>
  <done>Dependency addition is approved or fail-closed before implementation proceeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-03-02: Implement desktop output capability boundary and env-gated native proof</name>
  <files>Cargo.toml, Cargo.lock, crates/audio_output_desktop/Cargo.toml, crates/audio_output_desktop/src/lib.rs, crates/audio_output_desktop/src/cpal_output.rs, crates/audio_output_desktop/src/mock_output.rs, crates/audio_output_desktop/tests/audio_output_capabilities.rs, crates/audio_output_desktop/tests/native_audio.rs</files>
  <read_first>
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-CONTEXT.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-RESEARCH.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-PATTERNS.md
    - crates/audio_engine/src/output.rs
    - crates/media_runtime_desktop/src/capabilities.rs
    - crates/media_runtime_desktop/tests/macos_media_io.rs
    - crates/media_runtime_desktop/tests/windows_media_io.rs
  </read_first>
  <behavior>
    - Test: default CI capability test reports mock output readiness and platform capability summaries without requiring a physical output device.
    - Test: non-target platforms report explicit unsupported or degraded audio output domains rather than claiming native readiness.
    - Test: native audio proof runs only when `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1`; otherwise it exits as a skipped/diagnostic test without failure.
  </behavior>
  <action>After Task 15-03-01 approval, add `audio_output_desktop` to the Cargo workspace and add `cpal` only to this crate. Implement `DesktopAudioOutputCapabilities`, `DesktopAudioOutputBackend`, `DesktopAudioDeviceSummary`, and `DesktopAudioOutputDiagnostic` with safe labels, availability, sample-rate/channel summaries, and fallback reasons; do not expose platform handles, stream configs, or raw buffers. Implement `MockAudioOutputDevice` as the default CI implementation over `audio_engine::output::AudioOutputDevice`. Implement `CpalAudioOutputDevice` and `CpalAudioOutputStream` in `cpal_output.rs` with all native handles private to Rust. Add target/platform capability reporting for CoreAudio on macOS and WASAPI on Windows per D-10, while generic tests remain mock-backed. Add `native_audio.rs` that requires `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1` before opening a real stream and reports a safe diagnostic when the env var is absent.</action>
  <verify>
    <automated>cargo test -p audio_output_desktop audio_output_capabilities -- --nocapture</automated>
    <automated>cargo check -p audio_output_desktop --locked</automated>
    <human-check>Optional native proof: on macOS or Windows with a real output device, run `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1 cargo test -p audio_output_desktop native_audio -- --nocapture` and record the result in `15-03-SUMMARY.md`.</human-check>
  </verify>
  <acceptance_criteria>
    - Required CI gate `cargo test -p audio_output_desktop audio_output_capabilities -- --nocapture` passes without physical audio hardware.
    - Optional native gate is documented and env-gated; it is not required in generic CI.
    - `rg -n "pub .*Handle|pub .*Stream|native.*pointer|raw.*buffer|deviceHandle|outputDeviceHandle" crates/audio_output_desktop/src crates/audio_engine/src/output.rs` finds no public native handle leaks.
  </acceptance_criteria>
  <done>Desktop output is behind a Rust trait/capability boundary, with native proof optional and explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo registry -> Rust desktop runtime | A new package enters the native audio output path. |
| Rust audio_engine -> desktop output implementation | Bounded audio output trait calls enter platform-backed stream code. |
| Native OS audio backend -> bindings/UI | Device and backend capabilities are summarized without exposing native handles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | Cargo dependency addition | mitigate | Blocking human package legitimacy checkpoint for `cpal` before manifest or lockfile changes; fail closed on rejection. |
| T-15-03-02 | Information Disclosure / Elevation of Privilege | `cpal_output.rs` | mitigate | Keep native device/stream handles private; expose only safe `DesktopAudioDeviceSummary` and diagnostics. |
| T-15-03-03 | Denial of Service | native stream tests | mitigate | Default CI uses mock output; native proof requires `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1` and reports no-device conditions safely. |
| T-15-03-SC | Tampering | Cargo installs | mitigate | `cpal` is the only new package allowed here after checkpoint approval; `rubato` remains uninstalled unless a separate audit approves it. |
</threat_model>

<verification>
<automated>cargo info cpal && cargo search cpal --limit 5</automated>
<human-check>Package legitimacy approval for `cpal` before dependency addition.</human-check>
<automated>cargo test -p audio_output_desktop audio_output_capabilities -- --nocapture</automated>
<automated>cargo check -p audio_output_desktop --locked</automated>
<human-check>Optional native proof: run `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1 cargo test -p audio_output_desktop native_audio -- --nocapture` only on a suitable macOS or Windows host with an output device.</human-check>
</verification>

<success_criteria>
AUDIO2-03 has a Rust-owned desktop output boundary: macOS/CoreAudio and Windows/WASAPI support are represented through safe capabilities and private native output implementations, CI remains device-independent, and TypeScript cannot receive native handles or raw audio buffers.
</success_criteria>

<output>
Create `.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-03-SUMMARY.md` when done.
</output>
