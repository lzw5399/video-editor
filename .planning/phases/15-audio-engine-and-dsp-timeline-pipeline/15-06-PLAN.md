---
phase: 15-audio-engine-and-dsp-timeline-pipeline
plan: "06"
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - crates/render_graph/Cargo.toml
  - crates/render_graph/src/graph.rs
  - crates/render_graph/tests/render_graph_snapshots.rs
  - crates/ffmpeg_compiler/Cargo.toml
  - crates/ffmpeg_compiler/src/filters.rs
  - crates/ffmpeg_compiler/tests/ffmpeg_job_snapshots.rs
  - crates/testkit/src/lib.rs
  - crates/testkit/tests/audio_preview_export_parity.rs
autonomous: true
requirements:
  - AUDIO2-04
user_setup: []
must_haves:
  truths:
    - "AUDIO2-04: export audio mixdown consumes Rust-owned audio mix intent and is parity-tested against preview audio graph evaluation per D-07, D-08, and D-09."
    - "Any preview/export audio differences are classified diagnostics, not hidden UI behavior or renderer-side FFmpeg syntax per D-07."
    - "Parity tests use deterministic testkit fixtures and typed mix/sample summaries before heavyweight media-binary checks per D-09."
  artifacts:
    - path: "crates/render_graph/src/graph.rs"
      provides: "Render graph audio mix fields aligned with AudioMixIntent"
      exports: ["RenderAudioMix", "RenderAudioMixDiagnostic"]
    - path: "crates/ffmpeg_compiler/src/filters.rs"
      provides: "Export-only audio filter compilation from Rust-owned mix intent"
    - path: "crates/testkit/tests/audio_preview_export_parity.rs"
      provides: "Deterministic audio preview/export parity tests"
  key_links:
    - from: "crates/audio_engine/src/mix_intent.rs"
      to: "crates/render_graph/src/graph.rs"
      via: "AudioMixIntent to RenderAudioMix mapping"
      pattern: "AudioMixIntent"
    - from: "crates/render_graph/src/graph.rs"
      to: "crates/ffmpeg_compiler/src/filters.rs"
      via: "RenderAudioMix"
      pattern: "RenderAudioMix"
    - from: "crates/testkit/tests/audio_preview_export_parity.rs"
      to: "crates/audio_engine/src/dsp_timeline.rs"
      via: "typed sample summary comparison"
      pattern: "AudioMixSummary"
---

<objective>
Connect Rust-owned audio mix intent to export compilation and add deterministic preview/export parity diagnostics.

Purpose: AUDIO2-04 requires the preview audio graph and export audio mixdown to share DSP evaluation semantics, with differences classified rather than hidden or recreated in TypeScript.
Output: Render/export audio mix intent integration, FFmpeg compiler consumption of Rust-owned intent, parity diagnostics, and deterministic testkit coverage.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-CONTEXT.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-RESEARCH.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-PATTERNS.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-VALIDATION.md
@crates/audio_engine/src/mix_intent.rs
@crates/audio_engine/src/dsp_timeline.rs
@crates/render_graph/src/graph.rs
@crates/ffmpeg_compiler/src/filters.rs
@crates/testkit/tests/preview_export_parity.rs
</context>

## Artifacts This Phase Produces

- New render/export mapping from `audio_engine::AudioMixIntent` to exportable `RenderAudioMix` data.
- New diagnostics: `AudioPreviewExportParityDiagnostic`, `AudioMixParityStatus`, `AudioMixParityDifference`, and `AudioSampleSummary`.
- Updated FFmpeg compiler audio filter generation for gain, pan, fades, and classified unsupported effect slots from Rust-owned intent.
- New deterministic testkit parity file: `crates/testkit/tests/audio_preview_export_parity.rs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-06-01: Map AudioMixIntent through render graph and FFmpeg compiler</name>
  <files>crates/render_graph/Cargo.toml, crates/render_graph/src/graph.rs, crates/render_graph/tests/render_graph_snapshots.rs, crates/ffmpeg_compiler/Cargo.toml, crates/ffmpeg_compiler/src/filters.rs, crates/ffmpeg_compiler/tests/ffmpeg_job_snapshots.rs</files>
  <read_first>
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-CONTEXT.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-RESEARCH.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-PATTERNS.md
    - crates/audio_engine/src/mix_intent.rs
    - crates/render_graph/src/graph.rs
    - crates/ffmpeg_compiler/src/filters.rs
    - crates/ffmpeg_compiler/tests/ffmpeg_job_snapshots.rs
  </read_first>
  <behavior>
    - Test: audio mix intent containing gain, pan, fades, and volume keyframes maps to render/export audio mix data without TypeScript participation.
    - Test: FFmpeg compiler consumes render audio mix fields and emits export-only filter text for supported gain/pan/fades while classifying unsupported effect slots.
    - Test: preview frame output profiles do not synthesize export audio filters, and renderer code never assembles audio FFmpeg syntax.
  </behavior>
  <action>Integrate `audio_engine::AudioMixIntent` into the Rust render/export path per D-07 and D-08. Keep `ffmpeg_compiler` as the only owner of FFmpeg audio filter strings. Add or adapt `RenderAudioMix` fields for `gain_millis`, `pan_balance_millis`, `fade_in_duration`, `fade_out_duration`, volume keyframe facts, source/target timeranges, and effect-slot classification. Update `ffmpeg_compiler/src/filters.rs` so supported audio mix semantics compile into export filter fragments from Rust-owned intent; unsupported effect slots produce classified diagnostics instead of silent approximation. Do not add UI filter strings, renderer helpers, or FFmpeg argument construction outside Rust compiler modules.</action>
  <verify>
    <automated>cargo test -p render_graph audio -- --nocapture</automated>
    <automated>cargo test -p ffmpeg_compiler audio -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Render graph snapshots include gain, pan, fade, keyframe, and effect-slot classification facts for audio mixes.
    - FFmpeg compiler tests prove filter generation for supported gain/pan/fades and diagnostic classification for unsupported effect slots.
    - `rg -n "filter_complex|atrim|amix|volume=|pan=" apps/desktop-electron/src` returns no renderer-owned audio filter construction.
  </acceptance_criteria>
  <done>Export audio mixdown consumes Rust-owned audio mix intent and preserves compiler ownership of FFmpeg syntax.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-06-02: Add deterministic preview/export audio parity diagnostics</name>
  <files>crates/testkit/src/lib.rs, crates/testkit/tests/audio_preview_export_parity.rs, crates/testkit/tests/preview_export_parity.rs</files>
  <read_first>
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-CONTEXT.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-VALIDATION.md
    - crates/testkit/tests/preview_export_parity.rs
    - crates/testkit/src/lib.rs
    - crates/audio_engine/src/dsp_timeline.rs
    - crates/audio_engine/src/mix_intent.rs
    - crates/ffmpeg_compiler/src/filters.rs
  </read_first>
  <behavior>
    - Test: deterministic audio fixtures compare preview `AudioMixIntent` and export `RenderAudioMix` summaries for matching gain, pan, fade, keyframe, source range, and target range facts.
    - Test: classified differences distinguish unsupported preview-only, export-only, effect-slot unsupported, sample-rate mismatch, missing material, and muted-track silence cases.
    - Test: parity assertions run without requiring FFmpeg media rendering for typed intent/sample-summary checks.
  </behavior>
  <action>Add `audio_preview_export_parity.rs` tests per D-07 and D-09. Use deterministic `testkit` draft/material fixtures and typed summaries from `audio_engine` before any heavyweight media-binary checks. Implement `AudioSampleSummary`, `AudioMixParityStatus`, `AudioMixParityDifference`, and `AudioPreviewExportParityDiagnostic` either in `audio_engine` or `testkit` according to existing crate ownership patterns; keep reusable diagnostics in Rust crates, not TypeScript. Cover normal parity, muted track, pan, fades, keyframed volume, missing material classification, and unsupported effect-slot classification. Do not require native audio devices, CPAL streams, or FFmpeg output files for the required parity tests.</action>
  <verify>
    <automated>cargo test -p testkit audio_preview_export_parity -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p testkit audio_preview_export_parity -- --nocapture` passes and runs tests named with the `audio_preview_export_parity_` prefix.
    - Parity diagnostics are typed Rust data and can be surfaced safely by later UI/export status without raw sample buffers or FFmpeg filters.
    - Existing `cargo test -p testkit preview_export_parity -- --nocapture` still passes.
  </acceptance_criteria>
  <done>Preview/export audio parity is tested through shared Rust mix intent and classified diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio_engine mix intent -> render_graph/export | Rust audio semantics enter export render intent and compiler paths. |
| render_graph -> ffmpeg_compiler | Typed render audio mix data becomes FFmpeg filter syntax inside Rust compiler code. |
| parity diagnostics -> UI/export status | Classified differences may cross bindings as safe user-facing warnings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Tampering | export audio mix mapping | mitigate | Tests compare preview mix intent and export render mix summaries for identical semantic facts. |
| T-15-06-02 | Repudiation | parity diagnostics | mitigate | Classify preview/export differences instead of hiding or collapsing them into generic failures. |
| T-15-06-03 | Information Disclosure | compiler/filter output | mitigate | FFmpeg filter strings stay in Rust compiler modules and never appear in renderer production UI or generated UI commands. |
| T-15-06-04 | Denial of Service | parity tests | accept | Required parity tests use typed summaries and deterministic fixtures without native devices or media-binary render output. |
</threat_model>

<verification>
<automated>cargo test -p render_graph audio -- --nocapture</automated>
<automated>cargo test -p ffmpeg_compiler audio -- --nocapture</automated>
<automated>cargo test -p testkit audio_preview_export_parity -- --nocapture</automated>
<automated>cargo test -p testkit preview_export_parity -- --nocapture</automated>
</verification>

<success_criteria>
AUDIO2-04 is covered by Rust-owned parity: preview DSP evaluation and export mixdown share typed audio mix intent where possible, compiler-owned FFmpeg filters consume that intent, and differences are classified through deterministic tests.
</success_criteria>

<output>
Create `.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-06-SUMMARY.md` when done.
</output>
