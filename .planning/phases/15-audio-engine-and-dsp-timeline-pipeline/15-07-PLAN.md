---
phase: 15-audio-engine-and-dsp-timeline-pipeline
plan: "07"
type: execute
wave: 6
depends_on:
  - "15-04"
  - "15-05"
  - "15-06"
files_modified:
  - scripts/phase15-source-guards.sh
  - package.json
autonomous: true
requirements:
  - AUDIO2-01
  - AUDIO2-02
  - AUDIO2-03
  - AUDIO2-04
user_setup: []
must_haves:
  truths:
    - "AUDIO2-01: Phase 15 aggregate gates prove generation-gated audio preview sessions, cancellation, seek/pause/status, bounded buffers, and independence from FFmpeg preview frame generation per D-01, D-02, and D-03."
    - "AUDIO2-02: Phase 15 gates prove typed DSP timeline semantics for gain, mute, pan, fades, keyframed volume, and effect slots per D-04, D-05, and D-06."
    - "AUDIO2-03: Phase 15 gates prove CoreAudio/WASAPI output boundaries, safe device summaries, and waveform display-only artifact consumption per D-10, D-11, D-12, D-13, D-14, and D-15."
    - "AUDIO2-04: Phase 15 gates prove preview/export audio mix parity through Rust-owned typed diagnostics per D-07, D-08, and D-09."
  artifacts:
    - path: "scripts/phase15-source-guards.sh"
      provides: "Phase 15 renderer/core ownership source guard"
    - path: "package.json"
      provides: "Phase 15 aggregate scripts"
      exports: ["test:phase15-rust", "test:phase15-source-guards", "test:phase15-workspace", "test:phase15"]
  key_links:
    - from: "package.json"
      to: "scripts/phase15-source-guards.sh"
      via: "test:phase15-source-guards"
      pattern: "phase15-source-guards"
    - from: "package.json"
      to: "crates/audio_engine"
      via: "test:phase15-rust"
      pattern: "audio_engine"
    - from: "package.json"
      to: "apps/desktop-electron/tests/workspace.spec.ts"
      via: "test:phase15-workspace"
      pattern: "音频预览|波形|播放状态"
---

<objective>
Close Phase 15 with source ownership guards and public aggregate verification gates.

Purpose: The phase changes Rust model, audio runtime, desktop output, bindings, UI, waveform display, and export parity; final gates must prove requirement coverage and prevent renderer-owned audio/render/artifact/native behavior from regressing.
Output: `scripts/phase15-source-guards.sh`, `test:phase15-*` package scripts, final source audit, and required automated phase gate.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-CONTEXT.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-RESEARCH.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-PATTERNS.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-VALIDATION.md
@.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-UI-SPEC.md
@scripts/phase14-source-guards.sh
@scripts/phase13-source-guards.sh
@package.json
@apps/desktop-electron/src/renderer
@crates/audio_engine
@crates/audio_output_desktop
@crates/bindings_node/src
@crates/ffmpeg_compiler/src
</context>

## Artifacts This Phase Produces

- New CLI guard: `scripts/phase15-source-guards.sh`.
- New package scripts: `test:phase15-rust`, `test:phase15-source-guards`, `test:phase15-workspace`, and `test:phase15`.
- Final phase gate that composes Rust audio semantics/runtime/output/bindings/export parity tests, renderer source guards, workspace UI tests, and contract drift checks.

<tasks>

<task type="auto">
  <name>Task 15-07-01: Add Phase 15 source guards and aggregate package scripts</name>
  <files>scripts/phase15-source-guards.sh, package.json</files>
  <read_first>
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-CONTEXT.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-RESEARCH.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-PATTERNS.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-VALIDATION.md
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-UI-SPEC.md
    - scripts/phase14-source-guards.sh
    - scripts/phase13-source-guards.sh
    - package.json
  </read_first>
  <action>Create `scripts/phase15-source-guards.sh` using the comment-filtered helper style from Phase 14. The guard must enforce D-01 through D-15: renderer code cannot construct audio graphs, DSP plans, gain curves, pan laws, fade envelopes, effect graphs, sample indices, mix buffers, ring buffers, output-device handles, native backend handles, FFmpeg audio filters/args, render graphs, cache keys, artifact roots, waveform blob paths, SQLite queries, fingerprints, dirty ranges, timeline state, or `TimelineClock` / `PlaybackGeneration` mutations. The guard must require Rust-owned symbols and tests from Plans 15-01 through 15-06, require env-gated native audio proof text `VIDEO_EDITOR_TEST_NATIVE_AUDIO`, and require UI-safe Chinese labels from `15-UI-SPEC.md`. Use comment filtering before count checks; do not use bare `grep -c ... == 0` gates. Add injected negative checks for renderer `AudioGraph`, renderer `mixBuffer`, renderer `outputDeviceHandle`, renderer FFmpeg audio filter string, renderer waveform SQLite/blob path access, and production UI forbidden copy. Add package scripts: `test:phase15-rust`, `test:phase15-source-guards`, `test:phase15-workspace`, and `test:phase15`. `test:phase15-rust` must run focused tests for `draft_model` audio semantics, `draft_commands` audio commands, `audio_engine`, `audio_output_desktop` capability tests, `bindings_node` audio service, `render_graph` audio tests, `ffmpeg_compiler` audio tests, and `testkit` audio preview/export parity. `test:phase15-workspace` must run the workspace filter `音频预览|波形|播放状态|五大区域`. `test:phase15` must compose Rust, source guards, workspace, and `test:contracts`.</action>
  <verify>
    <automated>pnpm run test:phase15-source-guards</automated>
    <automated>node -e "const p=require('./package.json'); for (const k of ['test:phase15-rust','test:phase15-source-guards','test:phase15-workspace','test:phase15']) { if (!p.scripts[k]) throw new Error('missing '+k); }"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run test:phase15-source-guards` passes and its injected negative checks fail if forbidden renderer audio ownership patterns are allowed.
    - `package.json` contains all `test:phase15-*` scripts and the aggregate `test:phase15` includes `pnpm run test:contracts`.
    - The guard explicitly checks safe labels and forbids production copy named in `15-UI-SPEC.md`.
  </acceptance_criteria>
  <done>Phase 15 has executable source ownership guards and aggregate package-script entrypoints.</done>
</task>

<task type="auto">
  <name>Task 15-07-02: Run and repair final Phase 15 gates</name>
  <files>scripts/phase15-source-guards.sh, package.json</files>
  <read_first>
    - .planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-VALIDATION.md
    - package.json
    - scripts/phase15-source-guards.sh
  </read_first>
  <action>Run the full Phase 15 gate and repair only issues caused by Phase 15 files. Required commands are `pnpm run test:phase15-rust`, `pnpm run test:phase15-source-guards`, `pnpm run test:phase15-workspace`, `pnpm run test:phase15`, `pnpm run test:contracts`, and `cargo check --workspace --locked`. If the optional native proof is available on the host, run `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1 cargo test -p audio_output_desktop native_audio -- --nocapture` and record the result in `15-07-SUMMARY.md`; if the host lacks reliable audio hardware or is not Windows/macOS, record it as not run without marking the required gate failed. Do not modify unrelated phase guards, generated files, or renderer code except to fix Phase 15 ownership violations exposed by the gates.</action>
  <verify>
    <automated>pnpm run test:phase15-rust</automated>
    <automated>pnpm run test:phase15-source-guards</automated>
    <automated>pnpm run test:phase15-workspace</automated>
    <automated>pnpm run test:phase15</automated>
    <automated>pnpm run test:contracts</automated>
    <automated>cargo check --workspace --locked</automated>
  </verify>
  <acceptance_criteria>
    - All required automated commands pass.
    - Optional native proof is either passed under `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1` or explicitly recorded as not run because the environment is unsuitable.
    - No production renderer file contains forbidden audio graph, DSP, native backend, FFmpeg, artifact-store, cache, fingerprint, dirty-range, or raw session/device copy.
  </acceptance_criteria>
  <done>Phase 15 is ready for verification with Rust, UI, source guard, contract, and cargo-check gates green.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 15 | Independent low-latency audio graph and DSP timeline synchronized to `TimelineClock` and `PlaybackGeneration` | 15-01, 15-02, 15-07 | COVERED | Contracts and runtime session gates implement the goal.
REQ | AUDIO2-01 | Dedicated audio graph with shared clock/generation, seek/pause/cancel/buffering, independent from FFmpeg preview frames | 15-02, 15-04, 15-07 | COVERED | D-01, D-02, D-03 cited in actions and guards.
REQ | AUDIO2-02 | Typed gain, mute, pan, fades, keyframed volume, effect slots with integer/rational mapping | 15-01, 15-02, 15-07 | COVERED | D-04, D-05, D-06 cited in actions and guards.
REQ | AUDIO2-03 | WASAPI/CoreAudio output boundary plus waveform/peak display from artifact store only | 15-03, 15-04, 15-05, 15-07 | COVERED | D-10 through D-15 cited in actions and guards.
REQ | AUDIO2-04 | Export audio mixdown parity-tested against preview audio graph with classified differences | 15-06, 15-07 | COVERED | D-07, D-08, D-09 cited in actions and guards.
RESEARCH | Package Legitimacy Audit | `cpal` and `rubato` are `[ASSUMED]`; do not silently add them | 15-03 | COVERED | `cpal` has blocking checkpoint; `rubato` is not added.
RESEARCH | Native output CI | Native audio proof must be `VIDEO_EDITOR_TEST_NATIVE_AUDIO=1` gated | 15-03, 15-07 | COVERED | Required tests use mock output; native proof optional and recorded.
RESEARCH | Scheduler boundary | Carry generation/target-time fields without Phase 16 priority queues/backpressure | 15-02, 15-07 | COVERED | Audio session contracts include fields, no scheduler policy.
RESEARCH | Effects/retiming boundary | Include effect slots/classification without Phase 18 advanced audio effects or retiming curves | 15-01, 15-02, 15-06 | COVERED | Effect slots classify support only.
RESEARCH | Audio preview independence | FFmpeg remains export/fallback through Rust-owned compiler/runtime layers | 15-02, 15-06, 15-07 | COVERED | Source guard blocks renderer FFmpeg and session does not depend on preview frames.
CONTEXT | D-01 | Rust-owned audio preview; renderer command-only | 15-02, 15-04, 15-05, 15-07 | COVERED | Cited in actions.
CONTEXT | D-02 | Reuse `TimelineClock` and `PlaybackGeneration` | 15-02, 15-04, 15-07 | COVERED | Cited in actions.
CONTEXT | D-03 | Audio buffering independent from FFmpeg preview frames | 15-02, 15-04, 15-07 | COVERED | Cited in actions.
CONTEXT | D-04 | Typed integer/rational DSP semantics | 15-01, 15-02, 15-07 | COVERED | Cited in actions.
CONTEXT | D-05 | Audio effects extension points only | 15-01, 15-02, 15-06, 15-07 | COVERED | Cited in actions.
CONTEXT | D-06 | Commands source accepted audio edits | 15-01, 15-02, 15-07 | COVERED | Cited in actions.
CONTEXT | D-07 | Preview/export share DSP evaluation and classify differences | 15-06, 15-07 | COVERED | Cited in actions.
CONTEXT | D-08 | FFmpeg compiler consumes Rust-owned audio mix intent | 15-06, 15-07 | COVERED | Cited in actions.
CONTEXT | D-09 | Deterministic fixture/sample-summary parity first | 15-06, 15-07 | COVERED | Cited in actions.
CONTEXT | D-10 | CoreAudio/WASAPI behind Rust traits/capabilities | 15-03, 15-07 | COVERED | Cited in actions.
CONTEXT | D-11 | Native handles/buffers do not cross TypeScript | 15-03, 15-04, 15-07 | COVERED | Cited in actions.
CONTEXT | D-12 | Waveform/peak data are derived display artifacts | 15-04, 15-05, 15-07 | COVERED | Cited in actions.
CONTEXT | D-13 | UI consumes safe artifact summaries/display peaks only | 15-04, 15-05, 15-07 | COVERED | Cited in actions.
CONTEXT | D-14 | Avoid debug-heavy audio UI | 15-05, 15-07 | COVERED | Cited in actions.
CONTEXT | D-15 | Jianying-style feature-reduced audio UI | 15-05, 15-07 | COVERED | Cited in actions.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer source -> Rust-owned audio architecture | Source code can accidentally reintroduce renderer audio graph, device, artifact, or FFmpeg ownership. |
| Phase aggregate scripts -> CI/local verification | Public test scripts define what completion means for execution and verification. |
| Optional native proof -> developer machine | Native audio tests may touch local OS audio devices only when explicitly env-enabled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Tampering | renderer source | mitigate | Source guards reject renderer audio graph/DSP/mix/device/cache/artifact/timeline/FFmpeg ownership patterns with injected negative tests. |
| T-15-07-02 | Information Disclosure | production UI copy | mitigate | Guards reject native backend names, raw session/device IDs, artifact internals, fingerprints, dirty ranges, and raw logs in renderer production files. |
| T-15-07-03 | Repudiation | phase completion scripts | mitigate | Aggregate script composes focused Rust, source guard, workspace, contract drift, and cargo check gates. |
| T-15-07-04 | Denial of Service | native audio proof | mitigate | Native proof is env-gated and not required in generic CI; mock output tests remain required. |
| T-15-07-SC | Tampering | package installs | mitigate | Guard and package scripts preserve Plan 15-03 package checkpoint boundaries; no unapproved `rubato` install is accepted. |
</threat_model>

<verification>
<automated>pnpm run test:phase15-rust</automated>
<automated>pnpm run test:phase15-source-guards</automated>
<automated>pnpm run test:phase15-workspace</automated>
<automated>pnpm run test:phase15</automated>
<automated>pnpm run test:contracts</automated>
<automated>cargo check --workspace --locked</automated>
</verification>

<success_criteria>
Phase 15 is complete when all requirement-specific Rust/UI/export gates pass, source guards enforce Rust ownership and renderer presentation-only boundaries, contracts are drift-clean, and optional native audio proof is either passed under the env gate or explicitly recorded as not run.
</success_criteria>

<output>
Create `.planning/phases/15-audio-engine-and-dsp-timeline-pipeline/15-07-SUMMARY.md` when done.
</output>
