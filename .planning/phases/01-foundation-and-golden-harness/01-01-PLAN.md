---
phase: 01-foundation-and-golden-harness
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - rust-toolchain.toml
  - package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - .nvmrc
  - .gitignore
  - justfile
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "Developer can install dependencies and build through unified root entrypoints per D-01."
    - "Rust, Node, pnpm, Electron, Playwright, and napi-rs versions are pinned or declared per D-04."
    - "The root repository advertises the Phase 1 workspace shape without implementing product semantics."
  artifacts:
    - path: "Cargo.toml"
      provides: "Virtual Rust workspace root for planned crates per D-03."
    - path: "package.json"
      provides: "Corepack-pinned pnpm workspace root per D-02 and D-04."
    - path: "pnpm-workspace.yaml"
      provides: "Node workspace package discovery for apps and packages per D-02."
    - path: "rust-toolchain.toml"
      provides: "Pinned Rust 1.95.0 toolchain per resolved research question."
    - path: "justfile"
      provides: "Unified dev/build/test entrypoints per D-01."
  key_links:
    - from: "justfile"
      to: "package.json"
      via: "root pnpm scripts"
      pattern: "pnpm"
    - from: "Cargo.toml"
      to: "crates/*"
      via: "workspace members"
      pattern: "members"
---

<objective>
Create the root toolchain and workspace command foundation for Phase 1.

Purpose: Later plans need one stable repo-level build surface before adding crate shells, Electron packages, bindings, runtime discovery, and CI gates.
Output: Pinned Rust/Node/pnpm workspace files, root `just` entrypoints, lockfiles, and ignore rules for generated media/build outputs.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-golden-harness/01-CONTEXT.md
@.planning/phases/01-foundation-and-golden-harness/01-RESEARCH.md
@.planning/phases/01-foundation-and-golden-harness/01-VALIDATION.md
@.planning/phases/01-foundation-and-golden-harness/01-PATTERNS.md
</context>

## Artifacts this phase produces

This plan creates the root toolchain subset of the Phase 1 artifacts: `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`, `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `.nvmrc`, `.gitignore`, and `justfile`. Later plans add crate packages, Electron packages, generated contracts, fixtures, render smoke, and CI without changing the root decision that `just dev`, `just build`, and `just test` are the public entrypoints.

<tasks>

<task type="auto">
  <name>Task 01-W0-01: Pin toolchains and workspace package managers</name>
  <files>Cargo.toml, Cargo.lock, rust-toolchain.toml, package.json, pnpm-workspace.yaml, pnpm-lock.yaml, .nvmrc, .gitignore</files>
  <action>Create the root workspace files per D-02, D-03, and D-04. Pin Rust with `rust-toolchain.toml` to Rust 1.95.0. Pin Node guidance with `.nvmrc` to Node 24.12.0, matching the resolved 01-RESEARCH.md open question. Set root `packageManager` to `pnpm@10.32.1` for Corepack. Configure `pnpm-workspace.yaml` to include `apps/*` and `packages/*` so the Electron app and any generated binding package can be discovered. Create a virtual Cargo workspace with the planned `crates/*` members listed explicitly or by workspace glob, but do not implement crate logic here. Add `.gitignore` entries for `target/`, `node_modules/`, build outputs, generated media under `fixtures/media-generated/`, and native addon outputs; do not ignore committed generated schema/TypeScript contract files because D-06 requires them to be committed.</action>
  <verify>
    <automated>corepack enable; pnpm install --frozen-lockfile</automated>
    <automated>cargo metadata --format-version 1 --locked</automated>
  </verify>
  <done>Root Rust and Node workspace metadata resolves with pinned toolchains and package manager settings.</done>
</task>

<task type="auto">
  <name>Task 01-W0-02: Add unified just entrypoints for the scaffold</name>
  <files>justfile, package.json</files>
  <action>Create `just dev`, `just build`, and `just test` per D-01. At this foundation point the recipes must delegate to `pnpm` and `cargo` and may call package/crate commands that later plans create, but must not introduce alternate developer-facing commands that bypass `just`. Include a short prerequisite comment noting `just` is required and can be installed before running the gate because research found it absent locally. Keep these recipes implementation-ready for later refinement in Plan 01-08; do not add FFmpeg download, FFmpeg bundling, app packaging, or product behavior here per D-12.</action>
  <verify>
    <automated>command -v just >/dev/null || cargo install just --locked</automated>
    <automated>just --list</automated>
  </verify>
  <done>`just --list` shows `dev`, `build`, and `test`, and each recipe delegates to `pnpm` and/or `cargo` rather than replacing the package managers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell -> package managers | Local setup commands execute dependency manager behavior from root files. |
| package manager -> install scripts | npm/cargo packages may execute build/install hooks during dependency resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `package.json` / `pnpm-lock.yaml` | mitigate | Pin `packageManager` with Corepack per D-02 and D-04; use lockfile installs in verification. |
| T-01-02 | Tampering | `Cargo.toml` / `Cargo.lock` | mitigate | Use `cargo metadata --locked` and workspace lockfile resolution. |
| T-01-03 | Repudiation | root command surface | mitigate | Route setup/build/test through auditable `just` recipes per D-01. |
| T-01-SC | Tampering | npm/cargo installs | mitigate | Use only packages approved in 01-RESEARCH.md Package Legitimacy Audit; do not add unaudited dependencies. |
</threat_model>

<verification>
Run `corepack enable`, `pnpm install --frozen-lockfile`, `cargo metadata --format-version 1 --locked`, and `just --list`.
</verification>

<success_criteria>
- FOUND-01 root tooling foundation exists.
- D-01 through D-04 root-level decisions are represented in files and verification commands.
- No product behavior, FFmpeg bundling, or deferred mobile/server work is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-golden-harness/01-01-SUMMARY.md` when done.
</output>
