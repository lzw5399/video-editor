---
phase: 01-foundation-and-golden-harness
plan: 09
subsystem: tooling-ci
tags: [just, ci, github-actions, rust, electron, ffmpeg, pnpm]

requires:
  - phase: 01-01
    provides: Root Rust/pnpm workspace and initial `just` entrypoints
  - phase: 01-05
    provides: FFmpeg/ffprobe discovery and runtime probe gates
  - phase: 01-07
    provides: Required render smoke harness
  - phase: 01-08
    provides: Electron shell, native binding build, and Playwright smoke
provides:
  - Final explicit `just build` gate for Rust check/build, native binding build, and Electron build
  - Final explicit `just test` gate for Rust fmt/check/tests, schema/runtime/binding/Electron/render smoke, and generated contract drift
  - GitHub Actions CI parity running the same local build and test gates
  - Test-only CI FFmpeg/ffprobe installation without app bundling or distribution behavior
affects: [phase-1-foundation, tooling, ci, build-gates, render-smoke]

tech-stack:
  added: [github-actions-ci, xvfb-ci-display]
  patterns:
    - `just build` and `just test` are the single local and CI command path for Phase 1 gates
    - CI installs FFmpeg/ffprobe as runner test tools only and does not package release artifacts
    - Generated Rust-owned schema and TypeScript contracts are regenerated by tests and checked with `git diff`

key-files:
  created:
    - .github/workflows/ci.yml
  modified:
    - justfile
    - package.json
    - apps/desktop-electron/package.json

key-decisions:
  - "Made `just build` and `just test` list the concrete Phase 1 gate commands directly instead of relying only on recursive package scripts."
  - "Kept the desktop package responsible for native binding and Electron bundle sequencing while root scripts expose named gate aliases."
  - "Installed FFmpeg/ffprobe in CI strictly as test runner tools, with no release artifact, packaging, download-to-repo, bundling, or redistribution behavior."
  - "Started Xvfb in CI so the Playwright Electron smoke can run on Ubuntu while still invoking the unchanged `just test` command."

patterns-established:
  - "Root `package.json` provides named gate aliases (`test:schema`, `test:runtime`, `test:bindings`, `test:desktop`, `test:render-smoke`, `test:contracts`) that mirror the final `just test` recipe."
  - "CI parity workflows should call the same top-level `just` commands used locally and keep platform setup in preparatory steps."

requirements-completed: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, TEST-01]

duration: 5 min
completed: 2026-06-17
---

# Phase 1 Plan 09: Final Build, Test, and CI Gates Summary

**Unified local and CI gates now build the Rust workspace, native binding, Electron shell, generated contracts, FFmpeg discovery, and render smoke through `just build` and `just test`.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-16T22:55:04Z
- **Completed:** 2026-06-16T22:59:20Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Finalized `just build` so it performs frozen pnpm install, Rust workspace check/build, native binding build, and Electron shell build.
- Finalized `just test` so it performs Rust fmt/check/tests, schema/model generation, FFmpeg discovery, Node binding tests, Electron Playwright smoke, render smoke, and generated-contract drift checks.
- Added a GitHub Actions workflow that installs Rust 1.95.0, Node 24.12.0, pnpm 10.32.1, `just`, FFmpeg/ffprobe test tools, and runs the same local gates.
- Preserved D-12 by using CI FFmpeg only as a test dependency and adding no application distribution, packaging, artifact upload, or FFmpeg bundling behavior.

## Task Commits

1. **Task 01-W5-01: Finalize local just build and test gates** - `9132d6e` (chore)
2. **Task 01-W5-02: Add CI parity workflow** - `af7d6ac` (ci)

## Files Created/Modified

- `justfile` - Lists the concrete final `build` and `test` gate commands.
- `package.json` - Adds named root build/test gate aliases consumed by local scripts and the `just` recipes.
- `apps/desktop-electron/package.json` - Splits Electron bundling into `build:electron` while keeping `build` responsible for native binding plus Electron build.
- `.github/workflows/ci.yml` - Runs Phase 1 CI parity with pinned toolchains, test-only FFmpeg/ffprobe installation, and the same `just build` / `just test` commands.

## Verification

- `PATH="$HOME/.cargo/bin:$PATH" just build` - PASS.
- `PATH="$HOME/.cargo/bin:$PATH" just test` - PASS.
- `git diff --exit-code schemas apps/desktop-electron/src/generated` - PASS.
- `grep -n "just build\\|just test\\|24.12.0\\|ffmpeg" .github/workflows/ci.yml` - PASS.
- `grep -n "electron-builder\\|release\\|artifact\\|bundle.*ffmpeg" .github/workflows/ci.yml && exit 1 || true` - PASS.

## Decisions Made

- Kept the source of truth for Phase 1 gates in `justfile`, with root package scripts as named reusable aliases rather than broad recursive commands.
- Used `pnpm --filter @video-editor/desktop build` for the desktop build gate so native addon generation remains part of the desktop package's existing build contract.
- Installed FFmpeg with the runner package manager in CI only for tests; no binary is committed, downloaded into the repo, packaged, or redistributed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added Xvfb display setup for CI Electron smoke**
- **Found during:** Task 01-W5-02 (Add CI parity workflow)
- **Issue:** The plan required CI to run the Electron smoke through `just test`; Ubuntu CI runners need an available display for Electron/Playwright smoke execution.
- **Fix:** Installed `xvfb`, set `DISPLAY=:99`, and started Xvfb before running `just test`.
- **Files modified:** `.github/workflows/ci.yml`
- **Verification:** CI grep gates passed and the workflow still runs the unchanged `just test` command.
- **Committed in:** `af7d6ac`

---

**Total deviations:** 1 auto-fixed (Rule 2: 1)
**Impact on plan:** The change is CI test infrastructure only. It preserves local/CI command parity and does not add release packaging, FFmpeg bundling, or application distribution behavior.

## Issues Encountered

None.

## Known Stubs

None. Stub scan over touched files found no TODO/FIXME/placeholder markers or UI-facing empty/mock data stubs.

## Threat Flags

None. The CI shell, FFmpeg/ffprobe runner dependency, and generated-contract drift surfaces are covered by the plan threat model and mitigated by frozen installs, explicit local gate commands, runner-only media tool installation, and no release artifacts.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 1 is complete and ready for Phase 2 planning/execution. The build/test foundation now provides a single gate path for later draft, material, timeline, preview, and export work.

## Self-Check: PASSED

- Key files exist on disk: `justfile`, `package.json`, `apps/desktop-electron/package.json`, and `.github/workflows/ci.yml`.
- Task commits `9132d6e` and `af7d6ac` exist in git history.
- Plan verification commands passed.
- Stub scan found no blocking stubs.

---
*Phase: 01-foundation-and-golden-harness*
*Completed: 2026-06-17*
