---
phase: 01-foundation-and-golden-harness
plan: 06
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - crates/draft_model/Cargo.toml
  - crates/draft_model/tests/schema_exports.rs
  - schemas/command.schema.json
  - apps/desktop-electron/src/generated/CommandEnvelope.ts
  - apps/desktop-electron/src/generated/CommandResultEnvelope.ts
  - fixtures/draft/minimal-command.json
  - fixtures/draft/invalid-unknown-field.json
autonomous: true
requirements:
  - TEST-01
  - FOUND-04
  - FOUND-02
must_haves:
  truths:
    - "Generated JSON Schema and TypeScript contracts are committed and regenerated from Rust per D-06."
    - "Positive command fixtures validate through Rust model and schema tests per TEST-01."
    - "Unknown-field fixtures fail both Rust deserialization and schema validation per D-07."
  artifacts:
    - path: "schemas/command.schema.json"
      provides: "Generated JSON Schema from Rust command contracts."
    - path: "apps/desktop-electron/src/generated/CommandEnvelope.ts"
      provides: "Generated TypeScript command envelope type."
    - path: "apps/desktop-electron/src/generated/CommandResultEnvelope.ts"
      provides: "Generated TypeScript command result envelope type."
    - path: "fixtures/draft/minimal-command.json"
      provides: "Positive Phase 1 command fixture."
    - path: "fixtures/draft/invalid-unknown-field.json"
      provides: "Negative unknown-field fixture."
  key_links:
    - from: "crates/draft_model/tests/schema_exports.rs"
      to: "schemas/command.schema.json"
      via: "schemars export drift check"
      pattern: "schema_for!|command.schema.json"
    - from: "crates/draft_model/tests/schema_exports.rs"
      to: "apps/desktop-electron/src/generated"
      via: "ts-rs export drift check"
      pattern: "export_to|CommandEnvelope.ts"
---

<objective>
Generate and commit Rust-owned command schema/type contracts with fixture validation.

Purpose: Electron and later phases must consume contract files generated from Rust, not hand-written TypeScript or schema copies.
Output: Committed generated JSON Schema, generated TypeScript types, and positive/negative command fixtures with drift tests.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-golden-harness/01-CONTEXT.md
@.planning/phases/01-foundation-and-golden-harness/01-RESEARCH.md
@.planning/phases/01-foundation-and-golden-harness/01-VALIDATION.md
@.planning/phases/01-foundation-and-golden-harness/01-PATTERNS.md
@.planning/phases/01-foundation-and-golden-harness/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates the generated-contract and schema-fixture subset of Phase 1 artifacts: `schemas/command.schema.json`, `apps/desktop-electron/src/generated/CommandEnvelope.ts`, `apps/desktop-electron/src/generated/CommandResultEnvelope.ts`, `fixtures/draft/minimal-command.json`, `fixtures/draft/invalid-unknown-field.json`, and schema/type drift tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-W2-03: Export Rust contracts to schema and TypeScript</name>
  <files>crates/draft_model/Cargo.toml, crates/draft_model/tests/schema_exports.rs, schemas/command.schema.json, apps/desktop-electron/src/generated/CommandEnvelope.ts, apps/desktop-electron/src/generated/CommandResultEnvelope.ts</files>
  <behavior>
    - Running the schema export test regenerates JSON Schema from Rust contract types.
    - Running the schema export test regenerates TypeScript declarations from Rust contract types.
    - `git diff --exit-code schemas apps/desktop-electron/src/generated` fails if committed generated files drift.
  </behavior>
  <action>Add schema/type export tests per D-06 and the resolved 01-RESEARCH.md decision to commit generated schema/TS files. Use `schemars` for `schemas/command.schema.json` and `ts-rs` for `CommandEnvelope.ts` and `CommandResultEnvelope.ts`. If `jsonschema` is needed for validation tests, add the approved crate version from 01-RESEARCH.md to `draft_model` dev-dependencies. Generated TypeScript must use the same Rust-owned Jianying-aligned names; do not hand-write or manually patch generated files. The test must create parent directories as needed and compare/regenerate deterministic output.</action>
  <verify>
    <automated>cargo test -p draft_model schema -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <done>Generated schema and TypeScript contract files are committed and reproducible from Rust tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-W2-04: Validate positive and negative command fixtures</name>
  <files>crates/draft_model/tests/schema_exports.rs, fixtures/draft/minimal-command.json, fixtures/draft/invalid-unknown-field.json</files>
  <behavior>
    - `minimal-command.json` validates through Rust serde and generated JSON Schema.
    - `invalid-unknown-field.json` fails because of unknown command fields.
    - Every JSON file under `fixtures/draft` is included in a positive or negative validation path.
  </behavior>
  <action>Add Phase 1 command fixtures per D-07, D-13, and TEST-01. Create `fixtures/draft/minimal-command.json` as the canonical positive command fixture and `fixtures/draft/invalid-unknown-field.json` as the negative unknown-field fixture. Extend the schema/model tests so every JSON fixture under `fixtures/draft` is explicitly classified and validated. Do not add full `.veproj` draft schema, material fixtures, full draft goldens, or real editing commands; those are assigned to later phases.</action>
  <verify>
    <automated>cargo test -p draft_model schema -- --nocapture</automated>
  </verify>
  <done>Schema/model tests validate all Phase 1 command fixtures and prove unknown fields fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON -> Rust/schema validation | Test fixtures simulate user-controlled command payloads. |
| Rust contract types -> generated TypeScript | Generated UI-facing types cross from Rust source of truth into desktop code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | generated schema/types | mitigate | Generate from Rust with drift checks and `git diff --exit-code`. |
| T-01-02 | Tampering | `fixtures/draft/invalid-unknown-field.json` | mitigate | Assert unknown-field rejection through serde and JSON Schema validation. |
| T-01-03 | Repudiation | fixture coverage | mitigate | Tests must classify every JSON file under `fixtures/draft` as positive or negative. |
| T-01-SC | Tampering | cargo dependency additions | mitigate | Use only approved `schemars`, `ts-rs`, and `jsonschema` crates from 01-RESEARCH.md. |
</threat_model>

<verification>
Run `cargo test -p draft_model schema -- --nocapture` and `git diff --exit-code schemas apps/desktop-electron/src/generated`.
</verification>

<success_criteria>
- TEST-01 has executable schema/model fixture validation.
- FOUND-02 has generated TypeScript contracts available for Electron code.
- FOUND-04 has deterministic command fixture structure.
- D-06, D-07, and D-13 are implemented without hand-written TypeScript/schema drift.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-and-golden-harness/01-06-SUMMARY.md` when done.
</output>
