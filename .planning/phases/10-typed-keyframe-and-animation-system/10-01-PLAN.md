---
phase: "10-typed-keyframe-and-animation-system"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "crates/draft_model/src/timeline.rs"
  - "crates/draft_model/src/validation.rs"
  - "crates/draft_model/src/lib.rs"
  - "crates/draft_model/tests/draft_schema.rs"
  - "crates/draft_model/tests/schema_exports.rs"
  - "schemas/draft.schema.json"
  - "schemas/command.schema.json"
  - "apps/desktop-electron/src/generated/Draft.ts"
  - "apps/desktop-electron/src/generated/CommandEnvelope.ts"
autonomous: true
requirements:
  - ANIM-01
  - ANIM-02
user_setup: []
must_haves:
  truths:
    - "Segment keyframes are typed Jianying-style semantic data, not arbitrary property/value strings."
    - "Keyframe time is persisted as integer microseconds relative to the segment head."
    - "Static Phase 08/09 visual/text/audio values remain the base values that keyframes can override."
  artifacts:
    - path: "crates/draft_model/src/timeline.rs"
      provides: "typed keyframe model"
    - path: "crates/draft_model/src/validation.rs"
      provides: "keyframe property/value/time validation"
---

<objective>
Replace the placeholder string keyframe schema with typed keyframe properties, values, interpolation, easing, validation, and generated contracts.
</objective>

<context>
@AGENTS.md
@.planning/phases/10-typed-keyframe-and-animation-system/10-CONTEXT.md
@.planning/phases/10-typed-keyframe-and-animation-system/10-RESEARCH.md
@.planning/phases/08-segment-transform-and-visual-compositing/08-CONTEXT.md
@.planning/phases/09-complete-text-and-subtitle-system/09-CONTEXT.md
@crates/draft_model/src/timeline.rs
@crates/draft_model/src/validation.rs
@crates/draft_model/tests/schema_exports.rs
</context>

## Artifacts this plan produces

- `Keyframe`
- `KeyframeProperty`
- `KeyframeValue`
- `KeyframeInterpolation`
- `KeyframeEasing`
- schema and TypeScript exports for typed keyframes

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-01-01: Add typed keyframe schema and validation</name>
  <files>crates/draft_model/src/timeline.rs, crates/draft_model/src/validation.rs, crates/draft_model/src/lib.rs, crates/draft_model/tests/draft_schema.rs</files>
  <read_first>
    - `crates/draft_model/src/timeline.rs`
    - `crates/draft_model/src/validation.rs`
    - `crates/draft_model/tests/draft_schema.rs`
    - `.planning/phases/10-typed-keyframe-and-animation-system/10-RESEARCH.md`
  </read_first>
  <action>Replace `Keyframe { at, property: String, value: String }` with typed fields: `at`, `property`, `value`, `interpolation`, and `easing`. Add property/value enums that cover Phase 10 supported fields: visual position X/Y, scale X/Y, rotation, opacity, text font size/color/line height/letter spacing/layout X/Y/width/height, audio volume, and explicit unsupported/deferred boundaries for sticker/filter parameters where needed. Validate keyframe time against the owning segment target duration, value variant against property, duplicate property/time pairs, and the same numeric/color ranges already enforced for visual/text/volume fields.</action>
  <acceptance_criteria>
    Draft tests prove typed keyframes serialize/deserialize, invalid property/value combinations fail validation, out-of-range keyframe times fail validation, and existing segments with empty keyframe arrays still load.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model keyframe -- --nocapture</automated>
    <automated>cargo test -p draft_model schema -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-01-02: Export keyframe schemas and generated TypeScript contracts</name>
  <files>crates/draft_model/tests/schema_exports.rs, schemas/draft.schema.json, schemas/command.schema.json, apps/desktop-electron/src/generated/Draft.ts, apps/desktop-electron/src/generated/CommandEnvelope.ts</files>
  <read_first>
    - `crates/draft_model/tests/schema_exports.rs`
  </read_first>
  <action>Export the new keyframe types through schema/TypeScript generation. Add JSON Schema constraints for keyframe value ranges where schemars cannot infer enough detail. Keep `Microseconds` as integer-compatible generated number type and avoid any `f32`/`f64` persisted time surface.</action>
  <acceptance_criteria>
    Generated contracts contain `KeyframeProperty`, `KeyframeValue`, `KeyframeInterpolation`, and `KeyframeEasing`; generated schema rejects invalid keyframe examples; contract drift is clean after generation.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p draft_model keyframe -- --nocapture</automated>
<automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<source_audit>
REQ | ANIM-01 | typed animated value storage | 10-01 | COVERED
REQ | ANIM-02 | keyframe time/value/interpolation/easing | 10-01 | COVERED
</source_audit>

<output>
Create `.planning/phases/10-typed-keyframe-and-animation-system/10-01-SUMMARY.md` when done.
</output>
