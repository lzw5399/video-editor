---
phase: "10-typed-keyframe-and-animation-system"
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - "crates/draft_model/src/lib.rs"
  - "crates/draft_commands/src/keyframe.rs"
  - "crates/draft_commands/src/lib.rs"
  - "crates/draft_commands/tests/keyframe_commands.rs"
  - "crates/bindings_node/src/lib.rs"
  - "crates/bindings_node/tests/keyframe_commands.rs"
  - "crates/draft_model/tests/schema_exports.rs"
  - "schemas/command.schema.json"
  - "apps/desktop-electron/src/generated/CommandEnvelope.ts"
autonomous: true
requirements:
  - ANIM-01
  - ANIM-02
user_setup: []
must_haves:
  truths:
    - "All keyframe mutations route through Rust commands with validation and undo/redo snapshots."
    - "Renderer and bindings use generated command contracts only."
  artifacts:
    - path: "crates/draft_commands/src/keyframe.rs"
      provides: "Rust-owned keyframe command semantics"
    - path: "crates/bindings_node/tests/keyframe_commands.rs"
      provides: "Node binding route coverage"
---

<objective>
Add Rust-owned keyframe mutation commands and expose them through generated contracts and the Node binding.
</objective>

<context>
@AGENTS.md
@.planning/phases/10-typed-keyframe-and-animation-system/10-CONTEXT.md
@.planning/phases/10-typed-keyframe-and-animation-system/10-RESEARCH.md
@crates/draft_model/src/lib.rs
@crates/draft_commands/src/visual.rs
@crates/draft_commands/src/text.rs
@crates/draft_commands/src/audio.rs
@crates/draft_commands/src/history.rs
@crates/bindings_node/src/lib.rs
</context>

## Artifacts this plan produces

- `SetSegmentKeyframeCommandPayload`
- `RemoveSegmentKeyframeCommandPayload`
- `set_segment_keyframe`
- `remove_segment_keyframe`
- binding route coverage for both commands

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-02-01: Add keyframe command payloads and draft_commands module</name>
  <files>crates/draft_model/src/lib.rs, crates/draft_commands/src/keyframe.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/tests/keyframe_commands.rs</files>
  <read_first>
    - `crates/draft_model/src/lib.rs`
    - `crates/draft_commands/src/visual.rs`
    - `crates/draft_commands/src/audio.rs`
    - `crates/draft_commands/src/history.rs`
  </read_first>
  <action>Add `setSegmentKeyframe` and `removeSegmentKeyframe` command payloads with `draft`, `commandState`, `selection`, `segmentId`, and typed keyframe selectors. Implement a new `draft_commands::keyframe` module that locates the segment, validates track lock, adds/replaces/removes the typed keyframe atomically, sorts keyframes deterministically, validates the full draft, pushes an undo snapshot, preserves selection, and emits keyframe-specific events.</action>
  <acceptance_criteria>
    Command tests cover add, replace same property/time, remove, undo/redo, locked-track rejection, invalid property/value rejection, invalid time rejection, and no partial draft mutation on failure.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands keyframe -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-02-02: Route keyframe commands through generated contracts and bindings</name>
  <files>crates/draft_model/src/lib.rs, crates/draft_model/tests/schema_exports.rs, crates/bindings_node/src/lib.rs, crates/bindings_node/tests/keyframe_commands.rs, schemas/command.schema.json, apps/desktop-electron/src/generated/CommandEnvelope.ts</files>
  <read_first>
    - `crates/bindings_node/src/lib.rs`
    - `crates/bindings_node/tests/transform_commands.rs`
    - `crates/bindings_node/tests/text_commands.rs`
    - `crates/draft_model/tests/schema_exports.rs`
  </read_first>
  <action>Add command enum variants, payload matching, schema export inclusion, binding allow-list entries, and binding route tests for set/remove keyframe. Keep the binding as a thin dispatch layer into `draft_commands`; no preview/export/render graph logic belongs here.</action>
  <acceptance_criteria>
    Binding tests prove `executeCommand` accepts generated keyframe envelopes, returns Rust-shaped `TimelineCommandResponse`, and rejects malformed command/payload pairs through the existing envelope validation path.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bindings_node keyframe_commands -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p draft_commands keyframe -- --nocapture</automated>
<automated>cargo test -p bindings_node keyframe_commands -- --nocapture</automated>
<automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<source_audit>
REQ | ANIM-01 | command-owned keyframe mutations | 10-02 | COVERED
REQ | ANIM-02 | typed command payloads | 10-02 | COVERED
</source_audit>

<output>
Create `.planning/phases/10-typed-keyframe-and-animation-system/10-02-SUMMARY.md` when done.
</output>
