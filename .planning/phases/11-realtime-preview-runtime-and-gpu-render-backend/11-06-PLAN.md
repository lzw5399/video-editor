---
phase: "11-realtime-preview-runtime-and-gpu-render-backend"
plan: "06"
type: execute
wave: 6
depends_on:
  - "11-05"
files_modified:
  - "crates/realtime_preview_runtime/Cargo.toml"
  - "crates/realtime_preview_runtime/src/gpu/text.rs"
  - "crates/realtime_preview_runtime/src/capabilities.rs"
  - "crates/realtime_preview_runtime/tests/text_parity.rs"
  - "crates/testkit/Cargo.toml"
  - "crates/testkit/tests/realtime_preview_parity.rs"
autonomous: true
requirements:
  - RTPREV-02
  - RTPREV-03
  - RTPREV-04
user_setup: []
must_haves:
  truths:
    - "Text overlay preview has a tested Phase 11 outcome: GPU text path when parity passes or explicit degraded/unsupported fallback diagnostics when parity does not pass."
    - "Text GPU rendering never silently approximates export output when parity fails."
    - "Realtime preview/export parity diagnostics are covered by golden tests for supported and known-divergent drafts."
    - "Known divergent text/effect drafts remain classified as degraded or unsupported and route through fallback artifact/texture behavior from 11-05."
  artifacts:
    - path: "crates/realtime_preview_runtime/src/gpu/text.rs"
      provides: "text render path or explicit text fallback diagnostic boundary"
    - path: "crates/realtime_preview_runtime/tests/text_parity.rs"
      provides: "text GPU parity or degraded/unsupported fallback tests"
    - path: "crates/testkit/tests/realtime_preview_parity.rs"
      provides: "preview/export parity golden coverage"
  key_links:
    - from: "crates/realtime_preview_runtime/src/gpu/text.rs"
      to: "crates/realtime_preview_runtime/src/capabilities.rs"
      via: "text parity result updates supported/degraded/unsupported classification"
      pattern: "TextParityUnsupported"
    - from: "crates/testkit/tests/realtime_preview_parity.rs"
      to: "crates/realtime_preview_runtime/src/parity.rs"
      via: "golden parity diagnostics"
      pattern: "RealtimePreviewParityDiagnostic"
---

<objective>
Close the Phase 11 text preview question with parity-gated GPU text or explicit degraded/unsupported fallback diagnostics.

Purpose: satisfy RTPREV-02 and RTPREV-04 without allowing silent GPU/export drift for text overlays.
Output: text preview boundary, text parity tests, and testkit preview/export parity golden coverage.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/notes/production-editor-architecture-decisions.md
@.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-CONTEXT.md
@.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-RESEARCH.md
@.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-DESIGN.md
@.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-05-SUMMARY.md
@crates/realtime_preview_runtime/src/capabilities.rs
@crates/realtime_preview_runtime/src/gpu/compositor.rs
@crates/render_graph/src/graph.rs
</context>

## Artifacts this plan produces

- `gpu/text.rs`
- `text_parity` tests
- `realtime_preview_parity` testkit golden tests
- `TextParityUnsupported`
- supported text subset diagnostics
- degraded/unsupported text fallback diagnostics

<tasks>

<task type="auto" tdd="true">
  <name>Task 11-06-01: Add text preview outcome and parity golden tests</name>
  <files>crates/realtime_preview_runtime/Cargo.toml, crates/realtime_preview_runtime/src/gpu/text.rs, crates/realtime_preview_runtime/src/capabilities.rs, crates/realtime_preview_runtime/tests/text_parity.rs, crates/testkit/Cargo.toml, crates/testkit/tests/realtime_preview_parity.rs</files>
  <read_first>
    - `crates/realtime_preview_runtime/src/capabilities.rs`
    - `crates/realtime_preview_runtime/src/gpu/compositor.rs`
    - `crates/render_graph/src/graph.rs`
    - `.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-RESEARCH.md`
    - `.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-DESIGN.md`
    - `.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-05-SUMMARY.md`
  </read_first>
  <action>Add the Phase 11 text preview boundary. If `glyphon` parity is proven by repository fonts and tests, implement GPU text overlay rendering in `gpu/text.rs`; otherwise classify text as degraded or unsupported with `TextParityUnsupported` and route through the fallback artifact/texture path from 11-05. In both outcomes, text must have explicit diagnostics, and silent approximate GPU text rendering is forbidden. Add `testkit` golden parity tests comparing realtime preview capability/parity diagnostics against export render graph intent for supported canvas/image/opacity/transform/keyframe drafts and known divergent text/effect drafts.</action>
  <acceptance_criteria>
    Text overlay tests either prove GPU text parity for the supported text subset or prove explicit degraded/unsupported fallback diagnostics. Testkit parity tests cover at least one supported graph and one known divergent graph, and they fail if parity diagnostics disappear or silently mark unsupported intent as supported.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p realtime_preview_runtime text_parity -- --nocapture</automated>
    <automated>cargo test -p testkit realtime_preview_parity -- --nocapture</automated>
  </verify>
  <done>Task complete when text preview behavior is deterministic and parity diagnostics are enforced by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text/render graph -> realtime text path | Text semantics cross into GPU text rendering or explicit fallback diagnostics. |
| realtime text path -> fallback ladder | Text that cannot meet parity routes through the fallback artifact/texture behavior from 11-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-22 | Tampering | text preview parity | mitigate | Test GPU text parity or require explicit `TextParityUnsupported` degraded/unsupported fallback diagnostics. |
| T-11-23 | Repudiation | preview/export divergence | mitigate | Golden parity tests record supported and divergent text/effect outcomes. |
| T-11-SC | Tampering | package installs | mitigate | `glyphon` may be used only because it is approved in `11-RESEARCH.md`; no other package install proceeds without audit. |
</threat_model>

<verification>
<automated>cargo test -p realtime_preview_runtime text_parity -- --nocapture</automated>
<automated>cargo test -p testkit realtime_preview_parity -- --nocapture</automated>
</verification>

<source_audit>
GOAL | Phase 11 | supported preview subset includes an explicit text outcome without silent drift | 11-06 | COVERED
REQ | RTPREV-02 | text overlays are GPU-rendered only when parity passes, otherwise degraded/unsupported with fallback diagnostics | 11-06 | COVERED
REQ | RTPREV-03 | text fallback uses the Rust-owned fallback ladder rather than per-frame renderer FFmpeg decisions | 11-06 | COVERED
REQ | RTPREV-04 | realtime preview and export share semantics and produce parity diagnostics | 11-06 | COVERED
CONTEXT | CTX-RendererUIOnly | renderer does not own text GPU parity or fallback classification | 11-06 | COVERED
RESEARCH | Resolved Text Question | text GPU is parity-gated; fallback artifact/texture path is required when parity fails | 11-06 | COVERED
</source_audit>

<success_criteria>
Text overlay preview is either parity-proven for the supported GPU subset or explicitly classified as degraded/unsupported and routed through fallback artifact/texture behavior, with golden parity tests preventing silent preview/export drift.
</success_criteria>

<output>
Create `.planning/phases/11-realtime-preview-runtime-and-gpu-render-backend/11-06-SUMMARY.md` when done.
</output>
