---
phase: "07-project-canvas-space-and-coordinate-system"
plan: "05"
type: execute
wave: 4
depends_on:
  - "07-04"
files_modified:
  - "crates/preview_service/src/service.rs"
  - "crates/preview_service/tests/canvas_profile.rs"
  - "crates/bindings_node/src/preview_export_service.rs"
  - "crates/bindings_node/tests/export_commands.rs"
  - "crates/testkit/tests/preview_export_parity.rs"
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
user_setup: []
must_haves:
  truths:
    - "D-07: production preview/export semantics derive canvas size and frame rate from `Draft.canvasConfig`, not `EngineProfile::mvp_default()` or export preset dimensions."
    - "D-05: preview/export report background degraded/unsupported diagnostics rather than silently faking blur or image backgrounds."
    - "D-06: preview/export tests preserve the same normalized canvas coordinate contract through vertical/custom profiles."
  artifacts:
    - path: "crates/preview_service/src/service.rs"
      provides: "Draft-canvas-derived preview profile"
    - path: "crates/bindings_node/src/preview_export_service.rs"
      provides: "Draft-canvas-derived export dimensions and validation"
    - path: "crates/testkit/tests/preview_export_parity.rs"
      provides: "Preview/export parity coverage for custom canvas profiles"
  key_links:
    - from: "crates/preview_service/src/service.rs"
      to: "crates/engine_core/src/normalize.rs"
      via: "EngineProfile::from_draft_canvas"
      pattern: "from_draft_canvas"
    - from: "crates/bindings_node/src/preview_export_service.rs"
      to: "crates/ffmpeg_compiler/src/job.rs"
      via: "RenderOutputProfile validation expectations"
      pattern: "expected_width|expected_height|expected_frame_rate"
---

<objective>
Propagate draft canvas profile through preview and export services.

Purpose: after engine/render/compiler understand draft canvas semantics, production preview and export services must stop relying on hard-coded MVP profile defaults.
Output: draft-derived preview/export profile resolution and tests for vertical/custom canvas profiles.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-CONTEXT.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-RESEARCH.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-PATTERNS.md
@.planning/phases/05-preview-and-export-pipeline/05-04-SUMMARY.md
@.planning/phases/05-preview-and-export-pipeline/05-08-SUMMARY.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-04-SUMMARY.md
@crates/preview_service/src/service.rs
@crates/bindings_node/src/preview_export_service.rs
@crates/testkit/tests/preview_export_parity.rs
</context>

## Artifacts this phase produces

Preview/export behavior:
- preview frame/segment preparation derives profile from `Draft.canvasConfig`
- export validation expects draft canvas width/height/frameRate
- production preview/export no longer call `EngineProfile::mvp_default()`
- `crates/preview_service/tests/canvas_profile.rs`
- updated export and parity tests

<decision_map>
D-03: Canvas config includes width, height, rational frame rate, and background.
D-05: Background modes surface supported/degraded/unsupported diagnostics.
D-06: normalized coordinate semantics are the shared preview/export contract.
D-07: preview cache may scale for performance, but production preview/export semantics and validation derive from `Draft.canvasConfig`.
D-11: No new npm/Rust packages.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-05-01: Replace preview service hard-codes with draft-derived canvas profile</name>
  <files>crates/preview_service/src/service.rs, crates/preview_service/tests/canvas_profile.rs</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-04-SUMMARY.md`
    - `crates/preview_service/src/service.rs`
    - `crates/preview_service/tests/preview_generation.rs`
    - `crates/engine_core/src/normalize.rs`
    - `crates/render_graph/src/graph.rs`
  </read_first>
  <behavior>
    - Test 1: preview frame preparation calls `EngineProfile::from_draft_canvas` and preserves draft aspect ratio when deriving preview cache output.
    - Test 2: preview segment preparation uses draft frame rate and canvas dimensions through normalized state.
    - Test 3: background degraded/unsupported diagnostics from render graph remain visible in preview preparation results or errors.
  </behavior>
  <action>Replace production `normalize_draft(..., &EngineProfile::mvp_default())` calls in `preview_service` with `EngineProfile::from_draft_canvas(&draft)` per D-07. For preview cache output, derive any scaled preview dimensions from the draft canvas aspect ratio through a Rust-owned policy; do not keep fixed 960 x 540 as production semantic truth. Add preview tests for 1080 x 1920, square, and custom profile drafts proving dimensions/fps flow from `Draft.canvasConfig`. Preserve existing cache invalidation and runtime boundaries; do not let Electron or renderer mocks decide preview dimensions, frame rate, render graph, FFmpeg args, or cache semantics.</action>
  <acceptance_criteria>
    Preview frame and segment preparation derive engine profile from `Draft.canvasConfig`. `EngineProfile::mvp_default()` is absent from production preview service code. Tests prove vertical/custom profiles affect preview metadata through Rust-owned semantics.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p preview_service canvas -- --nocapture</automated>
    <automated>bash -lc '! rg -n "EngineProfile::mvp_default\\(" crates/preview_service/src'</automated>
  </verify>
  <done>Preview service uses draft canvas profile instead of hard-coded MVP defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-05-02: Replace export service preset dimensions with draft canvas validation</name>
  <files>crates/bindings_node/src/preview_export_service.rs, crates/bindings_node/tests/export_commands.rs, crates/testkit/tests/preview_export_parity.rs</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-04-SUMMARY.md`
    - `crates/bindings_node/src/preview_export_service.rs`
    - `crates/bindings_node/tests/export_commands.rs`
    - `crates/testkit/tests/preview_export_parity.rs`
    - `crates/ffmpeg_compiler/src/job.rs`
  </read_first>
  <behavior>
    - Test 1: export no longer uses `export_dimensions(payload.preset)` for canonical dimensions; validation expects draft canvas width/height/fps.
    - Test 2: export presets still select quality/codec behavior without changing the draft canvas profile.
    - Test 3: preview/export parity tests prove a vertical or custom draft changes output metadata through Rust semantics.
  </behavior>
  <action>Replace production `normalize_draft(..., &EngineProfile::mvp_default())` in `bindings_node::preview_export_service` with `EngineProfile::from_draft_canvas(&draft)` per D-07. Remove or repurpose `export_dimensions(payload.preset)` so presets select quality/codec only and output validation expects the draft canvas width, height, and `RationalFrameRate`. Update export command and parity tests to assert a 1080 x 1920 or custom canvas changes preview/export metadata through Rust semantics. Keep output path validation, runtime validation, FFmpeg execution, and error classification in their existing service/runtime tiers.</action>
  <acceptance_criteria>
    Export validation expects draft width, height, and fps. Preset choice no longer overrides canonical canvas dimensions. Production export service does not call `EngineProfile::mvp_default()` or `export_dimensions(`. Parity tests cover vertical/custom canvas profiles.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bindings_node export_commands -- --nocapture</automated>
    <automated>cargo test -p testkit preview_export_parity -- --nocapture</automated>
    <automated>bash -lc '! rg -n "EngineProfile::mvp_default\\(\\)|export_dimensions\\(" crates/bindings_node/src/preview_export_service.rs'</automated>
  </verify>
  <done>Export service validates output against draft canvas profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft canvas profile -> preview_service | Persisted canvas values become preview generation profile values. |
| export payload -> bindings export service | User output path and preset cross into export preparation. |
| compiler validation -> media runtime validation | Expected output metadata crosses into runtime verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Tampering | production preview/export profile | mitigate | Replace production MVP defaults with `EngineProfile::from_draft_canvas` and test vertical/custom profiles. |
| T-07-15 | Denial of Service | invalid or huge canvas dimensions | mitigate | Reuse `draft_model` validation before service profile resolution. |
| T-07-16 | Information Disclosure | preview/export diagnostics | mitigate | Surface bounded diagnostics and preserve existing path/error policies. |
| T-07-SC | Tampering | package installs | mitigate | No new package installs; reuse existing Rust services. |
</threat_model>

<verification>
<automated>cargo test -p preview_service canvas -- --nocapture</automated>
<automated>cargo test -p bindings_node export_commands -- --nocapture</automated>
<automated>cargo test -p testkit preview_export_parity -- --nocapture</automated>
</verification>

<success_criteria>
- CANVAS-01 drives preview/export profile resolution.
- CANVAS-02 background support/degraded/unsupported diagnostics are not hidden by services.
- CANVAS-03 preview/export tests preserve the shared canvas coordinate/profile contract.
- Production preview/export paths stop relying on hard-coded MVP canvas defaults.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | CANVAS-01 | Canvas profile drives preview/export | 07-05 | COVERED | Preview/export services use `EngineProfile::from_draft_canvas`.
REQ | CANVAS-02 | Background diagnostics remain visible | 07-05 | COVERED | Service tests preserve render graph diagnostics.
REQ | CANVAS-03 | Preview/export shared coordinate/profile semantics | 07-05 | COVERED | Parity tests for custom profiles.
RESEARCH | Replace production `EngineProfile::mvp_default()` and export dimension hard-codes | 07-05 | COVERED | Tasks 1 and 2.
CONTEXT | D-03, D-05, D-06, D-07, D-11 | 07-05 | COVERED | Cited in actions.
</source_audit>

<output>
Create `.planning/phases/07-project-canvas-space-and-coordinate-system/07-05-SUMMARY.md` when done.
</output>
