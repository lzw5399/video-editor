---
phase: "07-project-canvas-space-and-coordinate-system"
plan: "05"
type: execute
wave: 4
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
files_modified:
  - "scripts/phase7-source-guards.sh"
  - "package.json"
  - "justfile"
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
user_setup: []
must_haves:
  truths:
    - "D-01/D-10: guards enforce Jianying terminology and Simplified Chinese canvas UI copy."
    - "D-02/D-07/D-09: renderer cannot directly mutate `draft.canvasConfig` or own preview/export canvas dimensions."
    - "D-05/D-08: guards require visible background capability/deferred semantics and generated canvas contracts."
    - "D-11: final phase gates add no package installs."
  artifacts:
    - path: "scripts/phase7-source-guards.sh"
      provides: "canvas ownership, terminology, generated-contract, and UI guard"
    - path: "package.json"
      provides: "Phase 07 root test scripts"
    - path: "justfile"
      provides: "Phase 07 public just gate"
  key_links:
    - from: "package.json"
      to: "scripts/phase7-source-guards.sh"
      via: "test:phase7-source-guards"
      pattern: "test:phase7"
    - from: "justfile"
      to: "package.json"
      via: "pnpm run test:phase7"
      pattern: "test:phase7"
---

<objective>
Close Phase 07 with source guards and public gate wiring.

Purpose: canvas semantics must stay Rust-owned after implementation, and Phase 07 verification must be reachable from the normal local/CI command surface.
Output: `scripts/phase7-source-guards.sh`, root `pnpm run test:phase7`, and `/Users/zhiwen/.cargo/bin/just test` integration.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-CONTEXT.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-RESEARCH.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-PATTERNS.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-UI-SPEC.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-01-SUMMARY.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-02-SUMMARY.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-03-SUMMARY.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-04-SUMMARY.md
@scripts/phase4-source-guards.sh
@scripts/phase5-source-guards.sh
@package.json
@justfile
</context>

## Artifacts this phase produces

New file/scripts:
- `scripts/phase7-source-guards.sh`
- `pnpm run test:phase7-rust`
- `pnpm run test:phase7-source-guards`
- `pnpm run test:phase7-workspace`
- `pnpm run test:phase7`

Required final gates:
- focused Rust canvas tests
- `pnpm run test:phase7`
- `pnpm run test`
- `/Users/zhiwen/.cargo/bin/just test` when `just` is not on PATH

<decision_map>
D-01: Guards block alternate terms like Asset, Clip, Stage, and SceneSize in Phase 07 semantic surfaces.
D-02: Guards require Rust-owned generated canvas contracts and block renderer direct `draft.canvasConfig` mutation.
D-03/D-04: Guards require canvasConfig, aspectRatio, width, height, frameRate, and background contract artifacts.
D-05/D-08: Guards require visible background modes and degraded/unsupported/deferred states.
D-06: Guards require coordinate docs and block renderer-persisted pixel coordinate semantics.
D-07: Guards block production preview/export hard-coded canvas defaults and renderer-owned output dimensions/fps.
D-09: Guards verify no duplicate left menu and right-inspector `草稿参数` copy through workspace tests.
D-10: Guards verify Chinese canvas UI copy.
D-11: Guards verify no package additions are required for Phase 07.
</decision_map>

<tasks>

<task type="auto">
  <name>Task 07-05-01: Add Phase 07 source guards</name>
  <files>scripts/phase7-source-guards.sh</files>
  <read_first>
    - `scripts/phase4-source-guards.sh`
    - `scripts/phase5-source-guards.sh`
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-UI-SPEC.md`
    - `package.json`
  </read_first>
  <action>Create `scripts/phase7-source-guards.sh` with `set -euo pipefail` and the existing guard helper style. Require generated contracts to contain `canvasConfig`, `DraftCanvasConfig`, `CanvasAspectRatio`, `CanvasBackground`, and `UpdateDraftCanvasConfig`. Require `docs/canvas-coordinate-system.md` to contain the D-06 center-origin, +X right, +Y up, and half-width/half-height wording. Reject alternate terminology `Asset`, `Clip`, `Stage`, and `SceneSize` in Rust canvas/domain contracts, generated contracts, and UI-visible Phase 07 files per D-01. Reject renderer direct mutation patterns for `draft.canvasConfig`, `canvasConfig`, `aspectRatio`, `frameRate`, `canvasBackground`, width/height semantics, undo/redo stacks, render graph, FFmpeg args, export validation, or preview cache logic outside generated contracts and allowed command helper construction. Reject production `EngineProfile::mvp_default()` in `crates/preview_service/src` and `crates/bindings_node/src`, and reject `export_dimensions(` in production export service per D-07. Require UI source/test copy for `草稿参数`, `画布比例`, `画布尺寸`, `帧率`, `画布背景`, `黑色`, `纯色`, `模糊填充`, `图片背景`, `未接入`, and `应用草稿参数` per D-09 and D-10. Use `rg` and comment-filtered checks; do not use bare unfiltered `grep -c` gates. Do not call package managers or install dependencies per D-11.</action>
  <verify>
    <automated>bash scripts/phase7-source-guards.sh</automated>
  </verify>
  <acceptance_criteria>
    The guard fails if canvas contracts are missing, if renderer code directly mutates canvas draft semantics, if production preview/export keeps hard-coded profile defaults, if required Chinese UI copy is absent, or if forbidden terminology appears in Phase 07 semantic surfaces. The guard does not fail on generated TypeScript boilerplate, command helper envelope construction, tests that intentionally mention defaults, or comments after comment filtering.
  </acceptance_criteria>
  <done>Phase 07 ownership and UI/source invariants are executable as a script.</done>
</task>

<task type="auto">
  <name>Task 07-05-02: Wire Phase 07 root pnpm and just gates</name>
  <files>package.json, justfile</files>
  <read_first>
    - `package.json`
    - `justfile`
    - `apps/desktop-electron/package.json`
    - `scripts/phase7-source-guards.sh`
  </read_first>
  <action>Add root scripts without adding packages: `test:phase7-rust` runs focused Rust canvas gates for `draft_model`, `draft_commands`, `bindings_node`, `engine_core`, `render_graph`, `ffmpeg_compiler`, `preview_service`, and `testkit` using `cargo test -p ... canvas -- --nocapture` plus the existing export/parity tests needed by 07-03; `test:phase7-source-guards` runs `bash scripts/phase7-source-guards.sh`; `test:phase7-workspace` runs `pnpm --filter @video-editor/desktop test:workspace -g "草稿参数|画布"`; and `test:phase7` runs the Rust, source-guard, workspace, and contract drift gates. Chain `pnpm run test:phase7` into root `pnpm run test` after Phase 6 and before `test:contracts`. Update `justfile` so `just test` runs `pnpm run test:phase7`; if local `just` is unavailable during execution, use `/Users/zhiwen/.cargo/bin/just test` as the explicit fallback gate. Preserve existing Phase 1-6 gates and do not add packaged Phase 6 gates to ordinary root test.</action>
  <verify>
    <automated>pnpm run test:phase7</automated>
    <automated>pnpm run test</automated>
    <automated>/Users/zhiwen/.cargo/bin/just test</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    `pnpm run test:phase7` runs focused Rust, source guard, workspace, and contract drift checks. Root `pnpm run test` includes `test:phase7`. `/Users/zhiwen/.cargo/bin/just test` includes `pnpm run test:phase7`. No scripts install new packages or remove earlier phase gates.
  </acceptance_criteria>
  <done>Phase 07 verification is exposed through root pnpm and just gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> release gate | Source guard decides whether renderer/Rust ownership invariants hold. |
| root scripts -> CI/local verification | Public commands decide whether Phase 07 is considered complete. |
| generated contracts -> renderer | Drift between Rust contracts and TypeScript/schema can bypass validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Tampering | renderer canvas semantics | mitigate | Source guards reject direct `draft.canvasConfig` mutation and renderer-owned output dimensions/fps/cache/render behavior. |
| T-07-18 | Tampering | production preview/export profile | mitigate | Source guards reject production `EngineProfile::mvp_default()` and export dimension hard-codes in service paths. |
| T-07-19 | Repudiation | generated contracts | mitigate | Root Phase 07 gate includes generated contract drift check and requires canvas contract symbols. |
| T-07-20 | Denial of Service | root test surface | mitigate | Add focused `test:phase7` gate and preserve full `pnpm run test`/`just test` only as final aggregate gates. |
| T-07-SC | Tampering | npm/Rust packages | mitigate | No package install tasks; guards and scripts reuse existing dependencies and block package churn by review. |
</threat_model>

<verification>
<automated>bash scripts/phase7-source-guards.sh</automated>
<automated>pnpm run test:phase7</automated>
<automated>pnpm run test</automated>
<automated>/Users/zhiwen/.cargo/bin/just test</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- CANVAS-01 through CANVAS-04 have public focused gates.
- Renderer direct mutation, production hard-coded canvas profiles, missing generated canvas contracts, missing coordinate docs, missing Chinese UI copy, and forbidden terminology are guarded.
- Root `pnpm run test` and `/Users/zhiwen/.cargo/bin/just test` include Phase 07 checks.
- No new packages are introduced.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 7 | Establish draft-level canvas space for later transform/sticker/text/PIP/background/render behavior | 07-01, 07-02, 07-03, 07-04, 07-05 | COVERED | Schema, commands, engine/render propagation, UI, and guards.
REQ | CANVAS-01 | Canonical project canvas/profile model | 07-01, 07-03, 07-05 | COVERED | Draft model plus profile propagation and root gates.
REQ | CANVAS-02 | Semantic canvas background modes | 07-01, 07-03, 07-04, 07-05 | COVERED | Schema, diagnostics, UI states, guards.
REQ | CANVAS-03 | Shared coordinate semantics | 07-01, 07-03, 07-04, 07-05 | COVERED | Docs/tests, engine checks, UI help, guards.
REQ | CANVAS-04 | Chinese command-owned desktop canvas settings | 07-01, 07-02, 07-04, 07-05 | COVERED | Generated command payload, Rust route, UI, gates.
RESEARCH | Draft-owned `canvasConfig` and generated contracts | 07-01 | COVERED | Foundation plan.
RESEARCH | One undoable canvas command and binding route | 07-02 | COVERED | Command plan.
RESEARCH | Replace production `EngineProfile::mvp_default()`/export dimensions hard-codes | 07-03, 07-05 | COVERED | Propagation and guard plans.
RESEARCH | Right-inspector `草稿参数`, preview readout, no left menu | 07-04, 07-05 | COVERED | UI and tests.
RESEARCH | Phase source guards and public gates | 07-05 | COVERED | Guard/gate plan.
CONTEXT | D-01 | Jianying terminology; no Asset/Clip/Stage/SceneSize | 07-01, 07-04, 07-05 | COVERED | Contracts, UI, guards.
CONTEXT | D-02 | Rust-owned JSON `canvasConfig` | 07-01, 07-02, 07-04, 07-05 | COVERED | Schema, command, UI, guards.
CONTEXT | D-03 | Aspect, width, height, RationalFrameRate, background | 07-01, 07-03, 07-05 | COVERED | Schema and propagation.
CONTEXT | D-04 | Explicit typed aspect ratio and validation | 07-01, 07-02, 07-05 | COVERED | Validation and command tests.
CONTEXT | D-05 | Background modes and capability diagnostics | 07-01, 07-03, 07-04, 07-05 | COVERED | Schema, graph/compiler, UI, guards.
CONTEXT | D-06 | Coordinate contract | 07-01, 07-03, 07-04, 07-05 | COVERED | Docs/tests, engine, UI help, guards.
CONTEXT | D-07 | Preview/export derive from canvasConfig | 07-03, 07-04, 07-05 | COVERED | Production propagation, readout, guards.
CONTEXT | D-08 | Image background picker deferred/validated shape | 07-01, 07-02, 07-04, 07-05 | COVERED | Validated reference and disabled UI state.
CONTEXT | D-09 | Right inspector entry, preview status only, no left menu | 07-04, 07-05 | COVERED | UI and Playwright/source gates.
CONTEXT | D-10 | Simplified Chinese copy | 07-04, 07-05 | COVERED | UI labels and guards.
CONTEXT | D-11 | No new packages | 07-01, 07-02, 07-03, 07-04, 07-05 | COVERED | Each plan uses existing stack; final guards/gates install nothing.
DEFERRED | Phase 08 segment transform/background filling | NONE | EXCLUDED | Explicitly not planned in Phase 07.
DEFERRED | Phase 09 complete text layout | NONE | EXCLUDED | Explicitly not planned in Phase 07.
DEFERRED | Phase 10 keyframes, Phase 11 retiming, Phase 12 effects, Phase 13 transitions | NONE | EXCLUDED | Roadmap phases own this scope.
</source_audit>

<output>
Create `.planning/phases/07-project-canvas-space-and-coordinate-system/07-05-SUMMARY.md` when done.
</output>
