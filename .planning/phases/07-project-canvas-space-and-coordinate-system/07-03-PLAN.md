---
phase: "07-project-canvas-space-and-coordinate-system"
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - "crates/engine_core/src/normalize.rs"
  - "crates/engine_core/src/lib.rs"
  - "crates/engine_core/tests/canvas_profile.rs"
  - "crates/render_graph/src/graph.rs"
  - "crates/render_graph/src/lib.rs"
  - "crates/render_graph/tests/canvas_background.rs"
  - "crates/ffmpeg_compiler/src/job.rs"
  - "crates/ffmpeg_compiler/tests/canvas_profile_snapshots.rs"
  - "crates/preview_service/src/service.rs"
  - "crates/preview_service/tests/canvas_profile.rs"
  - "crates/bindings_node/src/preview_export_service.rs"
  - "crates/bindings_node/tests/export_commands.rs"
  - "crates/testkit/tests/preview_export_parity.rs"
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
user_setup: []
must_haves:
  truths:
    - "D-07: production preview/export semantics derive canvas size and frame rate from `Draft.canvasConfig`, not `EngineProfile::mvp_default()` or renderer hard-codes."
    - "D-05: blur fill and image background render gaps produce degraded/unsupported diagnostics rather than silent black fallback."
    - "D-06: engine/render paths share the same normalized canvas coordinate contract."
  artifacts:
    - path: "crates/engine_core/src/normalize.rs"
      provides: "EngineProfile resolution from draft canvas config"
    - path: "crates/render_graph/src/graph.rs"
      provides: "Render canvas/background diagnostics"
    - path: "crates/preview_service/src/service.rs"
      provides: "Draft-canvas-derived preview profile"
    - path: "crates/bindings_node/src/preview_export_service.rs"
      provides: "Draft-canvas-derived export dimensions and validation"
  key_links:
    - from: "crates/preview_service/src/service.rs"
      to: "crates/engine_core/src/normalize.rs"
      via: "EngineProfile::from_draft_canvas"
      pattern: "from_draft_canvas"
    - from: "crates/bindings_node/src/preview_export_service.rs"
      to: "crates/ffmpeg_compiler/src/job.rs"
      via: "RenderOutputProfile dimensions and validation"
      pattern: "expected_width|expected_height|expected_frame_rate"
---

<objective>
Propagate draft canvas semantics through engine, render graph, preview, and export.

Purpose: after Phase 07, canvas profile is not just persisted; it must drive production preview/export and capability diagnostics through the Rust render path.
Output: engine profile resolution from draft canvas, render background diagnostics, draft-derived preview/export dimensions and fps, and tests for vertical/custom profiles.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-CONTEXT.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-RESEARCH.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-PATTERNS.md
@.planning/phases/05-preview-and-export-pipeline/05-01-SUMMARY.md
@.planning/phases/05-preview-and-export-pipeline/05-04-SUMMARY.md
@.planning/phases/05-preview-and-export-pipeline/05-08-SUMMARY.md
@.planning/phases/05-preview-and-export-pipeline/05-09-SUMMARY.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-01-SUMMARY.md
@crates/engine_core/src/normalize.rs
@crates/render_graph/src/graph.rs
@crates/render_graph/src/profile.rs
@crates/ffmpeg_compiler/src/job.rs
@crates/preview_service/src/service.rs
@crates/bindings_node/src/preview_export_service.rs
</context>

## Artifacts this phase produces

New symbols/files:
- `EngineProfile::from_draft_canvas`
- `RenderCanvas` background/capability diagnostics
- `RenderIntentSupport::Unsupported` or equivalent canvas-background unsupported diagnostic
- `crates/engine_core/tests/canvas_profile.rs`
- `crates/render_graph/tests/canvas_background.rs`
- `crates/ffmpeg_compiler/tests/canvas_profile_snapshots.rs`
- `crates/preview_service/tests/canvas_profile.rs`

Changed production behavior:
- preview profile derives aspect-aware dimensions from `Draft.canvasConfig`
- export validation expects draft canvas width/height/frameRate
- production preview/export no longer call `EngineProfile::mvp_default()`

<decision_map>
D-03: Canvas config includes width, height, rational frame rate, and background.
D-05: Background modes surface supported/degraded/unsupported diagnostics.
D-06: normalized coordinate semantics are the shared engine/render contract.
D-07: preview cache may scale for performance, but production preview/export semantics and validation derive from `Draft.canvasConfig`.
D-11: No new npm/Rust packages.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-03-01: Resolve EngineProfile from Draft.canvasConfig</name>
  <files>crates/engine_core/src/normalize.rs, crates/engine_core/src/lib.rs, crates/engine_core/tests/canvas_profile.rs</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-01-SUMMARY.md`
    - `crates/engine_core/src/normalize.rs`
    - `crates/engine_core/src/frame_state.rs`
    - `crates/engine_core/src/text_layout.rs`
    - `crates/engine_core/tests/normalization.rs`
    - `docs/canvas-coordinate-system.md`
  </read_first>
  <behavior>
    - Test 1: a 1080 x 1920, 25/1 draft produces `EngineProfile` canvas width 1080, height 1920, frame rate 25/1, and valid text layout dimensions.
    - Test 2: a custom square draft produces square frame-state/render-range metadata without using MVP defaults.
    - Test 3: coordinate conversion tests align engine canvas dimensions with the documented D-06 center-origin contract.
  </behavior>
  <action>Add `EngineProfile::from_draft_canvas(&Draft)` that reads `draft.canvas_config`, validates profile dimensions/fps, and builds the existing `TextLayoutProfile` from those dimensions instead of fixed MVP dimensions. Keep `EngineProfile::mvp_default()` only as a test/convenience helper and mark production callers to use `from_draft_canvas`. Add tests for 9:16, 1:1, and custom canvas profiles proving normalization, frame range fps, and text layout dimensions come from `Draft.canvasConfig` per D-03, D-06, and D-07. Do not persist UI pixels or use renderer state as semantic input.</action>
  <verify>
    <automated>cargo test -p engine_core canvas -- --nocapture</automated>
    <automated>cargo test -p engine_core normalization -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    `EngineProfile::from_draft_canvas` exists, is exported, and tests prove non-1920x1080 and non-30fps drafts normalize correctly. `EngineProfile::mvp_default()` remains available only for intentional tests/helpers, not production preview/export services.
  </acceptance_criteria>
  <done>Engine normalization resolves canvas semantics from the draft.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-03-02: Carry canvas background diagnostics through render graph and compiler</name>
  <files>crates/render_graph/src/graph.rs, crates/render_graph/src/lib.rs, crates/render_graph/tests/canvas_background.rs, crates/ffmpeg_compiler/src/job.rs, crates/ffmpeg_compiler/tests/canvas_profile_snapshots.rs</files>
  <read_first>
    - `crates/render_graph/src/graph.rs`
    - `crates/render_graph/src/profile.rs`
    - `crates/render_graph/tests/render_graph_snapshots.rs`
    - `crates/ffmpeg_compiler/src/job.rs`
    - `crates/ffmpeg_compiler/tests/ffmpeg_job_snapshots.rs`
    - `crates/draft_model/src/canvas.rs`
  </read_first>
  <behavior>
    - Test 1: black and solid color backgrounds are represented in render graph canvas data as supported.
    - Test 2: blur fill and image background produce explicit degraded or unsupported diagnostics with mode and reason.
    - Test 3: FFmpeg job encode settings and output validation use draft-derived dimensions and rational fps for vertical/custom profiles.
  </behavior>
  <action>Extend render graph canvas/profile data so `RenderCanvas` carries canvas background semantics and explicit capability diagnostics from `Draft.canvasConfig` per D-05. Add `Unsupported` to `RenderIntentSupport` or an equivalent canvas-specific diagnostic shape if that better matches existing graph patterns; tests must assert blur fill and image background do not silently render as black without a diagnostic. Keep clip-level `backgroundFilling` out of scope; only draft-level canvas background belongs here. Update FFmpeg compiler job tests so `EncodeSettings` and validation snapshots use draft-derived width, height, and `RationalFrameRate` for vertical and square/custom canvases. Do not construct FFmpeg filter strings for unsupported blur/image backgrounds as fake support.</action>
  <verify>
    <automated>cargo test -p render_graph canvas -- --nocapture</automated>
    <automated>cargo test -p ffmpeg_compiler canvas -- --nocapture</automated>
    <automated>cargo test -p ffmpeg_compiler -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    Render graph snapshots include canvas width, height, background mode, and support/degraded/unsupported diagnostics. Compiler snapshots for non-1920x1080 drafts show expected width, height, and frame rate from the draft canvas profile. Unsupported blur/image behavior is visible as diagnostics, not hidden fallback.
  </acceptance_criteria>
  <done>Render graph and compiler carry draft canvas profile and background capability semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-03-03: Replace production preview/export hard-codes with draft-derived canvas profiles</name>
  <files>crates/preview_service/src/service.rs, crates/preview_service/tests/canvas_profile.rs, crates/bindings_node/src/preview_export_service.rs, crates/bindings_node/tests/export_commands.rs, crates/testkit/tests/preview_export_parity.rs</files>
  <read_first>
    - `crates/preview_service/src/service.rs`
    - `crates/preview_service/tests/preview_generation.rs`
    - `crates/bindings_node/src/preview_export_service.rs`
    - `crates/bindings_node/tests/export_commands.rs`
    - `crates/testkit/tests/preview_export_parity.rs`
    - `package.json`
  </read_first>
  <behavior>
    - Test 1: preview frame and preview segment preparation call `EngineProfile::from_draft_canvas` and preserve draft aspect ratio when deriving preview cache output.
    - Test 2: export no longer uses `export_dimensions(payload.preset)` for canonical dimensions; validation expects draft canvas width/height/fps.
    - Test 3: vertical/custom draft preview/export tests prove output metadata changes when `Draft.canvasConfig` changes.
  </behavior>
  <action>Replace production `normalize_draft(..., &EngineProfile::mvp_default())` calls in `preview_service` and `bindings_node::preview_export_service` with `EngineProfile::from_draft_canvas(&draft)` per D-07. For preview cache output, derive any scaled preview dimensions from the draft canvas aspect ratio through a Rust-owned policy; do not keep fixed 960 x 540 as production semantic truth. For export, remove or repurpose `export_dimensions(payload.preset)` so presets select quality/codec only and output validation expects the draft canvas width, height, and `RationalFrameRate`. Update parity and export tests to assert a 1080 x 1920 or custom canvas changes preview/export metadata through Rust semantics. Keep renderer, Electron, and test mocks from deciding dimensions/fps.</action>
  <verify>
    <automated>cargo test -p preview_service canvas -- --nocapture</automated>
    <automated>cargo test -p bindings_node export_commands -- --nocapture</automated>
    <automated>cargo test -p testkit preview_export_parity -- --nocapture</automated>
    <automated>bash -lc '! rg -n "EngineProfile::mvp_default\\(\\)|export_dimensions\\(" crates/preview_service/src crates/bindings_node/src'</automated>
  </verify>
  <acceptance_criteria>
    Production preview/export paths derive profile from `Draft.canvasConfig`. `EngineProfile::mvp_default()` appears only in intentional test helpers or tests. Export validation expects draft width, height, and fps. Preview cache output may be scaled only by a Rust-owned policy derived from draft canvas aspect ratio. Tests prove vertical/custom profiles flow through preview and export.
  </acceptance_criteria>
  <done>Preview and export production semantics no longer depend on hard-coded MVP canvas defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft canvas profile -> engine_core | Persisted canvas values become render-time profile values. |
| render graph -> FFmpeg compiler | Canvas/background diagnostics influence generated jobs and validation expectations. |
| bindings export payload -> filesystem/runtime | User output paths and draft profiles cross into export preparation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | production preview/export profile | mitigate | Replace production MVP defaults with `EngineProfile::from_draft_canvas` and test vertical/custom profiles. |
| T-07-10 | Information Disclosure | preview/export diagnostics | mitigate | Return bounded degraded/unsupported diagnostics without leaking local material paths beyond existing command response policy. |
| T-07-11 | Denial of Service | huge canvas dimensions | mitigate | Reuse `draft_model` validation before render profile resolution; tests cover invalid dimensions earlier in 07-01/07-02. |
| T-07-12 | Tampering | image background render support | mitigate | Render graph exposes unsupported/degraded diagnostics for image/blur modes instead of pretending support in compiler filters. |
| T-07-SC | Tampering | package installs | mitigate | No new package installs; reuse existing Rust crates and FFmpeg path. |
</threat_model>

<verification>
<automated>cargo test -p engine_core canvas -- --nocapture</automated>
<automated>cargo test -p render_graph canvas -- --nocapture</automated>
<automated>cargo test -p ffmpeg_compiler canvas -- --nocapture</automated>
<automated>cargo test -p preview_service canvas -- --nocapture</automated>
<automated>cargo test -p bindings_node export_commands -- --nocapture</automated>
<automated>cargo test -p testkit preview_export_parity -- --nocapture</automated>
</verification>

<success_criteria>
- CANVAS-01 drives engine/render/preview/export profile resolution.
- CANVAS-02 background support/degraded/unsupported diagnostics flow through graph/compiler.
- CANVAS-03 coordinate and canvas dimensions are shared by engine/render tests.
- Production preview/export paths stop relying on hard-coded MVP canvas defaults.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | CANVAS-01 | Canvas profile drives engine/preview/export | 07-03 | COVERED | `EngineProfile::from_draft_canvas`.
REQ | CANVAS-02 | Background diagnostics | 07-03 | COVERED | Render graph/FFmpeg compiler diagnostics.
REQ | CANVAS-03 | Coordinate semantics in engine/render path | 07-03 | COVERED | Engine tests use documented coordinate/canvas dimensions.
RESEARCH | Remove production `EngineProfile::mvp_default()` and export dimensions hard-codes | 07-03 | COVERED | Task 3.
CONTEXT | D-03, D-05, D-06, D-07, D-11 | 07-03 | COVERED | Cited in actions.
</source_audit>

<output>
Create `.planning/phases/07-project-canvas-space-and-coordinate-system/07-03-SUMMARY.md` when done.
</output>
