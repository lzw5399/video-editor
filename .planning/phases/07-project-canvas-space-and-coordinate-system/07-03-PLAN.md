---
phase: "07-project-canvas-space-and-coordinate-system"
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - "crates/draft_commands/src/canvas.rs"
  - "crates/draft_commands/src/lib.rs"
  - "crates/draft_commands/tests/canvas_commands.rs"
  - "crates/bindings_node/src/lib.rs"
  - "crates/bindings_node/tests/canvas_commands.rs"
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-04
user_setup: []
must_haves:
  truths:
    - "D-02/D-03: canvas updates mutate only Rust-owned `Draft.canvas_config` through generated command payloads."
    - "D-04/D-05/D-08: command validation rejects invalid aspect ratio, background color, and image material references atomically."
    - "D-09: renderer-facing command route exists for the right-inspector canvas UI without direct draft mutation."
  artifacts:
    - path: "crates/draft_commands/src/canvas.rs"
      provides: "undoable updateDraftCanvasConfig command"
    - path: "crates/draft_commands/tests/canvas_commands.rs"
      provides: "canvas command validation and undo/redo tests"
    - path: "crates/bindings_node/tests/canvas_commands.rs"
      provides: "binding route tests"
  key_links:
    - from: "crates/bindings_node/src/lib.rs"
      to: "crates/draft_commands/src/canvas.rs"
      via: "updateDraftCanvasConfig route"
      pattern: "UpdateDraftCanvasConfig|updateDraftCanvasConfig"
---

<objective>
Add the Rust-owned canvas mutation command and binding route.

Purpose: the desktop inspector must update canvas semantics through the same undoable Rust command path as timeline/text/audio edits.
Output: `draft_commands::canvas`, command tests, and `bindings_node` routing for `updateDraftCanvasConfig`.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-CONTEXT.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-RESEARCH.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-PATTERNS.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-01-SUMMARY.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-02-SUMMARY.md
@crates/draft_commands/src/timeline.rs
@crates/draft_commands/src/history.rs
@crates/draft_commands/src/lib.rs
@crates/bindings_node/src/lib.rs
</context>

## Artifacts this phase produces

Command artifacts:
- `crates/draft_commands/src/canvas.rs`
- `update_draft_canvas_config`
- `draftCanvasConfigUpdated` command event
- `crates/draft_commands/tests/canvas_commands.rs`
- `crates/bindings_node/tests/canvas_commands.rs`

<decision_map>
D-02: `canvasConfig` is Rust-owned persisted draft semantics.
D-03: update payload carries complete typed canvas config.
D-04: aspect ratio validation runs in Rust before commit.
D-05: background modes are semantic and classify degraded/unsupported support rather than faking rendering.
D-08: image background material references are rejected when missing or invalid.
D-09: right inspector is the UI entry; this plan only creates the command route it will call.
D-11: No new npm/Rust packages.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-03-01: Implement undoable draft canvas command in draft_commands</name>
  <files>crates/draft_commands/src/canvas.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/tests/canvas_commands.rs</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-01-SUMMARY.md`
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-02-SUMMARY.md`
    - `crates/draft_commands/src/timeline.rs`
    - `crates/draft_commands/src/history.rs`
    - `crates/draft_commands/src/error.rs`
    - `crates/draft_commands/tests/timeline_commands.rs`
    - `crates/draft_model/src/canvas.rs`
    - `crates/draft_model/src/lib.rs`
  </read_first>
  <behavior>
    - Test 1: valid canvas config replaces `draft.canvas_config`, preserves selection, returns updated draft/commandState/selection/events, and pushes one undo snapshot.
    - Test 2: undo and redo restore canvas config through existing `CommandState` history without creating persisted history fields.
    - Test 3: invalid aspect ratio, zero size/fps, invalid solid color, and bad image material references fail atomically with unchanged draft and command state.
  </behavior>
  <action>Create `crates/draft_commands/src/canvas.rs` using the clone/patch/validate/commit pattern from `timeline.rs` and `history.rs`. Implement `update_draft_canvas_config` for `UpdateDraftCanvasConfigCommandPayload`: clone the input `Draft`, replace `canvas_config`, call `validate_draft`, push an undo snapshot labeled `updateDraftCanvasConfig`, keep `TimelineSelection` unchanged, and return `TimelineCommandResponse` with a `draftCanvasConfigUpdated` event. Route `CommandPayload::UpdateDraftCanvasConfig` through a `canvas` module export in `draft_commands/src/lib.rs`; keep history session-only and never persist command state in `.veproj/project.json`. Map validation failures to existing command error patterns without partially mutating draft or command state. Use D-02 through D-05 and D-08 as the command truth; do not add renderer-owned shortcut semantics or new packages per D-11.</action>
  <acceptance_criteria>
    Canvas command tests prove update, validation rejection, undo, and redo. `Draft.canvas_config` only changes after validation passes. Existing timeline/text/audio command tests still pass. No persisted undo/redo fields are added to Draft or fixtures.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands canvas -- --nocapture</automated>
    <automated>cargo test -p draft_commands undo_redo -- --nocapture</automated>
  </verify>
  <done>Canvas mutation is an undoable Rust command with atomic validation and existing command response shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-03-02: Route updateDraftCanvasConfig through bindings_node</name>
  <files>crates/bindings_node/src/lib.rs, crates/bindings_node/tests/canvas_commands.rs</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-02-SUMMARY.md`
    - `crates/bindings_node/src/lib.rs`
    - `crates/bindings_node/tests/binding_smoke.rs`
    - `crates/draft_model/src/lib.rs`
    - `crates/draft_commands/src/canvas.rs`
  </read_first>
  <behavior>
    - Test 1: `execute_command` accepts a generated `updateDraftCanvasConfig` envelope and returns an ok `TimelineCommandResponse`.
    - Test 2: mismatched command/payload pairs and malformed canvas payloads return structured command errors.
    - Test 3: unsupported command names still return the existing unsupported-command error envelope.
  </behavior>
  <action>Add `updateDraftCanvasConfig` to the raw command allowlist and route `CommandName::UpdateDraftCanvasConfig` to `draft_commands::canvas` or a shared draft edit dispatcher. Preserve the existing `CommandEnvelope` deserialization, command/payload mismatch protection, and `TimelineCommandError` to command-error mapping. Add binding tests for success, invalid canvas config, bad image material reference, and mismatched envelope. The binding must not validate aspect ratio or background semantics itself; it must delegate to `draft_commands` and `draft_model` per D-02 through D-05. Do not introduce a canvas-specific renderer state channel per D-09.</action>
  <acceptance_criteria>
    `execute_command` supports `updateDraftCanvasConfig` through generated contracts. Invalid payloads are rejected with structured errors. Binding code contains no duplicated aspect-ratio, background, image-reference, preview, export, FFmpeg, or render graph semantics.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bindings_node canvas_commands -- --nocapture</automated>
    <automated>cargo test -p bindings_node -- --nocapture</automated>
  </verify>
  <done>Electron can call the Rust-owned canvas command through the existing binding envelope path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer command payload -> bindings_node | Untrusted canvas command JSON crosses into Rust. |
| bindings_node -> draft_commands | Command route must not bypass Rust validation. |
| draft_commands -> CommandState history | Undo/redo snapshots are session state and must not enter persisted draft JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | `updateDraftCanvasConfig` payload | mitigate | Deserialize generated payload, validate with `draft_model`, and reject invalid configs atomically. |
| T-07-08 | Elevation of Privilege | bindings route | mitigate | Add command allowlist and command/payload mismatch tests; binding delegates semantics to `draft_commands`. |
| T-07-09 | Repudiation | undo/redo history | mitigate | Use existing `CommandState` snapshots and event labels for canvas edits; tests cover undo/redo. |
| T-07-SC | Tampering | package installs | mitigate | No package-manager installs; reuse existing Rust crates and command patterns per D-11. |
</threat_model>

<verification>
<automated>cargo test -p draft_commands canvas -- --nocapture</automated>
<automated>cargo test -p bindings_node canvas_commands -- --nocapture</automated>
<automated>cargo test -p bindings_node -- --nocapture</automated>
</verification>

<success_criteria>
- CANVAS-01 and CANVAS-02 can be mutated through one Rust-owned command with validation.
- CANVAS-04 has a binding command route ready for desktop UI.
- Canvas command failures are atomic and undo/redo works through existing command history.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | CANVAS-01 | Canvas profile command mutation | 07-03 | COVERED | Update command mutates width/height/fps/aspect.
REQ | CANVAS-02 | Background command mutation and validation | 07-03 | COVERED | Background config is validated before commit.
REQ | CANVAS-04 | Rust command ownership for UI | 07-03 | COVERED | Binding route and tests.
RESEARCH | One `updateDraftCanvasConfig` command and `TimelineCommandResponse` reuse | 07-03 | COVERED | Tasks 1 and 2.
CONTEXT | D-02, D-03, D-04, D-05, D-08, D-09, D-11 | 07-03 | COVERED | Cited in actions.
</source_audit>

<output>
Create `.planning/phases/07-project-canvas-space-and-coordinate-system/07-03-SUMMARY.md` when done.
</output>
