---
phase: "07-project-canvas-space-and-coordinate-system"
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
files_modified:
  - "apps/desktop-electron/src/renderer/commandHelpers.ts"
  - "apps/desktop-electron/src/renderer/viewModel.ts"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/src/renderer/workspace/Inspector.tsx"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "apps/desktop-electron/src/renderer/workspace/preview-inspector.css"
  - "apps/desktop-electron/src/main/index.ts"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
user_setup: []
must_haves:
  truths:
    - "D-09: main UI entry is right-inspector no-selection `草稿参数`; no left menu or canvas page is added."
    - "D-10: all visible canvas UI copy is Simplified Chinese and Jianying-style."
    - "D-02/D-07: renderer builds generated command envelopes only and updates accepted state from Rust command responses."
    - "D-05/D-08: blur/image background deferred/degraded states remain visible and non-silent."
  artifacts:
    - path: "apps/desktop-electron/src/renderer/workspace/Inspector.tsx"
      provides: "editable `草稿参数` canvas controls"
    - path: "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
      provides: "draft-canvas-aware preview aspect/readout"
    - path: "apps/desktop-electron/tests/workspace.spec.ts"
      provides: "canvas UI command/layout tests at 1280x800 and 1120x720"
  key_links:
    - from: "Inspector.tsx"
      to: "commandHelpers.ts"
      via: "updateDraftCanvasConfig callback"
      pattern: "updateDraftCanvasConfig"
    - from: "PreviewMonitor.tsx"
      to: "viewModel.ts"
      via: "formatted canvas readout"
      pattern: "画布"
---

<objective>
Expose draft canvas settings in the Chinese desktop workspace.

Purpose: users need compact Jianying-style project canvas controls while Rust remains the only owner of draft semantics.
Output: generated-contract command helper, App handler, inspector controls, preview monitor readout/aspect, test mocks, and Playwright layout/interaction coverage.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-CONTEXT.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-RESEARCH.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-PATTERNS.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-UI-SPEC.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-02-SUMMARY.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-03-SUMMARY.md
@apps/desktop-electron/src/renderer/commandHelpers.ts
@apps/desktop-electron/src/renderer/viewModel.ts
@apps/desktop-electron/src/renderer/App.tsx
@apps/desktop-electron/src/renderer/workspace/Inspector.tsx
@apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx
@apps/desktop-electron/tests/workspace.spec.ts
</context>

## Artifacts this phase produces

New symbols/files:
- `buildUpdateDraftCanvasConfigCommand`
- `handleUpdateDraftCanvasConfig`
- `formatCanvasReadout`
- canvas background display helpers
- Playwright screenshots under `test-results/phase7/`

Visible UI copy:
- `草稿参数`
- `画布比例`
- `画布尺寸`
- `帧率`
- `画布背景`
- `黑色`
- `纯色`
- `模糊填充`
- `图片背景`
- `未接入`
- `降级`
- `应用草稿参数`
- `坐标以画布中心为原点`

<decision_map>
D-01: UI and code use Jianying terms, not Asset/Clip/Stage/SceneSize.
D-02: renderer reads/writes `canvasConfig` only through generated Rust command contracts.
D-05: background modes show degraded/unsupported states instead of fake support.
D-06: UI displays coordinate help and treats pixel space as derived only.
D-07: preview readout/aspect displays draft canvas profile from Rust-owned state.
D-08: image material picker is visible as disabled/deferred `未接入` unless validation-backed material reference exists.
D-09: right inspector no-selection `草稿参数` is the main entry; preview monitor only shows status/aspect; no left menu.
D-10: desktop visible copy is Simplified Chinese.
D-11: No new npm/Rust packages.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-04-01: Add renderer command helper, App handler, and display formatters</name>
  <files>apps/desktop-electron/src/renderer/commandHelpers.ts, apps/desktop-electron/src/renderer/viewModel.ts, apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/src/main/index.ts</files>
  <read_first>
    - `apps/desktop-electron/src/generated/CommandEnvelope.ts`
    - `apps/desktop-electron/src/generated/Draft.ts`
    - `apps/desktop-electron/src/renderer/commandHelpers.ts`
    - `apps/desktop-electron/src/renderer/viewModel.ts`
    - `apps/desktop-electron/src/renderer/App.tsx`
    - `apps/desktop-electron/src/main/index.ts`
  </read_first>
  <behavior>
    - Test 1: renderer helper builds `updateDraftCanvasConfig` envelopes from generated types and never mutates `draft.canvasConfig` directly.
    - Test 2: App handler applies `TimelineCommandResponse` using existing accepted-state logic and surfaces Chinese command errors.
    - Test 3: view model formats ratio, size, fps, background status, and preview readout from `workspace.draft.canvasConfig`.
  </behavior>
  <action>Add `buildUpdateDraftCanvasConfigCommand(context, canvasConfig)` in `commandHelpers.ts` using generated `UpdateDraftCanvasConfigCommandPayload` and existing envelope helper style. Update `initialWorkspaceDraft` in `viewModel.ts` to include `canvasConfig`; add pure display helpers for aspect ratio label, size label, fps label, background status, and `画布 {ratio} · {width} x {height} · {fps}`. Add `handleUpdateDraftCanvasConfig` in `App.tsx` using the same pending/error/apply flow as timeline commands; accepted state must update only from Rust `TimelineCommandResponse` per D-02 and D-07. Update test-only main-process mocks in `src/main/index.ts` only enough to return Rust-contract-shaped success/error envelopes for Playwright command recording; do not implement renderer-owned validation, render graph, FFmpeg, output dimensions, or preview/export behavior. Keep all visible errors Simplified Chinese per D-10.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test:workspace -g "画布"</automated>
  </verify>
  <acceptance_criteria>
    `updateDraftCanvasConfig` command calls are recorded by Playwright. App pending/error state works like existing timeline commands. Display helpers derive all canvas labels from accepted draft state. Source search shows no direct renderer assignment to `draft.canvasConfig` outside generated test mocks.
  </acceptance_criteria>
  <done>Renderer has an envelope-only canvas command path and draft-derived canvas display state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-04-02: Build inspector canvas controls and preview monitor readout</name>
  <files>apps/desktop-electron/src/renderer/workspace/Inspector.tsx, apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/src/renderer/workspace/preview-inspector.css</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-UI-SPEC.md`
    - `apps/desktop-electron/src/renderer/workspace/Inspector.tsx`
    - `apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx`
    - `apps/desktop-electron/src/renderer/workspace/preview-inspector.css`
    - `apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx`
  </read_first>
  <behavior>
    - Test 1: no-selection inspector shows `草稿参数`, ratio presets, width/height inputs, fps select, background modes, coordinate help, and `应用草稿参数`.
    - Test 2: invalid local width/height/fps/color disables apply and shows inline Chinese validation without mutating draft.
    - Test 3: preview monitor aspect ratio and readout follow accepted draft canvas config and long text truncates without overlapping controls.
  </behavior>
  <action>Replace hard-coded `画布比例 16:9` and `画布尺寸 1920 x 1080` in `Inspector.tsx` with the UI-SPEC no-selection `草稿参数` controls per D-09 and D-10: segmented presets `16:9`, `9:16`, `1:1`, `4:3`, `3:4`, `自定义`; numeric `宽` and `高` inputs; fps options `24 fps`, `25 fps`, `30 fps`, `50 fps`, `60 fps`; background modes `黑色`, `纯色`, `模糊填充`, `图片背景`; solid color swatch/hex input; disabled image `选择素材` with `未接入`; and coordinate help `坐标以画布中心为原点，X 向右，Y 向上`. Local form state may validate fields, but apply must call the App handler and update only from Rust command response. Update `PreviewMonitor.tsx` so `.preview-canvas` aspect ratio uses draft canvas width/height, title/status shows `画布 {ratio} · {width} x {height} · {fps}`, the ratio control symbol is current ratio/custom, and degraded/unsupported background status is visible. Preserve compact dark scrollbar, 1120px min-width, no nested cards, no duplicate left primary menu, and no transform inputs per deferred Phase 08 scope.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test:workspace -g "草稿参数|画布"</automated>
  </verify>
  <acceptance_criteria>
    Inspector controls match the approved UI-SPEC labels and compact layout. Preview monitor displays draft ratio/size/fps and uses draft aspect. Blur/image deferred/degraded states are visible in Chinese. The UI adds no new left menu/category and no transform controls. CSS keeps 4px dark scrollbars and stable region dimensions.
  </acceptance_criteria>
  <done>Desktop canvas UI is visible, compact, Chinese, and command-driven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-04-03: Add Playwright canvas interaction and layout gates</name>
  <files>apps/desktop-electron/tests/workspace.spec.ts</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-UI-SPEC.md`
    - `apps/desktop-electron/tests/workspace.spec.ts`
    - `apps/desktop-electron/tests/helpers/realWorkflow.ts`
  </read_first>
  <behavior>
    - Test 1: visible labels include `草稿参数`, `画布比例`, `画布尺寸`, `帧率`, `画布背景`, `黑色`, `纯色`, `模糊填充`, `图片背景`, `未接入`, and `应用草稿参数`.
    - Test 2: applying a canvas change records `updateDraftCanvasConfig` and UI updates from the mocked Rust response.
    - Test 3: 1280x800 and 1120x720 layout checks show no overlap, no cropped labels/inputs, compact dark scrollbars, and no duplicate left primary menu.
  </behavior>
  <action>Extend `workspace.spec.ts` with Phase 07 canvas tests using existing `launchWorkspaceApp`, command spy, geometry, and screenshot helper patterns. Assert no-selection inspector canvas controls, background deferred/degraded states, command-only `updateDraftCanvasConfig`, and preview monitor ratio/size/fps readout. Add viewport checks for 1280x800 and 1120x720, save screenshots under `test-results/phase7/canvas-1280x800.png` and `test-results/phase7/canvas-1120x720.png`, and assert no duplicate left-side primary menu is introduced. Add a command boundary assertion that canvas changes call `window.videoEditorCore.executeCommand` and no direct renderer draft mutation is required for UI update. Keep test copy Simplified Chinese per D-10.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test:workspace -g "草稿参数|画布"</automated>
    <automated>test -f test-results/phase7/canvas-1280x800.png || test -d test-results/phase7</automated>
  </verify>
  <acceptance_criteria>
    Playwright covers canvas controls, command call, preview readout, 1280x800 layout, 1120x720 layout, compact dark scrollbars, and absence of duplicate left menu. Screenshots are generated by tests under ignored `test-results/phase7/`. Tests do not rely on a renderer-owned state shortcut.
  </acceptance_criteria>
  <done>Canvas UI behavior and layout are executable gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> renderer form state | Width/height/fps/color edits are untrusted local input before command submission. |
| renderer -> Rust command bridge | Canvas changes cross via generated command envelopes. |
| Rust command response -> renderer display | Accepted draft state and diagnostics update UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | `Inspector.tsx` local form | mitigate | Validate local fields only to enable/disable submit; canonical validation and state update happen through Rust command response. |
| T-07-14 | Elevation of Privilege | renderer draft state | mitigate | Use generated command helper and App apply flow; source guards in 07-05 block direct `draft.canvasConfig` mutation. |
| T-07-15 | Spoofing | image background UI | mitigate | Keep image picker disabled/deferred `未接入` unless validation-backed material reference support exists. |
| T-07-16 | Information Disclosure | command errors | mitigate | Display bounded Chinese command errors without exposing internal FFmpeg/render graph/schema terms in normal UI copy. |
| T-07-SC | Tampering | package installs | mitigate | No new UI packages; use existing React/Playwright and dependency-free symbols per D-11. |
</threat_model>

<verification>
<automated>pnpm --filter @video-editor/desktop test:workspace -g "草稿参数|画布"</automated>
<automated>pnpm --filter @video-editor/desktop test</automated>
</verification>

<success_criteria>
- CANVAS-04 is implemented in the right inspector no-selection `草稿参数` surface.
- Preview monitor displays current canvas ratio/size/fps and uses draft aspect ratio.
- UI uses Simplified Chinese visible copy and no duplicate left menu.
- Canvas changes are command-only and update from Rust responses.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | CANVAS-01 | UI displays aspect/size/fps from canvas config | 07-04 | COVERED | Inspector/preview readouts.
REQ | CANVAS-02 | UI displays background modes and degraded/deferred states | 07-04 | COVERED | Inspector background controls.
REQ | CANVAS-03 | UI shows coordinate help | 07-04 | COVERED | Coordinate help label from UI-SPEC.
REQ | CANVAS-04 | Chinese command-owned canvas settings UI | 07-04 | COVERED | Inspector controls and Playwright command test.
RESEARCH | Right inspector entry, preview readout, no left menu | 07-04 | COVERED | Tasks 2 and 3.
CONTEXT | D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-10, D-11 | 07-04 | COVERED | Cited in actions.
</source_audit>

<output>
Create `.planning/phases/07-project-canvas-space-and-coordinate-system/07-04-SUMMARY.md` when done.
</output>
