---
phase: "07-project-canvas-space-and-coordinate-system"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "crates/draft_model/src/canvas.rs"
  - "crates/draft_model/src/draft.rs"
  - "crates/draft_model/src/validation.rs"
  - "crates/draft_model/src/lib.rs"
  - "crates/draft_model/tests/canvas_config.rs"
  - "docs/canvas-coordinate-system.md"
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
user_setup: []
must_haves:
  truths:
    - "D-02/D-03: `.veproj/project.json` has required Rust-owned `canvasConfig` with typed aspect ratio, dimensions, `RationalFrameRate`, and `CanvasBackground`."
    - "D-04: aspect ratio presets are validated against dimensions and custom ratios remain explicit semantic state."
    - "D-05/D-08: black, solid color, blur fill, and image background are represented semantically with capability status."
    - "D-06: normalized canvas coordinates are center-origin, +X right, +Y up, and 1.0 equals half canvas width/height."
  artifacts:
    - path: "crates/draft_model/src/canvas.rs"
      provides: "Draft canvas config, aspect ratio, background, and coordinate helpers"
    - path: "crates/draft_model/tests/canvas_config.rs"
      provides: "Focused model, validation, and coordinate tests"
    - path: "docs/canvas-coordinate-system.md"
      provides: "Shared coordinate contract for later transform, text, sticker, PIP, keyframe, preview, and export paths"
  key_links:
    - from: "crates/draft_model/src/draft.rs"
      to: "crates/draft_model/src/canvas.rs"
      via: "Draft.canvas_config"
      pattern: "canvas_config"
    - from: "crates/draft_model/src/validation.rs"
      to: "crates/draft_model/src/canvas.rs"
      via: "canvas validation helpers"
      pattern: "validate.*canvas"
---

<objective>
Define the canonical draft canvas model and coordinate contract.

Purpose: Phase 07 needs one Rust-owned project canvas source before fixtures, generated contracts, commands, render propagation, and UI depend on it.
Output: `Draft.canvas_config`, canvas validation, focused model tests, and `docs/canvas-coordinate-system.md`.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-CONTEXT.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-RESEARCH.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-PATTERNS.md
@.planning/phases/07-project-canvas-space-and-coordinate-system/07-UI-SPEC.md
@crates/draft_model/src/draft.rs
@crates/draft_model/src/material.rs
@crates/draft_model/src/validation.rs
@crates/draft_model/src/lib.rs
</context>

## Artifacts this phase produces

Model and docs:
- `crates/draft_model/src/canvas.rs`
- `DraftCanvasConfig`
- `CanvasAspectRatio`
- `CanvasAspectRatioPreset`
- `CanvasBackground`
- `CanvasBackgroundCapability`
- `UpdateDraftCanvasConfigCommandPayload` source contract for generated contracts in 07-02
- `docs/canvas-coordinate-system.md`

<decision_map>
D-01: Use Jianying/pyJianYingDraft terminology and do not introduce Asset/Clip/Stage/SceneSize.
D-02: Draft schema field is Rust-owned and persisted as JSON `canvasConfig`; Rust may use `canvas_config` with serde camelCase.
D-03: Canvas config includes typed aspect ratio preset/custom, width, height, `RationalFrameRate`, and `CanvasBackground`.
D-04: Aspect ratio is explicit typed semantic state with validation for preset ratios and explicit custom ratios.
D-05: Background modes are 黑色/black, 纯色/solid color, 模糊填充/blur fill, 图片背景/image background with capability diagnostics.
D-06: Coordinate contract is center-origin, +X right, +Y up, 1.0 equals half canvas width/height; UI pixel space is top-left/+Y-down and must convert before persisted coordinate edits.
D-08: Image background reference shape is allowed only with validation rejecting invalid or missing material references; UI material picker remains disabled/deferred.
D-10: Desktop visible copy is Simplified Chinese; schema/Rust names stay Jianying-aligned.
D-11: No new npm or Rust packages.
</decision_map>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-01-01: Add draft canvas config model and validation</name>
  <files>crates/draft_model/src/canvas.rs, crates/draft_model/src/draft.rs, crates/draft_model/src/validation.rs, crates/draft_model/src/lib.rs, crates/draft_model/tests/canvas_config.rs</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-CONTEXT.md`
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-RESEARCH.md`
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-PATTERNS.md`
    - `crates/draft_model/src/draft.rs`
    - `crates/draft_model/src/material.rs`
    - `crates/draft_model/src/validation.rs`
    - `crates/draft_model/src/lib.rs`
  </read_first>
  <behavior>
    - Test 1: `Draft::new` creates 1920 x 1080, 30/1 fps, `16:9`, and black `canvasConfig`.
    - Test 2: validation rejects zero dimensions, invalid rational frame rate, mismatched preset ratio, invalid solid color, and bad image background material references.
    - Test 3: `UpdateDraftCanvasConfigCommandPayload` exists as a Rust source contract but no generated JSON/TS artifacts are modified in this plan.
  </behavior>
  <action>Create `crates/draft_model/src/canvas.rs` per D-01 through D-06 and D-08 using existing `serde`, `schemars`, `ts-rs`, camelCase, and `deny_unknown_fields` patterns. Define `DraftCanvasConfig` with `aspect_ratio`, `width`, `height`, `frame_rate: RationalFrameRate`, and `background: CanvasBackground`. Define typed presets `16:9`, `9:16`, `1:1`, `4:3`, `3:4` plus explicit custom ratio state; validate preset dimensions by reduced integer ratio and require custom ratio to remain explicit. Define `CanvasBackground` modes for black, solid color, blur fill, and image background; validate solid colors as hex strings and image background references by existing `MaterialId` and image material kind. Add `CanvasBackgroundCapability` or equivalent semantic capability status so blur/image can be represented as degraded or unsupported later without silent fake support. Add `Draft.canvas_config` serialized as JSON `canvasConfig` with the MVP default. Extend `validate_draft` and migration required-field checks for `canvasConfig`. Update `lib.rs` only for canvas module exports and the generated-contract source payload/command enum entries needed by 07-02. Do not touch fixtures, generated schema, or generated TypeScript in this plan.</action>
  <acceptance_criteria>
    `Draft` has required `canvas_config` serialized as `canvasConfig`. `DraftCanvasConfig::mvp_default()` or equivalent returns 1920 x 1080, 30/1, 16:9, black. Validation rejects malformed dimensions, fps, aspect ratio mismatches, invalid colors, and invalid image background material references. Generated artifact files are unchanged by this plan.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model canvas -- --nocapture</automated>
    <automated>cargo test -p draft_model draft_schema -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <done>Canonical draft canvas semantics exist in `draft_model` with focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-01-02: Document normalized canvas coordinate system</name>
  <files>crates/draft_model/src/canvas.rs, crates/draft_model/tests/canvas_config.rs, docs/canvas-coordinate-system.md</files>
  <read_first>
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-CONTEXT.md`
    - `.planning/phases/07-project-canvas-space-and-coordinate-system/07-UI-SPEC.md`
    - `crates/draft_model/src/canvas.rs`
    - `crates/draft_model/tests/canvas_config.rs`
  </read_first>
  <behavior>
    - Test 1: coordinate helper tests prove center, edges, and UI top-left/+Y-down conversion for 16:9, 9:16, and 1:1 canvases.
    - Test 2: docs state origin center, +X right, +Y up, and 1.0 equals half canvas width/height.
    - Test 3: docs state renderer/UI pixel offsets are derived display values, not persisted semantics.
  </behavior>
  <action>Add `docs/canvas-coordinate-system.md` with the exact D-06 coordinate contract from the UI spec: origin at canvas center, +X right, +Y up, `1.0` equals half of canvas width on X and half of canvas height on Y, center `(0,0)`, right/top edges `1`, and left/bottom edges `-1`. Include UI pixel conversion guidance for top-left-origin/+Y-down preview pixels. Add or extend Rust helper tests in `canvas_config.rs` for normalized-to-pixel and pixel-to-normalized round trips across horizontal, vertical, and square canvases. Do not add transform, keyframe, text safe-area, sticker, PIP, crop, mask, or segment-level background filling implementation.</action>
  <acceptance_criteria>
    The coordinate document is present and contains the locked contract. Rust tests cover conversion behavior deterministically. No Phase 08+ transform/keyframe/effect/transition semantics are implemented.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model canvas -- --nocapture</automated>
    <automated>rg -n "origin at canvas center|\\+X right|\\+Y up|half canvas width|half canvas height|top-left" docs/canvas-coordinate-system.md</automated>
  </verify>
  <done>Normalized coordinate semantics are documented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project JSON -> Rust draft model | Untrusted `.veproj/project.json` data enters serde/migration/validation. |
| material registry -> image background reference | Canvas background references must only point at valid image materials. |
| canvas math -> later visual systems | Later visual systems depend on this coordinate contract without redefining it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `migrate_draft_json` / `validate_draft` | mitigate | Require `canvasConfig`; reject unknown fields, zero dimensions/fps, mismatched aspect presets, invalid colors, and invalid image material references. |
| T-07-02 | Spoofing | image background material IDs | mitigate | Validate that image background `materialId` exists and has image material kind before accepting the draft. |
| T-07-03 | Repudiation | coordinate contract | mitigate | Store the coordinate contract in docs and focused tests so later phases can cite and enforce it. |
| T-07-SC | Tampering | package installs | mitigate | No npm/Rust packages are installed in this plan per D-11. |
</threat_model>

<verification>
<automated>cargo test -p draft_model canvas -- --nocapture</automated>
<automated>cargo test -p draft_model draft_schema -- --nocapture</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- CANVAS-01: Draft has canonical canvas aspect, dimensions, and rational frame rate.
- CANVAS-02: Draft has semantic black, solid color, blur fill, and image background modes with validation/capability boundaries.
- CANVAS-03: normalized coordinate semantics are documented and tested.
- CANVAS-04: Rust source command payload types exist for generated contracts in 07-02.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 7 | Establish draft-level project canvas space | 07-01 | COVERED | Foundation model, validation, and coordinate docs.
REQ | CANVAS-01 | Canonical canvas/profile model | 07-01 | COVERED | `Draft.canvas_config`, dimensions, aspect ratio, fps.
REQ | CANVAS-02 | Semantic canvas background modes | 07-01 | COVERED | Background enum and validation/capability status.
REQ | CANVAS-03 | Shared coordinate semantics | 07-01 | COVERED | Coordinate docs and tests.
REQ | CANVAS-04 | Command-owned UI foundation | 07-01 | COVERED | Source payload/command contract only; no UI implementation here.
RESEARCH | Draft-owned `canvasConfig` and normalized coordinates | 07-01 | COVERED | Tasks 1 and 2.
CONTEXT | D-01..D-06, D-08, D-10, D-11 | 07-01 | COVERED | Cited in decision map and task actions.
</source_audit>

<output>
Create `.planning/phases/07-project-canvas-space-and-coordinate-system/07-01-SUMMARY.md` when done.
</output>
