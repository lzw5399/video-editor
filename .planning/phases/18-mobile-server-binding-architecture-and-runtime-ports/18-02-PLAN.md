---
phase: 18-mobile-server-binding-architecture-and-runtime-ports
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase18-source-guards.sh
  - scripts/phase18-abi-drift.sh
  - scripts/phase18-mobile-contract-guards.sh
  - package.json
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - BIND-01
  - BIND-02
  - BIND-03
  - BIND-04
  - BIND-05
must_haves:
  truths:
    - "Phase 18 has executable source guards for duplicated adapter semantics, adapter-owned lifetime policy, Electron/main render behavior, and fallback/mock/artifact success per D-03, D-09, D-16, and D-17."
    - "Header drift is checked through a pinned cbindgen 0.29.4 invocation per D-10 and BIND-05."
    - "Aggregate scripts include `pnpm run test:phase18`, `pnpm run test:no-product-fallback`, and `pnpm run test:contracts` per 18-VALIDATION.md."
  artifacts:
    - path: "scripts/phase18-source-guards.sh"
      provides: "Architecture source guard"
      contains: "phase18 source guard violation"
    - path: "scripts/phase18-abi-drift.sh"
      provides: "Pinned cbindgen header drift gate"
      contains: "cbindgen 0.29.4"
    - path: "scripts/phase18-mobile-contract-guards.sh"
      provides: "Mobile lifecycle contract guard"
      contains: "mobile runtime contract"
    - path: "package.json"
      provides: "Phase 18 aggregate test scripts"
      contains: "test:phase18"
  key_links:
    - from: "package.json"
      to: "scripts/phase18-source-guards.sh"
      via: "test:phase18-source-guards"
      pattern: "test:phase18-source-guards"
    - from: "package.json"
      to: "scripts/phase18-abi-drift.sh"
      via: "test:phase18-abi"
      pattern: "test:phase18-abi"
    - from: "package.json"
      to: "scripts/phase18-mobile-contract-guards.sh"
      via: "test:phase18-mobile-contracts"
      pattern: "test:phase18-mobile-contracts"
user_setup: []
---

<objective>
Add the Phase 18 guard and validation scaffold before adapter/server implementation.

Purpose: The shared runtime split must fail if an executor reintroduces Node-owned semantics, C/server duplication, Electron render/export behavior, fallback success, or ABI drift. This plan creates the executable gates required by D-16 and D-17.
Output: Phase 18 source guard, ABI drift gate with pinned `cbindgen` 0.29.4, mobile contract guard, and root package scripts.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-CONTEXT.md
@.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-RESEARCH.md
@.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-VALIDATION.md
@.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-PATTERNS.md
@docs/no-product-fallback-policy.md
@docs/refactor-and-legacy-cleanup-policy.md
@docs/product-e2e-acceptance-policy.md
@scripts/phase17-1-source-guards.sh
@scripts/phase16-source-guards.sh
@scripts/no-product-fallback-guards.sh
@package.json
@.planning/phases/17.1-interaction-session-and-template-import-main-chain-hardening/17.1-06-SUMMARY.md
</context>

## Artifacts This Phase Produces

- `scripts/phase18-source-guards.sh` with negative self-tests and final architecture scans.
- `scripts/phase18-abi-drift.sh` with pinned `cbindgen` 0.29.4 header regeneration and diff checking.
- `scripts/phase18-mobile-contract-guards.sh` for JNI/Swift lifecycle and handle contract coverage.
- Root scripts `test:phase18-rust`, `test:phase18-source-guards`, `test:phase18-abi`, `test:phase18-server`, `test:phase18-mobile-contracts`, and `test:phase18`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 18 architecture source guard</name>
  <files>scripts/phase18-source-guards.sh</files>
  <read_first>
    @scripts/phase17-1-source-guards.sh
    @scripts/phase16-source-guards.sh
    @scripts/no-product-fallback-guards.sh
    @.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: Guard self-test catches an injected `bindings_c` dependency on `bindings_node`.
    - Test 2: Guard self-test catches injected server runtime references to Electron, BrowserWindow, preload, DOM, or desktop app source.
    - Test 3: Guard self-test catches injected renderer/main render graph, FFmpeg job construction, fallback/mock/artifact success, and adapter-owned lifetime policy.
  </behavior>
  <action>Create `scripts/phase18-source-guards.sh` in the established guard-helper style. Include comment-filtered matching, negative self-tests, required-file checks, and scans that fail per D-16 if: `bindings_c` depends on `bindings_node`; `server_runtime` imports Electron/Node adapter/desktop UI code; adapters contain duplicated project/export/session semantics instead of calling `editor_runtime`; Electron renderer/main constructs render graphs, FFmpeg jobs, export behavior, or fallback selectors; product success can be satisfied by mock, artifact, CPU readback, DOM, debug, or legacy paths per D-03 and D-09. The script should support a `--self-test` mode so this Wave 1 plan can verify guard patterns before later files exist, while full mode enforces final Phase 18 artifacts.</action>
  <acceptance_criteria>
    - `bash scripts/phase18-source-guards.sh --self-test` proves every forbidden family is detected and comment-only text is ignored.
    - Full guard mode names all Phase 18 required files and fails clearly when a required artifact is missing after its owning plan should have run.
    - Guard messages explain the ownership rule violated, not just the regex that matched.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase18-source-guards.sh --self-test</automated>
  </verify>
  <done>Architecture guard exists with negative self-tests for duplicated semantics, wrong dependencies, Electron-owned render/export behavior, and fallback product success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ABI and mobile contract guard scripts</name>
  <files>scripts/phase18-abi-drift.sh, scripts/phase18-mobile-contract-guards.sh</files>
  <read_first>
    @.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-RESEARCH.md
    @.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-VALIDATION.md
    @scripts/phase17-1-source-guards.sh
  </read_first>
  <behavior>
    - Test 1: ABI guard self-test verifies the configured tool version is exactly `cbindgen` 0.29.4 and rejects manual header drift.
    - Test 2: Mobile contract guard self-test verifies required sections for Android JNI lifecycle, Swift/ObjC C import ownership, sandboxed file handles, texture/device handles, cancellation, and session close.
    - Test 3: Neither script upgrades, reinstalls, or edits `@napi-rs/cli`; if a later executor needs that change, the plan must stop for a human checkpoint because research marked it SUS.
  </behavior>
  <action>Create `scripts/phase18-abi-drift.sh` with a reproducible project-local `cbindgen` 0.29.4 invocation per D-10 and BIND-05. The script must regenerate `crates/bindings_c/include/video_editor_runtime.h` from `crates/bindings_c/cbindgen.toml`, run `git diff --exit-code` on the header, and fail if the resolved `cbindgen` is not 0.29.4. Use the RESEARCH.md Package Legitimacy Audit: `cbindgen` is OK, `@napi-rs/cli` is SUS only for upgrade/reinstall, and no `@napi-rs/cli` change is permitted in this plan. Create `scripts/phase18-mobile-contract-guards.sh` to require the Phase 18 mobile contract doc sections and the `bindings_c` mobile handle smoke test name per D-11 and D-12.</action>
  <acceptance_criteria>
    - ABI script pins and verifies `cbindgen` 0.29.4 and checks generated header drift.
    - Mobile guard script fails if docs omit background/foreground, sandbox permission invalidation, file handles, texture/device identity, cancellation, explicit release, or cascading session close.
    - Scripts are executable, use `set -euo pipefail`, and follow existing guard output style.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase18-abi-drift.sh --self-test</automated>
    <automated>bash scripts/phase18-mobile-contract-guards.sh --self-test</automated>
  </verify>
  <done>ABI drift and mobile contract guard scripts exist and self-test without changing Node/NAPI tooling.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 18 aggregate package scripts</name>
  <files>package.json</files>
  <read_first>
    @package.json
    @.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-VALIDATION.md
    @.planning/phases/17.1-interaction-session-and-template-import-main-chain-hardening/17.1-06-SUMMARY.md
  </read_first>
  <action>Add root package scripts in the existing phase-script style: `test:phase18-rust`, `test:phase18-source-guards`, `test:phase18-abi`, `test:phase18-server`, `test:phase18-mobile-contracts`, and `test:phase18`. Compose `test:phase18` from Rust editor runtime/Node/C/server smoke tests, source guards, ABI drift, server export smoke, mobile contracts, `cargo check --workspace --locked`, `pnpm run test:no-product-fallback`, and `pnpm run test:contracts` per 18-VALIDATION.md. Do not add watch flags. Do not change `@napi-rs/cli`; if implementation discovers a package update is needed, stop and create a blocking checkpoint per the research warning.</action>
  <acceptance_criteria>
    - `package.json` contains every Phase 18 script named in 18-VALIDATION.md.
    - `test:phase18` includes `pnpm run test:no-product-fallback` and `pnpm run test:contracts`.
    - Script names are stable for downstream `$gsd-execute-phase` and `$gsd-verify-work`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm run test:phase18-source-guards -- --self-test</automated>
    <automated>pnpm run test:phase18-abi -- --self-test</automated>
    <automated>pnpm run test:phase18-mobile-contracts -- --self-test</automated>
  </verify>
  <done>Root scripts expose the Phase 18 per-wave and aggregate gates required by D-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code -> architecture claim | Passing tests must not hide adapter-owned semantics or fallback product success. |
| ABI source -> generated header | C callers rely on header layout matching exported Rust ABI. |
| Package tool resolution -> local execution | Tool install or upgrade could introduce a malicious or wrong package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | `scripts/phase18-source-guards.sh` | mitigate | Negative self-tests prove guard patterns catch forbidden adapter/server/Electron/fallback constructs. |
| T-18-06 | Repudiation | Phase aggregate scripts | mitigate | `test:phase18` records exact commands for Rust, ABI, server, mobile, no-fallback, and contracts evidence. |
| T-18-07 | Tampering | C ABI header | mitigate | Regenerate header with pinned cbindgen 0.29.4 and fail dirty diffs. |
| T-18-08 | Spoofing | Electron IPC/render behavior | mitigate | Source guard blocks renderer/main-owned render/export/fallback success and preserves allowed IPC sender checks in later plans. |
| T-18-SC | Tampering | cargo/npm installs | mitigate | Use RESEARCH.md Package Legitimacy Audit: `cbindgen` 0.29.4 OK; no `@napi-rs/cli` upgrade/reinstall without a blocking checkpoint. |
</threat_model>

<verification>
Run self-test modes after this plan, then run full `pnpm run test:phase18` after Plans 03-06 create the required artifacts.
</verification>

<success_criteria>
- Phase 18 guard and aggregate script names exist before adapter/server work depends on them.
- Pinned `cbindgen` header drift is enforceable.
- Product success cannot be satisfied by fallback/mock/artifact/CPU/DOM evidence.
</success_criteria>

<output>
Create `.planning/phases/18-mobile-server-binding-architecture-and-runtime-ports/18-02-SUMMARY.md` when done.
</output>
