---
phase: "08-segment-transform-and-visual-compositing"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "crates/draft_model/src/timeline.rs"
  - "crates/draft_model/src/validation.rs"
  - "crates/draft_model/src/lib.rs"
  - "crates/draft_commands/src/timeline.rs"
  - "crates/draft_commands/src/visual.rs"
  - "crates/draft_commands/src/lib.rs"
  - "crates/draft_commands/tests/visual_transform_commands.rs"
  - "crates/draft_model/tests/schema_exports.rs"
  - "schemas/draft.schema.json"
  - "schemas/command.schema.json"
  - "apps/desktop-electron/src/generated/Draft.ts"
  - "apps/desktop-electron/src/generated/CommandEnvelope.ts"
autonomous: true
requirements:
  - XFORM-01
  - XFORM-02
  - XFORM-03
  - LAYER-01
  - LAYER-03
user_setup: []
must_haves:
  truths:
    - "Segment has typed static `transform`, `fitMode`, `backgroundFilling`, `blendMode`, `mask`, and `visible` semantics."
    - "Transform edits are Rust-owned commands with validation, undo/redo, generated contracts, and atomic rejection."
    - "Persisted transform coordinates are normalized canvas semantics, not renderer pixel offsets."
  artifacts:
    - path: "crates/draft_model/src/timeline.rs"
      provides: "segment visual semantics"
    - path: "crates/draft_commands/src/visual.rs"
      provides: "Rust-owned visual transform command"
  key_links:
    - from: "crates/draft_model/src/lib.rs"
      to: "crates/draft_commands/src/visual.rs"
      via: "UpdateSegmentVisualCommandPayload"
      pattern: "updateSegmentVisual"
---

<objective>
Add typed segment visual semantics and a Rust-owned command route.
</objective>

<context>
@AGENTS.md
@.planning/phases/08-segment-transform-and-visual-compositing/08-CONTEXT.md
@.planning/phases/08-segment-transform-and-visual-compositing/08-RESEARCH.md
@crates/draft_model/src/timeline.rs
@crates/draft_model/src/validation.rs
@crates/draft_commands/src/timeline.rs
@crates/draft_commands/src/canvas.rs
</context>

## Artifacts this phase produces

- `SegmentVisual`
- `SegmentTransform`
- `SegmentPosition`
- `SegmentScale`
- `SegmentRotation`
- `SegmentOpacity`
- `SegmentCrop`
- `SegmentAnchor`
- `SegmentFitMode`
- `SegmentBackgroundFilling`
- `SegmentBlendMode`
- `SegmentMask`
- `UpdateSegmentVisualCommandPayload`
- `update_segment_visual`

<tasks>

<task type="auto" tdd="true">
  <name>Task 08-01-01: Add typed visual segment model and validation</name>
  <files>crates/draft_model/src/timeline.rs, crates/draft_model/src/validation.rs, crates/draft_model/src/lib.rs, crates/draft_model/tests/schema_exports.rs</files>
  <read_first>
    - `crates/draft_model/src/timeline.rs`
    - `crates/draft_model/src/validation.rs`
    - `crates/draft_model/src/canvas.rs`
    - `.planning/phases/08-segment-transform-and-visual-compositing/08-RESEARCH.md`
  </read_first>
  <action>Add static typed visual fields to `Segment` with defaults that preserve MVP rendering: `visible=true`, centered transform, `scale=1000`, `opacity=1000`, `rotation=0`, zero crop, center anchor, `fitMode=stretch`, `backgroundFilling=none`, `blendMode=normal`, `mask=none`. Use integer normalized units: position `x/y` where 1000 equals canvas edge; scale/opacity/crop/anchor use millesimal values. Validate opacity <= 1000, crop insets <= 1000 and opposing crop sums < 1000, scale values > 0, anchor in range, and unsupported blend/mask names non-empty. Export all types through Rust schema/TS generation.</action>
  <acceptance_criteria>
    New `Segment::new` contains default visual semantics. Invalid visual values fail `validate_draft`. Generated schema and TS include the visual types.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model visual -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 08-01-02: Add Rust-owned updateSegmentVisual command</name>
  <files>crates/draft_model/src/lib.rs, crates/draft_commands/src/visual.rs, crates/draft_commands/src/timeline.rs, crates/draft_commands/src/lib.rs, crates/draft_commands/tests/visual_transform_commands.rs, schemas/command.schema.json, apps/desktop-electron/src/generated/CommandEnvelope.ts</files>
  <read_first>
    - `crates/draft_commands/src/canvas.rs`
    - `crates/draft_commands/src/timeline.rs`
    - `crates/draft_commands/src/history.rs`
    - `crates/draft_model/src/lib.rs`
  </read_first>
  <action>Add `updateSegmentVisual` command payload with `draft`, `commandState`, `selection`, `segmentId`, and `visual`. Implement `draft_commands::visual::update_segment_visual` by cloning the draft, locating the segment, replacing `segment.visual`, validating timeline/draft rules, pushing undo snapshot, preserving selection, and emitting a `segmentVisualUpdated` event. Route it through `execute_timeline_edit`, generated contracts, and bindings in the next plan if needed.</action>
  <acceptance_criteria>
    Command updates only through Rust, supports undo/redo, rejects invalid values atomically, and generated command contracts include `updateSegmentVisual`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_commands visual_transform -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p draft_model visual -- --nocapture</automated>
<automated>cargo test -p draft_commands visual_transform -- --nocapture</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<source_audit>
REQ | XFORM-01 | typed transform | 08-01 | COVERED
REQ | XFORM-02 | fit/fill/stretch/background filling semantics | 08-01 | COVERED
REQ | XFORM-03 | command ownership | 08-01 | COVERED
REQ | LAYER-01 | visibility/layer semantic field | 08-01 | PARTIAL
REQ | LAYER-03 | blend/mask boundaries | 08-01 | COVERED
</source_audit>

<output>
Create `.planning/phases/08-segment-transform-and-visual-compositing/08-01-SUMMARY.md` when done.
</output>
