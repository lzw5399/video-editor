---
phase: "08-segment-transform-and-visual-compositing"
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - "crates/ffmpeg_compiler/src/filters.rs"
  - "crates/ffmpeg_compiler/tests/ffmpeg_job_snapshots.rs"
  - "crates/ffmpeg_compiler/tests/transform_snapshots.rs"
  - "apps/desktop-electron/src/renderer/App.tsx"
autonomous: true
requirements:
  - XFORM-01
  - XFORM-02
  - LAYER-02
  - LAYER-03
user_setup: []
must_haves:
  truths:
    - "FFmpeg compiler uses render graph visual semantics for supported placement/scale/crop/opacity subset."
    - "Preview/export derived state is invalidated after successful visual transform edits."
  artifacts:
    - path: "crates/ffmpeg_compiler/src/filters.rs"
      provides: "transform-aware filter script generation"
    - path: "apps/desktop-electron/src/renderer/App.tsx"
      provides: "derived preview/export invalidation on transform edits"
---

<objective>
Compile the supported transform subset and clear stale derived UI state after visual edits.
</objective>

<context>
@crates/ffmpeg_compiler/src/filters.rs
@crates/render_graph/src/graph.rs
@apps/desktop-electron/src/renderer/App.tsx
</context>

## Artifacts this phase produces

- transform-aware filter script helper functions
- visual edit derived-state invalidation branch

<tasks>

<task type="auto" tdd="true">
  <name>Task 08-03-01: Compile supported visual transform subset</name>
  <files>crates/ffmpeg_compiler/src/filters.rs, crates/ffmpeg_compiler/tests/ffmpeg_job_snapshots.rs, crates/ffmpeg_compiler/tests/transform_snapshots.rs</files>
  <read_first>
    - `crates/ffmpeg_compiler/src/filters.rs`
    - `crates/render_graph/src/graph.rs`
    - `crates/ffmpeg_compiler/tests/ffmpeg_job_snapshots.rs`
  </read_first>
  <action>Replace unconditional `scale={outputWidth}:{outputHeight}` plus `overlay=x=0:y=0` with a helper that compiles fit/fill/stretch, crop, scale, normalized position, and opacity for each visual layer. Keep default `stretch` output equal to current snapshots. Add transform-focused snapshot tests for a shifted/scaled/opacity layer and a fit-mode layer with background fill diagnostics. Classify unimplemented rotation/mask/blend beyond normal/none as diagnostics instead of fake support if not fully compiled.</action>
  <acceptance_criteria>
    Existing default compiler snapshots remain stable or are intentionally updated. New transform snapshots contain deterministic scale/crop/overlay/opacity filters and no renderer-owned FFmpeg strings.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ffmpeg_compiler transform -- --nocapture</automated>
    <automated>cargo test -p ffmpeg_compiler ffmpeg_job_snapshots -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

<task type="auto">
  <name>Task 08-03-02: Invalidate stale derived UI state after visual edits</name>
  <files>apps/desktop-electron/src/renderer/App.tsx</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/App.tsx`
  </read_first>
  <action>When `updateSegmentVisual` returns a successful timeline command response, clear preview frame, preview segment, export job/progress/result, validation, and related stale derived state using the same pattern as Phase 07 canvas updates.</action>
  <acceptance_criteria>
    Visual transform edits do not leave stale preview/export artifacts visible.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test:workspace -g "画面变换|旧预览|导出"</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p ffmpeg_compiler transform -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop test:workspace -g "画面变换"</automated>
</verification>

<output>
Create `.planning/phases/08-segment-transform-and-visual-compositing/08-03-SUMMARY.md` when done.
</output>
