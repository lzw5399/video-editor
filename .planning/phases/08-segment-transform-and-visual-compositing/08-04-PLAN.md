---
phase: "08-segment-transform-and-visual-compositing"
plan: "04"
type: execute
wave: 4
depends_on:
  - "08-01"
  - "08-03"
files_modified:
  - "crates/bindings_node/src/lib.rs"
  - "crates/bindings_node/tests/transform_commands.rs"
  - "apps/desktop-electron/src/renderer/commandHelpers.ts"
  - "apps/desktop-electron/src/renderer/workspace/Inspector.tsx"
  - "apps/desktop-electron/src/renderer/workspace/preview-inspector.css"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - XFORM-01
  - XFORM-02
  - XFORM-03
  - LAYER-01
user_setup: []
must_haves:
  truths:
    - "Right inspector selected-segment `画面` tab has editable Chinese transform controls."
    - "Renderer emits generated command envelopes only."
    - "No duplicate left primary menu is added."
  artifacts:
    - path: "apps/desktop-electron/src/renderer/workspace/Inspector.tsx"
      provides: "Jianying-style transform controls"
    - path: "apps/desktop-electron/tests/workspace.spec.ts"
      provides: "command-only transform UI tests"
---

<objective>
Expose selected-segment visual controls in the desktop inspector.
</objective>

<context>
@.planning/phases/08-segment-transform-and-visual-compositing/08-UI-SPEC.md
@apps/desktop-electron/src/renderer/workspace/Inspector.tsx
@apps/desktop-electron/src/renderer/commandHelpers.ts
@apps/desktop-electron/tests/workspace.spec.ts
</context>

## Artifacts this phase produces

- `buildUpdateSegmentVisualCommand`
- `handleUpdateSegmentVisual`
- Chinese selected-segment `基础` controls
- Playwright transform command tests

<tasks>

<task type="auto" tdd="true">
  <name>Task 08-04-01: Wire binding and renderer command helper</name>
  <files>crates/bindings_node/src/lib.rs, crates/bindings_node/tests/transform_commands.rs, apps/desktop-electron/src/renderer/commandHelpers.ts</files>
  <read_first>
    - `crates/bindings_node/src/lib.rs`
    - `crates/bindings_node/tests/canvas_commands.rs`
    - `apps/desktop-electron/src/renderer/commandHelpers.ts`
  </read_first>
  <action>Add `updateSegmentVisual` to the binding allowlist and timeline command routing. Add a binding test that sends a command envelope and receives updated draft visual values. Add `buildUpdateSegmentVisualCommand` in the renderer helper using generated types.</action>
  <acceptance_criteria>
    Binding and renderer helper route `updateSegmentVisual` through existing command APIs with no direct renderer mutation.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bindings_node transform_commands -- --nocapture</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 08-04-02: Build inspector transform controls and Playwright coverage</name>
  <files>apps/desktop-electron/src/renderer/workspace/Inspector.tsx, apps/desktop-electron/src/renderer/workspace/preview-inspector.css, apps/desktop-electron/tests/workspace.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/Inspector.tsx`
    - `apps/desktop-electron/src/renderer/workspace/preview-inspector.css`
    - `apps/desktop-electron/tests/workspace.spec.ts`
  </read_first>
  <action>Replace selected segment shell controls with editable compact rows for `位置`, `缩放`, `旋转`, `不透明度`, `适应方式`, `裁剪`, and `背景填充`. Add disabled/deferred rows for `混合模式` and `蒙版`. Use sliders plus numeric inputs where appropriate, keep fixed heights, and route apply/change through `onUpdateSelectedSegmentVisual`. Add Playwright tests for visibility, command call, derived state invalidation, 1280x800/1120x720 layout, and no duplicate left menu.</action>
  <acceptance_criteria>
    Selecting a segment shows Chinese transform controls. Editing at least one value calls `window.videoEditorCore.executeCommand` with `updateSegmentVisual`. Layout remains stable at both desktop viewports.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test:workspace -g "画面变换|command-only transform|五大区域"</automated>
  </verify>
  <done>Task complete with acceptance criteria and verification passing.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p bindings_node transform_commands -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop test:workspace -g "画面变换"</automated>
</verification>

<output>
Create `.planning/phases/08-segment-transform-and-visual-compositing/08-04-SUMMARY.md` when done.
</output>
