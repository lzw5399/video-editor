---
phase: "16-task-scheduler-job-isolation-and-performance-telemetry"
plan: "06"
type: execute
wave: 5
depends_on:
  - "16-02"
  - "16-03"
  - "16-04"
  - "16-05"
files_modified:
  - "scripts/phase16-source-guards.sh"
  - "scripts/no-product-fallback-guards.sh"
  - "package.json"
  - "apps/desktop-electron/tests/product-scheduler-stress.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "apps/desktop-electron/tests/helpers/realWorkflow.ts"
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
user_setup: []
must_haves:
  truths:
    - "SCHED-01/D-02/D-04/D-15/D-16: Source guards block renderer scheduler-policy mutation, scheduler bypasses, and fallback/mock/product-substitute success."
    - "SCHED-02/D-06/D-11/D-15: Product stress E2E proves visible preview/audio remain live during export/artifact/probe/IO pressure."
    - "SCHED-03/D-10: Package scripts run Rust scheduler, binding, guard, and desktop stress gates through explicit commands."
    - "SCHED-04/D-13/D-14: E2E asserts scheduler telemetry budgets while default product UI remains product-safe."
  artifacts:
    - path: "scripts/phase16-source-guards.sh"
      provides: "Scheduler ownership, renderer-policy, and bypass guards"
    - path: "scripts/no-product-fallback-guards.sh"
      provides: "Strengthened no-product-fallback scheduler success checks"
    - path: "apps/desktop-electron/tests/product-scheduler-stress.spec.ts"
      provides: "Normal product workflow stress gate"
    - path: "package.json"
      provides: "test:phase16-rust, test:phase16-source-guards, test:phase16-desktop, test:phase16"
  key_links:
    - from: "package.json"
      to: "scripts/phase16-source-guards.sh"
      via: "test:phase16-source-guards"
      pattern: "test:phase16-source-guards"
    - from: "apps/desktop-electron/tests/product-scheduler-stress.spec.ts"
      to: "apps/desktop-electron/src/main/realtimePreviewHost.ts"
      via: "product readiness evidence"
      pattern: "renderGraphGpuComposited"
---

<objective>
Add the Phase 16 source guards, aggregate package scripts, no-product-fallback
strengthening, and product scheduler stress E2E. This implements D-04, D-06,
D-10, D-11, D-13, D-14, D-15, D-16, and D-17 as executable product gates.

Purpose: make old binding-owned worker/heavy-work paths and fallback-success paths fail in automation, and prove the scheduler under a normal desktop workflow.
Output: source guard script, package scripts, strengthened fallback guard, product scheduler stress E2E, and helper updates.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-CONTEXT.md
@.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-RESEARCH.md
@.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-PATTERNS.md
@.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-VALIDATION.md
@docs/no-product-fallback-policy.md
@docs/refactor-and-legacy-cleanup-policy.md
@docs/product-e2e-acceptance-policy.md
@scripts/no-product-fallback-guards.sh
@scripts/phase15-3-source-guards.sh
@scripts/phase14-source-guards.sh
@apps/desktop-electron/tests/product-preview-cadence.spec.ts
@apps/desktop-electron/tests/helpers/userJourney.ts
@package.json
</context>

<must_haves>
  <truths>
    - D-02/D-04: legacy synchronous frame pump, binding-owned worker/thread, direct FFmpeg/probe/artifact, renderer policy mutation, and fallback-success paths are removed or gated from product paths.
    - D-15: product stress test fails the known bad state where export/artifact load blocks visible preview cadence or audio while tests still pass through playhead-only motion.
    - D-16: fallback, CPU probe, artifact, mock, or DOM evidence may explain diagnostics but cannot satisfy scheduler/preview success.
    - D-17: preview, artifact generation, export, media probe, and audio-preview boundaries are integrated far enough for cross-domain starvation and cancellation tests.
  </truths>
  <prohibitions>
    - Do not allow normal product E2E to pass with `VIDEO_EDITOR_TEST_MOCK_RUNTIME_CAPABILITIES`, mock export responses, artifact previews, or DOM-only motion as success.
    - Do not allow preload or renderer code to mutate scheduler capacities, queue depths, priorities, freshness, retry/fallback behavior, or FFmpeg/export policy.
    - Do not write source guards that target `media_runtime` internal helper threads used for stdout/stderr/progress; guard binding-owned policy bypasses instead.
    - Do not create a product-facing scheduler dashboard of raw queues.
  </prohibitions>
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 16-06-01: Add Phase 16 scheduler source guards</name>
  <files>scripts/phase16-source-guards.sh, scripts/no-product-fallback-guards.sh</files>
  <read_first>
    - `scripts/no-product-fallback-guards.sh`
    - `scripts/phase15-3-source-guards.sh`
    - `scripts/phase14-source-guards.sh`
    - `.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-PATTERNS.md`
  </read_first>
  <action>Create `scripts/phase16-source-guards.sh` in the existing guard style. The guard blocks binding-owned scheduler bypasses, direct binding FFmpeg/probe/artifact execution, renderer/preload scheduler policy mutation, product scheduler success from test mocks, and duplicate freshness definitions. Scope allowlists to scheduler/runtime internals, native/dev diagnostics config overrides, and executor-internal process helper threads only. Extend `scripts/no-product-fallback-guards.sh` so scheduler success cannot be satisfied by fallback/mock/artifact/CPU/DOM evidence. This implements D-02/D-04/D-15/D-16.</action>
  <verify>
    <automated>bash scripts/phase16-source-guards.sh</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <acceptance_criteria>
    - Guard fails if binding preview/audio/export/artifact/probe paths own scheduler worker policy.
    - Guard fails if product scheduler success can come from test runtime or export mocks.
    - Guard fails if preload/renderer code can set resource capacities, queue depths, priorities, freshness, retry/fallback behavior, or FFmpeg/export policy.
    - Guard fails if duplicate `PlaybackGeneration` definitions appear.
    - Guard does not fail legitimate `media_runtime` process helper threads.
  </acceptance_criteria>
  <done>Scheduler ownership and no-product-fallback rules are executable source gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-06-02: Add product scheduler stress E2E</name>
  <files>apps/desktop-electron/tests/product-scheduler-stress.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts, apps/desktop-electron/tests/helpers/realWorkflow.ts</files>
  <read_first>
    - `apps/desktop-electron/tests/product-preview-cadence.spec.ts`
    - `apps/desktop-electron/tests/product-user-journey.spec.ts`
    - `apps/desktop-electron/tests/real-workflow.spec.ts`
    - `apps/desktop-electron/tests/helpers/userJourney.ts`
    - `docs/product-e2e-acceptance-policy.md`
  </read_first>
  <action>Add a Playwright/Electron product workflow that creates or opens a project, imports repository-owned moving video and audio fixtures through the product path, adds material to the timeline, starts visible playback, starts export from the top-right modal, triggers artifact refresh and slow/saturated media probe load, scrubs the timeline while probes are queued or running, performs a normal right-inspector edit on a selected segment while that load is active, optionally exercises cancellation, and asserts visible preview motion plus scheduler telemetry budgets. The inspector edit must use a real product control such as transform position/scale/opacity, audio volume, or text property, then assert accepted command responsiveness, updated inspector/display state, no stalled preview, and no fallback success. The test must assert `renderGraphGpuComposited`, no fallback active, preview target-time advancement, frame pacing budget, scheduler queue latency/depth/resource saturation, queued/pending probe status, bounded import/probe and inspector command responsiveness, and audio no-underrun evidence where available. This implements D-06/D-11/D-13/D-15/D-16/D-17.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-scheduler-stress.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Test begins from normal product project entry or open-project workflow.
    - Test uses repository-owned fixtures, not machine-local manual files.
    - Test launches export from the production top-right modal.
    - Test fails if visible preview does not move while export/artifact/probe pressure is active.
    - Test fails if slow or saturated media probing blocks product preview/scrub/status responsiveness instead of returning queued/pending status.
    - Test selects a timeline segment and changes a visible inspector property during export/artifact/probe pressure.
    - Test fails if the inspector edit command or visible inspector/preview state is blocked by heavy scheduler load.
    - Test fails if fallback/mock/artifact/CPU/DOM evidence satisfies success.
    - Test reads scheduler telemetry through the product-safe/native diagnostic boundary added in Plan 16-05.
  </acceptance_criteria>
  <done>Product stress E2E proves scheduler isolation through the same workflow a user exercises.</done>
</task>

<task type="auto">
  <name>Task 16-06-03: Add Phase 16 aggregate package scripts</name>
  <files>package.json</files>
  <read_first>
    - `package.json`
    - `.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-VALIDATION.md`
    - `apps/desktop-electron/package.json`
  </read_first>
  <action>Add root scripts `test:phase16-rust`, `test:phase16-source-guards`, `test:phase16-desktop`, and `test:phase16` near existing phase scripts. `test:phase16-rust` runs `task_runtime`, relevant binding scheduler tests, and scheduler-adjacent domain tests. `test:phase16-source-guards` runs the new guard. `test:phase16-desktop` runs the product scheduler stress spec through the desktop package/build path. `test:phase16` composes Rust, source guards, no-product-fallback, desktop stress, contracts, and workspace check commands per the validation strategy.</action>
  <verify>
    <automated>pnpm run test:phase16-source-guards</automated>
    <automated>pnpm run test:phase16-rust</automated>
    <automated>pnpm run test:phase16-desktop</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains the four Phase 16 scripts.
    - `test:phase16` invokes `test:no-product-fallback`.
    - Script commands do not remove existing phase scripts.
    - Script commands use project-local package/build/test paths.
  </acceptance_criteria>
  <done>Phase 16 has explicit package-level gates for Rust, guards, desktop stress, and aggregate verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source guards -> product code | Guards enforce that product source cannot reintroduce scheduler bypasses or fallback success. |
| Product E2E -> desktop app | Normal user workflow crosses renderer/main/binding/Rust scheduler under load. |
| Test mocks -> product evidence | Test harness mocks must not satisfy product scheduler success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06A | Spoofing | product scheduler evidence | mitigate | E2E and no-product-fallback guards require `renderGraphGpuComposited`, visible motion, and scheduler telemetry; mock/fallback evidence fails. |
| T-16-06B | Denial of Service | export/artifact/probe stress | mitigate | Product stress launches background load while asserting preview/audio budgets and queue depth/saturation telemetry. |
| T-16-06C | Tampering | scheduler ownership regressions | mitigate | Source guards reject binding-owned worker/heavy-work bypasses and duplicate generation definitions. |
| T-16-06D | Repudiation | phase verification | mitigate | Package scripts create reproducible focused and aggregate gates for Phase 16. |
</threat_model>

<verification>
<automated>pnpm run test:phase16-source-guards</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>pnpm run test:phase16-rust</automated>
<automated>pnpm run test:phase16-desktop</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Phase 16 has executable source guards, no-product-fallback strengthening, package
scripts, and product stress E2E proving scheduler isolation under normal use.
</success_criteria>

## Artifacts this phase produces

- `scripts/phase16-source-guards.sh`
- Strengthened scheduler checks in `scripts/no-product-fallback-guards.sh`
- `apps/desktop-electron/tests/product-scheduler-stress.spec.ts`
- Product E2E helper updates for scheduler stress load
- `test:phase16-rust`
- `test:phase16-source-guards`
- `test:phase16-desktop`
- `test:phase16`

<output>
Create `.planning/phases/16-task-scheduler-job-isolation-and-performance-telemetry/16-06-SUMMARY.md` when done.
</output>
