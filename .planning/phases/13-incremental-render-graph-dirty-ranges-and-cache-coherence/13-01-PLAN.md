---
phase: "13-incremental-render-graph-dirty-ranges-and-cache-coherence"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "scripts/phase13-source-guards.sh"
  - "crates/draft_model/tests/schema_exports.rs"
  - "crates/draft_model/tests/contract.rs"
  - "crates/draft_commands/tests/command_delta.rs"
  - "crates/render_graph/tests/node_identity.rs"
  - "crates/preview_service/tests/dirty_propagation.rs"
  - "crates/testkit/src/lib.rs"
  - "crates/testkit/src/large_timeline.rs"
  - "crates/testkit/tests/large_timeline_incremental.rs"
autonomous: true
requirements:
  - INCR-01
  - INCR-02
  - INCR-03
  - INCR-04
  - INCR-05
must_haves:
  truths:
    - "Phase 13 has executable gates before behavior implementation starts."
    - "Source guards reject renderer-owned graph/cache/dirty/FFmpeg decisions per D-06."
    - "Large-timeline fixture helpers can create deterministic drafts using integer microseconds per D-07."
  artifacts:
    - path: "scripts/phase13-source-guards.sh"
      provides: "Phase 13 architecture guard checks"
    - path: "crates/testkit/src/large_timeline.rs"
      provides: "large draft fixture builder"
    - path: "crates/draft_commands/tests/command_delta.rs"
      provides: "command delta behavior test target"
    - path: "crates/render_graph/tests/node_identity.rs"
      provides: "render graph identity/fingerprint test target"
    - path: "crates/preview_service/tests/dirty_propagation.rs"
      provides: "dirty propagation/cache coherence test target"
  key_links:
    - from: "package.json"
      to: "scripts/phase13-source-guards.sh"
      via: "test:phase13-source-guards script"
      pattern: "test:phase13-source-guards"
    - from: "crates/testkit/src/lib.rs"
      to: "crates/testkit/src/large_timeline.rs"
      via: "module export"
      pattern: "pub mod large_timeline"
---

<objective>
Create the Phase 13 executable test harness, source guards, package scripts, and deterministic large-timeline fixture helpers.

Purpose: Make INCR-01 through INCR-05 measurable before the implementation plans touch command, graph, and cache behavior.
Output: Phase 13 guard script, package scripts, test targets, and shared large-timeline helper artifacts.
</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/notes/production-editor-architecture-decisions.md
@.planning/phases/13-incremental-render-graph-dirty-ranges-and-cache-coherence/13-CONTEXT.md
@.planning/phases/13-incremental-render-graph-dirty-ranges-and-cache-coherence/13-RESEARCH.md
@.planning/phases/13-incremental-render-graph-dirty-ranges-and-cache-coherence/13-DESIGN.md
@package.json
@justfile
@docs/runtime-boundaries.md
@crates/draft_model/tests/schema_exports.rs
@crates/draft_model/tests/contract.rs
@crates/preview_service/tests/cache_invalidation.rs
@crates/render_graph/tests/render_graph_snapshots.rs
@crates/testkit/tests/preview_export_parity.rs
</context>

## Artifacts this plan produces

- `scripts/phase13-source-guards.sh`
- `test:phase13-rust`, `test:phase13-source-guards`, and `test:phase13` package scripts
- `crates/testkit/src/large_timeline.rs`
- `testkit::large_timeline` module export
- `crates/draft_commands/tests/command_delta.rs`
- `crates/render_graph/tests/node_identity.rs`
- `crates/preview_service/tests/dirty_propagation.rs`
- `crates/testkit/tests/large_timeline_incremental.rs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 13-01-01: Add Phase 13 source guards and package gates</name>
  <files>package.json, scripts/phase13-source-guards.sh</files>
  <read_first>
    - `package.json`
    - `scripts/phase10-source-guards.sh`
    - `.planning/phases/13-incremental-render-graph-dirty-ranges-and-cache-coherence/13-DESIGN.md`
  </read_first>
  <action>Create `scripts/phase13-source-guards.sh` with strict shell settings and architecture checks for D-03, D-05, D-06, and D-07. The guard must reject naked `f32`/`f64` time in persisted or contract surfaces, renderer-owned dirty ranges/cache keys/render graph IDs/FFmpeg command construction, and derived artifact metadata leaking into `.veproj/project.json` schema or fixtures. Add root `package.json` scripts `test:phase13-rust`, `test:phase13-source-guards`, and `test:phase13` following the existing phase script style. Do not install packages or alter lockfiles.</action>
  <acceptance_criteria>
    The guard script is executable, package scripts are present, and guard categories cover renderer drift, float time, contract symbols, and derived-artifact leakage without modifying source code outside this plan's files.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/phase13-source-guards.sh</automated>
    <automated>node -e 'const p=require("./package.json"); for (const k of ["test:phase13-rust","test:phase13-source-guards","test:phase13"]) if (!p.scripts[k]) throw new Error(`${k} missing`);'</automated>
  </verify>
  <done>Phase 13 guard script and package gates exist with syntax and script wiring verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-01-02: Add contract and behavior test targets</name>
  <files>crates/draft_model/tests/schema_exports.rs, crates/draft_model/tests/contract.rs, crates/draft_commands/tests/command_delta.rs, crates/render_graph/tests/node_identity.rs, crates/preview_service/tests/dirty_propagation.rs</files>
  <read_first>
    - `crates/draft_model/tests/schema_exports.rs`
    - `crates/draft_model/tests/contract.rs`
    - `crates/draft_commands/tests/timeline_commands.rs`
    - `crates/render_graph/tests/render_graph_snapshots.rs`
    - `crates/preview_service/tests/cache_invalidation.rs`
  </read_first>
  <action>Create Phase 13 test targets that compile independently and establish the assertion locations for D-01 through D-04. Add helper assertions for command response serialization, schema/export contract lookup, dirty range half-open expectations, graph identity snapshots, and preview invalidation retention. Keep tests deterministic and integer-microsecond based per D-07. Each downstream Phase 13 plan must add its behavior assertions to these targets rather than scattering one-off tests.</action>
  <acceptance_criteria>
    Test targets exist and can be discovered by Cargo. They do not require renderer logic, FFmpeg runtime execution, SQLite artifact persistence, or scheduler behavior.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p draft_model contract -- --nocapture</automated>
    <automated>cargo test -p draft_commands --test command_delta -- --nocapture</automated>
    <automated>cargo test -p render_graph --test node_identity -- --nocapture</automated>
    <automated>cargo test -p preview_service --test dirty_propagation -- --nocapture</automated>
  </verify>
  <done>Phase 13 test targets compile and expose the contract, delta, graph identity, and dirty propagation test locations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-01-03: Add deterministic large-timeline fixtures</name>
  <files>crates/testkit/src/lib.rs, crates/testkit/src/large_timeline.rs, crates/testkit/tests/large_timeline_incremental.rs</files>
  <read_first>
    - `crates/testkit/src/lib.rs`
    - `crates/testkit/tests/material_fixtures.rs`
    - `crates/testkit/tests/preview_export_parity.rs`
    - `crates/draft_model/src/draft.rs`
    - `crates/draft_model/src/timeline.rs`
  </read_first>
  <action>Add a `testkit::large_timeline` helper module that builds deterministic drafts with hundreds or thousands of material-backed video, audio, and text segments using `Microseconds`, `TargetTimerange`, and `RationalFrameRate` only. Include fixture knobs for segment count, track mix, localized edit target, and canvas profile. Add smoke tests proving fixture determinism, no overlaps, and stable IDs. This prepares INCR-05 without introducing Phase 14 artifact storage or Phase 16 scheduling per D-05.</action>
  <acceptance_criteria>
    Large-timeline helpers produce valid drafts with stable IDs and localized edit coordinates, and they compile without FFmpeg, SQLite, Electron, or scheduler dependencies.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p testkit large_timeline -- --nocapture</automated>
    <automated>cargo test -p draft_model draft_fixtures -- --nocapture</automated>
  </verify>
  <done>Large-timeline fixtures are available through `testkit` and validated by deterministic smoke tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production crates | Tests may encode incorrect expectations if they bypass Rust-owned semantics. |
| renderer source -> Rust command/core boundary | Renderer must not compute dirty ranges, graph diffs, cache keys, invalidation decisions, or FFmpeg commands per D-06. |
| generated contracts -> persisted draft schema | Derived artifact metadata must not become canonical `.veproj/project.json` semantics per D-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `scripts/phase13-source-guards.sh` | mitigate | Add explicit grep guards for renderer-owned dirty/cache/render/FFmpeg decisions and run the guard in final phase gates. |
| T-13-02 | DoS | large-timeline fixtures | mitigate | Bound fixture construction, use deterministic counts, and avoid runtime/FFmpeg execution in fixture tests. |
| T-13-03 | Tampering | contract/schema gates | mitigate | Require generated contracts to expose Phase 13 symbols while draft schema remains free of derived artifact metadata. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Phase 13 requires no package installation; Package Legitimacy Audit says no install needed. |
</threat_model>

<verification>
<automated>bash -n scripts/phase13-source-guards.sh</automated>
<automated>cargo test -p testkit large_timeline -- --nocapture</automated>
<automated>cargo test -p draft_commands --test command_delta -- --nocapture</automated>
<automated>cargo test -p render_graph --test node_identity -- --nocapture</automated>
<automated>cargo test -p preview_service --test dirty_propagation -- --nocapture</automated>
</verification>

<source_audit>
GOAL | Phase 13 | Incremental semantic timeline and render graph updates | 13-01..13-06 | COVERED
REQ | INCR-01 | Stable graph identities are test-gated | 13-01, 13-04, 13-06 | COVERED
REQ | INCR-02 | CommandDelta contract is test-gated | 13-01, 13-02, 13-03 | COVERED
REQ | INCR-03 | Dirty propagation consumers are test-gated | 13-01, 13-05, 13-06 | COVERED
REQ | INCR-04 | Undo/redo coherence is test-gated | 13-01, 13-03, 13-06 | COVERED
REQ | INCR-05 | Large-timeline tests and helpers exist | 13-01, 13-06 | COVERED
CONTEXT | D-01 | CommandDelta response contract | 13-02, 13-03 | COVERED
CONTEXT | D-02 | Node identity separate from fingerprints | 13-04 | COVERED
CONTEXT | D-03 | Dirty propagation across preview/export/audio/thumb/wave/proxy/snapshot/cache | 13-03, 13-05 | COVERED
CONTEXT | D-04 | Undo/redo semantic restore then exact snapshot or deterministic invalidation | 13-03, 13-04 | COVERED
CONTEXT | D-05 | Phase 13 in-memory contracts only; Phase 14 SQLite and Phase 16 scheduler excluded | 13-01..13-06 | COVERED
CONTEXT | D-06 | UI/renderer does not compute graph/cache/dirty/FFmpeg decisions | 13-01, 13-05, 13-06 | COVERED
CONTEXT | D-07 | No naked float time in persisted or contract surfaces | 13-01..13-06 | COVERED
RESEARCH | Package Legitimacy Audit | No external packages installed | 13-01..13-06 | COVERED
</source_audit>

<success_criteria>
Plan 13-01 is complete when Phase 13 gates and fixtures exist, all listed verification commands pass, and no files outside the plan's declared source paths are modified by execution.
</success_criteria>

<output>
Create `.planning/phases/13-incremental-render-graph-dirty-ranges-and-cache-coherence/13-01-SUMMARY.md` when done.
</output>
