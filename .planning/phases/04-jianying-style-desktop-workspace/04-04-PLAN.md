---
phase: 04-jianying-style-desktop-workspace
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - apps/desktop-electron/tests/electron-smoke.spec.ts
  - apps/desktop-electron/tests/workspace.spec.ts
  - apps/desktop-electron/package.json
  - package.json
  - justfile
  - scripts/phase4-source-guards.sh
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - TEST-06
must_haves:
  truths:
    - "Playwright Electron verifies the app opens directly to Chinese workspace regions `顶部功能区`, `素材面板`, `预览窗口`, `属性检查器`, and `时间线` per D-16, D-17, UI-01, and UI-06."
    - "Playwright verifies visible categories, panels, inspector state, material rows, command-only timeline edit, and layout stability at 1280x800 and 1120x720 per D-16 and D-17."
    - "Source guards prevent direct renderer mutation, Electron/Node imports in renderer, renderer FFmpeg/ffprobe construction, English-only key labels, and generated-contract drift per D-18."
    - "Phase 4-specific scripts are wired into root `test` and `just test` gates per D-19."
  artifacts:
    - path: apps/desktop-electron/tests/electron-smoke.spec.ts
      provides: "preload bridge, IPC allowlist, and generated executeCommand smoke updated for Chinese workspace"
    - path: apps/desktop-electron/tests/workspace.spec.ts
      provides: "Chinese workspace, panels, command-only timeline, and layout stability Playwright tests"
    - path: scripts/phase4-source-guards.sh
      provides: "source guard script for renderer mutation, privileged imports, FFmpeg leakage, English labels, and generated headers"
    - path: package.json
      provides: "root Phase 4 source guard and workspace test scripts included in test gate"
    - path: apps/desktop-electron/package.json
      provides: "desktop focused workspace Playwright script"
    - path: justfile
      provides: "Phase 4 scripts included in public just test gate"
  key_links:
    - from: package.json
      to: scripts/phase4-source-guards.sh
      via: "test:phase4-source-guards"
      pattern: "test:phase4-source-guards"
    - from: apps/desktop-electron/package.json
      to: apps/desktop-electron/tests/workspace.spec.ts
      via: "test:workspace"
      pattern: "test:workspace"
    - from: apps/desktop-electron/tests/workspace.spec.ts
      to: apps/desktop-electron/src/renderer/workspace/Timeline.tsx
      via: "command-only timeline flow"
      pattern: "command-only timeline|命令"
---

<objective>
Add Electron IPC contract tests, Playwright workspace smoke/layout coverage, Phase 4 source guards, and final gate wiring.

Purpose: Phase 4 is only complete when the Chinese workspace, command-only UI boundary, stable layout, and generated-contract discipline are executable. This plan closes UI-01 through UI-06 and the Phase 4 subset of TEST-06 while explicitly leaving deterministic preview/export execution to Phase 5/6 roadmap dependencies.

Output: Updated Electron smoke, new workspace Playwright test, source guard script, package scripts, `just test` inclusion, and final verification commands.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-jianying-style-desktop-workspace/04-CONTEXT.md
@.planning/phases/04-jianying-style-desktop-workspace/04-RESEARCH.md
@.planning/phases/04-jianying-style-desktop-workspace/04-UI-SPEC.md
@.planning/phases/04-jianying-style-desktop-workspace/04-PATTERNS.md
@.planning/phases/04-jianying-style-desktop-workspace/04-VALIDATION.md
@.planning/phases/03-timeline-command-core/03-VERIFICATION.md
@apps/desktop-electron/tests/electron-smoke.spec.ts
@apps/desktop-electron/src/main/index.ts
@apps/desktop-electron/src/preload/index.ts
@apps/desktop-electron/src/renderer/App.tsx
@apps/desktop-electron/src/renderer/styles.css
@apps/desktop-electron/src/generated/CommandEnvelope.ts
@apps/desktop-electron/src/generated/CommandResultEnvelope.ts
@apps/desktop-electron/src/generated/Draft.ts
@apps/desktop-electron/package.json
@package.json
@justfile
</context>

## Artifacts this phase produces

- New file: `apps/desktop-electron/tests/workspace.spec.ts`.
- New file: `scripts/phase4-source-guards.sh`.
- Modified files: `apps/desktop-electron/tests/electron-smoke.spec.ts`, `apps/desktop-electron/package.json`, `package.json`, `justfile`.
- Created test helpers/symbols/functions: `launchWorkspaceApp`, `expectVisibleWorkspaceRegions`, `expectNoOverlap`, `expectStableBox`, `setViewportSizeAndVerifyLayout`, `spyExecuteCommandCalls`.
- Created scripts: root `test:phase4-source-guards`, root `test:phase4-workspace`, desktop `test:workspace`, and public `just test` inclusion.

## Source Coverage Audit

| Source | ID | Item | Plan Coverage | Status |
|--------|----|------|---------------|--------|
| GOAL | Phase 04 | Build desktop editor workspace with Jianying-like structure and command-only Rust integration | 04-01 to 04-04 | COVERED |
| REQ | UI-01 | First screen workspace regions | 04-01, 04-04 | COVERED |
| REQ | UI-02 | Material, text, audio panels and reserved categories | 04-02, 04-04 | COVERED |
| REQ | UI-03 | Jianying terms and no alternate jargon | 04-01, 04-02, 04-04 | COVERED |
| REQ | UI-04 | Typed Rust commands, no direct draft mutation | 04-02, 04-03, 04-04 | COVERED |
| REQ | UI-05 | Stable timeline/panel dimensions | 04-01, 04-03, 04-04 | COVERED |
| REQ | UI-06 | Simplified Chinese visible language | 04-01, 04-02, 04-04 | COVERED |
| REQ | TEST-06 | Electron E2E import/edit/preview/export | 04-04 and later phases | PARTIAL BY ROADMAP DEPENDENCY |
| RESEARCH | No new UI packages; existing package legitimacy audit approved current stack | 04-01 to 04-04 | COVERED |
| RESEARCH | Source guards for English labels, direct mutation, privileged imports, FFmpeg leakage, generated drift | 04-04 | COVERED |
| RESEARCH | Playwright layout at 1280x800 and 1120x720 | 04-04 | COVERED |
| CONTEXT | D-01 through D-19 | Locked Phase 4 workspace, language, panels, command boundary, and verification decisions | 04-01 to 04-04 | COVERED |
| CONTEXT | Deferred preview/export/render features | Phase 4 validates preview shell only; deterministic preview/export depend on Phase 5/6 | 04-04 | COVERED AS DEPENDENCY CONFLICT |

TEST-06 note: Phase 4 can and must prove the Electron workspace subset: material import/list UI path, command-only timeline edit, and preview shell. Deterministic preview frames, render graph, FFmpeg export, output metadata verification, and packaged import-preview-export smoke are assigned to Phase 5 and Phase 6 in ROADMAP.md; this plan must not fake those behaviors or create renderer FFmpeg/export semantics.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Electron smoke for Chinese workspace and preload IPC contract</name>
  <read_first>
    @.planning/phases/04-jianying-style-desktop-workspace/04-CONTEXT.md
    @.planning/phases/04-jianying-style-desktop-workspace/04-RESEARCH.md
    @.planning/phases/04-jianying-style-desktop-workspace/04-VALIDATION.md
    @apps/desktop-electron/tests/electron-smoke.spec.ts
    @apps/desktop-electron/src/main/index.ts
    @apps/desktop-electron/src/preload/index.ts
    @apps/desktop-electron/src/generated/CommandEnvelope.ts
    @apps/desktop-electron/src/generated/CommandResultEnvelope.ts
  </read_first>
  <files>apps/desktop-electron/tests/electron-smoke.spec.ts</files>
  <action>
    Update `electron-smoke.spec.ts` so it still launches `dist/main/index.cjs`, verifies context-isolated preload behavior, verifies untrusted navigation cannot access native APIs, and verifies `window.videoEditorCore` exposes only `ping`, `version`, and `executeCommand` unless an earlier Phase 4 implementation has added an explicitly planned non-mutating shell helper. The test must assert the first window contains `剪映风格编辑工作区` and Chinese workspace regions instead of `Video editor smoke workbench`. Keep the existing generated `CommandEnvelope` ping command check. Remove English smoke material-row assertions and replace them with Chinese workspace readiness assertions that do not require preview/export implementation. Preserve main-process non-loopback URL rejection and untrusted navigation coverage.
  </action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop playwright test tests/electron-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    Electron smoke proves the Chinese workspace opens through the existing main/preload/native path, raw `ipcRenderer` is not exposed, untrusted navigation cannot use the bridge, and generated `executeCommand` still works.
  </acceptance_criteria>
  <done>Electron smoke contract is updated for Phase 4 workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add workspace Playwright flow and layout tests</name>
  <read_first>
    @.planning/phases/04-jianying-style-desktop-workspace/04-UI-SPEC.md
    @.planning/phases/04-jianying-style-desktop-workspace/04-VALIDATION.md
    @.planning/phases/04-jianying-style-desktop-workspace/04-PATTERNS.md
    @apps/desktop-electron/tests/electron-smoke.spec.ts
    @apps/desktop-electron/src/renderer/App.tsx
    @apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx
    @apps/desktop-electron/src/renderer/workspace/Inspector.tsx
    @apps/desktop-electron/src/renderer/workspace/Timeline.tsx
  </read_first>
  <files>apps/desktop-electron/tests/workspace.spec.ts, apps/desktop-electron/package.json</files>
  <action>
    Create `apps/desktop-electron/tests/workspace.spec.ts` using the existing Playwright Electron launch pattern. Add tests named so `-g "Chinese editor workspace"`, `-g "workspace panels"`, `-g "command-only timeline"`, and `-g "layout stability"` run meaningful subsets. The Chinese workspace test must assert visible regions `顶部功能区`, `素材面板`, `预览窗口`, `属性检查器`, `时间线`; visible categories `媒体`, `音频`, `文字`, `贴纸`, `特效`, `转场`, `滤镜`, `调节`; preview copy `预览将在下一阶段接入`; no-selection copy `未选择片段`; and material/status labels when rows exist: `视频`, `图片`, `音频`, `可用`, `素材丢失`, `解析失败`. The panels test must exercise category switching and verify deferred categories show Chinese empty states without disappearing. The command-only timeline test must prove at least one visible timeline edit calls `window.videoEditorCore.executeCommand` with a generated timeline command and updates UI from a `TimelineCommandResponse`; use Playwright-side bridge spying only to observe calls, not to replace the Rust core as the accepted edit source unless native binding setup is unavailable, in which case the test must fail with a clear setup error. The layout stability test must set viewport sizes to `1280x800` and `1120x720`, assert top/left/preview/inspector/timeline bounding boxes are non-null, visible, non-overlapping, and not clipped, then trigger hover/selection/playback display state and assert key boxes retain stable dimensions. Add a desktop script `test:workspace` to `apps/desktop-electron/package.json` that builds then runs `playwright test tests/workspace.spec.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test:workspace</automated>
  </verify>
  <acceptance_criteria>
    Playwright verifies Chinese workspace structure, panels, command-only timeline edit, and layout stability at required viewport sizes. The tests do not require Phase 5 deterministic preview frames or Phase 6 packaged export.
  </acceptance_criteria>
  <done>Workspace Playwright flow and layout tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Phase 4 source guards and final gate wiring</name>
  <read_first>
    @.planning/phases/04-jianying-style-desktop-workspace/04-RESEARCH.md
    @.planning/phases/04-jianying-style-desktop-workspace/04-VALIDATION.md
    @package.json
    @justfile
    @apps/desktop-electron/package.json
    @apps/desktop-electron/src/generated/CommandEnvelope.ts
    @apps/desktop-electron/src/generated/CommandResultEnvelope.ts
    @apps/desktop-electron/src/generated/Draft.ts
  </read_first>
  <files>scripts/phase4-source-guards.sh, package.json, justfile, apps/desktop-electron/package.json</files>
  <action>
    Create `scripts/phase4-source-guards.sh` and make it executable. The script must use `rg` and explicit allowlists, not `grep -c`. Scope the English-copy guard to user-facing string literal surfaces per D-07: JSX text, `aria-label`/`aria-labelledby` values, `title`, placeholders, button/input labels, Playwright visible locators such as `getByRole`/`getByText`/`getByLabel`, and known smoke-copy strings. It must fail user-facing English smoke labels in those surfaces, including `Video editor smoke workbench`, `Feature categories`, `Material bin`, `Preview monitor`, `Inspector`, `Timeline`, `Materials`, `Media`, `Text`, `Audio`, `Effects`, `Asset`, `Clip`, and `workbench`. It must explicitly allow internal TypeScript identifiers, generated command/type names, component/type symbols, enum or union values, object keys, file names, CSS class names, and test names such as `Timeline`, `Media`, `Text`, and `Audio` when they are used as code identifiers rather than visible copy. Also fail renderer direct mutation patterns for `draft.tracks`, `track.segments`, `targetTimerange`, `sourceTimerange`, and `mainTrackMagnet`; renderer imports or usage of `electron`, `node:`, `fs`, `path`, `child_process`, `createRequire`, `process.`, raw `ipcRenderer`, or native binding modules; renderer `ffmpeg`, `ffprobe`, `filter_complex`, `renderGraph`, `ffmpegScripts`, `previewCache`, or `waveform`; and missing generated-file header `This file was generated by Rust ts-rs declarations. Do not edit this file manually.` in `CommandEnvelope.ts`, `CommandResultEnvelope.ts`, and `Draft.ts`. Keep `git diff --exit-code schemas apps/desktop-electron/src/generated` as part of the guard or root test path. Add root scripts `test:phase4-source-guards` and `test:phase4-workspace` in `package.json`, include both in the root `test` script after Phase 3 guards and before `test:contracts`, and add both commands to `just test` without removing any prior gates. Do not add package dependencies or modify `pnpm-lock.yaml` for Phase 4 unless a separate package legitimacy checkpoint is introduced.
  </action>
  <verify>
    <automated>pnpm run test:phase4-source-guards</automated>
    <automated>pnpm run test:phase4-workspace</automated>
    <automated>pnpm run test:desktop</automated>
    <automated>pnpm run test:contracts</automated>
    <automated>pnpm run test</automated>
  </verify>
  <acceptance_criteria>
    Phase 4 source guards and workspace tests are named, executable, and included in root `test` and `just test`. Generated contract drift remains checked. No new dependency is added.
  </acceptance_criteria>
  <done>Phase 4 gates and source guards are wired into public test commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner -> Electron app | Tests launch the built app and observe user-visible behavior and bridge exposure. |
| Source guard script -> renderer source | Guards enforce architecture and language constraints before merge. |
| Package scripts -> public gates | Root and just gates become the executable phase completion contract. |
| Renderer -> generated contracts | Drift checks protect generated TS as Rust-owned outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Spoofing/Elevation of Privilege | Electron preload bridge | mitigate | Smoke test verifies exact safe bridge shape, no raw `ipcRenderer`, and untrusted navigation isolation. |
| T-04-17 | Tampering | Renderer direct draft mutation | mitigate | `phase4-source-guards.sh` fails mutation patterns in renderer and generated drift. |
| T-04-18 | Information Disclosure/Elevation of Privilege | Renderer privileged imports | mitigate | Source guard fails Electron/Node/fs/path/child_process/native imports in renderer. |
| T-04-19 | Tampering/Information Disclosure | Renderer FFmpeg/render leakage | mitigate | Source guard fails FFmpeg/ffprobe/render graph/preview cache/waveform construction in renderer. |
| T-04-20 | Denial of Service | Workspace layout | mitigate | Playwright geometry checks cover 1280x800 and 1120x720 and assert no overlap/clipping after state changes. |
| T-04-21 | Repudiation | TEST-06 overclaiming | mitigate | Workspace tests prove Phase 4 import/edit/preview-shell subset and explicitly leave deterministic preview/export to Phase 5/6 roadmap dependencies. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned; if a new dependency is proposed, stop and require package legitimacy verification. |
</threat_model>

<verification>
<automated>pnpm run test:phase4-source-guards</automated>
<automated>pnpm run test:phase4-workspace</automated>
<automated>pnpm run test:desktop</automated>
<automated>pnpm run test:contracts</automated>
<automated>pnpm run test</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- UI-01 through UI-06 are verified by Playwright and source guards.
- Phase 4 covers the TEST-06 workspace subset without faking Phase 5 preview/render or Phase 6 export/package work.
- Renderer code has no direct Electron/Node/native/FFmpeg access and no direct draft mutation.
- Generated TypeScript files remain Rust-owned outputs and are not hand-edited.
- Root `test` and `just test` include Phase 4 source guard and workspace checks.
</success_criteria>

<output>
Create `.planning/phases/04-jianying-style-desktop-workspace/04-04-SUMMARY.md` when done.
</output>
