---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04D"
type: execute
wave: 10
depends_on:
  - "15.2-04C"
files_modified:
  - "apps/desktop-electron/src/main/index.ts"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "apps/desktop-electron/src/renderer/workspace/preview-inspector.css"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "crates/realtime_preview_runtime/src/platform/macos.rs"
  - "crates/realtime_preview_runtime/src/gpu/device.rs"
  - "crates/realtime_preview_runtime/src/gpu/surface.rs"
  - "crates/realtime_preview_runtime/src/gpu/compositor.rs"
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - TEST-E2E-01
  - NO-FALLBACK-01
autonomous: true
user_setup: []
---

<objective>
Resolve the 04C finding that both Electron and WGPU overlay windows are
`isVisible=true` but not AppKit occlusion-visible, then prove real
render-graph GPU playback through an Electron-window native surface rather than
a fallback route.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-04D-01: Make foreground activation an explicit product prerequisite</name>
  <files>apps/desktop-electron/src/main/index.ts, apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts, crates/realtime_preview_runtime/src/platform/macos.rs</files>
  <action>Add RED/diagnostic coverage for macOS activation and active-space state before native WGPU acquire. Ensure the Electron app uses regular foreground activation on macOS and the preview host records whether activation/foreground preparation changed AppKit occlusion visibility. Diagnostics must remain failure evidence only and cannot mark playback success.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>The product test can distinguish activation/active-space failure from render failure, and macOS launch/focus setup is production-shaped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04D-02: Replace the top-level overlay attempt with an Electron-window native subview surface</name>
  <files>crates/realtime_preview_runtime/src/platform/macos.rs, crates/realtime_preview_runtime/src/gpu/device.rs, crates/realtime_preview_runtime/src/gpu/surface.rs, apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/src/renderer/workspace/preview-inspector.css</files>
  <action>Use the macOS `NSView*` handle from Electron as the parent for an in-window native WGPU child view/layer aligned to the preview host rect. Keep the native surface in the Electron window hierarchy, update it on rect changes, and make the DOM preview host transparent enough for the native view to be visible without using DOM canvas/video rendering as success. Remove or gate the 04C top-level overlay strategy if it is no longer the selected product path.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop build:native</automated>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
  </verify>
  <done>The selected macOS product attachment is an Electron-window native surface, not an independent fallback/diagnostic overlay, and WGPU can acquire it when the window is foreground-visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04D-03: Prove real visible playback or stop with a narrower blocker</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts, apps/desktop-electron/src/main/realtimePreviewHost.ts, crates/realtime_preview_runtime/src/gpu/compositor.rs</files>
  <action>Run the normal import-add-play journey. Success requires non-occluded WGPU acquire, `present()`, `renderGraphGpuComposited` evidence, presented-frame telemetry, advancing target time, and visible preview-region change. If it still fails, record whether the remaining blocker is app activation/occlusion, WGPU surface acquire, native texture sampling, or visible capture mismatch; do not proceed to 15.2-05/06.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
    <automated>git diff --check -- . ':!reference'</automated>
  </verify>
  <done>Product E2E passes only with real render-graph GPU visible playback, or the remaining blocker is narrower than the 04C occlusion diagnosis.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop build:native</automated>
<automated>pnpm --filter @video-editor/desktop build:electron</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Normal desktop import-add-play presents real render-graph GPU frames through an
Electron-window native WGPU surface, emits `renderGraphGpuComposited`, advances
telemetry/time, and visibly changes the preview region. No fallback or
diagnostic route can satisfy success.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04D-SUMMARY.md` when done.
</output>

