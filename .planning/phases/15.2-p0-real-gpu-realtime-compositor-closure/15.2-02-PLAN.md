---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "02"
type: execute
wave: 2
depends_on:
  - "15.2-01"
files_modified:
  - "crates/realtime_preview_runtime/src/gpu/compositor.rs"
  - "crates/realtime_preview_runtime/src/gpu/pipelines.rs"
  - "crates/realtime_preview_runtime/src/gpu/texture_cache.rs"
  - "crates/realtime_preview_runtime/src/gpu/text.rs"
  - "crates/realtime_preview_runtime/tests/gpu_subset.rs"
  - "crates/realtime_preview_runtime/tests/text_parity.rs"
autonomous: true
requirements:
  - P0-GPU-01
  - P0-GPU-02
  - P0-GPU-04
  - NO-FALLBACK-01
user_setup: []
---

<objective>
Implement the supported render-graph GPU compositor subset as real GPU work for
canvas, video/image layers, transforms, opacity, fit/fill/stretch, stack order,
and bundled-font text overlays.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-02-01: Add GPU compositor behavior tests for mainstream visuals</name>
  <files>crates/realtime_preview_runtime/tests/gpu_subset.rs, crates/realtime_preview_runtime/tests/text_parity.rs</files>
  <action>Add tests for render graph stack order, fit/fill/stretch, position, scale, rotation, opacity, crop where exposed, sampled keyframe visual state, and bundled-font text overlay parity. Tests must fail if the compositor uses unsupported/fallback diagnostics as success.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_subset -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime text_parity -- --nocapture</automated>
  </verify>
  <done>Mainstream visual behavior is expressed as executable compositor requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-02-02: Replace CPU draw loop success with real GPU render pass</name>
  <files>crates/realtime_preview_runtime/src/gpu/compositor.rs, crates/realtime_preview_runtime/src/gpu/pipelines.rs, crates/realtime_preview_runtime/src/gpu/texture_cache.rs</files>
  <action>Route supported visual layers through `wgpu` render passes and texture sampling. CPU/offscreen readback may remain for deterministic assertions, but product support must be classified separately from CPU draw-loop output. Unsupported graph intent must fail closed before presentation.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_subset -- --nocapture</automated>
    <manual>VIDEO_EDITOR_TEST_WGPU=1 cargo test -p realtime_preview_runtime real_wgpu_adapter -- --ignored --nocapture</manual>
  </verify>
  <done>Supported visual subset is rendered by the GPU compositor path and not by a CPU-only loop accepted as product success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-02-03: Add text overlay GPU/parity support for bundled fonts</name>
  <files>crates/realtime_preview_runtime/src/gpu/text.rs, crates/realtime_preview_runtime/src/gpu/compositor.rs, crates/realtime_preview_runtime/tests/text_parity.rs</files>
  <action>Connect baseline text overlays using the bundled font registry and explicit glyph/file validation. Remove `TextParityUnsupported` from the supported baseline path; unsupported text effects remain explicit unavailable diagnostics.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime text_parity -- --nocapture</automated>
    <automated>cargo test -p testkit realtime_preview_parity -- --nocapture</automated>
  </verify>
  <done>Baseline text overlay preview/export parity is a supported compositor path, not fallback rasterization success.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime gpu_subset -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime text_parity -- --nocapture</automated>
<automated>cargo test -p testkit realtime_preview_parity -- --nocapture</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The realtime runtime has a real GPU compositor implementation for the supported
mainstream visual subset and fails closed for unsupported graph intent.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-02-SUMMARY.md` when done.
</output>

