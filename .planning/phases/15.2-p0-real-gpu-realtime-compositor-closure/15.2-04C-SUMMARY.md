---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04C"
status: "blocked"
subsystem: "realtime-preview"
tags: ["wgpu", "electron", "macos", "appkit", "coreanimation", "no-fallback", "surface-occlusion"]

requires:
  - phase: "15.2-04B"
    provides: "Product E2E reaches native WGPU surface acquire and fails closed when macOS reports `surface is occluded`"
provides:
  - "Drawable lifecycle diagnostics for macOS AppKit/CoreAnimation surface acquisition"
  - "Blocked repair attempt using AppKit raw-handle top-level overlay WGPU surface attachment"
  - "Preserved fail-closed product E2E behavior without accepting fallback preview routes"
affects: ["15.2-05", "15.2-06", "realtime-preview", "native-wgpu-surface"]

tech-stack:
  added: ["objc2-foundation direct dependency in realtime_preview_runtime"]
  patterns:
    - "WGPU occluded acquire errors include AppKit/CoreAnimation drawable lifecycle diagnostics"
    - "Product E2E prints realtime preview host calls when compositor evidence times out"
    - "Unsupported/occluded native surface states remain fail-closed and do not emit product evidence"

key-files:
  created:
    - ".planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04C-SUMMARY.md"
  modified:
    - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
    - "apps/desktop-electron/tests/product-user-journey.spec.ts"
    - "apps/desktop-electron/tests/helpers/userJourney.ts"
    - "crates/realtime_preview_runtime/Cargo.toml"
    - "crates/realtime_preview_runtime/src/gpu/compositor.rs"
    - "crates/realtime_preview_runtime/src/gpu/device.rs"
    - "crates/realtime_preview_runtime/src/gpu/surface.rs"
    - "crates/realtime_preview_runtime/src/platform/macos.rs"
    - "Cargo.lock"

key-decisions:
  - "Do not execute 15.2-05 or 15.2-06 because normal product playback still cannot acquire a non-occluded macOS WGPU surface."
  - "Treat drawable lifecycle diagnostics as investigation evidence only; they cannot satisfy product playback success."
  - "Keep product E2E fail-closed on `surface is occluded` instead of routing through nativeVideoBridge, offscreen, CPU readback, screenshots, synthetic colors, DOM overlays, or requestPreviewFrame playback loops."

patterns-established:
  - "Occluded WGPU acquire errors should include attachment strategy, window visibility, occlusion state, view/layer hidden state, bounds, and drawable size."
  - "Product E2E timeout errors include host-call history so native presenter failures are actionable."

requirements-completed: []

duration: "17min"
started: "2026-06-19T23:42:52Z"
completed: "2026-06-19T23:59:27Z"
---

# Phase 15.2 Plan 04C: Native WGPU Drawable Lifecycle Summary

**MacOS drawable lifecycle diagnostics and production-shaped WGPU attachment attempts; product playback remains blocked because AppKit still reports the WGPU surface as occluded.**

## Performance

- **Duration:** 17 min
- **Started:** 2026-06-19T23:42:52Z
- **Completed:** 2026-06-19T23:59:27Z
- **Tasks completed:** 2 of 3
- **Files modified:** 9
- **Status:** Blocked

## Accomplishments

- Added AppKit/CoreAnimation drawable lifecycle diagnostics to occluded WGPU acquire failures.
- Extended product E2E failure output to include realtime preview host calls and require drawable lifecycle fields when occlusion occurs.
- Tried multiple production surface repairs without adding or accepting fallback success: parent subview, AppKit runloop/transaction commit, AppKit raw-handle surface creation, top-level overlay window, and explicit foreground/window-level ordering.
- Preserved no-fallback behavior: product E2E still fails closed with no `renderGraphGpuComposited` evidence, no presented-frame telemetry increment, and no preview success.

## Task Commits

1. **Task 15.2-04C-01: Add drawable-lifecycle diagnostics for the native surface blocker** - `c53a80e` (test)
2. **Task 15.2-04C-02: Make the macOS Electron WGPU surface drawable before acquire** - `2e9d533` (fix attempt, blocked)
3. **Task 15.2-04C-03: Prove visible compositor playback in the product journey** - Not executed as a success task because Task 04C-02 still fails at non-occluded surface acquisition.

## Files Created/Modified

- `crates/realtime_preview_runtime/src/platform/macos.rs` - Adds drawable lifecycle diagnostics, AppKit/CoreAnimation commit handling, and a top-level overlay-window attachment attempt.
- `crates/realtime_preview_runtime/src/gpu/compositor.rs` - Includes drawable lifecycle facts in `WgpuSurfaceAcquire` errors after transient occluded acquire retries.
- `crates/realtime_preview_runtime/src/gpu/surface.rs` - Exposes presentation-target drawable lifecycle diagnostics to the compositor.
- `crates/realtime_preview_runtime/src/gpu/device.rs` - Uses AppKit raw-window-handle surface creation for the macOS attachment attempt.
- `crates/realtime_preview_runtime/Cargo.toml` and `Cargo.lock` - Add direct `objc2-foundation` dependency for `NSRunLoop`/`NSDate` AppKit commit timing.
- `apps/desktop-electron/src/main/realtimePreviewHost.ts` - Moves the Electron window topmost in test recording mode before native attach/play while preserving the same product WGPU path.
- `apps/desktop-electron/tests/helpers/userJourney.ts` - Prints host-call history on compositor evidence timeouts and validates occluded-acquire diagnostics.
- `apps/desktop-electron/tests/product-user-journey.spec.ts` - Requires drawable diagnostics when the product path fails closed due to occluded acquire.

## Decisions Made

- 15.2-05 and 15.2-06 remain blocked until product E2E observes visible `renderGraphGpuComposited` playback.
- Diagnostics are not product evidence. The success gate still requires non-occluded WGPU acquire, `present()`, presented-frame telemetry, advancing target time, and visible preview hash change.
- The current macOS execution environment reports both the Electron parent window and the overlay WGPU window as not occlusion-visible, even when `isVisible=true`, bounds are nonzero, the layer is not hidden, and drawable size is nonzero.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Replaced the child-window CoreAnimation surface target with an AppKit raw-handle overlay attempt**
- **Found during:** Task 15.2-04C-02
- **Issue:** Diagnostics showed the 04B child-window/CAMetalLayer path had nonzero bounds and a visible layer, but AppKit still reported both parent and child windows as not occlusion-visible before WGPU acquire.
- **Fix:** Tried a parent-subview attachment, AppKit runloop/transaction commits, Electron foreground ordering, AppKit raw-handle surface creation, and finally a top-level overlay `NSWindow` at screen-saver level.
- **Files modified:** `crates/realtime_preview_runtime/src/platform/macos.rs`, `crates/realtime_preview_runtime/src/gpu/device.rs`, `apps/desktop-electron/src/main/realtimePreviewHost.ts`, `crates/realtime_preview_runtime/Cargo.toml`, `Cargo.lock`
- **Verification:** Rust/build/no-fallback gates pass, but product E2E still reports `surface is occluded`.
- **Committed in:** `2e9d533`

---

**Total deviations:** 1 blocking repair attempt documented
**Impact on plan:** The repair attempts stayed inside the real native WGPU product path and did not add fallback success, but they did not satisfy the P0 product success criterion.

## Verification

Passed:

- `cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture`
- `cargo test -p realtime_preview_runtime -- --nocapture`
- `cargo test -p bindings_node realtime_preview -- --nocapture`
- `pnpm --filter @video-editor/desktop build:native`
- `pnpm --filter @video-editor/desktop build:electron`
- `pnpm run test:no-product-fallback`
- `git diff --check -- . ':!reference'`

Blocked / failed closed:

- `pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line`

Observed failure:

```text
NativePresenter: render graph GPU scheduler failed: wgpu surface texture acquire failed: surface is occluded; drawableLifecycle{attachmentStrategy=topLevelOverlayWindow, prepareCount=2, parentWindowVisible=true, parentWindowOcclusionVisible=false, childWindowVisible=true, childWindowOcclusionVisible=false, childHasParent=false, parentViewHidden=false, parentViewHiddenOrAncestor=false, childViewHidden=false, childViewHiddenOrAncestor=false, childViewAlpha=1.000, childWindowAlpha=1.000, layerHidden=false, parentWindowFrame={x=116.00,y=91.00,w=1280.00,h=800.00}, parentViewBounds={x=0.00,y=0.00,w=1280.00,h=768.00}, childWindowFrame={x=368.00,y=696.00,w=160.00,h=90.00}, childViewFrame={x=0.00,y=0.00,w=160.00,h=89.50}, childViewBounds={x=0.00,y=0.00,w=160.00,h=89.50}, layerBounds={x=0.00,y=0.00,w=160.00,h=89.50}, drawableSize={w=320.00,h=179.00} }
```

## Issues Encountered

The plan did not complete because every attempted AppKit/CoreAnimation attachment still produced `wgpu::CurrentSurfaceTexture::Occluded` during normal product playback. The final diagnostics show:

- Parent Electron `NSWindow`: `isVisible=true`, `occlusionVisible=false`
- Native WGPU overlay `NSWindow`: `isVisible=true`, `occlusionVisible=false`
- Child view/layer: not hidden, alpha 1.0, nonzero frame/bounds
- `CAMetalLayer`: not hidden, nonzero bounds, `drawableSize={w=320.00,h=179.00}`
- Product telemetry: `presentedFrameCount=0`, `targetTimeMicroseconds=0`
- Product evidence: no `renderGraphGpuComposited`

## Known Stubs

None. Stub scan found only diagnostic/test guard wording and existing preview-region screenshot hashing in product E2E. No fallback path was added or accepted as product success.

## Threat Flags

| Flag | File | Description |
| ---- | ---- | ----------- |
| threat_flag: native-window-surface | `crates/realtime_preview_runtime/src/platform/macos.rs` | Adds native AppKit window/view/layer lifecycle handling for a product WGPU preview surface. No network, auth, file, schema, FFmpeg, CPU readback, or fallback rendering surface was introduced. |

## User Setup Required

None.

## Next Phase Readiness

Not ready. 15.2-05 and 15.2-06 must remain blocked. The next repair should start from the recorded drawable lifecycle facts and determine why this environment reports occlusion false for both the Electron parent and a top-level overlay window with nonzero CAMetalLayer bounds. Product success still requires real WGPU surface presentation and visible compositor playback, not a fallback route.

## Self-Check: PASSED

- Summary file exists: `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04C-SUMMARY.md`
- Task commit found: `c53a80e`
- Task commit found: `2e9d533`
- Product success remains blocked and is documented above; no requirement completion was recorded for P0 GPU playback.

---

*Phase: 15.2-p0-real-gpu-realtime-compositor-closure*
*Status: blocked*
