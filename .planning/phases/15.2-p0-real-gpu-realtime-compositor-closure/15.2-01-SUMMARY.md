---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "01"
subsystem: "realtime-preview-evidence"
status: complete
completed_at: "2026-06-19T19:36:52Z"
tags:
  - no-fallback
  - product-e2e
  - realtime-preview
key-files:
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "crates/bindings_node/src/native_preview_presenter.rs"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "scripts/no-product-fallback-guards.sh"
---

# Plan 15.2-01 Summary

## Outcome

Product realtime preview evidence is now honest: `renderGraphGpu` is the only
product success backend. `AVPlayerLayer`/native video bridge evidence remains
available for diagnostics, but it is not `available` product presentation, does
not increment compositor presented-frame telemetry, and `play()` fails closed
until the render-graph GPU compositor is connected.

## Commits

| Commit | Type | Description |
|--------|------|-------------|
| `0ac84f7` | test | Added failing product/Rust evidence tests requiring render-graph GPU compositor evidence. |
| `6ddabe3` | feat | Split product compositor evidence from diagnostics and rejected native bridge playback as product success. |
| `f421fb4` | docs | Hardened no-fallback guards, added legacy cleanup policy, and recorded UI convergence requirements. |

## Verification

| Command | Result |
|---------|--------|
| `cargo test -p bindings_node realtime_preview -- --nocapture` | PASS |
| `cargo test -p bindings_node native_preview -- --nocapture` | PASS |
| `pnpm --filter @video-editor/desktop build` | PASS |
| `pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line` | PASS: 1 passed, 1 skipped future renderGraphGpu-positive case |
| `pnpm run test:no-product-fallback` | PASS |
| `git diff --check -- . ':!reference'` | PASS |

## Deviations from Plan

**[Rule 2 - Missing critical acceptance detail] Positive playback E2E changed to fail-closed gate** - Found during: Task 15.2-01-02. The original RED test asserted full render-graph GPU playback immediately, but user clarification made clear that Phase 15.2-01 must first stop native bridge from being treated as success. The product E2E now asserts native bridge rejection and keeps the positive renderGraphGpu playback case skipped until Phase 15.2-04 connects desktop compositor presentation.

**[Rule 3 - Guard gap] Legacy cleanup policy added** - Found during: Task 15.2-01-03. User added a project-wide rule that greenfield refactors should remove old compatibility/fallback paths rather than preserve them. Added `docs/refactor-and-legacy-cleanup-policy.md`, referenced it from project/no-fallback/E2E policy docs, and extended source guards.

**Total deviations:** 2 auto-fixed. **Impact:** The plan now matches the clarified P0: no fake playback success before the real compositor exists.

## Self-Check: PASSED

- Product host backend is `renderGraphGpu | none` only.
- Native video bridge is diagnostic-only evidence and product `play()` fails closed without compositor evidence.
- Rust binding `play()` rejects non-`RenderGraphGpu` presentation.
- Product E2E performs normal import/add/click-play workflow and verifies no fallback success.
- `reference/` was not touched.

