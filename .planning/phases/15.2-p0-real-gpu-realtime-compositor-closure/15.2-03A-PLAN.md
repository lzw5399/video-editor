---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "03A"
type: execute
wave: 4
depends_on:
  - "15.2-02"
  - "15.2-03"
files_modified:
  - "crates/realtime_preview_runtime/src/gpu/device.rs"
  - "crates/realtime_preview_runtime/src/gpu/surface.rs"
  - "crates/realtime_preview_runtime/src/gpu/compositor.rs"
  - "crates/realtime_preview_runtime/src/gpu/pipelines.rs"
  - "crates/realtime_preview_runtime/tests/gpu_surface_presenter.rs"
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - NO-FALLBACK-01
autonomous: true
user_setup: []
---

<objective>
Add a production WGPU surface presentation target for the desktop preview host
so the realtime compositor can render directly to the visible preview surface
without treating offscreen readback, CPU pixels, native-video, or mock output as
product playback success.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-03A-01: Add RED tests for no-readback surface presentation</name>
  <files>crates/realtime_preview_runtime/tests/gpu_surface_presenter.rs, crates/realtime_preview_runtime/src/gpu/compositor.rs</files>
  <action>Add runtime tests proving product presentation has a separate render-to-surface path from offscreen diagnostic readback. The product path must emit compositor presentation evidence only after rendering to a surface frame or externally supplied presentation target; `pixels`, CPU hashes, generated colors, and offscreen buffers cannot satisfy product evidence.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
  </verify>
  <done>The tests fail against the current offscreen/readback-only compositor contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-03A-02: Create native WGPU surface targets from preview descriptors</name>
  <files>crates/realtime_preview_runtime/src/gpu/device.rs, crates/realtime_preview_runtime/src/gpu/surface.rs</files>
  <action>Store the WGPU instance/adapter information needed for surface-compatible presentation. Convert validated macOS `NSView` and Windows `HWND` descriptors into WGPU surfaces, configure them with the requested preview bounds, and reject mock/offscreen/unsupported descriptors for product presentation. Surface attachment must remain renderer-neutral and must not leak GPU handles to Electron renderer code.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
  </verify>
  <done>Runtime can create, validate, resize, and classify native WGPU preview surfaces; unsupported hosts fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-03A-03: Present compositor frames to the surface without readback success</name>
  <files>crates/realtime_preview_runtime/src/gpu/compositor.rs, crates/realtime_preview_runtime/src/gpu/pipelines.rs, crates/realtime_preview_runtime/src/gpu/surface.rs</files>
  <action>Split compositor rendering so the same render graph draw path can render to a swapchain/surface texture view and call present. Keep offscreen readback only for deterministic tests and diagnostics. Product presentation evidence must be `renderGraphGpuComposited` from the presented surface path, not the offscreen output buffer.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime gpu_subset -- --nocapture</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>A render graph can be presented through the realtime WGPU surface path and all fallback success routes remain rejected.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime gpu_subset -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The realtime runtime owns a native WGPU surface presentation path that can
present render-graph compositor output to the desktop preview host and cannot be
satisfied by offscreen, CPU, native-video, or mock evidence.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-03A-SUMMARY.md` when done.
</output>

