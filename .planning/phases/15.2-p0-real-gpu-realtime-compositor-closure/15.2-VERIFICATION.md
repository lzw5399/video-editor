---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
status: "invalidated"
verified_at: "2026-06-20T04:52:19Z"
aggregate_gate: "pnpm run test:phase15-2"
invalidated_at: "2026-06-20T05:28:40Z"
invalidation_debug: ".planning/debug/15-2-product-playback-regression.md"
---

# Phase 15.2 Verification

## Post-Closeout Invalidation

This verification is invalidated by manual product UAT on 2026-06-20. The
aggregate gate passed, but it did not prove the full normal-user playback flow:

- preview playback surface alignment inside the preview monitor
- native/audible audio output during playback
- video playback synchronized with the timeline through pause/stop/end
- direct text/font and on-canvas transform interactions for visible controls

Phase 15.2 is reopened through `15.2-07-PLAN.md`. Do not route to Phase 15.3
or later scheduler/UI work as if 15.2 were complete until the 15.2-07 UAT gates
pass.

The previous verification result below is retained as historical evidence only.

## Historical Aggregate Result

Phase 15.2 aggregate verification passed on 2026-06-20.

## Aggregate Gate

Passed:

```bash
pnpm run test:phase15-2
```

The aggregate gate is wired as:

```bash
pnpm run test:phase15-2-rust \
  && pnpm run test:no-product-fallback \
  && pnpm run test:phase15-2-desktop \
  && cargo check --workspace --locked
```

## Covered Gates

Rust/runtime:

- `cargo test -p realtime_preview_runtime -- --nocapture`
- `cargo test -p media_runtime_desktop capabilities -- --nocapture`
- `cargo test -p bindings_node realtime_preview -- --nocapture`
- `cargo test -p testkit realtime_preview_parity -- --nocapture`
- `cargo test -p ffmpeg_compiler -- --nocapture`
- `cargo test -p bindings_node --test export_commands -- --nocapture --test-threads=1`

Product/no-fallback:

- `pnpm run test:no-product-fallback`

Desktop/Electron:

- `pnpm --filter @video-editor/desktop package:dir`
- `pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line`
- `pnpm --filter @video-editor/desktop exec playwright test tests/real-workflow.spec.ts --reporter=line`
- `pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line`

Workspace/API:

- `cargo check --workspace --locked`

Diff hygiene:

- `git diff --check -- . ':!reference'`

## Result Summary

- `realtime_preview_runtime`: passed; opt-in real WGPU smoke tests remain ignored unless `VIDEO_EDITOR_TEST_WGPU` / `VIDEO_EDITOR_TEST_WGPU_SURFACE` is set.
- `media_runtime_desktop capabilities`: passed.
- `bindings_node realtime_preview`: passed.
- `testkit realtime_preview_parity`: passed.
- `ffmpeg_compiler`: passed, including still-image timing and target-time overlay enable regressions.
- `bindings_node export_commands`: passed.
- `no-product-fallback`: passed and now also requires the Phase 15.2 aggregate gate plus visible preview motion assertions.
- `product-user-journey`: 3 passed, including missing-compositor fail-closed behavior and visible render-graph GPU playback motion.
- `real-workflow`: 2 passed, dev and packaged, covering repo video/image/audio fixtures, text, save/reopen, and 6s H.264/AAC export validation.
- `workspace`: 48 passed.
- `cargo check --workspace --locked`: passed.
- `git diff --check -- . ':!reference'`: passed.

## Acceptance Notes

- Product playback success is not a timer/playhead assertion. It requires `renderGraphGpuComposited` evidence, advancing presented-frame telemetry, and visible preview-region pixel motion in normal user Playwright workflows.
- `requestPreviewFrame`, preview artifacts, mock displays, native video bridge evidence, and fallback paths remain invalid as product playback success.
- `reference/` was not touched.
