---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "docs/no-product-fallback-policy.md"
  - "docs/product-e2e-acceptance-policy.md"
  - "scripts/no-product-fallback-guards.sh"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "apps/desktop-electron/src/main/nativeBinding.ts"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "crates/bindings_node/src/native_preview_presenter.rs"
  - "crates/bindings_node/src/realtime_preview_service.rs"
autonomous: true
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - TEST-E2E-01
  - NO-FALLBACK-01
user_setup: []
---

<objective>
Make the product evidence taxonomy honest so native single-video playback,
fallback, mock, CPU, artifact, or first-frame paths cannot be labeled as real
render-graph GPU compositor success.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-01-01: Add failing product evidence tests</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts, crates/bindings_node/src/native_preview_presenter.rs</files>
  <action>Add or adjust tests so product playback success requires explicit render-graph GPU compositor evidence. The existing macOS `AVPlayerLayer` native-video bridge must be represented as native video evidence and must fail the Phase 15.2 product compositor gate.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>cargo test -p bindings_node native_preview -- --nocapture</automated>
  </verify>
  <done>The tests fail against the current native single-video playback bridge because it is not render-graph GPU composition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-01-02: Split native-video and GPU-compositor evidence</name>
  <files>apps/desktop-electron/src/main/nativeBinding.ts, apps/desktop-electron/src/main/realtimePreviewHost.ts, crates/bindings_node/src/native_preview_presenter.rs, crates/bindings_node/src/realtime_preview_service.rs</files>
  <action>Rename or extend evidence/backend enums so `AVPlayerLayer` single-video presentation is not reported as `gpu` compositor success. Preserve product fail-closed behavior and keep diagnostics available for developer mode.</action>
  <verify>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>Product state can distinguish `nativeVideo` from the future render-graph GPU compositor path, and product success no longer accepts the former.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-01-03: Harden reusable no-fallback guards</name>
  <files>scripts/no-product-fallback-guards.sh, docs/no-product-fallback-policy.md, docs/product-e2e-acceptance-policy.md</files>
  <action>Extend source guards and docs so future reviews reject native-video, fallback, mock, offscreen, artifact, first-frame, CPU-probe, or DOM-only evidence as product compositor success.</action>
  <verify>
    <automated>pnpm run test:no-product-fallback</automated>
    <automated>git diff --check -- . ':!reference'</automated>
  </verify>
  <done>The guard trips if product tests or host code accept non-compositor evidence as Phase 15.2 success.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The codebase can no longer misrepresent native single-video playback as real
GPU compositor success. Product tests are ready to fail until the compositor
path is connected.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-01-SUMMARY.md` when done.
</output>

