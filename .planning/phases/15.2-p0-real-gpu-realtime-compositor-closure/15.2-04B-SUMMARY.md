---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04B"
status: "blocked"
subsystem: "realtime-preview"
tags: ["wgpu", "electron", "macos", "appkit", "no-fallback", "surface-occlusion"]

requires:
  - phase: "15.2-04A"
    provides: "Rust realtime scheduler reaches native WGPU surface acquire but fails closed when macOS reports the surface is occluded"
provides:
  - "Product E2E RED coverage that rejects WGPU `surface is occluded` as successful playback"
  - "Host-call diagnostics for occluded native surface acquisition"
  - "Production AppKit child-window/CAMetalLayer attachment attempt that keeps fallback routes rejected"
affects: ["15.2-05", "15.2-06", "realtime-preview", "native-wgpu-surface"]

tech-stack:
  added: ["objc2-quartz-core direct dependency in realtime_preview_runtime"]
  patterns:
    - "Fail closed when WGPU native surface acquire reports occluded/lost/outdated states"
    - "Main-process Electron visibility preparation before native WGPU attach/play"
    - "Rust-owned native surface lifecycle and presentation preparation"

key-files:
  created:
    - ".planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04B-SUMMARY.md"
  modified:
    - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
    - "apps/desktop-electron/tests/product-user-journey.spec.ts"
    - "apps/desktop-electron/tests/helpers/userJourney.ts"
    - "crates/realtime_preview_runtime/Cargo.toml"
    - "crates/realtime_preview_runtime/src/gpu/compositor.rs"
    - "crates/realtime_preview_runtime/src/gpu/surface.rs"
    - "crates/realtime_preview_runtime/src/platform/macos.rs"
    - "Cargo.lock"

key-decisions:
  - "Do not advance to 15.2-05/15.2-06 because macOS/Electron WGPU surface acquisition remains occluded in the product E2E."
  - "Preserve no-fallback product policy: AVPlayerLayer, nativeVideoBridge, mock, offscreen, CPU, screenshot, synthetic color, DOM overlay, and requestPreviewFrame routes remain rejected."
  - "Treat AppKit child-window attachment as useful but insufficient evidence until WGPU presents visible `renderGraphGpuComposited` frames."

patterns-established:
  - "Product E2E must inspect host calls and fail if normal playback reports `surfaceAcquireOccluded`."
  - "Native preview host records occluded surface acquisition separately from missing-compositor rejection."

requirements-completed: []

duration: "20min"
started: "2026-06-19T23:11:29Z"
completed: "2026-06-19T23:30:58Z"
---

# Phase 15.2 Plan 04B: Native WGPU Surface Visibility Summary

**Occluded-surface RED coverage and AppKit child-window WGPU attachment attempt; product playback remains blocked because macOS still reports the Electron preview surface as occluded.**

## Performance

- **Duration:** 20 min
- **Started:** 2026-06-19T23:11:29Z
- **Completed:** 2026-06-19T23:30:58Z
- **Tasks completed:** 2 of 3
- **Files modified:** 8
- **Status:** Blocked

## Accomplishments

- Added product E2E coverage that rejects `surface is occluded` as successful realtime preview playback.
- Added host-call diagnostics so occluded WGPU surface acquisition is visible as `surfaceAcquireOccluded`.
- Reworked the macOS attachment path to retain an AppKit child `NSWindow` with an explicit `CAMetalLayer`, refresh visibility before present, and preserve fail-closed behavior.
- Verified native builds, Rust realtime preview tests, and no-product-fallback policy still pass.

## Task Commits

1. **Task 15.2-04B-01: Add RED coverage for non-occluded native surface acquisition** - `6afdb21` (test)
2. **Task 15.2-04B-02: Repair macOS Electron WGPU surface attachment** - `13c9db9` (fix, blocked by unchanged product E2E occlusion)
3. **Task 15.2-04B-03: Prove product playback presents visible compositor frames** - Not executed because Task 04B-02 did not reach non-occluded surface acquisition.

## Files Created/Modified

- `apps/desktop-electron/tests/helpers/userJourney.ts` - Captures host error messages and adds `expectNoRejectedSurfaceAcquire`.
- `apps/desktop-electron/tests/product-user-journey.spec.ts` - Requires compositor evidence before treating playback as successful and rejects occluded surface acquisition.
- `apps/desktop-electron/src/main/realtimePreviewHost.ts` - Records occluded acquire diagnostics and prepares the Electron `BrowserWindow` before native attach/play.
- `crates/realtime_preview_runtime/Cargo.toml` - Adds direct `objc2-quartz-core` dependency for explicit `CAMetalLayer` ownership.
- `crates/realtime_preview_runtime/src/gpu/compositor.rs` - Prepares the presentation target before acquiring the WGPU surface texture.
- `crates/realtime_preview_runtime/src/gpu/surface.rs` - Exposes presentation-target preparation through the Rust-owned surface abstraction.
- `crates/realtime_preview_runtime/src/platform/macos.rs` - Replaces the helper sublayer attachment with a retained AppKit child-window/CAMetalLayer surface path.
- `Cargo.lock` - Records dependency graph changes.

## Decisions Made

- Normal product playback must not pass when WGPU reports `surface is occluded`; that state is an explicit unavailable diagnostic.
- The AppKit child-window path is not sufficient for release until product E2E emits visible `renderGraphGpuComposited` evidence with advancing frames.
- 15.2-05 and 15.2-06 should remain blocked until this surface-acquire issue is resolved without fallback routes.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Replaced the original native layer attachment with a retained AppKit child-window surface path**
- **Found during:** Task 15.2-04B-02
- **Issue:** The existing Electron child-view/sublayer approach still let WGPU reach surface acquire but macOS reported the surface as occluded.
- **Fix:** Retained the parent view/window, created a borderless child `NSWindow`, attached an explicit `CAMetalLayer`, refreshed AppKit visibility and drawable bounds before present, and detached the child window on drop.
- **Files modified:** `crates/realtime_preview_runtime/src/platform/macos.rs`, `crates/realtime_preview_runtime/src/gpu/surface.rs`, `crates/realtime_preview_runtime/src/gpu/compositor.rs`, `apps/desktop-electron/src/main/realtimePreviewHost.ts`
- **Verification:** Rust tests and builds passed, but product E2E still reports `wgpu surface texture acquire failed: surface is occluded`.
- **Committed in:** `13c9db9`

---

**Total deviations:** 1 blocking repair attempt documented
**Impact on plan:** The repair preserved product no-fallback constraints but did not satisfy the plan success criterion.

## Verification

Passed:

- `pnpm --filter @video-editor/desktop build:electron`
- `cargo test -p bindings_node realtime_preview -- --nocapture`
- `cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture`
- `pnpm --filter @video-editor/desktop build:native`
- `pnpm run test:no-product-fallback`
- `git diff --check -- . ':!reference'`

Expected RED / blocked:

- `pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line`

Observed failure:

```text
NativePresenter: render graph GPU scheduler failed: wgpu surface texture acquire failed: surface is occluded
```

Not run after the blocker:

- `cargo test -p realtime_preview_runtime -- --nocapture`

## Issues Encountered

The macOS/Electron production path still reaches Rust scheduler and WGPU surface acquire, but native WGPU reports the surface as occluded. Several scoped repair attempts were made before stopping at the fix-attempt limit:

1. Configured the helper-created `CAMetalLayer` from `raw-window-metal` and refreshed AppKit visibility before present.
2. Added Electron `BrowserWindow` restore/show/focus and Rust presentation preparation before acquire.
3. Replaced the helper sublayer with an explicitly retained `CAMetalLayer`.
4. Replaced the subview/sublayer attachment with a retained borderless AppKit child `NSWindow` above the Electron parent.

The final attempt is committed because it is production-shaped and keeps unsupported/hidden compositor states fail-closed, but it does not close the P0 success criterion.

## Known Stubs

None. Stub scan found only test assertion text and nullable runtime state fields; no fake preview success, mock data source, synthetic color, DOM overlay, or fallback product path was introduced.

## Threat Flags

| Flag | File | Description |
| ---- | ---- | ----------- |
| threat_flag: native-window-surface | `crates/realtime_preview_runtime/src/platform/macos.rs` | Adds retained AppKit child-window/CAMetalLayer handling for product preview. This adds native UI surface lifecycle complexity, but no network, auth, file, schema, FFmpeg, CPU readback, or fallback-rendering surface. |

## User Setup Required

None.

## Next Phase Readiness

Not ready. The next executor should continue from `13c9db9`, keep the RED coverage in place, and identify a macOS/Electron WGPU native surface path that can acquire and present while the preview region is visibly on screen. Product success must still require `renderGraphGpu` / `renderGraphGpuComposited` evidence and must not route through fallback preview implementations.

## Self-Check: PASSED

- Summary file exists: `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04B-SUMMARY.md`
- Task commit found: `6afdb21`
- Task commit found: `13c9db9`
- Product success remains blocked and is documented above; no requirement completion was recorded for P0 GPU playback.

---

*Phase: 15.2-p0-real-gpu-realtime-compositor-closure*
*Status: blocked*
