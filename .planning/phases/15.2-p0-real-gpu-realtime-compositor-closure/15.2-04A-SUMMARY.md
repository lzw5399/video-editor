---
phase: 15.2-p0-real-gpu-realtime-compositor-closure
plan: 04A
status: blocked
subsystem: realtime-preview
tags: [wgpu, electron, rust, scheduler, metal, corevideo, no-fallback]

requires:
  - phase: 15.2-03A
    provides: Native WGPU surface presentation target
  - phase: 15.2-03B
    provides: Decoded texture lease/import contract
  - phase: 15.2-04
    provides: NV12 ExternalTexture sampling and fail-closed product gating
provides:
  - Rust-owned realtime playback scheduler skeleton and binding path
  - macOS CoreVideo/Metal lease to WGPU NV12 plane import bridge
  - Electron host wiring that drives Rust play and rejects fallback success
  - Product E2E regression requiring renderGraphGpuComposited evidence
affects: [15.2-05, 15.2-06, realtime-preview, desktop-electron]

tech-stack:
  added: [wgpu direct binding dependency, objc2-core-video binding dependency]
  patterns:
    - Rust binding installs a native texture importer callback; realtime GPU module does not depend on desktop media runtime
    - Scheduler evidence is emitted only after presented surface frames

key-files:
  created:
    - crates/realtime_preview_runtime/src/scheduler.rs
    - crates/realtime_preview_runtime/src/platform/macos.rs
  modified:
    - Cargo.lock
    - apps/desktop-electron/src/main/realtimePreviewHost.ts
    - apps/desktop-electron/tests/product-user-journey.spec.ts
    - crates/bindings_node/Cargo.toml
    - crates/bindings_node/src/realtime_preview_service.rs
    - crates/media_runtime_desktop/src/lib.rs
    - crates/media_runtime_desktop/src/platform/macos.rs
    - crates/media_runtime_desktop/src/platform/mod.rs
    - crates/realtime_preview_runtime/Cargo.toml
    - crates/realtime_preview_runtime/src/gpu/compositor.rs
    - crates/realtime_preview_runtime/src/gpu/device.rs
    - crates/realtime_preview_runtime/src/gpu/surface.rs
    - crates/realtime_preview_runtime/src/gpu/texture_cache.rs
    - crates/realtime_preview_runtime/src/lib.rs

key-decisions:
  - "Do not release 15.2-05/06: 04A still blocks on WGPU native surface occlusion during real product playback."
  - "CoreVideo/Metal lease inspection belongs at the Rust binding/platform edge; realtime_preview_runtime::gpu keeps a generic importer hook and does not depend on media_runtime_desktop."
  - "renderGraphGpuComposited evidence remains fail-closed and is emitted only when the scheduler reports presented_frames > 0."

patterns-established:
  - "Native texture importer callback: bindings validate platform lease types and runtime GPU code only consumes generic imported ExternalTexture planes."
  - "Product play first calls Rust scheduler presentation; playhead movement is rolled back/paused when presentation fails."

requirements-completed: [NO-FALLBACK-01]

duration: 61min
completed: 2026-06-19
---

# Phase 15.2 Plan 04A: Rust-Owned Realtime Playback Scheduler Summary

**Rust scheduler and native texture bridge were implemented, but product playback remains blocked by WGPU native surface occlusion before compositor evidence can be emitted.**

## Performance

- **Duration:** 61 min
- **Started:** 2026-06-19T22:08:49Z
- **Completed:** 2026-06-19T23:01:22Z
- **Tasks:** 3 planned tasks executed, plus 1 boundary fix
- **Files modified:** 16

## Accomplishments

- Added RED binding and product E2E coverage requiring scheduler-driven `renderGraphGpu` / `renderGraphGpuComposited` evidence for normal import-add-play.
- Added a Rust-owned `RealtimePlaybackScheduler` that builds render graph state from accepted draft snapshots and only records evidence after real surface presentation.
- Added macOS CoreVideo/Metal NV12 lease import into WGPU sampleable ExternalTexture planes, while preserving fail-closed behavior for unsupported native resources.
- Wired Electron host playback to call Rust scheduler play first and reject product success when the compositor surface cannot present.
- Kept fallback guards passing: no AVPlayerLayer/nativeVideoBridge, mock/offscreen, FFmpeg artifact, CPU readback, screenshots, synthetic colors, or DOM overlay can satisfy product playback success.

## Task Commits

1. **Task 15.2-04A-01: Add RED scheduler-driven desktop playback tests** - `55daac3` (test)
2. **Approved bridge addition: Add macOS Metal texture import bridge** - `681a6ec` (feat)
3. **Task 15.2-04A-02: Implement Rust-owned realtime playback scheduler** - `b6a02da` (feat)
4. **Task 15.2-04A-03: Wire Electron host to scheduler-driven product playback** - `ac30a96` (feat)
5. **Boundary fix: Keep native import bridge outside GPU module dependency boundary** - `f9fb175` (fix)

## Files Created/Modified

- `crates/realtime_preview_runtime/src/scheduler.rs` - Rust-owned scheduler, presentation evidence, and fail-closed prerequisite checks.
- `crates/realtime_preview_runtime/src/gpu/compositor.rs` - Surface presentation and generic native NV12 import hook usage.
- `crates/realtime_preview_runtime/src/gpu/device.rs` - WGPU ExternalTexture plane creation from macOS Metal textures.
- `crates/realtime_preview_runtime/src/gpu/surface.rs` - Native presentation target state and macOS attachment retention.
- `crates/realtime_preview_runtime/src/gpu/texture_cache.rs` - Native texture importer callback attached to the texture cache.
- `crates/realtime_preview_runtime/src/platform/macos.rs` - Rust-owned child NSView / CoreAnimationLayer attachment for WGPU surface creation.
- `crates/bindings_node/src/realtime_preview_service.rs` - Scheduler state, media provider registration, macOS lease importer, play rollback on presentation failure.
- `apps/desktop-electron/src/main/realtimePreviewHost.ts` - Product host drives Rust scheduler playback and records no success on fallback/errors.
- `apps/desktop-electron/tests/product-user-journey.spec.ts` - Product E2E requires visible advancement plus render-graph GPU compositor evidence.
- `crates/media_runtime_desktop/src/platform/macos.rs` - Public macOS texture lease accessors for luma/chroma CVMetalTexture planes.

## Decisions Made

- 15.2-05 and 15.2-06 remain blocked. 04A does not satisfy product playback success because the product E2E cannot acquire a visible WGPU surface texture in the current Electron/native child view path.
- The native import bridge is Rust-owned but not GPU-module-owned: `bindings_node` knows the desktop lease type, and `realtime_preview_runtime::gpu` only consumes generic imported planes.
- The scheduler is allowed to fail closed; it is not allowed to synthesize `renderGraphGpuComposited` evidence.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 4 approved by user - Architecture] Added native WGPU import bridge before scheduler completion**
- **Found during:** Task 15.2-04A continuation
- **Issue:** The plan assumed 15.2-04 had enough import support, but real desktop playback also required CoreVideo/Metal lease to WGPU sampleable texture/ExternalTexture import.
- **Fix:** Added macOS Metal plane wrapping and lease accessors, then connected the binding-side importer to the compositor texture cache.
- **Files modified:** `crates/media_runtime_desktop/src/platform/macos.rs`, `crates/realtime_preview_runtime/src/gpu/device.rs`, `crates/realtime_preview_runtime/src/gpu/compositor.rs`, `crates/realtime_preview_runtime/src/gpu/texture_cache.rs`, `crates/bindings_node/src/realtime_preview_service.rs`
- **Verification:** `cargo test -p media_runtime_desktop texture -- --nocapture`; `cargo test -p realtime_preview_runtime -- --nocapture`; `cargo test -p bindings_node realtime_preview -- --nocapture`
- **Committed in:** `681a6ec`, `f9fb175`

**2. [Rule 1 - Bug] Prevented playback success when scheduler presentation fails**
- **Found during:** Task 15.2-04A-03
- **Issue:** Runtime play could advance before scheduler presentation succeeded.
- **Fix:** Binding play now pauses/rolls back product playback state when scheduler presentation fails.
- **Files modified:** `crates/bindings_node/src/realtime_preview_service.rs`
- **Verification:** `cargo test -p bindings_node realtime_preview -- --nocapture`
- **Committed in:** `ac30a96`

**3. [Rule 3 - Blocking] Kept GPU runtime boundary free of desktop media dependency**
- **Found during:** Full runtime verification
- **Issue:** `realtime_preview_runtime::gpu` directly referenced `media_runtime_desktop::MacosRegisteredTextureLease`, failing the existing forbidden-boundary test.
- **Fix:** Added a generic native texture importer callback on `RealtimePreviewTextureCache`; moved desktop lease downcast into `bindings_node`.
- **Files modified:** `crates/realtime_preview_runtime/src/gpu/texture_cache.rs`, `crates/realtime_preview_runtime/src/gpu/compositor.rs`, `crates/bindings_node/src/realtime_preview_service.rs`, `crates/realtime_preview_runtime/Cargo.toml`, `crates/bindings_node/Cargo.toml`, `Cargo.lock`
- **Verification:** `cargo test -p realtime_preview_runtime gpu_subset_gpu_module_does_not_import_forbidden_runtime_boundaries -- --nocapture`; `cargo test -p realtime_preview_runtime -- --nocapture`
- **Committed in:** `f9fb175`

**Total deviations:** 3 auto-handled deviations.
**Impact on plan:** Required for correctness and architecture compliance. The work still fails closed at product surface presentation and does not unblock later plans.

## Verification

- `cargo test -p realtime_preview_runtime scheduler -- --nocapture` - passed.
- `cargo test -p realtime_preview_runtime -- --nocapture` - passed.
- `cargo test -p bindings_node realtime_preview -- --nocapture` - passed.
- `cargo test -p media_runtime_desktop texture -- --nocapture` - passed.
- `pnpm --filter @video-editor/desktop build:native` - passed.
- `pnpm --filter @video-editor/desktop build:electron` - passed.
- `pnpm run test:no-product-fallback` - passed.
- `git diff --check -- . ':!reference'` - passed.
- `pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line` - failed closed: missing pause/evidence state because scheduler presentation reports `wgpu surface texture acquire failed: surface is occluded`.

## Blocker

Product playback still cannot satisfy the 04A success criterion on this host. The normal import-add-play workflow reaches the Rust scheduler path, rejects fallback routes, and surfaces:

`NativePresenter: render graph GPU scheduler failed: wgpu surface texture acquire failed: surface is occluded`

No `renderGraphGpuComposited` evidence is emitted, and the product E2E correctly fails. This blocks 15.2-05 and 15.2-06 until the native WGPU surface attachment becomes non-occluded/visible to WGPU in Electron, or the platform surface API path is replaced with a working attached compositor surface. The blocker is fail-closed and not masked by CPU decode/readback, FFmpeg artifacts, nativeVideoBridge, mock/offscreen, synthetic colors, screenshots, or DOM overlays.

## Known Stubs

None. Stub scan found only test assertion text containing `placeholder` and existing TypeScript nullable state fields; neither feeds a fake product success path.

## Threat Flags

None. Changes add native window/view and texture lease handling but no new network endpoints, auth paths, schema trust boundary, or file access surface beyond existing local media decode path.

## User Setup Required

None.

## Next Phase Readiness

15.2-05/06 are not ready. The required next work is still inside the 04A blocker: make the Rust-owned native WGPU surface presentation path acquire a non-occluded surface texture in Electron and then rerun the product journey until visible content advancement and `renderGraphGpuComposited` evidence pass.

## Self-Check: PASSED

- Found summary file: `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04A-SUMMARY.md`
- Found commits: `55daac3`, `681a6ec`, `b6a02da`, `ac30a96`, `f9fb175`

---
*Phase: 15.2-p0-real-gpu-realtime-compositor-closure*
*Completed: 2026-06-19*
