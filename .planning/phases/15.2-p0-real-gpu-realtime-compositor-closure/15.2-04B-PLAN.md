---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04B"
type: execute
wave: 8
depends_on:
  - "15.2-04A"
files_modified:
  - "crates/realtime_preview_runtime/src/platform/macos.rs"
  - "crates/realtime_preview_runtime/src/gpu/device.rs"
  - "crates/realtime_preview_runtime/src/gpu/surface.rs"
  - "crates/realtime_preview_runtime/src/gpu/compositor.rs"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - TEST-E2E-01
  - NO-FALLBACK-01
autonomous: true
user_setup: []
---

<objective>
Make the macOS/Electron native WGPU preview surface visible and non-occluded so
the Rust-owned scheduler from Plan 04A can acquire a surface texture, present a
render-graph compositor frame, and emit `renderGraphGpuComposited` evidence in
the normal product user journey.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-04B-01: Add RED coverage for non-occluded native surface acquisition</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts, crates/bindings_node/src/realtime_preview_service.rs</files>
  <action>Tighten the product E2E and binding diagnostics so `surface is occluded` is a hard fail for normal playback, while still accepted as a fail-closed unavailable state before this plan is green. Capture host calls and scheduler diagnostics proving the path reaches WGPU surface acquire and does not fall back to native video, mock, offscreen, CPU readback, screenshots, synthetic colors, DOM overlays, or requestPreviewFrame.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
  </verify>
  <done>Tests fail with the current occluded surface acquisition path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04B-02: Repair macOS Electron WGPU surface attachment</name>
  <files>crates/realtime_preview_runtime/src/platform/macos.rs, crates/realtime_preview_runtime/src/gpu/device.rs, crates/realtime_preview_runtime/src/gpu/surface.rs, apps/desktop-electron/src/main/realtimePreviewHost.ts</files>
  <action>Investigate and fix the macOS native preview surface attachment so WGPU can acquire from a visible child surface inside the Electron window. Validate the native handle type, attach the child NSView/CoreAnimationLayer to the correct parent, set frame/layer visibility and ordering, keep the attachment retained, and update bounds before play. If the BrowserWindow handle is not a valid parent for this purpose, add the correct main-process native parent acquisition path. Unsupported or hidden surfaces must continue to fail closed; do not use offscreen readback or DOM rendering.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
  </verify>
  <done>Surface attachment diagnostics prove WGPU can acquire a surface texture when the Electron preview region is visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04B-03: Prove product playback presents visible compositor frames</name>
  <files>crates/realtime_preview_runtime/src/gpu/compositor.rs, crates/bindings_node/src/realtime_preview_service.rs, apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts</files>
  <action>Run the normal import-add-play product journey through the Rust scheduler and native WGPU surface until it emits `renderGraphGpuComposited` evidence with advancing presented frame count, target time, and content digest. Preserve fail-closed behavior for attach failure, occluded/lost/outdated surfaces, missing texture leases, unsupported formats, and all fallback routes.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>Product E2E passes only when a visible render-graph GPU compositor frame is presented through the native WGPU surface.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop build:native</automated>
<automated>pnpm --filter @video-editor/desktop build:electron</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Normal desktop import-add-play acquires a non-occluded native WGPU surface,
presents render-graph GPU compositor frames from the Rust-owned scheduler, and
emits `renderGraphGpuComposited` evidence. All unavailable surface states remain
explicit fail-closed diagnostics rather than product success.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04B-SUMMARY.md` when done.
</output>

