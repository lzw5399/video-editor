---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04C"
type: execute
wave: 9
depends_on:
  - "15.2-04B"
files_modified:
  - "crates/realtime_preview_runtime/src/platform/macos.rs"
  - "crates/realtime_preview_runtime/src/gpu/device.rs"
  - "crates/realtime_preview_runtime/src/gpu/surface.rs"
  - "crates/realtime_preview_runtime/src/gpu/compositor.rs"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - TEST-E2E-01
  - NO-FALLBACK-01
autonomous: true
user_setup: []
---

<objective>
Close the remaining macOS/Electron native WGPU surface blocker by making the
preview surface drawable to WGPU during the normal product play flow, then prove
visible render-graph GPU compositor playback end to end.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-04C-01: Add drawable-lifecycle diagnostics for the native surface blocker</name>
  <files>crates/realtime_preview_runtime/src/platform/macos.rs, crates/realtime_preview_runtime/src/gpu/surface.rs, crates/bindings_node/src/realtime_preview_service.rs, apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts</files>
  <action>Extend the existing RED product E2E and binding/native diagnostics so the current failure identifies the native surface drawable lifecycle, not just a generic play failure. Capture whether the parent window is visible, the attached view/window/layer has nonzero bounds, the layer is not hidden, CoreAnimation/AppKit preparation ran before acquire, and WGPU still returned `surface is occluded`. These diagnostics are for fail-closed investigation only and must not become product success evidence.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
  </verify>
  <done>The failing product test reports precise native drawable/acquire facts while still rejecting occluded acquisition as success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04C-02: Make the macOS Electron WGPU surface drawable before acquire</name>
  <files>crates/realtime_preview_runtime/src/platform/macos.rs, crates/realtime_preview_runtime/src/gpu/device.rs, crates/realtime_preview_runtime/src/gpu/surface.rs, crates/realtime_preview_runtime/src/gpu/compositor.rs, apps/desktop-electron/src/main/realtimePreviewHost.ts</files>
  <action>Repair the native surface path that remained occluded in 04B. Validate the Electron macOS handle type and parent selection, keep the native preview surface in a drawable AppKit/CoreAnimation tree, flush or otherwise commit CoreAnimation/AppKit visibility work before `surface.get_current_texture()`, update bounds on every host rect change, and replace the attachment strategy if the current child-window/CAMetalLayer path cannot become drawable. Preserve fail-closed behavior for hidden/lost/outdated surfaces. Do not add native-video, mock, offscreen, CPU readback, screenshot, synthetic-color, DOM overlay, or requestPreviewFrame playback routes.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop build:native</automated>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
  </verify>
  <done>Normal Electron preview attachment can acquire a non-occluded WGPU `SurfaceTexture` when the preview region is visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04C-03: Prove visible compositor playback in the product journey</name>
  <files>crates/realtime_preview_runtime/src/gpu/compositor.rs, crates/bindings_node/src/realtime_preview_service.rs, apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts</files>
  <action>Run the normal import-add-play user journey until the Rust scheduler decodes the current media frame, builds render graph intent, presents to the native WGPU surface, emits `renderGraphGpuComposited` evidence, increments presented-frame telemetry, advances target time, and changes the visible preview region hash. The test must continue to fail on occluded acquire, missing compositor evidence, native video bridge, mock/offscreen targets, CPU readback, preview artifacts, or requestPreviewFrame loops.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>Product E2E passes only when a normal user sees visible render-graph GPU compositor playback advance after pressing play.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop build:native</automated>
<automated>pnpm --filter @video-editor/desktop build:electron</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Normal desktop import-add-play acquires a non-occluded native WGPU surface,
presents render-graph GPU compositor frames from the Rust-owned scheduler, emits
`renderGraphGpuComposited` evidence, and visibly changes the preview region as
timeline time advances. All unavailable or fallback routes remain fail-closed
diagnostics and cannot satisfy product success.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04C-SUMMARY.md` when done.
</output>

