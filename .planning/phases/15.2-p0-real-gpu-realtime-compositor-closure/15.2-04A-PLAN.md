---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04A"
type: execute
wave: 7
depends_on:
  - "15.2-03A"
  - "15.2-03B"
  - "15.2-04"
files_modified:
  - "crates/realtime_preview_runtime/src/scheduler.rs"
  - "crates/realtime_preview_runtime/src/lib.rs"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "crates/bindings_node/src/native_preview_presenter.rs"
  - "apps/desktop-electron/src/main/nativeBinding.ts"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - TEST-E2E-01
  - NO-FALLBACK-01
autonomous: true
user_setup: []
---

<objective>
Add the Rust-owned realtime playback scheduler that converts accepted draft
state and media IO frames into render-graph GPU surface presentation evidence
during normal desktop play/pause/seek/scrub, closing the blocker recorded in
Plan 15.2-04.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-04A-01: Add RED scheduler-driven desktop playback tests</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts, crates/bindings_node/src/realtime_preview_service.rs</files>
  <action>Extend product E2E and binding tests so normal user playback cannot remain in the fail-closed state when a valid compositor surface, accepted draft, registered material, and sampleable texture lease are available. The test must require scheduler calls to decode the current timeline time, build frame-provider state, call surface presentation, and record `renderGraphGpuComposited` evidence. Playhead movement alone, native-video, requestPreviewFrame, mock, offscreen, CPU readback, screenshots, synthetic colors, and DOM overlays must still fail.</action>
  <verify>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>Tests fail because the current desktop path has no Rust-owned scheduler that presents compositor frames during play.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04A-02: Implement Rust-owned realtime playback scheduler</name>
  <files>crates/realtime_preview_runtime/src/scheduler.rs, crates/realtime_preview_runtime/src/lib.rs, crates/bindings_node/src/realtime_preview_service.rs</files>
  <action>Implement a scheduler owned by Rust runtime/bindings that stores accepted draft snapshots, preview profile, compositor surface state, media IO material registrations, native texture registry, timeline clock, and playback generation. On play/seek/scrub ticks it must resolve the target time, request the decoded material frame, validate live texture leases, build render graph intent from Rust-owned draft semantics, call `present_to_surface`, and update presentation evidence. Scheduler state must fail closed when any prerequisite is missing; it must not select fallback paths.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime scheduler -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
  </verify>
  <done>Bindings can drive scheduler playback and receive `renderGraphGpuComposited` evidence only after a compositor frame is actually presented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04A-03: Wire Electron host to scheduler-driven product playback</name>
  <files>apps/desktop-electron/src/main/nativeBinding.ts, apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/src/preload/index.ts, apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts</files>
  <action>Wire the Electron main host to attach surface bounds, push accepted draft/material facts, and call scheduler play/pause/seek/scrub APIs. The renderer remains a UI/display surface only. Product host state must report `backend: renderGraphGpu`, `source: renderGraphGpuComposited`, advancing presented frame counts/timecodes/digests, and no nativeVideoBridge/requestPreviewFrame/frameDisplay success.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>Normal import-add-play product flow presents render-graph GPU compositor output and unsupported paths fail closed.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime scheduler -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Desktop product playback is driven by a Rust-owned scheduler that decodes the
current timeline frame, presents it through the render-graph GPU compositor
surface, records `renderGraphGpuComposited` evidence, and fails closed for every
missing prerequisite or unsupported fallback route.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04A-SUMMARY.md` when done.
</output>

