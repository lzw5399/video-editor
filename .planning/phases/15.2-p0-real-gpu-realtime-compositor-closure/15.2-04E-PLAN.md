---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04E"
type: execute
wave: 11
depends_on:
  - "15.2-04D"
files_modified:
  - "apps/desktop-electron/tests/helpers/foregroundProductApp.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/src/main/index.ts"
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "crates/realtime_preview_runtime/src/platform/macos.rs"
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - TEST-E2E-01
  - TEST-E2E-02
  - NO-FALLBACK-01
autonomous: true
user_setup: []
---

<objective>
Replace the macOS product E2E launch path that leaves Electron/AppKit inactive
with a foreground packaged-app + CDP user journey, expose only test-gated
main-process observations needed by that harness, and prove real
render-graph GPU playback or fail closed with a narrower production blocker.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-04E-01: Add a foreground packaged-app CDP harness for product E2E</name>
  <files>apps/desktop-electron/tests/helpers/foregroundProductApp.ts, apps/desktop-electron/tests/helpers/userJourney.ts, apps/desktop-electron/tests/product-user-journey.spec.ts</files>
  <action>Add a macOS product launcher that starts the packaged `Video Editor.app` with a remote-debugging port, attaches through Playwright CDP, and drives the same normal import-add-play user journey from the renderer page. The harness must record pid, bundle id, frontmost/visible/window-count evidence, and AppKit preview-surface diagnostics when playback fails. Keep `_electron.launch` only where it is still the explicit dev harness; product E2E playback proof must prefer the packaged foreground path on macOS.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
    <automated>pnpm --filter @video-editor/desktop package:dir</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>The product E2E harness can distinguish foreground-launch failure from compositor failure and no longer depends on ElectronApplication-only APIs for the product playback assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04E-02: Add test-only observation IPC for CDP-attached journeys</name>
  <files>apps/desktop-electron/src/main/index.ts, apps/desktop-electron/src/preload/index.ts, apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/tests/helpers/userJourney.ts</files>
  <action>Move product test observations currently read through `ElectronApplication.evaluate` behind a test-only IPC/preload bridge that is enabled only when product test recording is explicitly requested. Expose host calls, execute-command calls, and realtime preview evidence as observations, not as control paths. Normal production UI must not expose this bridge, and the renderer must still only emit commands instead of owning render graph, GPU, FFmpeg, cache, or timeline semantics.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>CDP-attached product tests can read the same evidence as ElectronApplication tests without introducing production-facing debug UI or fallback playback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04E-03: Prove visible real GPU playback through the foreground product journey</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts, apps/desktop-electron/src/main/realtimePreviewHost.ts, crates/realtime_preview_runtime/src/platform/macos.rs</files>
  <action>Run the normal user path end to end: create/prepare project state as required by the test harness, import repo-owned video, add it to the timeline, press play, observe advancing target time, and assert real compositor presentation. Success requires non-occluded WGPU acquire, `present()`, `renderGraphGpuComposited`, increasing presented frame count, advancing playback time, and visible preview-region pixel change. DOM overlays, native single-video layers, offscreen CPU readback, requestPreviewFrame loops, first-frame stills, screenshots alone, or synthetic colors must fail the assertion.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop build:native</automated>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
    <automated>pnpm --filter @video-editor/desktop package:dir</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
    <automated>git diff --check -- . ':!reference'</automated>
  </verify>
  <done>The packaged foreground product journey proves real visible render-graph GPU playback, or the summary records a narrower blocker with fail-closed evidence and leaves 15.2-05/06 unreleased.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04E-04: Bound the remaining production surface strategy if foreground launch still cannot present</name>
  <files>crates/realtime_preview_runtime/src/platform/macos.rs, crates/realtime_preview_runtime/src/gpu/device.rs, apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/tests/product-user-journey.spec.ts</files>
  <action>If the packaged foreground CDP journey still cannot produce an occlusion-visible drawable, perform one bounded production-path repair only: either a native presenter ownership change that keeps the surface in a real product window, or a WGPU surface lifecycle repair such as resize/recreate on foreground transition. Do not add or accept AVPlayerLayer, DOM canvas/WebGPU, CPU readback, FFmpeg preview artifacts, screenshots-only validation, mock backend, or any fallback route as product playback.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop build:native</automated>
    <automated>pnpm --filter @video-editor/desktop build:electron</automated>
    <automated>pnpm --filter @video-editor/desktop package:dir</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>There is either a real product native WGPU presentation fix, or the phase stops with a precise unsupported-environment blocker; no fallback is introduced.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop build:native</automated>
<automated>pnpm --filter @video-editor/desktop build:electron</automated>
<automated>pnpm --filter @video-editor/desktop package:dir</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The macOS product E2E route launches a real foreground packaged app, drives the
normal user import-add-play flow through Playwright, and proves visible
render-graph GPU compositor playback with non-occluded WGPU presentation and
no fallback success path. If the host environment cannot produce a foreground
AppKit drawable, the blocker is recorded precisely and 15.2-05/06 remain
blocked.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04E-SUMMARY.md` when done.
</output>
