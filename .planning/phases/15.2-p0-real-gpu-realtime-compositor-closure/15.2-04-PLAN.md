---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04"
type: execute
wave: 4
depends_on:
  - "15.2-02"
  - "15.2-03"
files_modified:
  - "crates/realtime_preview_runtime/src/gpu/surface.rs"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "crates/bindings_node/src/native_preview_presenter.rs"
  - "apps/desktop-electron/src/main/nativeBinding.ts"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
autonomous: true
requirements:
  - P0-GPU-01
  - P0-GPU-04
  - TEST-E2E-01
  - NO-FALLBACK-01
user_setup: []
---

<objective>
Connect the render-graph GPU compositor to the desktop preview host so product
playback presents compositor output, not native-video or artifact output.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-04-01: Add desktop host tests for compositor presentation</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts</files>
  <action>Add E2E assertions that desktop playback reports render-graph GPU compositor evidence, visible preview content changes as time advances, no `requestPreviewFrame` playback loop occurs, and native single-video presentation is rejected as compositor success.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>E2E fails until the desktop host presents the render-graph compositor output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04-02: Implement compositor presentation bridge</name>
  <files>crates/realtime_preview_runtime/src/gpu/surface.rs, crates/bindings_node/src/realtime_preview_service.rs, crates/bindings_node/src/native_preview_presenter.rs, apps/desktop-electron/src/main/nativeBinding.ts, apps/desktop-electron/src/main/realtimePreviewHost.ts</files>
  <action>Attach a compositor-backed preview surface or presentation bridge to the Electron preview host. The bridge must consume Rust-owned render graph/frame state, update with play/pause/seek/scrub generation, and expose compositor evidence to UI/tests without leaking GPU commands or native handles into renderer code.</action>
  <verify>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>Desktop preview shows compositor output and state evidence identifies it as render-graph GPU composition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-04-03: Fail closed on unsupported presentation platforms</name>
  <files>crates/realtime_preview_runtime/src/gpu/surface.rs, crates/bindings_node/src/realtime_preview_service.rs, apps/desktop-electron/src/main/realtimePreviewHost.ts</files>
  <action>When the compositor presentation bridge is unavailable, return productized unavailable diagnostics and keep playback disabled/limited. Do not route to `AVPlayerLayer`, mock, offscreen, artifact, or CPU readback success.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>Unsupported hosts fail closed with clear diagnostics and cannot satisfy product gates.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
<automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The desktop preview host presents render-graph GPU compositor output as the
normal product playback path and fails closed otherwise.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04-SUMMARY.md` when done.
</output>

