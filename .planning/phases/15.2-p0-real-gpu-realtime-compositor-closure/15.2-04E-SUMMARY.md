---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "04E"
status: "blocked"
subsystem: "realtime-preview"
tags: ["wgpu", "electron", "macos", "appkit", "cdp", "product-e2e", "no-fallback", "surface-occlusion"]

requires:
  - phase: "15.2-04D"
    provides: "Foreground/AppKit occlusion diagnosis from Electron-window child NSView presenter"
provides:
  - "A foreground packaged-app + CDP product E2E harness that no longer depends on ElectronApplication.evaluate"
  - "A test-only preload/IPC observation bridge for command and realtime preview host calls"
  - "A bounded macOS child-window native WGPU presenter attempt with narrowed occlusion diagnostics"
  - "A fail-closed blocker: packaged foreground/CDP launch still leaves AppKit app/window occlusion-invisible in this automation environment"
affects: ["15.2-05", "15.2-06", "realtime-preview", "native-wgpu-surface", "product-e2e"]

tech-stack:
  added: []
  patterns:
    - "MacOS product E2E launches the packaged app through LaunchServices and attaches Playwright over CDP"
    - "CDP tests read main-process evidence through test-only IPC/preload observations"
    - "No fallback, native video bridge, CPU readback, DOM canvas, screenshot-only, or preview artifact route satisfies playback success"

key-files:
  created:
    - "apps/desktop-electron/tests/helpers/foregroundProductApp.ts"
    - ".planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04E-SUMMARY.md"
  modified:
    - "apps/desktop-electron/tests/helpers/userJourney.ts"
    - "apps/desktop-electron/src/main/index.ts"
    - "apps/desktop-electron/src/preload/index.ts"
    - "crates/realtime_preview_runtime/src/platform/macos.rs"

key-decisions:
  - "Do not proceed to 15.2-05 or 15.2-06: real visible render-graph GPU playback still fails before WGPU present."
  - "The packaged/CDP harness is retained because it removes the old ElectronApplication-only test dependency and proves product app evidence honestly."
  - "The macOS child-window presenter remains a real native WGPU product path attempt, not a fallback; it still fails closed when AppKit reports occlusion-invisible."
  - "Product success still requires non-occluded WGPU acquire, present, renderGraphGpuComposited evidence, presented frame count, advancing time, and visible preview-region change."

patterns-established:
  - "Local CDP endpoints must force 127.0.0.1/localhost/::1 through NO_PROXY/no_proxy to avoid system proxy 502 responses."
  - "Packaged-app product tests pass test options through CLI switches because LaunchServices `open --args` is the stable transport."
  - "Test-only observation bridges must be read-only and gated by explicit test recording mode."

requirements-completed: []

duration: "35min"
started: "2026-06-20T00:48:00Z"
completed: "2026-06-20T01:22:11Z"
---

# Phase 15.2 Plan 04E: Foreground Packaged Product Harness Summary

**The product E2E harness now launches the packaged app and attaches over CDP, but real visible GPU playback remains blocked because AppKit still reports both the parent and child native WGPU windows as occlusion-invisible.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-06-20T00:48:00Z
- **Completed:** 2026-06-20T01:22:11Z
- **Tasks completed:** 3 of 4 as implementation/diagnostic tasks
- **Status:** Blocked

## Accomplishments

- Added `foregroundProductApp.ts` to launch packaged `Video Editor.app` with `open -n -F ... --args --remote-debugging-port=...` and attach Playwright over CDP.
- Fixed local CDP attach under proxy-enabled environments by forcing localhost/127.0.0.1/::1 into `NO_PROXY`/`no_proxy`.
- Added CLI test switches for LaunchServices-launched packaged apps:
  - `--video-editor-test-record-commands=1`
  - `--video-editor-test-open-material-files=...`
  - `--video-editor-test-disable-render-graph-compositor=1`
- Added a test-only preload/IPC observation bridge:
  - `window.videoEditorTestObservations.getExecuteCommandCalls()`
  - `window.videoEditorTestObservations.getRealtimePreviewHostCalls()`
- Preserved the no-fallback product test: the missing-compositor test passes through packaged/CDP and still fails closed.
- Tried a real macOS native WGPU `childWindow` presenter, attached to the parent Electron window and carrying the same CAMetalLayer surface.

## Task Commits

1. **Task 15.2-04E-01: Add a foreground packaged-app CDP harness for product E2E** - `2ed2a97` (test)
2. **Task 15.2-04E-02: Add test-only observation IPC for CDP-attached journeys** - `c3483bd` (fix)
3. **Task 15.2-04E-04: Bound the remaining production surface strategy if foreground launch still cannot present** - `28ec677` (fix)

Task 15.2-04E-03 remains blocked: the product journey reaches native WGPU surface presentation, then WGPU returns `surface is occluded`.

## Verification

Passed:

- `cargo test -p realtime_preview_runtime gpu_surface_presenter -- --nocapture`
- `cargo test -p bindings_node realtime_preview -- --nocapture`
- `pnpm --filter @video-editor/desktop build:electron`
- `pnpm --filter @video-editor/desktop package:dir`
- `pnpm run test:no-product-fallback`
- `git diff --check -- . ':!reference'`

Blocked / failed closed:

- `pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line`

Observed product E2E result:

- Missing-compositor product test: **passed**. The harness rejects fallback/native bridge playback as product success.
- Real GPU playback product test: **failed closed** waiting for `renderGraphGpuComposited`.

Representative failure:

```text
NativePresenter: render graph GPU scheduler failed: wgpu surface texture acquire failed: surface is occluded; drawableLifecycle{attachmentStrategy=childWindow, prepareCount=2, appActive=false, appHidden=false, runningAppActive=false, runningAppHidden=false, appActivationPolicy=regular, appOcclusionVisible=false, parentWindowVisible=true, parentWindowOcclusionVisible=false, parentWindowOnActiveSpace=true, childWindowVisible=true, childWindowOcclusionVisible=false, childWindowOnActiveSpace=true, childHasParent=true, childViewHasSuperview=true, layerHidden=false, drawableSize={w=320.00,h=179.00} }
```

## Issues Encountered

The 04E packaged/CDP harness removed the old `_electron.launch`/`ElectronApplication.evaluate` limitation, so the remaining failure is no longer a test-observation artifact. The app still reports:

- `appActive=false`
- `runningAppActive=false`
- `appOcclusionVisible=false`
- `parentWindowOcclusionVisible=false`
- `childWindowVisible=true`
- `childWindowOcclusionVisible=false`
- `childHasParent=true`
- `drawableSize` nonzero

The child-window attempt proves the CAMetalLayer can be hosted in a real AppKit child window, but WGPU still refuses to acquire the surface because the associated AppKit window occlusion state is not visible.

## Known Stubs

None. No fallback, mock, offscreen, CPU readback, DOM canvas/WebGPU, native video bridge, screenshot-only success, FFmpeg artifact loop, or requestPreviewFrame playback path was added or accepted.

## Threat Flags

| Flag | File | Description |
| ---- | ---- | ----------- |
| threat_flag: test-only-ipc | `apps/desktop-electron/src/main/index.ts`, `apps/desktop-electron/src/preload/index.ts` | Read-only observation bridge is gated by explicit test recording mode and does not expose production debug UI. |
| threat_flag: native-window-surface | `crates/realtime_preview_runtime/src/platform/macos.rs` | Owns AppKit child-window/CAMetalLayer lifecycle for native WGPU presentation; it is a product surface attempt, not a fallback rendering route. |
| threat_flag: product-e2e-blocker | `apps/desktop-electron/tests/product-user-journey.spec.ts` | Product journey correctly fails closed until real visible render-graph GPU playback is proven. |

## User Setup Required

None for the committed harness. The remaining blocker appears to require an AppKit foreground/occlusion-visible environment or a different native surface ownership approach; 15.2-05/06 must not run until that is resolved.

## Next Phase Readiness

Not ready. Phase 15.2 remains blocked before 15.2-05/06 because real visible GPU realtime playback has not passed the product user journey.

## Self-Check: FAILED

- Summary file exists: `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-04E-SUMMARY.md`
- Task commits found: `2ed2a97`, `c3483bd`, `28ec677`
- Build/native/no-fallback targeted gates pass.
- Product E2E still fails closed before `present()` and before `renderGraphGpuComposited`.
- No requirement completion was recorded for P0 real GPU playback.

---

*Phase: 15.2-p0-real-gpu-realtime-compositor-closure*
*Status: blocked*
