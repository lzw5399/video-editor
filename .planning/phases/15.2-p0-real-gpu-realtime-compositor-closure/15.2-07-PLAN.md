---
phase: "15.2-p0-real-gpu-realtime-compositor-closure"
plan: "07"
type: execute
wave: 14
depends_on:
  - "15.2-06"
files_modified:
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "crates/bindings_node/src/audio_service.rs"
  - "crates/realtime_preview_runtime/src/**"
  - "crates/audio_output_desktop/src/**"
  - "crates/audio_engine/src/**"
autonomous: true
requirements:
  - P0-GPU-02
  - P0-GPU-03
  - P0-GPU-04
  - TEST-E2E-01
  - TEST-E2E-02
  - NO-FALLBACK-01
user_setup: []
---

<objective>
Reopen Phase 15.2 and close the product playback UAT gap found after aggregate
verification: preview playback must appear in the correct monitor location,
produce real audio output, stay synchronized with the timeline, and expose only
editing interactions that work end-to-end.
</objective>

<context>
Manual UAT after `15.2-06` invalidated the previous closeout. The existing
product E2E suite still passes because it only checks compositor evidence,
center-pixel motion, command calls, and short telemetry advancement. It does not
prove surface alignment, audible/native audio output, clock synchronization to
playback completion, or direct on-canvas text/transform interaction.

This plan must not restore fallback behavior. Mock, native-video bridge,
preview artifact frames, CPU probes, DOM overlays, and first-frame success still
cannot satisfy product playback.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.2-07-01: Add failing normal-user playback UAT gates</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts</files>
  <action>Add Playwright/Electron cases that reproduce the reported failures from the UI: import a repo-owned real video, add it to the timeline, press play, and assert preview surface placement, native/audio playback evidence, timeline/video sync through completion, pause/stop behavior, and no post-stop frame advancement.</action>
  <details>
  - Placement must compare the visible native/WGPU child surface screen frame with the preview host/container rect within a small tolerance, not just hash a center crop.
  - Audio must prove the product path opened a native output device and presented buffers for the current playback generation. Mock output, status-only runtime state, or device-list presence is not enough.
  - Sync must assert the product clock, UI playhead, scheduler target time, and presented content stay within a defined tolerance during playback and after sequence end.
  - The test must fail if playback success is satisfied by `requestPreviewFrame`, preview artifacts, mock/offscreen evidence, native video bridge evidence, DOM overlays, or a first-frame-only path.
  </details>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line -g "product playback UAT"</automated>
  </verify>
  <done>The new UAT tests fail against the current implementation for the real reported gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-07-02: Replace renderer-owned playback time with one product clock</name>
  <files>apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/src/main/realtimePreviewHost.ts, crates/bindings_node/src/realtime_preview_service.rs, crates/realtime_preview_runtime/src/**</files>
  <action>Make Rust/main own playback clock advancement and scheduler presentation. The renderer should display accepted playback state and send user commands, but it must not advance the authoritative playhead with a standalone `requestAnimationFrame` loop.</action>
  <details>
  - Playback start must create one generation shared by video scheduler, audio preview/output, and UI clock.
  - The scheduler must continuously present frames while playing, not rely on occasional telemetry polling as the frame pump.
  - Pause/stop/end-of-sequence must stop both scheduler advancement and UI time advancement deterministically.
  - Telemetry exposed to the UI/tests must include safe clock facts: state, generation, position, scheduler target time, presented frame count, dropped/repeated counts, and stopped reason.
  </details>
  <verify>
    <automated>cargo test -p realtime_preview_runtime playback -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
  </verify>
  <done>Timeline time and presented video are driven by the same product clock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-07-03: Fix native/WGPU preview surface geometry</name>
  <files>apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/src/main/realtimePreviewHost.ts, crates/realtime_preview_runtime/src/platform/macos.rs, crates/bindings_node/src/realtime_preview_service.rs</files>
  <action>Correct the coordinate contract from DOM preview host to native child surface. Compute and verify window/content-view-local geometry in one place, account for titlebar/content bounds and display scale, and expose safe placement diagnostics for E2E.</action>
  <details>
  - Do not let product success depend on a center crop that can still change when the surface is shifted.
  - Keep geometry data as UI/native presentation facts only; do not move draft, render graph, FFmpeg, cache, or timeline semantics into the renderer.
  - Add tests that fail if the native surface is left/down/up/right from the preview host beyond tolerance.
  </details>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line -g "preview surface placement"</automated>
  </verify>
  <done>The WGPU presentation surface is visually aligned with the preview monitor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-07-04: Connect real audio output to product playback</name>
  <files>crates/bindings_node/src/audio_service.rs, crates/audio_output_desktop/src/**, crates/audio_engine/src/**, apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/tests/product-user-journey.spec.ts</files>
  <action>Wire product audio preview to the native desktop audio output path and the shared playback generation. Status-only audio runtime state and mock output cannot satisfy product playback.</action>
  <details>
  - Product playback must fail closed with a clear unavailable diagnostic if native audio output is unavailable.
  - The E2E gate may assert safe native-output telemetry such as backend, device class, generation, presented buffer count, and active/stopped state; it must not require listening through a microphone.
  - Export audio validation remains separate and cannot be used as preview audio success.
  </details>
  <verify>
    <automated>cargo test -p audio_output_desktop native_output -- --nocapture</automated>
    <automated>cargo test -p bindings_node audio_service -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line -g "audio playback"</automated>
  </verify>
  <done>Pressing play starts native audio output synchronized with the video/playhead generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-07-05: Gate or implement direct text and transform interactions</name>
  <files>apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/src/renderer/workspace/InspectorPanel.tsx, apps/desktop-electron/tests/product-user-journey.spec.ts</files>
  <action>Normal users must be able to edit exposed text/font controls and directly manipulate visible canvas handles when those controls appear functional. Otherwise the default production UI must hide/gate the control until the interaction is implemented.</action>
  <details>
  - Add text content/font/size/color editing through visible production controls.
  - Add or gate on-canvas selection, drag, resize, rotate, crop handles, and deletion.
  - E2E must simulate mouse/keyboard operations and assert accepted Rust commands plus visible preview/export effect for the supported subset.
  </details>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line -g "text and transform interaction"</automated>
  </verify>
  <done>No visible production editing affordance remains fake or untested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.2-07-06: Reclose Phase 15.2 with invalidation-resistant verification</name>
  <files>package.json, scripts/no-product-fallback-guards.sh, .planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Update aggregate gates so Phase 15.2 cannot close unless the new normal-user UAT cases pass. Record the manual UAT regression as the reason 15.2 was reopened and the evidence that closes it again.</action>
  <verify>
    <automated>pnpm run test:phase15-2</automated>
    <automated>pnpm run test:no-product-fallback</automated>
    <automated>cargo check --workspace --locked</automated>
    <automated>git diff --check -- . ':!reference'</automated>
  </verify>
  <done>Phase 15.2 is only marked complete after the real product playback UAT suite passes.</done>
</task>

</tasks>

<verification>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm run test:phase15-2</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>cargo check --workspace --locked</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Phase 15.2 can be reclosed only when a normal user can import a real video,
add it to the timeline, press play, and observe correctly placed, audible,
smooth, synchronized playback, with exposed text/transform interactions either
working end-to-end or hidden/gated from the production UI.
</success_criteria>

<output>
Create `.planning/phases/15.2-p0-real-gpu-realtime-compositor-closure/15.2-07-SUMMARY.md` when done.
</output>
