---
phase: "04.1-professional-jianying-workspace-ui-refinement"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "apps/desktop-electron/src/renderer/workspace/Inspector.tsx"
  - "apps/desktop-electron/src/renderer/workspace/preview-inspector.css"
autonomous: true
requirements:
  - UI-09
  - UI-10
  - UI-12
must_haves:
  truths:
    - "Preview region reads as a professional monitor shell with title, black 16:9 canvas, transport controls, ratio/fit/fullscreen controls, and preview status slots."
    - "Inspector exposes Jianying-style tabs and compact rows for draft parameters or selected segment controls."
    - "Text/audio inspector edits still commit only through existing callback props from App.tsx."
  artifacts:
    - path: "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
      provides: "Shell-only preview monitor structure and accessible controls"
    - path: "apps/desktop-electron/src/renderer/workspace/Inspector.tsx"
      provides: "Tabbed inspector with draft parameters, segment controls, and keyframe reserved buttons"
    - path: "apps/desktop-electron/src/renderer/workspace/preview-inspector.css"
      provides: "Component-scoped preview and inspector density styles"
  key_links:
    - from: "PreviewMonitor.tsx"
      to: "viewModel.ts"
      via: "formatMicroseconds for read-only time display"
      pattern: "formatMicroseconds"
    - from: "Inspector.tsx"
      to: "App.tsx"
      via: "onEditSelectedText, onSetSelectedSegmentVolume, onSetSelectedTrackMute props"
      pattern: "onEditSelectedText"
---

<objective>
Refine the preview monitor shell and right inspector into professional Jianying-style control surfaces while keeping real preview/export/rendering outside Phase 04.1 for UI-09, UI-10, and UI-12.

Purpose: Phase 05 needs stable UI slots for preview/export integration, but this phase must not implement media/render behavior in the renderer.
Output: Monitor shell, transport/status controls, inspector tabs, compact parameter rows, and shell-only keyframe controls.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-professional-jianying-workspace-ui-refinement/04.1-CONTEXT.md
@.planning/phases/04.1-professional-jianying-workspace-ui-refinement/04.1-UI-SPEC.md
@.planning/phases/04.1-professional-jianying-workspace-ui-refinement/04.1-RESEARCH.md
@.planning/phases/04.1-professional-jianying-workspace-ui-refinement/04.1-PATTERNS.md
@apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx
@apps/desktop-electron/src/renderer/workspace/Inspector.tsx
@apps/desktop-electron/src/renderer/viewModel.ts
@apps/desktop-electron/src/renderer/App.tsx
@apps/desktop-electron/src/renderer/commandHelpers.ts
</context>

## Artifacts this phase produces

- `preview-inspector.css` imported only by `PreviewMonitor.tsx` and `Inspector.tsx`.
- `MONITOR_CONTROLS` or equivalent local preview control metadata with dependency-free symbols.
- `INSPECTOR_TABS` for `画面`, `音频`, `变速`, `动画`, `调节`, `AI效果`.
- Compact inspector sections for `草稿参数`, segment details, text controls, audio controls, transform shell controls, and `关键帧功能待接入` buttons.

<tasks>

<task type="auto">
  <name>Task 1: Build shell-only professional preview monitor</name>
  <read_first>
    <file>apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx</file>
    <file>apps/desktop-electron/src/renderer/viewModel.ts</file>
    <file>.planning/phases/04.1-professional-jianying-workspace-ui-refinement/04.1-UI-SPEC.md</file>
    <file>.planning/phases/04.1-professional-jianying-workspace-ui-refinement/04.1-CONTEXT.md</file>
  </read_first>
  <files>apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/src/renderer/workspace/preview-inspector.css</files>
  <action>Implement UI-09 by expanding `PreviewMonitor.tsx` into a monitor shell with `preview-shell`, `preview-titlebar`, `preview-canvas`, `preview-transport`, and `preview-status-line` regions. Import `./preview-inspector.css` and `formatMicroseconds` from `viewModel.ts`. Keep props limited to `draftName`, `bindingStatus`, and an optional `playheadUs` defaulting to 0 so the component remains compatible with the existing `WorkspaceShell.tsx` call during Wave 1. Title bar left copy is the draft name, title bar right copy is `预览待接入`. Canvas is black, 16:9, centered, and shows small copy `预览画面将在下一阶段接入`. Add bottom dependency-free symbol controls for `播放`, `停止`, `上一帧`, `下一帧`, `适应窗口`, `画面比例`, `全屏`; every icon-only button must have Chinese `aria-label` and `title`, fixed 32px dimensions, and disabled/shell-only behavior except any existing display-only stop behavior. Include read-only time text from `formatMicroseconds(playheadUs ?? 0)`, status `等待预览帧接入`, and resolution/FPS slots such as `16:9` and `30 fps` as shell labels. Do not create preview frame objects, cache keys, thumbnails, waveform data, FFmpeg/ffprobe calls, render graph structures, export script strings, file paths, Node/Electron imports, or bridge calls.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build</automated>
    <automated>pnpm run test:phase4-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - Preview monitor has title bar, black 16:9 canvas, bottom transport controls, status line, and reserved integration slots without media/render semantics.
    - Every preview icon-only button has Chinese `aria-label` and `title`.
    - Visible copy is Simplified Chinese and does not expose render graph, FFmpeg, cache, export, schema, or binding terminology.
    - `PreviewMonitor.tsx` does not call `window.videoEditorCore` and does not import Node/Electron APIs.
  </acceptance_criteria>
  <done>UI-09 preview shell is visually ready and renderer-safe without implementing preview/export behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build Jianying-style inspector tabs and compact controls</name>
  <read_first>
    <file>apps/desktop-electron/src/renderer/workspace/Inspector.tsx</file>
    <file>apps/desktop-electron/src/renderer/App.tsx</file>
    <file>apps/desktop-electron/src/renderer/commandHelpers.ts</file>
    <file>apps/desktop-electron/src/renderer/viewModel.ts</file>
    <file>.planning/phases/04.1-professional-jianying-workspace-ui-refinement/04.1-PATTERNS.md</file>
  </read_first>
  <files>apps/desktop-electron/src/renderer/workspace/Inspector.tsx, apps/desktop-electron/src/renderer/workspace/preview-inspector.css</files>
  <action>Implement UI-10 by adding local `INSPECTOR_TABS` with `画面`, `音频`, `变速`, `动画`, `调节`, `AI效果` and local active tab state in `Inspector.tsx`. Replace the no-selection empty state with a `草稿参数` panel showing read-only draft name, canvas ratio/resolution shell labels, sequence duration from existing draft/timeline data, track count, material count, snapping status from `workspace.commandState.snapping`, and `workspace.bindingStatus.label`. For selected segments, keep existing `getSelectedSegmentView` derivation and existing `onEditSelectedText`, `onSetSelectedSegmentVolume`, and `onSetSelectedTrackMute` callback paths. Add compact rows for material name, track, source time, target time, text controls, audio volume/mute controls, and non-mutating transform controls for 位置, 缩放, 旋转, 不透明度. Add keyframe buttons with diamond/plus symbols, Chinese `aria-label="关键帧功能待接入"`, matching `title`, and disabled state. Use sliders, numeric inputs, switches/checkboxes, segmented controls, and color swatches as specified; row heights must stay 28-36px via `preview-inspector.css`. Do not add command builders, draft mutation, undo/redo logic, snapping computation, preview/cache/render/export behavior, or package dependencies.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build</automated>
    <automated>pnpm run test:phase4-source-guards</automated>
  </verify>
  <acceptance_criteria>
    - Inspector shows the six primary tabs and compact rows with Simplified Chinese copy.
    - No selection shows `草稿参数` instead of a blank-only state.
    - Selected text segment editing still calls `onEditSelectedText`; selected audio controls still call `onSetSelectedSegmentVolume` and `onSetSelectedTrackMute`.
    - Transform and keyframe controls are visible, disabled or shell-only, and non-mutating because Rust semantics do not exist in this phase.
    - Every icon-only keyframe button has Chinese `aria-label` and `title`.
  </acceptance_criteria>
  <done>UI-10 inspector is tabbed, dense, segment-aware, and still command-only for committed edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer preview shell to Phase 05 render services | Preview UI reserves controls but must not construct media/render artifacts. |
| inspector local form state to Rust command callbacks | Inspector may hold form values, but committed semantics flow through App callback props. |
| compact symbol controls to accessibility tree | Symbol-only controls must retain Chinese accessible names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-04 | Tampering | `Inspector.tsx` selected segment controls | mitigate | Keep text/audio commits on existing callback props and never mutate selected segment fields, track mute, timeranges, undo/redo, or snapping locally. |
| T-04.1-05 | Elevation of Privilege | `PreviewMonitor.tsx` controls | mitigate | Preview controls remain shell-only and source guards block FFmpeg, ffprobe, render graph, preview cache, thumbnail, waveform, proxy, and export script terms. |
| T-04.1-06 | Information Disclosure | icon-only preview/keyframe controls | mitigate | Every icon-only control gets Chinese `aria-label` and `title`; tests in Plan 04 assert role locators. |
| T-04.1-SC | Tampering | npm installs | mitigate | No package install in this plan; use text/CSS symbols only. |
</threat_model>

<verification>
Run after both tasks:

<automated>pnpm --filter @video-editor/desktop build</automated>
<automated>pnpm run test:phase4-source-guards</automated>

Plan 04 updates the Playwright expectations for the new preview and inspector copy.
</verification>

<success_criteria>
- UI-09 and UI-10 are implemented with professional monitor and inspector structure.
- UI-12 remains true: no renderer-owned preview/export/media semantics and no direct draft mutation.
- Preview and inspector controls remain accessible with Chinese labels and no icon package churn.
</success_criteria>

<output>
Create `.planning/phases/04.1-professional-jianying-workspace-ui-refinement/04.1-02-SUMMARY.md` when done.
</output>
