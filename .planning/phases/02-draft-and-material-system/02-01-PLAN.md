---
phase: 02-draft-and-material-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/draft_model/src/lib.rs
  - crates/draft_model/src/ids.rs
  - crates/draft_model/src/time.rs
  - crates/draft_model/src/draft.rs
  - crates/draft_model/src/material.rs
  - crates/draft_model/src/timeline.rs
  - crates/draft_model/src/validation.rs
  - crates/draft_model/tests/draft_schema.rs
autonomous: true
requirements:
  - DRAFT-03
  - DRAFT-04
  - DRAFT-05
must_haves:
  truths:
    - "Draft schema uses Jianying-aligned names directly: Draft, Material, Track, Segment, SourceTimerange, TargetTimerange, MainTrackMagnet, Keyframe, Filter, and Transition per D-06 and DRAFT-03."
    - "Persisted draft time values are integer microseconds or rational structures, with no naked floating persisted seconds per D-07."
    - "Schema version 1 loads through explicit migration hooks and unknown future versions fail with a structured recoverable error per D-09."
    - "The persisted Draft model excludes derived thumbnails, waveforms, preview caches, render graphs, FFmpeg scripts, exports, and raw probe JSON per D-01 and DRAFT-04."
  artifacts:
    - path: crates/draft_model/src/draft.rs
      provides: "Draft, DraftMetadata, DraftSchemaVersion, Draft::new, migration entrypoint"
    - path: crates/draft_model/src/material.rs
      provides: "Material, MaterialKind, MaterialStatus, MaterialMetadata, RationalFrameRate"
    - path: crates/draft_model/src/timeline.rs
      provides: "Track, TrackKind, Segment, SourceTimerange, TargetTimerange, MainTrackMagnet, Keyframe, Filter, Transition"
    - path: crates/draft_model/src/ids.rs
      provides: "deterministic caller-supplied ID newtypes for DraftId, MaterialId, TrackId, SegmentId"
    - path: crates/draft_model/src/validation.rs
      provides: "DraftValidationError, validate_draft, migrate_draft_json"
  key_links:
    - from: crates/draft_model/src/validation.rs
      to: crates/draft_model/src/draft.rs
      via: "validate_draft and migrate_draft_json"
      pattern: "validate_draft|migrate_draft_json"
---

<objective>
Define the pure Rust draft/material/track/segment semantic model and migration hooks for Phase 2.

Purpose: Establish the canonical `.veproj/project.json` model before persistence, material import, binding commands, Electron smoke, or fixtures consume it. This implements DRAFT-03, DRAFT-04, and DRAFT-05 while honoring D-01, D-02, D-03, D-06, D-07, D-08, and D-09.

Output: Pure `draft_model` types, validation/migration APIs, and unit tests for strict schema behavior.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-draft-and-material-system/02-CONTEXT.md
@.planning/phases/02-draft-and-material-system/02-RESEARCH.md
@.planning/phases/02-draft-and-material-system/02-VALIDATION.md
@.planning/phases/02-draft-and-material-system/02-PATTERNS.md
@docs/runtime-boundaries.md
@crates/draft_model/src/lib.rs
@crates/draft_model/tests/contract.rs
</context>

## Artifacts this phase produces

- `Draft`, `DraftMetadata`, `DraftSchemaVersion`, `DraftId`, `MaterialId`, `TrackId`, `SegmentId`.
- `Material`, `MaterialKind`, `MaterialStatus`, `MaterialMetadata`, `RationalFrameRate`.
- `Track`, `TrackKind`, `Segment`, `SourceTimerange`, `TargetTimerange`, `MainTrackMagnet`, `Keyframe`, `Filter`, `Transition`.
- `DraftValidationError`, `validate_draft`, `migrate_draft_json`, and schema-version error paths.

<tasks>

<task type="auto">
  <name>Task 1: Define pure draft/material/timeline schema types</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
    @.planning/phases/02-draft-and-material-system/02-PATTERNS.md
    @docs/runtime-boundaries.md
    @crates/draft_model/src/lib.rs
  </read_first>
  <files>crates/draft_model/src/lib.rs, crates/draft_model/src/ids.rs, crates/draft_model/src/time.rs, crates/draft_model/src/draft.rs, crates/draft_model/src/material.rs, crates/draft_model/src/timeline.rs</files>
  <action>
    Extend `draft_model` with pure semantic modules and re-exports. Create caller-supplied string newtypes `DraftId`, `MaterialId`, `TrackId`, and `SegmentId` without adding `uuid`, preserving deterministic fixture IDs per the package constraint and D-02. Define `Microseconds(u64)` or equivalent integer persisted time wrapper, `RationalFrameRate { numerator, denominator }`, `SourceTimerange`, and `TargetTimerange`; no persisted `f32`, `f64`, `durationSeconds`, or raw decimal seconds fields are allowed per D-07. Define `Draft` with `schemaVersion`, `draftId`, `metadata`, `materials`, `tracks`, and enough empty sequence/timeline shell state to create a valid saveable new draft per D-02 and D-08. Define `Material`, `MaterialKind`, `MaterialStatus`, `MaterialMetadata`, `Track`, `TrackKind`, `Segment`, `MainTrackMagnet`, `Keyframe`, `Filter`, and `Transition` using `serde(rename_all = "camelCase", deny_unknown_fields)`, `JsonSchema`, `TS`, `Debug`, `Clone`, `PartialEq`, and `Eq` where valid. Use Jianying-aligned names exactly per D-06 and do not introduce `Asset` or `Clip` aliases. Keep this crate free of filesystem, FFmpeg, Electron, `PlatformFileSystem`, and `FfmpegExecutor` imports per D-03.
  </action>
  <verify>
    <automated>cargo test -p draft_model draft_schema -- --nocapture</automated>
    <automated>grep -R "FfmpegExecutor\|PlatformFileSystem\|std::fs\|std::process" crates/draft_model/src && exit 1 || true</automated>
    <automated>grep -R "durationSeconds\|seconds: f32\|seconds: f64\|Asset\|Clip" crates/draft_model/src crates/draft_model/tests | grep -v "must not" && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p draft_model draft_schema -- --nocapture` includes passing assertions for valid empty `Draft::new`, valid material/track/segment records, strict unknown-field rejection, integer microsecond serialization, rational frame rate serialization, and absence of derived artifact fields from `Draft`. Source grep shows no pure semantic dependency on runtime/filesystem/process traits and no internal `Asset`/`Clip` aliases.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add validation and migration hooks</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
    @.planning/phases/02-draft-and-material-system/02-VALIDATION.md
    @crates/draft_model/src/lib.rs
    @crates/draft_model/tests/contract.rs
  </read_first>
  <files>crates/draft_model/src/lib.rs, crates/draft_model/src/validation.rs, crates/draft_model/tests/draft_schema.rs</files>
  <action>
    Implement `DraftSchemaVersion` with current version `1`, `DraftValidationError` with structured variants for invalid schema version, missing required semantic fields, invalid timerange, invalid rational frame rate, duplicate IDs, and derived artifact leakage. Add `validate_draft(&Draft) -> Result<(), DraftValidationError>` and `migrate_draft_json(serde_json::Value) -> Result<Draft, DraftValidationError>` or an equivalent API that loads version `1` and rejects unknown future versions with a recoverable structured error per D-09. Keep migration semantics in `draft_model`; `project_store` will only call these APIs per D-03. Tests must compare semantic Rust values and explicit error variants, not string-only errors.
  </action>
  <verify>
    <automated>cargo test -p draft_model migration -- --nocapture</automated>
    <automated>cargo test -p draft_model draft_schema -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    Unknown future `schemaVersion` JSON fails with the expected structured migration/version error. Version `1` valid JSON migrates into `Draft` and passes `validate_draft`. Invalid timeranges, zero frame-rate denominator, duplicate IDs, and derived artifact keys fail through explicit validation or strict serde/schema rejection.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project.json -> draft_model | Untrusted persisted JSON crosses into strict Rust schema and migration logic. |
| draft_model -> generated contracts | Rust-owned semantic types later feed generated JSON Schema and TypeScript artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `Draft` serde/schema | mitigate | Use `deny_unknown_fields`, semantic validation, and negative tests for malformed `project.json`, with explicit validation errors. |
| T-02-02 | Tampering | Draft terminology/model | mitigate | Use D-06 names in Rust/schema/TS and add source grep/tests preventing internal `Asset`/`Clip` aliases. |
| T-02-03 | Tampering | Schema migration | mitigate | Reject unknown future schema versions with structured recoverable error per D-09; never silently coerce. |
| T-02-04 | Information Disclosure/Tampering | Semantic draft model | mitigate | Exclude derived paths and raw probe JSON from persisted `Draft` per D-01 and DRAFT-04. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new external package install is planned; deterministic ID newtypes avoid `uuid`. If an executor introduces a new package, stop and add a blocking human legitimacy checkpoint before install. |
</threat_model>

<verification>
Run the narrow gates in task order, then:

<automated>cargo test -p draft_model -- --nocapture</automated>
<automated>grep -R "FfmpegExecutor\|PlatformFileSystem\|std::fs\|std::process" crates/draft_model/src && exit 1 || true</automated>
</verification>

<success_criteria>
- DRAFT-03 is covered by Rust tests using Jianying-aligned terms.
- DRAFT-04 is covered by the absence of derived artifacts in persisted `Draft` and validation/schema tests.
- DRAFT-05 is covered by current schema version `1`, explicit migration entrypoint, and structured unknown-version failure.
</success_criteria>

<output>
Create `.planning/phases/02-draft-and-material-system/02-01-SUMMARY.md` when done.
</output>
