---
phase: 02-draft-and-material-system
plan: 06
type: execute
wave: 5
depends_on:
  - 02-05
files_modified:
  - fixtures/draft/positive/minimal-draft/project.json
  - fixtures/draft/positive/materials-round-trip/project.json
  - fixtures/draft/positive/missing-material/project.json
  - fixtures/draft/negative/invalid-unknown-field/project.json
  - fixtures/draft/negative/invalid-schema-version/project.json
  - fixtures/draft/negative/derived-artifact-in-project-json/project.json
  - crates/draft_model/tests/draft_fixtures.rs
  - justfile
  - package.json
autonomous: true
requirements:
  - DRAFT-01
  - DRAFT-02
  - DRAFT-03
  - DRAFT-04
  - DRAFT-05
  - MAT-01
  - MAT-02
  - MAT-03
  - MAT-04
must_haves:
  truths:
    - "Positive and negative `.veproj/project.json` fixtures cover draft creation, semantic round trips, material import metadata, missing-material preservation, derived-artifact exclusion, and schema migration failures per D-17 and D-18."
    - "Every Phase 2 requirement ID is covered by automated tests and at least one committed fixture, integration test, generated contract, or smoke test."
    - "Generated schema and TypeScript artifacts have no drift after Rust tests run."
    - "The final phase gate passes through `PATH=\"$HOME/.cargo/bin:$PATH\" just build`, `PATH=\"$HOME/.cargo/bin:$PATH\" just test`, and `git diff --exit-code schemas apps/desktop-electron/src/generated`."
  artifacts:
    - path: fixtures/draft/positive/minimal-draft/project.json
      provides: "valid new draft fixture"
    - path: fixtures/draft/positive/materials-round-trip/project.json
      provides: "valid materials metadata fixture"
    - path: fixtures/draft/positive/missing-material/project.json
      provides: "recoverable missing material fixture"
    - path: fixtures/draft/negative/invalid-unknown-field/project.json
      provides: "strict schema rejection fixture"
    - path: fixtures/draft/negative/invalid-schema-version/project.json
      provides: "unknown version rejection fixture"
    - path: fixtures/draft/negative/derived-artifact-in-project-json/project.json
      provides: "derived artifact exclusion fixture"
  key_links:
    - from: crates/draft_model/tests/draft_fixtures.rs
      to: fixtures/draft
      via: "explicit positive/negative fixture classifier"
      pattern: "positive|negative"
    - from: package.json
      to: justfile
      via: "named Phase 2 gate scripts invoked by just test"
      pattern: "test:project-store|test:material-probe|test:draft-fixtures"
---

<objective>
Add draft/material golden fixtures and final schema/model/project-store/media-runtime/binding validation gates for the full Phase 2 requirement set.

Purpose: Convert Phase 2 behaviors into deterministic, executable proof so later timeline, UI, preview, and export work build on stable draft/material semantics. This implements DRAFT-01 through DRAFT-05 and MAT-01 through MAT-04 while honoring D-01 through D-19.

Output: Classified `.veproj/project.json` fixtures, fixture tests, explicit Phase 2 gate scripts, source guards, and final drift checks.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-draft-and-material-system/02-CONTEXT.md
@.planning/phases/02-draft-and-material-system/02-RESEARCH.md
@.planning/phases/02-draft-and-material-system/02-VALIDATION.md
@.planning/phases/02-draft-and-material-system/02-PATTERNS.md
@.planning/phases/02-draft-and-material-system/02-01-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-02-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-03-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-04-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-05-SUMMARY.md
@schemas/draft.schema.json
@justfile
@package.json
</context>

## Artifacts this phase produces

- Positive `.veproj/project.json` fixtures for minimal draft, material round trip, and missing material.
- Negative `.veproj/project.json` fixtures for unknown fields, unsupported schema version, and derived artifact leakage.
- Fixture classification and schema/model validation tests.
- Explicit Phase 2 package/just gate commands for draft fixtures, project-store bundle tests, material probe tests, material service tests, binding smoke, Electron smoke, and generated contract drift.
- Final source guards for Jianying terminology, integer time semantics, renderer FFmpeg isolation, and derived-artifact exclusion.

## Source coverage audit

| Source | ID | Coverage |
|--------|----|----------|
| GOAL | Phase 2 goal | Covered by Plans 02-01 through 02-06: schema, persistence, probing, import service, smoke display, and final gates. |
| REQ | DRAFT-01, DRAFT-02, DRAFT-03, DRAFT-04, DRAFT-05 | Covered in plan frontmatter and verification for 02-01, 02-02, 02-04, and 02-06. |
| REQ | MAT-01, MAT-02, MAT-03, MAT-04 | Covered in plan frontmatter and verification for 02-03, 02-04, 02-05, and 02-06. |
| RESEARCH | Responsibility map | Covered by explicit plan split: `project_store` persists, `media_runtime` probes, material service orchestrates, Electron smokes. |
| CONTEXT | D-01 through D-19 | Covered by citations across plan truths, actions, verification, and acceptance criteria; no deferred ideas are implemented. |

<tasks>

<task type="auto">
  <name>Task 1: Add classified draft/material project fixtures</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
    @.planning/phases/02-draft-and-material-system/02-VALIDATION.md
    @schemas/draft.schema.json
  </read_first>
  <files>fixtures/draft/positive/minimal-draft/project.json, fixtures/draft/positive/materials-round-trip/project.json, fixtures/draft/positive/missing-material/project.json, fixtures/draft/negative/invalid-unknown-field/project.json, fixtures/draft/negative/invalid-schema-version/project.json, fixtures/draft/negative/derived-artifact-in-project-json/project.json, crates/draft_model/tests/draft_fixtures.rs</files>
  <action>
    Create deterministic `.veproj`-style fixture folders under `fixtures/draft/positive` and `fixtures/draft/negative`, each containing a `project.json`. Exclude existing Phase 1 command fixtures from the project fixture walker with clear test code; do not leave unclassified project JSON. Positive fixtures must cover a valid new empty draft per D-02, a draft with video/image/audio material metadata per D-11, and a draft preserving a missing material entry per D-14/D-15. Negative fixtures must cover unknown field rejection, unsupported schema version rejection per D-09, and derived artifact leakage such as thumbnail/probe/render/cache fields per D-01/DRAFT-04. Add `draft_fixtures.rs` so every fixture path is explicitly classified and positive fixtures pass both Rust deserialization/migration and JSON Schema validation while negative fixtures fail the intended gate.
  </action>
  <verify>
    <automated>cargo test -p draft_model schema_fixtures -- --nocapture</automated>
    <automated>cargo test -p draft_model draft_fixtures -- --nocapture</automated>
    <automated>find fixtures/draft -name project.json | sort</automated>
  </verify>
  <acceptance_criteria>
    Fixture tests fail if any `.veproj/project.json` fixture is unclassified. Positive fixtures load to semantically valid `Draft` values and validate against generated JSON Schema. Negative fixtures fail for the expected reason, including strict unknown fields, unknown schema version, and derived artifacts in semantic JSON.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize explicit Phase 2 gates and source guards</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-VALIDATION.md
    @justfile
    @package.json
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
  </read_first>
  <files>justfile, package.json</files>
  <action>
    Add named root scripts for Phase 2 gates if they are not already present, such as `test:project-store`, `test:material-probe`, `test:material-service`, and `test:draft-fixtures`, then invoke them from `just test` or the root `test` script while preserving existing Phase 1 gates. Use executable commands from `02-VALIDATION.md`, including `cargo test -p project_store create_project_bundle -- --nocapture`, `cargo test -p project_store round_trip -- --nocapture`, `cargo test -p project_store path_resolution -- --nocapture`, `cargo test -p bindings_node material_service -- --nocapture`, `cargo test -p media_runtime material_probe -- --nocapture`, `cargo test -p testkit material -- --nocapture`, `pnpm --filter @video-editor/desktop test`, and `git diff --exit-code schemas apps/desktop-electron/src/generated`. Add grep gates in package scripts or documented test commands that fail if `Asset`/`Clip` internal aliases, persisted float seconds, renderer ffprobe/ffmpeg construction, material import orchestration in `project_store`, or derived artifact fields appear in source/generated draft semantics.
  </action>
  <verify>
    <automated>PATH="$HOME/.cargo/bin:$PATH" just build</automated>
    <automated>PATH="$HOME/.cargo/bin:$PATH" just test</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    The full Phase 2 gate path is executable through `just build` and `just test` with the local PATH form required by `02-VALIDATION.md`. Generated schema/TS drift is clean. Source guards fail on the terminology, time model, renderer FFmpeg, project-store import-orchestration, and derived-artifact regressions called out by D-01, D-03, D-06, D-07, D-12, and D-18.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures -> draft_model | Golden and negative project fixtures exercise untrusted `project.json` parsing. |
| generated contracts -> git | Generated schema/TS artifacts must match Rust source of truth before later phases consume them. |
| local gates -> CI parity | `just`/package scripts execute all Phase 2 security and drift checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-23 | Tampering | Fixture corpus | mitigate | Explicitly classify every project fixture and assert positive/negative behavior through Rust model and JSON Schema. |
| T-02-24 | Tampering | Generated schema/TS | mitigate | Require Rust generator tests plus `git diff --exit-code schemas apps/desktop-electron/src/generated`. |
| T-02-25 | Repudiation | Requirement coverage | mitigate | Map every Phase 2 requirement ID to an automated test command and fixture/integration proof. |
| T-02-26 | Elevation of Privilege/DoS | Final gates | mitigate | Include renderer FFmpeg grep, runtime ffprobe timeout tests, strict malformed JSON tests, missing-material corruption tests, and project-store responsibility grep in the public gate path. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned in the gate/fixture pass. Stop for human legitimacy verification if a new dependency is proposed. |
</threat_model>

<verification>
Run the final phase gates:

<automated>cargo test -p draft_model draft_schema -- --nocapture</automated>
<automated>cargo test -p draft_model migration -- --nocapture</automated>
<automated>cargo test -p draft_model schema_fixtures -- --nocapture</automated>
<automated>cargo test -p project_store create_project_bundle -- --nocapture</automated>
<automated>cargo test -p project_store round_trip -- --nocapture</automated>
<automated>cargo test -p project_store path_resolution -- --nocapture</automated>
<automated>cargo test -p bindings_node material_service -- --nocapture</automated>
<automated>cargo test -p media_runtime material_probe -- --nocapture</automated>
<automated>cargo test -p testkit material -- --nocapture</automated>
<automated>cargo test -p bindings_node -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop test</automated>
<automated>PATH="$HOME/.cargo/bin:$PATH" just build</automated>
<automated>PATH="$HOME/.cargo/bin:$PATH" just test</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- All Phase 2 roadmap requirements DRAFT-01, DRAFT-02, DRAFT-03, DRAFT-04, DRAFT-05, MAT-01, MAT-02, MAT-03, and MAT-04 are covered by fixture, integration, generated-contract, or smoke tests.
- `.veproj/project.json` remains the canonical semantic source of truth.
- Jianying-aligned terminology is enforced across Rust model, schema, generated TypeScript, command contracts, tests, and docs.
- Missing material detection surfaces recoverable diagnostics and does not corrupt the draft.
- Material import orchestration remains outside `project_store`.
</success_criteria>

<output>
Create `.planning/phases/02-draft-and-material-system/02-06-SUMMARY.md` when done.
</output>
