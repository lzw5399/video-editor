---
phase: 02-draft-and-material-system
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - fixtures/draft/positive/minimal-draft/project.json
  - fixtures/draft/positive/materials-round-trip/project.json
  - fixtures/draft/positive/missing-material/project.json
  - fixtures/draft/negative/invalid-unknown-field/project.json
  - fixtures/draft/negative/invalid-schema-version/project.json
  - fixtures/draft/negative/derived-artifact-in-project-json/project.json
  - crates/draft_model/tests/schema_exports.rs
  - crates/draft_model/tests/draft_fixtures.rs
  - crates/project_store/tests/project_bundle.rs
  - crates/project_store/tests/material_import.rs
  - crates/media_runtime/tests/material_probe.rs
  - justfile
  - package.json
autonomous: true
requirements:
  - DRAFT-01
  - DRAFT-02
  - DRAFT-03
  - DRAFT-04
  - DRAFT-05
  - MAT-01
  - MAT-02
  - MAT-03
  - MAT-04
must_haves:
  truths:
    - "Positive and negative `.veproj/project.json` fixtures cover draft creation, semantic round trips, material import metadata, missing-material preservation, derived-artifact exclusion, and schema migration failures per D-17 and D-18."
    - "Every Phase 2 requirement ID is covered by automated tests and at least one committed fixture or integration test."
    - "Generated schema and TypeScript artifacts have no drift after Rust tests run."
    - "The final phase gate passes through `PATH=\"$HOME/.cargo/bin:$PATH\" just build`, `PATH=\"$HOME/.cargo/bin:$PATH\" just test`, and `git diff --exit-code schemas apps/desktop-electron/src/generated`."
  artifacts:
    - path: fixtures/draft/positive/minimal-draft/project.json
      provides: "valid new draft fixture"
    - path: fixtures/draft/positive/materials-round-trip/project.json
      provides: "valid materials metadata fixture"
    - path: fixtures/draft/positive/missing-material/project.json
      provides: "recoverable missing material fixture"
    - path: fixtures/draft/negative/invalid-unknown-field/project.json
      provides: "strict schema rejection fixture"
    - path: fixtures/draft/negative/invalid-schema-version/project.json
      provides: "unknown version rejection fixture"
    - path: fixtures/draft/negative/derived-artifact-in-project-json/project.json
      provides: "derived artifact exclusion fixture"
  key_links:
    - from: crates/draft_model/tests/schema_exports.rs
      to: fixtures/draft
      via: "explicit positive/negative fixture classifier"
      pattern: "positive|negative"
    - from: package.json
      to: justfile
      via: "named Phase 2 gate scripts invoked by just test"
      pattern: "test:project-store|test:material-probe"
---

<objective>
Add draft/material golden fixtures and final schema/model/project-store/media-runtime validation gates for the full Phase 2 requirement set.

Purpose: Convert Phase 2 behaviors into deterministic, executable proof so later timeline/UI/preview phases build on stable draft/material semantics. This implements DRAFT-01 through DRAFT-05 and MAT-01 through MAT-04 while honoring D-01 through D-19.

Output: Classified `.veproj/project.json` fixtures, fixture tests, tightened project-store/media-runtime assertions, explicit Phase 2 gate scripts, and final drift checks.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-draft-and-material-system/02-CONTEXT.md
@.planning/phases/02-draft-and-material-system/02-RESEARCH.md
@.planning/phases/02-draft-and-material-system/02-VALIDATION.md
@.planning/phases/02-draft-and-material-system/02-PATTERNS.md
@.planning/phases/02-draft-and-material-system/02-01-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-02-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-03-SUMMARY.md
@crates/draft_model/tests/schema_exports.rs
@crates/project_store/tests/project_bundle.rs
@crates/project_store/tests/material_import.rs
@crates/media_runtime/tests/material_probe.rs
@justfile
@package.json
</context>

## Artifacts This Phase Produces

- Positive `.veproj/project.json` fixtures for minimal draft, material round trip, and missing material.
- Negative `.veproj/project.json` fixtures for unknown fields, unsupported schema version, and derived artifact leakage.
- Fixture classification and schema/model validation tests.
- Explicit Phase 2 package/just gate commands for project-store and material-probe tests.
- Final source and drift gates for generated schema and TypeScript artifacts.

<tasks>

<task type="auto">
  <name>Task 1: Add classified draft/material project fixtures</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
    @.planning/phases/02-draft-and-material-system/02-VALIDATION.md
    @crates/draft_model/tests/schema_exports.rs
    @schemas/draft.schema.json
  </read_first>
  <files>fixtures/draft/positive/minimal-draft/project.json, fixtures/draft/positive/materials-round-trip/project.json, fixtures/draft/positive/missing-material/project.json, fixtures/draft/negative/invalid-unknown-field/project.json, fixtures/draft/negative/invalid-schema-version/project.json, fixtures/draft/negative/derived-artifact-in-project-json/project.json, crates/draft_model/tests/schema_exports.rs, crates/draft_model/tests/draft_fixtures.rs</files>
  <action>
    Create deterministic `.veproj`-style fixture folders under `fixtures/draft/positive` and `fixtures/draft/negative`, each containing a `project.json`. Keep existing Phase 1 command fixtures either migrated into an explicit command fixture classifier or excluded from the project fixture walker with clear test code; do not leave unclassified JSON. Positive fixtures must cover a valid new empty draft per D-02, a draft with video/image/audio material metadata per D-11, and a draft preserving a missing material entry per D-14/D-15. Negative fixtures must cover unknown field rejection, unsupported schema version rejection per D-09, and derived artifact leakage such as thumbnail/probe/render/cache fields per D-01/DRAFT-04. Update `schema_exports.rs` or a new `draft_fixtures.rs` so every fixture path is explicitly classified and positive fixtures pass both Rust deserialization/migration and JSON Schema validation while negative fixtures fail the intended gate.
  </action>
  <verify>
    <automated>cargo test -p draft_model schema_fixtures -- --nocapture</automated>
    <automated>cargo test -p draft_model draft_fixtures -- --nocapture</automated>
    <automated>find fixtures/draft -name project.json | sort</automated>
  </verify>
  <acceptance_criteria>
    Fixture tests fail if any `.veproj/project.json` fixture is unclassified. Positive fixtures load to semantically valid `Draft` values and validate against generated JSON Schema. Negative fixtures fail for the expected reason, including strict unknown fields, unknown schema version, and derived artifacts in semantic JSON.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten round-trip, import, and missing-material coverage</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-VALIDATION.md
    @crates/project_store/tests/project_bundle.rs
    @crates/project_store/tests/material_import.rs
    @crates/media_runtime/tests/material_probe.rs
    @crates/testkit/src/lib.rs
  </read_first>
  <files>crates/project_store/tests/project_bundle.rs, crates/project_store/tests/material_import.rs, crates/media_runtime/tests/material_probe.rs</files>
  <action>
    Extend project-store tests to load the positive project fixtures, save them to temp `.veproj` bundles, reopen them, and assert semantic equality per D-05. Add fixture-backed assertions that `project.json` remains the only semantic file in the bundle, derived artifacts stay outside draft semantics, and missing materials preserve original URI/status/diagnostic details per D-14 through D-16. Extend media-runtime and material-import tests so video, image, and audio material paths generated by `testkit` produce persisted metadata matching expected kind, duration microseconds, dimensions or audio details, rational fps where applicable, stream flags, and stable material IDs per MAT-01/MAT-02. Keep relink UI, timeline editing, preview playback, render graph compilation, and export behavior out of these tests.
  </action>
  <verify>
    <automated>cargo test -p project_store round_trip -- --nocapture</automated>
    <automated>cargo test -p project_store import_material -- --nocapture</automated>
    <automated>cargo test -p project_store missing_material -- --nocapture</automated>
    <automated>cargo test -p media_runtime material_probe -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    Requirement-level tests pass for DRAFT-01, DRAFT-02, DRAFT-04, MAT-01, MAT-02, and MAT-04. No test accepts raw JSON byte equality as the only round-trip proof. Missing materials are represented as recoverable diagnostics and do not remove or rewrite material entries.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize explicit Phase 2 gates and source guards</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-VALIDATION.md
    @justfile
    @package.json
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
  </read_first>
  <files>justfile, package.json</files>
  <action>
    Add named root scripts for Phase 2 gates if they are not already present, such as `test:project-store`, `test:material-probe`, and `test:draft-fixtures`, then invoke them from `just test` or the root `test` script while preserving existing Phase 1 gates. Use executable commands from `02-VALIDATION.md`, including `cargo test -p project_store create_project_bundle -- --nocapture`, `cargo test -p project_store round_trip -- --nocapture`, `cargo test -p project_store path_resolution -- --nocapture`, `cargo test -p project_store import_material -- --nocapture`, `cargo test -p project_store missing_material -- --nocapture`, `cargo test -p media_runtime material_probe -- --nocapture`, `pnpm --filter @video-editor/desktop test`, and `git diff --exit-code schemas apps/desktop-electron/src/generated`. Add grep gates in package scripts or documented test commands that fail if `Asset`/`Clip` internal aliases, persisted float seconds, renderer ffprobe/ffmpeg construction, or derived artifact fields appear in source/generated draft semantics.
  </action>
  <verify>
    <automated>PATH="$HOME/.cargo/bin:$PATH" just build</automated>
    <automated>PATH="$HOME/.cargo/bin:$PATH" just test</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    The full Phase 2 gate path is executable through `just build` and `just test` with the local PATH form required by `02-VALIDATION.md`. Generated schema/TS drift is clean. Source guards fail on the terminology, time model, renderer FFmpeg, and derived-artifact regressions called out by D-01, D-06, D-07, D-12, and D-18.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures -> draft_model | Golden and negative project fixtures exercise untrusted `project.json` parsing. |
| generated contracts -> git | Generated schema/TS artifacts must match Rust source of truth before later phases consume them. |
| local gates -> CI parity | `just`/package scripts execute all Phase 2 security and drift checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | Fixture corpus | mitigate | Explicitly classify every project fixture and assert positive/negative behavior through Rust model and JSON Schema. |
| T-02-16 | Tampering | Generated schema/TS | mitigate | Require Rust generator tests plus `git diff --exit-code schemas apps/desktop-electron/src/generated`. |
| T-02-17 | Repudiation | Requirement coverage | mitigate | Map every Phase 2 requirement ID to an automated test command and fixture/integration proof. |
| T-02-18 | Elevation of Privilege/DoS | Final gates | mitigate | Include renderer FFmpeg grep, runtime ffprobe timeout tests, strict malformed JSON tests, and missing-material corruption tests in the public gate path. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager install is planned in the gate/fixture pass. Stop for human legitimacy verification if a new dependency is proposed. |
</threat_model>

<verification>
Run the final phase gates:

<automated>cargo test -p draft_model draft_schema -- --nocapture</automated>
<automated>cargo test -p draft_model migration -- --nocapture</automated>
<automated>cargo test -p draft_model schema_fixtures -- --nocapture</automated>
<automated>cargo test -p project_store create_project_bundle -- --nocapture</automated>
<automated>cargo test -p project_store round_trip -- --nocapture</automated>
<automated>cargo test -p project_store path_resolution -- --nocapture</automated>
<automated>cargo test -p project_store import_material -- --nocapture</automated>
<automated>cargo test -p project_store missing_material -- --nocapture</automated>
<automated>cargo test -p media_runtime material_probe -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop test</automated>
<automated>PATH="$HOME/.cargo/bin:$PATH" just build</automated>
<automated>PATH="$HOME/.cargo/bin:$PATH" just test</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- All Phase 2 roadmap requirements DRAFT-01, DRAFT-02, DRAFT-03, DRAFT-04, DRAFT-05, MAT-01, MAT-02, MAT-03, and MAT-04 are covered by fixture or integration tests.
- `.veproj/project.json` remains the canonical semantic source of truth.
- Jianying-aligned terminology is enforced across Rust model, schema, generated TypeScript, command contracts, tests, and docs.
- Missing material detection surfaces recoverable diagnostics and does not corrupt the draft.
</success_criteria>

<output>
Create `.planning/phases/02-draft-and-material-system/02-04-SUMMARY.md` when done.
</output>
