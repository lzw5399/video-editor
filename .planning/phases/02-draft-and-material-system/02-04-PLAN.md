---
phase: 02-draft-and-material-system
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - crates/bindings_node/Cargo.toml
  - crates/bindings_node/src/lib.rs
  - crates/bindings_node/src/material_service.rs
  - crates/bindings_node/tests/material_service.rs
  - crates/draft_model/src/lib.rs
  - crates/draft_model/src/material.rs
  - crates/draft_model/tests/schema_exports.rs
  - docs/runtime-boundaries.md
  - schemas/draft.schema.json
  - apps/desktop-electron/src/generated/Draft.ts
autonomous: true
requirements:
  - MAT-01
  - MAT-02
  - MAT-04
  - DRAFT-04
must_haves:
  truths:
    - "User-selected video, image, and audio materials can be imported through a Rust-owned binding-facing command/API service per D-10 and MAT-01."
    - "Imported materials persist stable IDs, URI, duration, dimensions, rational fps, stream flags, audio sample rate/channel count, and probe status/errors per D-11 and MAT-02."
    - "Missing materials remain in the draft and are returned as classified recoverable diagnostics per D-14, D-15, D-16, and MAT-04."
    - "Material import orchestration is not in `project_store`: the service coordinates `project_store` path helpers, `media_runtime::probe_material_metadata`, pure `draft_model` registry mutation, validation, and save per the required architecture fix."
    - "Thumbnail/cache policy is represented as derived-artifact boundary documentation and no thumbnail/probe JSON field enters `project.json` per D-01, D-13, and DRAFT-04."
  artifacts:
    - path: crates/bindings_node/src/material_service.rs
      provides: "binding-facing Rust service for import_material and list_missing_materials orchestration"
    - path: crates/bindings_node/tests/material_service.rs
      provides: "service-level material import and missing-material diagnostics tests"
    - path: crates/draft_model/src/material.rs
      provides: "pure Material registry mutation/status helpers"
    - path: crates/draft_model/tests/schema_exports.rs
      provides: "Rust-generated draft schema and Draft.ts export path"
    - path: docs/runtime-boundaries.md
      provides: "documented boundary: project_store persists, media_runtime probes, binding-facing service orchestrates import"
  key_links:
    - from: crates/bindings_node/src/material_service.rs
      to: crates/project_store/src/paths.rs
      via: "material URI resolution/classification helpers"
      pattern: "resolve|classify"
    - from: crates/bindings_node/src/material_service.rs
      to: crates/media_runtime/src/probe.rs
      via: "probe_material_metadata"
      pattern: "probe_material_metadata"
    - from: crates/bindings_node/src/material_service.rs
      to: crates/draft_model/src/material.rs
      via: "pure registry mutation helper"
      pattern: "add_material|upsert_material|mark_material"
---

<objective>
Implement material import orchestration in a binding-facing Rust service, not in `project_store`.

Purpose: Preserve the Phase 2 responsibility map: `project_store` persists `.veproj/project.json` and resolves paths, `media_runtime` probes media, `draft_model` owns semantic material records and pure registry helpers, and the command/API layer coordinates import. This implements MAT-01, MAT-02, and MAT-04 while honoring D-01, D-03, D-04, D-10 through D-16, and the checker-required architectural fix.

Output: Material import service, missing-material diagnostic service, pure material registry helpers, generated draft schema/TypeScript artifacts, and runtime-boundary documentation.
</objective>

<execution_context>
@/Users/zhiwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhiwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-draft-and-material-system/02-CONTEXT.md
@.planning/phases/02-draft-and-material-system/02-RESEARCH.md
@.planning/phases/02-draft-and-material-system/02-VALIDATION.md
@.planning/phases/02-draft-and-material-system/02-PATTERNS.md
@.planning/phases/02-draft-and-material-system/02-01-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-02-SUMMARY.md
@.planning/phases/02-draft-and-material-system/02-03-SUMMARY.md
@docs/runtime-boundaries.md
@crates/bindings_node/src/lib.rs
@crates/draft_model/src/lib.rs
@crates/project_store/src/lib.rs
@crates/media_runtime/src/probe.rs
@crates/testkit/src/lib.rs
</context>

## Artifacts this phase produces

- `bindings_node::material_service` or equivalent binding-facing Rust service for `import_material`, `list_materials`, and `list_missing_materials` behavior.
- Pure `draft_model` material registry helpers for adding/updating materials and marking missing/probe-failed states.
- `MissingMaterialDiagnostic` or equivalent classified diagnostic type with original URI and last-known resolved path detail.
- `schemas/draft.schema.json` and `apps/desktop-electron/src/generated/Draft.ts` generated from Rust semantic types.
- Runtime-boundary documentation making material import orchestration ownership explicit.

<tasks>

<task type="auto">
  <name>Task 1: Add pure material registry helpers and generated draft contracts</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
    @.planning/phases/02-draft-and-material-system/02-PATTERNS.md
    @crates/draft_model/src/lib.rs
    @crates/draft_model/tests/schema_exports.rs
  </read_first>
  <files>crates/draft_model/src/lib.rs, crates/draft_model/src/material.rs, crates/draft_model/tests/schema_exports.rs, schemas/draft.schema.json, apps/desktop-electron/src/generated/Draft.ts</files>
  <action>
    Add pure `draft_model` helper APIs for material registry mutation, such as `add_material`, `upsert_material`, or `mark_material_missing`, using `MaterialId`, `Material`, `MaterialStatus`, and validation rules already defined in Plan 02-01. These helpers must not import filesystem, FFmpeg, Electron, `PlatformFileSystem`, or `FfmpegExecutor` per D-03. Extend `schema_exports.rs` to generate `schemas/draft.schema.json` with `schema_for!(Draft)` and `apps/desktop-electron/src/generated/Draft.ts` with ts-rs declarations for Phase 2 draft/material/timeline symbols. Keep generated files committed and use the existing `VE_UPDATE_GENERATED_CONTRACTS=1` update-or-compare pattern. Generated schema/TypeScript must include material metadata/status but must not include thumbnail, waveform, probe JSON, render graph, FFmpeg script, preview cache, or export fields per D-01 and D-13.
  </action>
  <verify>
    <automated>VE_UPDATE_GENERATED_CONTRACTS=1 cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
    <automated>grep -R "FfmpegExecutor\|PlatformFileSystem\|std::fs\|std::process" crates/draft_model/src && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    Pure registry helpers compile and are covered by draft-model tests. `schemas/draft.schema.json` and `apps/desktop-electron/src/generated/Draft.ts` exist, are regenerated by Rust tests, and pass the drift gate. No generated contract contains derived cache artifacts or raw ffprobe JSON.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Implement material import service outside project_store</name>
  <read_first>
    @.planning/phases/02-draft-and-material-system/02-CONTEXT.md
    @.planning/phases/02-draft-and-material-system/02-RESEARCH.md
    @.planning/phases/02-draft-and-material-system/02-VALIDATION.md
    @crates/bindings_node/src/lib.rs
    @crates/project_store/src/bundle.rs
    @crates/project_store/src/paths.rs
    @crates/media_runtime/src/probe.rs
    @crates/draft_model/src/material.rs
  </read_first>
  <files>crates/bindings_node/Cargo.toml, crates/bindings_node/src/lib.rs, crates/bindings_node/src/material_service.rs, crates/bindings_node/tests/material_service.rs, docs/runtime-boundaries.md</files>
  <action>
    Create a binding-facing Rust service module, for example `crates/bindings_node/src/material_service.rs`, that owns material import orchestration. The service may coordinate `project_store` path helpers, `media_runtime::probe_material_metadata`, pure `draft_model` material registry helpers, `validate_draft`, and `save_project_bundle`, but `project_store` must remain limited to persistence/path helpers and `media_runtime` must remain limited to probe execution/normalization. Implement service APIs for importing video/image/audio materials, listing current materials, and listing missing materials. Assign stable deterministic string material IDs from caller input or a deterministic service helper; do not add `uuid`. Missing files must produce recoverable diagnostics without deleting or corrupting `Material` records per D-14 and D-15. Store enough original URI and last-known resolved path detail for relink UI consumption without UI reparsing per D-16. Update `docs/runtime-boundaries.md` to state that thumbnails, waveform data, raw probe JSON, preview caches, and render artifacts are derived outputs outside `project.json`, and that material import orchestration lives in this command/API layer rather than `project_store`.
  </action>
  <verify>
    <automated>cargo test -p bindings_node material_service -- --nocapture</automated>
    <automated>cargo test -p media_runtime material_probe -- --nocapture</automated>
    <automated>cargo test -p project_store round_trip -- --nocapture</automated>
    <automated>grep -R "probe_material_metadata\|FfmpegExecutor\|ffprobe\|ffmpeg" crates/project_store/src && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    Service tests import generated video, image, and audio materials with stable IDs, normalized metadata, and saved URI/path semantics. Missing-material tests prove open/save preserves missing entries and the service returns classified diagnostics. Grep proves `project_store` does not call ffprobe or own import orchestration.
  </acceptance_criteria>
  <done>All task acceptance criteria pass and every automated verification command succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-selected path -> material_service | Local file paths/URIs enter Rust import orchestration. |
| material_service -> project_store | The service asks project_store only to classify paths and persist validated `project.json`. |
| material_service -> media_runtime | The service asks media_runtime only to probe normalized metadata. |
| generated Rust contracts -> Electron TS | Rust-owned draft schema/TS artifacts are consumed by later binding and Electron tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering/Information Disclosure | Material URI resolution | mitigate | Resolve relative/external material URIs through project-store helpers; reject traversal/ambiguous bundle paths and preserve original URI for diagnostics. |
| T-02-15 | Tampering | Material import responsibility | mitigate | Keep import orchestration in `material_service`; add grep/tests proving `project_store` does not call ffprobe or mutate registry as import owner. |
| T-02-16 | Tampering | Material registry mutation | mitigate | Use pure draft_model helpers and validate the draft before save. |
| T-02-17 | Tampering | Missing material handling | mitigate | Missing files produce recoverable diagnostics and preserve material entries exactly per D-14/D-15. |
| T-02-18 | Tampering | Generated draft schema/TS | mitigate | Generate from Rust and require `git diff --exit-code schemas apps/desktop-electron/src/generated`. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager install is planned; all dependencies are existing workspace/local crates. Stop for human legitimacy verification if a new dependency is proposed. |
</threat_model>

<verification>
Run:

<automated>cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
<automated>cargo test -p bindings_node material_service -- --nocapture</automated>
<automated>cargo test -p media_runtime material_probe -- --nocapture</automated>
<automated>cargo test -p project_store round_trip -- --nocapture</automated>
<automated>grep -R "probe_material_metadata\|FfmpegExecutor\|ffprobe\|ffmpeg" crates/project_store/src && exit 1 || true</automated>
<automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
</verification>

<success_criteria>
- MAT-01 is satisfied by Rust-owned import service tests for video, image, and audio materials.
- MAT-02 is satisfied by stable IDs and normalized ffprobe metadata persisted in `Material`.
- MAT-04 is satisfied by recoverable missing-material diagnostics that preserve draft entries.
- DRAFT-04 is preserved because derived cache artifacts and raw ffprobe JSON are excluded from schema and generated TypeScript.
- The required architectural fix is satisfied because material import orchestration lives in a binding-facing Rust command/API service, not in `project_store`.
</success_criteria>

<output>
Create `.planning/phases/02-draft-and-material-system/02-04-SUMMARY.md` when done.
</output>
