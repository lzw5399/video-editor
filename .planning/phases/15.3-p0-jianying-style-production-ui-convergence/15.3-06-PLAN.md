---
phase: "15.3-p0-jianying-style-production-ui-convergence"
plan: "06"
type: execute
wave: 6
depends_on:
  - "15.3-05"
files_modified:
  - "apps/desktop-electron/src/renderer/workspace/Timeline.tsx"
  - "apps/desktop-electron/src/renderer/workspace/timeline.css"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - P0-UI-05
  - P0-UI-02
  - P0-UI-01
user_setup: []
---

<objective>
Convert the bottom timeline toolbar and segment interactions from numeric debug
controls into production editor interactions: drag move, edge trim, split,
delete, snapping, add-track, undo/redo, draggable playhead, and zoom.
</objective>

<threat_model>
- T-15.3-06A: Hidden debug inputs remain the only tested way to move/split/trim. Mitigation: product E2E uses pointer/toolbar interactions by default.
- T-15.3-06B: Renderer mutates draft timing during drag. Mitigation: pointer interactions calculate intent and submit existing Rust timeline commands.
- T-15.3-06C: Timeline controls appear functional without E2E coverage. Mitigation: visible default controls either have product E2E or are gated.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 15.3-06-01: Replace default numeric timeline toolbar</name>
  <files>Timeline.tsx, timeline.css, workspace.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/Timeline.tsx`
    - `apps/desktop-electron/src/renderer/workspace/timeline.css`
    - `docs/ui-reference/jianying-pro/screenshots/07-bottom-timeline.png`
    - `.planning/phases/15.3-p0-jianying-style-production-ui-convergence/15.3-UI-SPEC.md`
  </read_first>
  <action>Default timeline toolbar shows compact icon buttons for undo, redo, play/pause, split-at-playhead, delete, add-track menu, snapping, and zoom. Move numeric `播放头`, `移动`, `分割`, and `裁剪` inputs behind developer diagnostics or remove them from default product UI.</action>
  <acceptance_criteria>
    - Default mode has zero spinbuttons named `播放头`, `移动`, `分割`, or `裁剪`.
    - Toolbar still has accessible buttons for `撤销`, `重做`, `播放`, `分割所选片段`, `删除所选片段`, `放大时间线`, and `缩小时间线`.
    - `workspace.spec.ts` verifies controls fit inside the timeline strip at 1280x800 and 1120x720.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
  </verify>
  <done>Timeline default toolbar no longer reads as a numeric command console.</done>
</task>

<task type="auto">
  <name>Task 15.3-06-02: Add direct segment move and edge trim interactions</name>
  <files>Timeline.tsx, timeline.css, helpers/userJourney.ts, product-user-journey.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/Timeline.tsx`
    - `apps/desktop-electron/tests/helpers/userJourney.ts`
    - `docs/product-e2e-acceptance-policy.md`
  </read_first>
  <action>Add pointer-based segment drag move and edge trim handles that submit existing `moveSegment` and `trimSegment` command envelopes. Use integer microsecond conversion from the timeline lane, with snapping behavior coming from Rust command results.</action>
  <acceptance_criteria>
    - Dragging a segment horizontally emits `moveSegment`.
    - Dragging a segment left/right edge emits `trimSegment`.
    - Product E2E no longer uses numeric `移动` or `裁剪` inputs for the default editing matrix.
    - Undo/redo still restores Rust-accepted timeline states after drag/trim.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>Move and trim are normal direct timeline interactions.</done>
</task>

<task type="auto">
  <name>Task 15.3-06-03: Cover timeline playhead, snapping, add-track, and zoom interactions</name>
  <files>Timeline.tsx, product-user-journey.spec.ts, workspace.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/Timeline.tsx`
    - `apps/desktop-electron/tests/product-user-journey.spec.ts`
    - `apps/desktop-electron/tests/workspace.spec.ts`
  </read_first>
  <action>Keep draggable playhead and add product tests for ruler seek/playhead drag, split at playhead, add-track menu, snapping toggle/status, delete, undo/redo, and zoom. Visible controls must either be covered or hidden.</action>
  <acceptance_criteria>
    - Product E2E seeks by clicking/dragging timeline, not filling a playhead input.
    - Split uses current playhead when no numeric split input is visible.
    - Add-track is available through a compact menu or grouped icon controls.
    - Zoom control changes timeline density without causing layout overlap.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
  </verify>
  <done>Default visible timeline controls are backed by product-level interactions and tests.</done>
</task>

</tasks>

<verification>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The timeline is operated primarily through direct manipulation and compact
production controls, not debug numeric command fields.
</success_criteria>

## Artifacts this phase produces

- Pointer drag move and edge trim UI.
- Product timeline toolbar.
- Product E2E helpers for direct timeline interactions.

<output>
Create `15.3-06-SUMMARY.md` when done.
</output>
