---
phase: "15.3-p0-jianying-style-production-ui-convergence"
plan: "02"
type: execute
wave: 2
depends_on:
  - "15.3-01"
files_modified:
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "apps/desktop-electron/src/renderer/styles.css"
  - "apps/desktop-electron/src/renderer/workspace/preview-inspector.css"
  - "apps/desktop-electron/tests/export-modal.spec.ts"
  - "apps/desktop-electron/tests/helpers/realWorkflow.ts"
  - "apps/desktop-electron/tests/real-workflow.spec.ts"
autonomous: true
requirements:
  - P0-UI-03
  - P0-UI-02
  - P0-UI-01
user_setup: []
---

<objective>
Move export from the preview monitor into a top-right product action and modal
flow that covers export settings, progress, cancel, and open-location behavior.
</objective>

<threat_model>
- T-15.3-02A: The old preview-area export panel remains reachable in production. Mitigation: remove production rendering of `.export-panel` from `PreviewMonitor`; developer diagnostics may expose only diagnostic controls if explicitly enabled.
- T-15.3-02B: Modal export exposes raw FFmpeg logs or paths as status. Mitigation: default modal copy uses product-safe phase/progress/validation labels; raw diagnostics are developer-only.
- T-15.3-02C: Export command semantics move into renderer. Mitigation: modal callbacks continue to call existing generated command envelope builders.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 15.3-02-01: Add top-right export action and modal shell</name>
  <files>WorkspaceShell.tsx, App.tsx, styles.css</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx`
    - `apps/desktop-electron/src/renderer/App.tsx`
    - `.planning/phases/15.3-p0-jianying-style-production-ui-convergence/15.3-UI-SPEC.md`
  </read_first>
  <action>Add a top-right `导出` button in the product bar. Clicking it opens an accessible modal named `导出` with output path, resolution, frame rate, bitrate, audio toggle/settings, progress, cancel, and open-location action slots.</action>
  <acceptance_criteria>
    - `WorkspaceShell.tsx` top bar contains a button with accessible name `导出`.
    - Modal has role dialog/name `导出`.
    - Preview monitor no longer shows a production `导出面板`.
    - `export-modal.spec.ts` opens and closes the modal from the top-right button.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/export-modal.spec.ts --reporter=line</automated>
  </verify>
  <done>Export is a product-bar modal flow, not preview-panel furniture.</done>
</task>

<task type="auto">
  <name>Task 15.3-02-02: Move existing export command wiring into modal</name>
  <files>App.tsx, PreviewMonitor.tsx, helpers/realWorkflow.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx`
    - `apps/desktop-electron/src/renderer/App.tsx`
    - `apps/desktop-electron/tests/helpers/realWorkflow.ts`
  </read_first>
  <action>Move `onExportOutputPathChange`, `onExportPresetChange`, `onStartExport`, `onRefreshExportStatus`, and `onCancelExport` usage from `PreviewMonitor` into the export modal. Keep command construction in `commandHelpers.ts` and App-level callbacks unchanged unless a new product-safe open-location platform action is needed.</action>
  <acceptance_criteria>
    - `PreviewMonitorProps` no longer include export command callbacks in production preview.
    - Export modal can start export and poll status through existing command envelope calls.
    - `realWorkflow.ts` exports from top-right modal, not `预览窗口` embedded controls.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/real-workflow.spec.ts --reporter=line</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/export-modal.spec.ts --reporter=line</automated>
  </verify>
  <done>Real export workflow uses the production modal path.</done>
</task>

<task type="auto">
  <name>Task 15.3-02-03: Cover modal dropdown and advanced states</name>
  <files>export-modal.spec.ts, styles.css</files>
  <read_first>
    - `docs/ui-reference/jianying-pro/README.md`
    - `docs/ui-reference/jianying-pro/REGRESSION.md`
    - `docs/ui-reference/jianying-pro/screenshots/28-export-modal-advanced-expanded-fullscreen.png`
    - `docs/ui-reference/jianying-pro/screenshots/42-export-audio-samplerate-dropdown-fullscreen.png`
  </read_first>
  <action>Add tests for export modal advanced section expansion, dropdown visibility, progress area, cancel disabled/enabled state, and no default debug copy. Use the screenshots as layout/state references only.</action>
  <acceptance_criteria>
    - Test asserts advanced section expands inside the modal without overlapping controls.
    - Test asserts audio sample-rate or equivalent audio option dropdown opens inside the modal.
    - Default modal text does not contain `FFmpeg`, `ffprobe`, `artifact`, `cache`, or raw local debug paths.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/export-modal.spec.ts --reporter=line</automated>
  </verify>
  <done>Export modal interaction states are covered, not just the closed modal screenshot.</done>
</task>

</tasks>

<verification>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/export-modal.spec.ts --reporter=line</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/real-workflow.spec.ts --reporter=line</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
Export is launched from the top-right production action and completed through a
modal without a permanent preview-area export panel in default UI.
</success_criteria>

## Artifacts this phase produces

- Export modal component/state.
- `tests/export-modal.spec.ts`.
- Updated real workflow helper using top-right export.

<output>
Create `15.3-02-SUMMARY.md` when done.
</output>
