# Phase 15.3 Pattern Map

## Existing Components

| Area | Current File | Pattern To Reuse | Change Direction |
|------|--------------|------------------|------------------|
| App state orchestration | `apps/desktop-electron/src/renderer/App.tsx` | App owns command callbacks and workspace state; components receive intent callbacks | Add product entry state and top-level modal state here, not in leaf components |
| Shell layout | `apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx` | Five-zone React component with top categories and prop callbacks | Add product bar/export action and entry-aware workspace rendering |
| Resource panels | `FeaturePanel.tsx` | Category switch with media/text/audio/deferred panels | Keep category state local/presentation-only; hide debug resource machinery by default |
| Preview | `PreviewMonitor.tsx` | Native realtime host bridge, selection/text overlays, transport controls | Remove permanent export panel; keep playback controls and product-safe status only |
| Inspector | `Inspector.tsx` | Contextual controls emitting App callbacks | Add draft parameter modal entry and tighten selection-specific surfaces |
| Timeline | `Timeline.tsx` | Ruler, playhead pointer handling, segment buttons, command callbacks | Replace numeric debug controls with direct product interactions and icon toolbar |
| Styles | `styles.css`, `preview-inspector.css`, `timeline.css` | Dark compact Jianying-style surfaces, stable five-zone sizes | Preserve density while avoiding overlapping text and nested card structures |

## Test Patterns

| Test Area | Current File | Pattern To Reuse |
|-----------|--------------|------------------|
| Region stability | `workspace.spec.ts` | `setViewportSizeAndVerifyLayout`, no-overlap assertions, default diagnostics hidden |
| Product playback | `product-user-journey.spec.ts` | `waitForVisiblePreviewCenterChange`, `waitForCompositedPreviewEvidence`, no-fallback host calls |
| Real workflow | `real-workflow.spec.ts`, `helpers/realWorkflow.ts` | Repo-owned generated fixtures, dev/packaged launch, export output validation, save/reopen |
| Product app launcher | `helpers/userJourney.ts` | Default diagnostics off, repo fixture import, foreground macOS handling |

## Source Guards

- Extend `scripts/no-product-fallback-guards.sh` for product playback success evidence.
- Add a Phase 15.3 UI guard for default debug copy, embedded preview export panel, backend/mock selectors, and production code referencing external `icons/` directly.
- Keep developer diagnostics tests explicitly opted in through env variables.

## Asset Pattern

- Copy selected SVGs from `/Users/zhiwen/code/video-editor/icons` into `apps/desktop-electron/src/renderer/assets/icons/`.
- Import through local renderer code or CSS. Do not reference the root `icons/` path at runtime.
- Keep an asset manifest documenting source and copied target paths.
