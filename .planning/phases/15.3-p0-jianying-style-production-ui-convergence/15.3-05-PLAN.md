---
phase: "15.3-p0-jianying-style-production-ui-convergence"
plan: "05"
type: execute
wave: 5
depends_on:
  - "15.3-04"
files_modified:
  - "apps/desktop-electron/src/renderer/workspace/Inspector.tsx"
  - "apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx"
  - "apps/desktop-electron/src/renderer/workspace/preview-inspector.css"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/tests/inspector-modal.spec.ts"
  - "apps/desktop-electron/tests/workspace.spec.ts"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
autonomous: true
requirements:
  - P0-UI-06
  - P0-UI-02
  - P0-UI-01
user_setup: []
---

<objective>
Make the right inspector contextual and production-facing, including a modal for
draft/canvas parameter editing and focused controls for selected segment types.
</objective>

<threat_model>
- T-15.3-05A: Inspector exposes raw IDs, diagnostics, or internal paths by default. Mitigation: default rows show product labels only; raw details stay developer-only.
- T-15.3-05B: Draft/canvas edits become renderer-owned state. Mitigation: modal submits through `updateDraftCanvasConfig` command only.
- T-15.3-05C: Inactive tabs imply unsupported behavior works. Mitigation: unsupported inspector tabs are hidden or product-safe unavailable, not fake controls.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 15.3-05-01: Add draft parameter edit modal</name>
  <files>Inspector.tsx, App.tsx, WorkspaceShell.tsx, inspector-modal.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/Inspector.tsx`
    - `apps/desktop-electron/src/renderer/App.tsx`
    - `docs/ui-reference/jianying-pro/README.md`
  </read_first>
  <action>When nothing is selected, show compact draft/canvas summary and a `修改` action. Clicking it opens a modal for canvas size, aspect/frame rate where supported, background, and related draft settings. Submitting uses existing `onUpdateDraftCanvasConfig` command ownership.</action>
  <acceptance_criteria>
    - No-selection inspector has a section named `草稿参数` and a button named `修改`.
    - Clicking `修改` opens a dialog named `草稿参数`.
    - Applying canvas changes records an `updateDraftCanvasConfig` command.
    - Modal closes without changing draft when canceled.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/inspector-modal.spec.ts --reporter=line</automated>
  </verify>
  <done>Draft parameter editing follows the observed modal interaction pattern.</done>
</task>

<task type="auto">
  <name>Task 15.3-05-02: Tighten contextual selected-segment inspector</name>
  <files>Inspector.tsx, workspace.spec.ts, product-user-journey.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/Inspector.tsx`
    - `apps/desktop-electron/tests/helpers/userJourney.ts`
    - `.planning/phases/15.3-p0-jianying-style-production-ui-convergence/15.3-UI-SPEC.md`
  </read_first>
  <action>For video/image selections, show visual transform/fit/opacity/crop controls. For text selections, prioritize text content/style controls. For audio selections, prioritize volume/pan/fade/mute controls. Hide irrelevant panels and internal IDs unless developer diagnostics are enabled.</action>
  <acceptance_criteria>
    - Selecting video/image exposes `画面` controls and command-backed `应用画面`.
    - Selecting text exposes `文字内容` and text-specific controls.
    - Selecting audio exposes audio controls and does not show visual-only controls as primary content.
    - Default inspector text does not contain `segmentId`, `trackId`, raw material URI, or diagnostic path strings.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
  </verify>
  <done>Inspector content follows selected entity context instead of a static debug table.</done>
</task>

<task type="auto">
  <name>Task 15.3-05-03: Guard inspector diagnostics and responsive layout</name>
  <files>Inspector.tsx, preview-inspector.css, workspace.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/preview-inspector.css`
    - `apps/desktop-electron/tests/workspace.spec.ts`
    - `docs/ui-reference/jianying-pro/REGRESSION.md`
  </read_first>
  <action>Ensure right inspector scrolls within its panel, does not overlap preview/timeline at 1280x800 and 1120x720, and hides developer-only data by default. Keep compact rows, sliders, steppers, segmented controls, checkboxes, and keyframe buttons stable.</action>
  <acceptance_criteria>
    - Workspace layout tests show no inspector overlap at 1280x800 and 1120x720.
    - Default inspector has no raw debug copy from the forbidden list.
    - Long labels/values are clipped or ellipsized inside stable rows without changing panel width.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
  </verify>
  <done>Right inspector stays readable and scoped across supported viewport sizes.</done>
</task>

</tasks>

<verification>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/inspector-modal.spec.ts --reporter=line</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts --reporter=line</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>git diff --check -- . ':!reference'</automated>
</verification>

<success_criteria>
The inspector is contextual, modal where appropriate, and free of default
developer/internal details.
</success_criteria>

## Artifacts this phase produces

- Draft parameter modal.
- `tests/inspector-modal.spec.ts`.
- Context-specific inspector behavior assertions.

<output>
Create `15.3-05-SUMMARY.md` when done.
</output>
