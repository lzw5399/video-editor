---
phase: "06-mvp-hardening-and-packaging"
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
autonomous: true
requirements:
  - TEST-06
files_modified:
  - apps/desktop-electron/src/renderer/commandHelpers.ts
  - apps/desktop-electron/src/renderer/viewModel.ts
  - apps/desktop-electron/src/renderer/App.tsx
  - apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx
  - apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx
  - apps/desktop-electron/src/renderer/workspace/preview-inspector.css
  - apps/desktop-electron/tests/runtime-diagnostics.spec.ts
summary: "Display Rust-owned runtime diagnostics in the Chinese preview shell"
must_haves:
  truths:
    - "Runtime diagnostics are read-only summaries returned by Rust."
    - "Diagnostics live inside the existing preview/export shell, with no route, modal-first flow, or duplicate navigation."
    - "All user-visible copy is Simplified Chinese and uses Jianying-style product language."
  artifacts:
    - path: "apps/desktop-electron/tests/runtime-diagnostics.spec.ts"
      provides: "desktop viewport coverage for diagnostics UI"
  key_links:
    - from: "commandHelpers.ts"
      to: "probeRuntimeCapabilities"
      via: "generated command envelope helper"
    - from: "PreviewMonitor.tsx"
      to: "RuntimeCapabilityReport"
      via: "display-only diagnostics state"
---

<objective>
Display runtime diagnostics in the existing Chinese preview/export shell.

Purpose: show whether preview/export runtime capabilities are ready without turning runtime setup into a new page or renderer-owned media subsystem.
Output: command helper, read-only diagnostics display state, compact Jianying-style diagnostics panel, and Playwright coverage at the required desktop viewports.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-mvp-hardening-and-packaging/06-CONTEXT.md
@.planning/phases/06-mvp-hardening-and-packaging/06-UI-SPEC.md
@.planning/phases/06-mvp-hardening-and-packaging/06-RESEARCH.md
@apps/desktop-electron/src/renderer/commandHelpers.ts
@apps/desktop-electron/src/renderer/viewModel.ts
@apps/desktop-electron/src/renderer/App.tsx
@apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx
@apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx
@apps/desktop-electron/src/renderer/workspace/preview-inspector.css
</context>

## Artifacts this plan produces

New file paths:
- `apps/desktop-electron/tests/runtime-diagnostics.spec.ts`

Named commands/scripts:
- `@video-editor/desktop test:runtime-diagnostics`

Named symbols:
- `buildProbeRuntimeCapabilitiesCommand`
- `RuntimeDiagnosticsDisplayState`

<tasks>

<task id="06-03-01" type="auto">
  <name>1. Add runtime diagnostics state and generated command helper</name>
  <files>apps/desktop-electron/src/renderer/commandHelpers.ts, apps/desktop-electron/src/renderer/viewModel.ts, apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/commandHelpers.ts`
    - `apps/desktop-electron/src/renderer/viewModel.ts`
    - `apps/desktop-electron/src/renderer/App.tsx`
    - `apps/desktop-electron/src/renderer/workspace/WorkspaceShell.tsx`
  </read_first>
  <action>
    Add `buildProbeRuntimeCapabilitiesCommand` using the generated Rust-owned command envelope. Add read-only diagnostics state that stores loading/error/report display data returned by `window.videoEditorCore.executeCommand`. Do not inspect PATH, parse FFmpeg output, construct FFmpeg commands, infer render graph state, or run process APIs in renderer code. The state should support initial detection and manual re-detection.
  </action>
  <verify>
    <automated>pnpm run test:phase5-source-guards</automated>
    <automated>rg -n "buildProbeRuntimeCapabilitiesCommand|probeRuntimeCapabilities" apps/desktop-electron/src/renderer</automated>
  </verify>
  <acceptance_criteria>
    Renderer calls only the generated command bridge for runtime diagnostics and stores returned status as display state. Existing source guards still pass.
  </acceptance_criteria>
  <done>Renderer has command-only runtime diagnostics state and helper wiring.</done>
</task>

<task id="06-03-02" type="auto">
  <name>2. Render compact Chinese diagnostics inside preview monitor</name>
  <files>apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/src/renderer/workspace/preview-inspector.css, apps/desktop-electron/tests/runtime-diagnostics.spec.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx`
    - `apps/desktop-electron/src/renderer/workspace/preview-inspector.css`
    - `.planning/phases/06-mvp-hardening-and-packaging/06-UI-SPEC.md`
  </read_first>
  <action>
    Place diagnostics inside the existing `.preview-shell` without adding a route, modal-first flow, marketing page, or duplicate navigation. Use the locked Simplified Chinese copy from D-16 and the UI-SPEC: `运行环境诊断`, `运行环境状态`, `运行能力列表`, `打包状态`, `FFmpeg 状态`, `ffprobe 状态`, `编码能力`, `字幕能力`, `字体环境`, `检查运行环境`, `重新检测运行环境`, `运行环境就绪`, `部分能力不可用，可继续编辑，但预览或导出可能受限。`, and `运行环境检测失败，请检查 FFmpeg/ffprobe 路径后重试。`. Disable only affected preview/export actions when capabilities are missing, using `预览暂不可用` and `导出暂不可用` as disabled CTA state text or accessible labels. Preserve compact 1280x800 and 1120x720 proportions and dark scrollbars.
  </action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test:runtime-diagnostics</automated>
    <automated>pnpm run test:phase5-source-guards</automated>
    <automated>rg -n "运行环境诊断|运行环境状态|运行能力列表|重新检测运行环境" apps/desktop-electron/src/renderer apps/desktop-electron/tests/runtime-diagnostics.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    `test:runtime-diagnostics` passes at 1280x800 and 1120x720. The diagnostics panel is visible inside `预览窗口`, row accessible names match the UI-SPEC, valid runtime reports show `运行环境就绪`, invalid runtime paths show actionable Chinese copy, and no region overlaps or clipped scrollbars are introduced.
  </acceptance_criteria>
  <done>The existing preview/export shell displays Rust-owned runtime diagnostics with locked Chinese copy.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter @video-editor/desktop test:runtime-diagnostics` and `pnpm run test:phase5-source-guards`.
</verification>

<success_criteria>
- Runtime diagnostics are visible before preview/export actions.
- UI remains a compact Chinese Jianying-style workspace, not a new settings flow.
- Renderer displays Rust-owned report data without owning FFmpeg/runtime/render semantics.
- Required 1280x800 and 1120x720 diagnostics coverage passes.
</success_criteria>

<must_haves>
Truths:
- Diagnostics panel lives inside the preview monitor area.
- Diagnostics copy is Simplified Chinese.
- Preview/export CTAs can reflect runtime readiness without renderer-owned runtime logic.

Artifacts:
- `apps/desktop-electron/tests/runtime-diagnostics.spec.ts`
- updated preview monitor diagnostics UI

Key links:
- `PreviewMonitor.tsx` displays data returned by `probeRuntimeCapabilities`.
- `phase5-source-guards` remains green.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> preload command bridge | Renderer can only request diagnostics through generated command envelopes. |
| Rust report -> UI display | UI summarizes report fields but does not compute runtime availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | renderer diagnostics | mitigate | Diagnostics render Rust-owned report data; source guards reject renderer FFmpeg/process/render graph ownership. |
| T-06-08 | Information Disclosure | runtime path copy | mitigate | Display compact path/status summaries; avoid raw command lines and logs. |
| T-06-09 | Denial of Service | diagnostics UI | mitigate | Failed probes become stable Chinese error state and do not block editing. |
</threat_model>

## Source Coverage Audit

| Source Item | Coverage |
|-------------|----------|
| GOAL: User can understand runtime readiness | Covered by diagnostics panel. |
| REQ TEST-06 UI prerequisite | Covered by diagnostics test. |
| RESEARCH: UI diagnostics, Chinese copy, compact shell | Covered by Tasks 1 and 2. |
| CONTEXT D-08, D-09, D-12, D-16 | Implemented here. |
| Deferred ideas | No new settings page, route, bundled FFmpeg, or advanced editor semantics. |

<output>
Create `.planning/phases/06-mvp-hardening-and-packaging/06-03-SUMMARY.md` when done.
</output>
