---
phase: "06-mvp-hardening-and-packaging"
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
autonomous: true
requirements:
  - TEST-06
  - TEST-07
files_modified:
  - crates/media_runtime/src/capabilities.rs
  - crates/media_runtime/src/lib.rs
  - crates/media_runtime/tests/runtime_capability.rs
  - crates/draft_model/src/lib.rs
  - crates/draft_model/tests/schema_exports.rs
  - schemas/command.schema.json
  - apps/desktop-electron/src/generated/CommandEnvelope.ts
  - apps/desktop-electron/src/generated/CommandResultEnvelope.ts
  - crates/bindings_node/src/lib.rs
  - crates/bindings_node/src/runtime_capability_service.rs
  - crates/bindings_node/tests/runtime_capabilities.rs
summary: "Add Rust-owned runtime capability report and generated command route"
must_haves:
  truths:
    - "D-09: Product runtime exposes a Rust-owned capability report before preview/export actions are trusted."
    - "D-10: MVP reports external/user-provided FFmpeg and does not claim redistributable bundled runtime capability."
    - "D-12: Renderer and Electron do not probe PATH, parse FFmpeg output, or build FFmpeg commands."
  artifacts:
    - path: "crates/media_runtime/src/capabilities.rs"
      provides: "RuntimeCapabilityReport probing"
    - path: "crates/bindings_node/src/runtime_capability_service.rs"
      provides: "binding route for Rust-owned capability report"
    - path: "apps/desktop-electron/src/generated/CommandEnvelope.ts"
      provides: "generated TypeScript command contract"
  key_links:
    - from: "draft_model command contracts"
      to: "apps/desktop-electron/src/generated"
      via: "Rust-generated schema/TypeScript artifacts"
    - from: "bindings_node"
      to: "media_runtime::capabilities"
      via: "DesktopFfmpegExecutor"
---

<objective>
Add the Rust-owned runtime capability report and expose it through generated command contracts.

Purpose: make runtime readiness queryable before preview/export while preserving the existing boundary: Rust owns FFmpeg discovery/capability interpretation, Electron only routes the command, and renderer only displays returned data.
Output: `probeRuntimeCapabilities` command, generated schema/TypeScript artifacts, binding route, and direct Rust tests for configured/missing runtimes.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-mvp-hardening-and-packaging/06-CONTEXT.md
@.planning/phases/06-mvp-hardening-and-packaging/06-RESEARCH.md
@.planning/phases/06-mvp-hardening-and-packaging/06-VALIDATION.md
@crates/media_runtime/src/discovery.rs
@crates/media_runtime/src/lib.rs
@crates/testkit/src/render_compare.rs
@crates/draft_model/src/lib.rs
@crates/bindings_node/src/lib.rs
</context>

## Artifacts this plan produces

New file paths:
- `crates/media_runtime/src/capabilities.rs`
- `crates/media_runtime/tests/runtime_capability.rs`
- `crates/bindings_node/src/runtime_capability_service.rs`
- `crates/bindings_node/tests/runtime_capabilities.rs`

Named symbols:
- `RuntimeCapabilityReport`
- `RuntimeBinaryCapability`
- `RuntimeCapabilityStatus`
- `RuntimeLicensePosture`
- `probe_runtime_capabilities`
- Rust command contract `probeRuntimeCapabilities`

<tasks>

<task id="06-02-01" type="auto">
  <name>1. Add media runtime capability probing</name>
  <files>crates/media_runtime/src/capabilities.rs, crates/media_runtime/src/lib.rs, crates/media_runtime/tests/runtime_capability.rs</files>
  <read_first>
    - `crates/media_runtime/src/discovery.rs`
    - `crates/media_runtime/src/job.rs`
    - `crates/media_runtime/src/validate.rs`
    - `crates/testkit/src/render_compare.rs`
  </read_first>
  <action>
    Create `media_runtime::capabilities` with serializable `RuntimeCapabilityReport`, `RuntimeBinaryCapability`, `RuntimeCapabilityStatus`, and `RuntimeLicensePosture`. `probe_runtime_capabilities` must use `RuntimeConfig` plus `FfmpegExecutor` argument-array probes; it must not shell-concatenate commands. Cover discovered ffmpeg/ffprobe path and source, bounded version/configure summary, H.264 encoder availability, AAC encoder availability, ASS/subtitles filter availability, deterministic font readiness, and external-runtime/license posture. Treat env/PATH FFmpeg as `externalRuntime` and `redistributableBuild: false` per D-06 and D-10; do not download or bundle FFmpeg. Missing binaries must classify setup failure without panics.
  </action>
  <verify>
    <automated>cargo test -p media_runtime runtime_capability -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    Tests prove the report includes ffmpeg path, ffprobe path, version/configure summary, H.264, AAC, ASS, subtitles, font readiness, `externalRuntime`, and `redistributableBuild: false`. Invalid env paths return classified missing-runtime state instead of panic or skip.
  </acceptance_criteria>
  <done>`media_runtime` can produce a bounded runtime capability report from Rust-owned probing.</done>
</task>

<task id="06-02-02" type="auto">
  <name>2. Add generated command contract and binding route</name>
  <files>crates/draft_model/src/lib.rs, crates/draft_model/tests/schema_exports.rs, schemas/command.schema.json, apps/desktop-electron/src/generated/CommandEnvelope.ts, apps/desktop-electron/src/generated/CommandResultEnvelope.ts, crates/bindings_node/src/lib.rs, crates/bindings_node/src/runtime_capability_service.rs, crates/bindings_node/tests/runtime_capabilities.rs</files>
  <read_first>
    - `crates/draft_model/src/lib.rs`
    - `crates/draft_model/tests/schema_exports.rs`
    - `crates/bindings_node/src/lib.rs`
    - `crates/media_runtime/src/capabilities.rs`
  </read_first>
  <action>
    Add `probeRuntimeCapabilities` to the Rust-owned command contract and regenerate `schemas/command.schema.json` plus generated TypeScript artifacts. Route the command in `bindings_node` through a new runtime capability service using `DesktopFfmpegExecutor`. Missing binaries should return the existing classified command envelope error shape with actionable Chinese wording for `未找到 FFmpeg` or `未找到 ffprobe` per D-08 and D-16. Keep all runtime interpretation in Rust; the generated TypeScript is a transport/display contract only.
  </action>
  <verify>
    <automated>cargo test -p bindings_node runtime_capabilities -- --nocapture</automated>
    <automated>VE_UPDATE_GENERATED_CONTRACTS=1 cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture</automated>
    <automated>git diff --exit-code schemas apps/desktop-electron/src/generated</automated>
  </verify>
  <acceptance_criteria>
    Generated TypeScript includes `probeRuntimeCapabilities`, `ProbeRuntimeCapabilitiesCommandPayload`, and `RuntimeCapabilityReport`. Binding tests prove configured and missing runtime paths are returned through standardized envelopes.
  </acceptance_criteria>
  <done>`probeRuntimeCapabilities` is available through Rust contracts, generated TS/schema, and the Node binding route.</done>
</task>

</tasks>

<verification>
Run `cargo test -p media_runtime runtime_capability -- --nocapture`, `cargo test -p bindings_node runtime_capabilities -- --nocapture`, `VE_UPDATE_GENERATED_CONTRACTS=1 cargo test -p draft_model schema_exports_generated_contract_artifacts_from_rust -- --nocapture`, and `git diff --exit-code schemas apps/desktop-electron/src/generated`.
</verification>

<success_criteria>
- Runtime readiness is represented by a Rust-owned capability report.
- Electron/renderer do not probe FFmpeg, inspect PATH, parse configure lines, or own runtime fallback decisions.
- Generated contracts expose the capability command for later UI and E2E plans.
- Missing runtime paths produce classified, actionable errors rather than crashes.
</success_criteria>

<must_haves>
Truths:
- User-visible preview/export readiness can be driven by Rust-owned `RuntimeCapabilityReport`.
- MVP runtime posture remains external/user-provided FFmpeg only.
- Generated command artifacts remain the single TypeScript contract source.

Artifacts:
- `crates/media_runtime/src/capabilities.rs`
- `crates/bindings_node/src/runtime_capability_service.rs`
- updated generated command schema/TypeScript

Key links:
- `probeRuntimeCapabilities` is generated from Rust into TypeScript.
- Binding route delegates to `media_runtime`, not renderer logic.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| binding -> media_runtime | Binding invokes Rust-owned discovery/capability probes. |
| generated TS -> renderer | Renderer receives typed report data but does not compute runtime readiness. |
| runtime -> external FFmpeg | FFmpeg remains user-provided through env/PATH. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | runtime report | mitigate | Generate report in Rust and expose it through command contracts; source guards later reject renderer-owned FFmpeg/process logic. |
| T-06-05 | Information Disclosure | runtime paths/configure summary | mitigate | Bound displayed/provided summaries; avoid raw process logs as the report body. |
| T-06-06 | Denial of Service | runtime probes | mitigate | Reuse bounded FFmpeg executor behavior and classified setup failures. |
| T-06-SC | Tampering | generated contracts | mitigate | Regenerate contracts from Rust and verify no uncommitted schema/TS drift. |
</threat_model>

## Source Coverage Audit

| Source Item | Coverage |
|-------------|----------|
| GOAL: Verify runtime is available before preview/export | Covered by capability report and binding route. |
| REQ TEST-06 / TEST-07 prerequisite | Provides command used by later diagnostics and no-mock workflows. |
| RESEARCH: runtime capability report, external runtime posture | Covered by Tasks 1 and 2. |
| CONTEXT D-06, D-08, D-09, D-10, D-12, D-16 | Implemented here. |
| Deferred ideas | No bundled FFmpeg, signing, notarization, adapters, mobile/server, or advanced editor semantics. |

<output>
Create `.planning/phases/06-mvp-hardening-and-packaging/06-02-SUMMARY.md` when done.
</output>
