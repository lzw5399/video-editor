---
phase: 17.1-interaction-session-and-template-import-main-chain-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bindings_node/src/project_session_service.rs
  - crates/bindings_node/tests/project_session.rs
  - apps/desktop-electron/src/main/nativeBinding.ts
  - apps/desktop-electron/src/renderer/App.tsx
  - apps/desktop-electron/tests/template-import.spec.ts
autonomous: true
requirements:
  - UI-04
  - UI-12
  - COMP-01
  - COMP-02
  - NO-FALLBACK-01
  - NO-FALLBACK-02
requirements_addressed:
  - UI-04
  - UI-12
  - COMP-01
  - COMP-02
  - NO-FALLBACK-01
  - NO-FALLBACK-02
must_haves:
  truths:
    - "ProjectSessionTemplateImportResponse includes events and CommandDelta like other project-session timeline mutation responses."
    - "Renderer preview invalidation is driven by Rust CommandDelta/InvalidationScope rather than a hardcoded intent-kind switch."
    - "Template import remains adapter-owned input plus canonical draft mutation; UI does not construct draft semantics or report classifications."
    - "Decision coverage: D-01 D-02 D-23 D-24 D-25."
  prohibitions:
    - statement: "Do not add new TypeScript intent-name invalidation cases for template import or future interaction commands."
      status: "resolved"
      verification: "source guard rejects previewAffectingIntentNeedsRefresh-style switches for new paths"
---

<objective>
Fix the current main-chain response and invalidation contract before adding high-frequency interaction sessions.
</objective>

<execution_context>
@/Users/zhiwen/.codex/gsd-core/workflows/execute-plan.md
@/Users/zhiwen/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-interaction-session-and-template-import-main-chain-hardening/17.1-CONTEXT.md
@.planning/phases/17.1-interaction-session-and-template-import-main-chain-hardening/17.1-RESEARCH.md
@crates/bindings_node/src/project_session_service.rs
@apps/desktop-electron/src/main/nativeBinding.ts
@apps/desktop-electron/src/renderer/App.tsx
@crates/draft_model/src/delta.rs
</context>

## Artifacts This Phase Produces

- `ProjectSessionTemplateImportResponse.events`
- `ProjectSessionTemplateImportResponse.delta`
- `applyProjectSessionCommandDelta`
- `previewRefreshTargetFromDelta`
- `scripts` or tests guarding against new hardcoded preview invalidation switches

## Decision Coverage

- D-01: Keep template import/Kaipai adapter behavior separate from provider-neutral command/delta contracts.
- D-02: Replace the inconsistent response/invalidation chain instead of patching around it.
- D-23: Add `events` and `CommandDelta` to `ProjectSessionTemplateImportResponse`.
- D-24: Replace hardcoded TypeScript preview invalidation with Rust delta consumption.
- D-25: Keep template report data product-facing and free of raw provider payloads.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add binding test for template import response shape</name>
  <files>crates/bindings_node/tests/project_session.rs, apps/desktop-electron/tests/template-import.spec.ts</files>
  <read_first>
    - `crates/bindings_node/tests/project_session.rs`
    - `apps/desktop-electron/tests/template-import.spec.ts`
    - `crates/bindings_node/src/project_session_service.rs`
  </read_first>
  <action>Add failing assertions that template import returns `events` as an array and `delta` as a `CommandDelta` with a command name, changed domains, changed ranges, and invalidation scope. Extend the desktop template-import test to rely on returned delta-driven preview refresh rather than a template-specific UI refresh branch.</action>
  <verify>
    <automated>cargo test -p bindings_node project_session_template --locked</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/template-import.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Binding test fails when `ProjectSessionTemplateImportResponse` omits `events`.
    - Binding test fails when `ProjectSessionTemplateImportResponse` omits `delta`.
    - Desktop test proves imported template preview refresh is not a DOM-only or artifact-only success.
  </acceptance_criteria>
  <done>Template import response shape is protected before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add events and delta to template import response</name>
  <files>crates/bindings_node/src/project_session_service.rs, apps/desktop-electron/src/main/nativeBinding.ts</files>
  <read_first>
    - `crates/bindings_node/src/project_session_service.rs` response structs and import apply path.
    - `apps/desktop-electron/src/main/nativeBinding.ts` `ProjectSessionTimelineIntentResponse` and `ProjectSessionTemplateImportResponse` types.
    - `crates/draft_commands/src/delta.rs` import/material/full-draft delta patterns.
  </read_first>
  <action>Update Rust template import response to include `events: Vec<draft_model::CommandEvent>` and `delta: draft_model::CommandDelta`. Use a Rust-owned delta that represents the imported draft changes and marks affected preview/export/artifact consumers. Update TypeScript binding types so `ProjectSessionTemplateImportResponse` is structurally consistent with timeline mutation responses plus `adaptationReport`.</action>
  <verify>
    <automated>cargo test -p bindings_node project_session_template --locked</automated>
    <automated>pnpm run test:contracts</automated>
  </verify>
  <acceptance_criteria>
    - Serialized template import JSON contains `events` and `delta`.
    - TypeScript `ProjectSessionTemplateImportResponse` does not lie about fields Rust omits.
    - No renderer code constructs adaptation report statuses or draft semantics.
  </acceptance_criteria>
  <done>Template import participates in the same mutation response contract as other project-session commands.</done>
</task>

<task type="auto">
  <name>Task 3: Replace hardcoded preview invalidation switch with delta consumer</name>
  <files>apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/src/main/nativeBinding.ts</files>
  <read_first>
    - `apps/desktop-electron/src/renderer/App.tsx` `executeProjectTimelineIntent`, `previewAffectingIntentNeedsRefresh`, and `previewRefreshTarget`.
    - `crates/draft_model/src/delta.rs` `DirtyDomain` and `InvalidationScope`.
    - `crates/preview_service/src/cache.rs` delta-driven cache invalidation.
  </read_first>
  <action>Add a renderer helper that consumes `CommandDelta` and decides preview refresh from `changedDomains`, `changedRanges`, and `invalidation.consumerDomains`. Remove the template/timeline-specific hardcoded intent-name switch from normal product refresh. Keep selection-only deltas from doing expensive draft snapshot refresh unless Rust marks a preview consumer.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop test -- --runInBand</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/template-import.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `previewAffectingIntentNeedsRefresh` is removed or reduced to a tested compatibility shim with no new command names.
    - Preview refresh runs when delta includes preview/realtime consumer domains.
    - Selection-only deltas do not force full preview snapshot refresh unless Rust explicitly asks.
  </acceptance_criteria>
  <done>Renderer preview invalidation follows Rust delta facts.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-17.1-01 | Tampering | Renderer invalidation | mitigate | Renderer consumes Rust delta and cannot invent semantic invalidation by command name. |
| T-17.1-02 | Information Disclosure | Template report | mitigate | Response keeps report structured and product-facing; no raw formula/provider URL exposure. |
| T-17.1-03 | Repudiation | Import success | mitigate | Tests require response delta plus product preview/export evidence. |
</threat_model>

<verification>
Run:
- `cargo test -p bindings_node project_session_template --locked`
- `pnpm run test:contracts`
- `pnpm --filter @video-editor/desktop exec playwright test tests/template-import.spec.ts --reporter=line`
</verification>

<success_criteria>
Plan 01 is complete when template import responses carry events/delta, renderer preview refresh uses Rust delta facts, and tests fail the old inconsistent response/hardcoded invalidation behavior.
</success_criteria>

<output>
Create `.planning/phases/17.1-interaction-session-and-template-import-main-chain-hardening/17.1-01-SUMMARY.md` when done.
</output>
