---
phase: "15.1-p0-basic-editing-chain-repair"
plan: "05"
type: execute
wave: 3
depends_on:
  - "15.1-01"
files_modified:
  - "crates/draft_model/src/timeline.rs"
  - "crates/draft_commands/src/timeline.rs"
  - "crates/draft_commands/src/delta.rs"
  - "crates/draft_commands/tests/track_commands.rs"
  - "schemas/command.schema.json"
  - "schemas/draft.schema.json"
  - "apps/desktop-electron/src/generated"
  - "apps/desktop-electron/src/renderer/commandHelpers.ts"
  - "apps/desktop-electron/src/renderer/workspace/Timeline.tsx"
  - "apps/desktop-electron/src/renderer/viewModel.ts"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - P0-EDIT-05
user_setup: []
---

<objective>
Add explicit Rust-owned multitrack editing commands and minimal desktop controls for add, target, rename, lock, show/hide, and mute.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.1-05-01: Add multitrack command tests</name>
  <files>crates/draft_commands/tests/track_commands.rs</files>
  <action>Add RED tests for add video/audio/text track, rename track, lock/unlock track, show/hide visual track, mute/unmute audio track, target add segment to selected track, deterministic visual stack order, deterministic audio mix order, and undo/redo behavior.</action>
  <verify>
    <automated>cargo test -p draft_commands track_commands -- --nocapture</automated>
  </verify>
  <done>Tests define multitrack behavior and fail until commands exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-05-02: Implement model, command, delta, and generated contracts</name>
  <files>crates/draft_model/src/timeline.rs, crates/draft_commands/src/timeline.rs, crates/draft_commands/src/delta.rs, crates/draft_model/tests/schema_exports.rs, schemas, apps/desktop-electron/src/generated</files>
  <action>Add backward-compatible track visibility if required. Implement `addTrack`, `renameTrack`, `setTrackLock`, `setTrackVisibility`, and reuse/extend `setTrackMute`. Validate kind/name/state in Rust, emit track deltas, preserve undo/redo, and regenerate command/schema/TypeScript contracts.</action>
  <verify>
    <automated>cargo test -p draft_commands track_commands -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
  </verify>
  <done>Multitrack operations are Rust-owned commands with generated desktop contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-05-03: Wire minimal timeline UI controls through commands</name>
  <files>apps/desktop-electron/src/renderer/commandHelpers.ts, apps/desktop-electron/src/renderer/workspace/Timeline.tsx, apps/desktop-electron/src/renderer/viewModel.ts, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Enable add/target/rename/lock/show/mute track controls through command envelopes. Do not mutate tracks in renderer. Keep broad Jianying polish and drag/edge-trim refinements for Phase 15.2 unless required for this acceptance gate.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "multitrack"</automated>
  </verify>
  <done>Users can manage tracks through command-backed controls.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p draft_commands track_commands -- --nocapture</automated>
<automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "multitrack"</automated>
</verification>

<success_criteria>
Video/audio/text tracks can be created, targeted, renamed, locked, shown/hidden, and muted through Rust-owned commands, with deterministic stacking and mixing.
</success_criteria>

<output>
Create `.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-05-SUMMARY.md` when done.
</output>
