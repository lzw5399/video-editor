---
phase: "15.1-p0-basic-editing-chain-repair"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "crates/realtime_preview_runtime/src/session.rs"
  - "crates/realtime_preview_runtime/src/lib.rs"
  - "crates/realtime_preview_runtime/tests/clock_generation.rs"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "crates/bindings_node/src/lib.rs"
  - "crates/bindings_node/tests/preview_commands.rs"
  - "apps/desktop-electron/src/main/nativeBinding.ts"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "apps/desktop-electron/src/renderer/workspace/Timeline.tsx"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - P0-EDIT-01
  - P0-EDIT-02
user_setup: []
must_haves:
  truths:
    - "Play, pause, stop, seek, and scrub use realtime preview runtime session state and PlaybackGeneration."
    - "Normal playback does not continuously call requestPreviewFrame."
    - "requestPreviewFrame remains available only for paused still-frame refresh and developer diagnostics."
    - "Renderer does not own TimelineClock, PlaybackGeneration, render graph, FFmpeg, or preview cache semantics."
---

<objective>
Repair the default playback chain so the visible play controls drive the formal realtime preview runtime instead of a React-owned still-frame loop.
</objective>

<context>
@AGENTS.md
@.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-CONTEXT.md
@.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-RESEARCH.md
@.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-PATTERNS.md
@crates/realtime_preview_runtime/src/session.rs
@crates/realtime_preview_runtime/src/clock.rs
@crates/bindings_node/src/realtime_preview_service.rs
@apps/desktop-electron/src/main/realtimePreviewHost.ts
@apps/desktop-electron/src/renderer/App.tsx
@apps/desktop-electron/tests/workspace.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.1-01-01: Add playback-control runtime and binding tests</name>
  <files>crates/realtime_preview_runtime/tests/clock_generation.rs, crates/bindings_node/tests/preview_commands.rs, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Add RED tests proving realtime sessions expose play, pause, stop, and seek with generation changes. Add an Electron workspace regression that clicks play after a segment is on the timeline and asserts no repeated `requestPreviewFrame` commands are recorded while realtime preview host playback calls are recorded.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime clock_generation -- --nocapture</automated>
    <automated>cargo test -p bindings_node preview_commands -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "realtime playback"</automated>
  </verify>
  <done>Tests fail for the current UI-owned playback loop and missing binding controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-01-02: Expose play, pause, stop, seek, and draft snapshot through realtime runtime bindings</name>
  <files>crates/realtime_preview_runtime/src/session.rs, crates/realtime_preview_runtime/src/lib.rs, crates/bindings_node/src/realtime_preview_service.rs, crates/bindings_node/src/lib.rs, apps/desktop-electron/src/main/nativeBinding.ts</files>
  <action>Add public runtime methods for play/resume/stop as needed, keep seek existing, and expose Node-API functions such as `playRealtimePreview`, `pauseRealtimePreview`, `stopRealtimePreview`, `seekRealtimePreview`, and `updateRealtimePreviewDraftSnapshot`. Return typed playback generation/state responses. Do not introduce FFmpeg, render graph construction, cache keys, or Electron renderer types.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime clock_generation -- --nocapture</automated>
    <automated>cargo test -p bindings_node preview_commands -- --nocapture</automated>
  </verify>
  <done>Binding callers can control a realtime preview session and observe the updated PlaybackGeneration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-01-03: Wire desktop play controls to realtime preview host</name>
  <files>apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/src/preload/index.ts, apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, apps/desktop-electron/src/renderer/workspace/Timeline.tsx, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Add a narrow `videoEditorRealtimePreviewHost` API for draft snapshot, play, pause, stop, and seek/scrub. Update renderer play controls to call that API and only use `requestPreviewFrame` for paused still frame requests or developer actions. Play should be enabled when a supported timeline segment exists and realtime host setup is available; productized errors should appear when runtime setup fails.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop build</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "realtime playback"</automated>
  </verify>
  <done>The user can click play after adding media, and playback no longer behaves as repeated PNG preview-frame requests.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime clock_generation -- --nocapture</automated>
<automated>cargo test -p bindings_node preview_commands -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop build</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "realtime playback"</automated>
</verification>

<success_criteria>
The current user symptom is fixed at the correct layer: first frame can appear, play is actionable for supported timelines, and normal playback does not continuously request preview-frame artifacts.
</success_criteria>

<output>
Create `.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-01-SUMMARY.md` when done.
</output>
