---
phase: "15.1-p0-basic-editing-chain-repair"
plan: "02"
type: execute
wave: 2
depends_on:
  - "15.1-01"
files_modified:
  - "crates/realtime_preview_runtime/src/capabilities.rs"
  - "crates/realtime_preview_runtime/src/diagnostics.rs"
  - "crates/realtime_preview_runtime/src/parity.rs"
  - "crates/realtime_preview_runtime/tests/capability_matrix.rs"
  - "crates/testkit/tests/realtime_preview_parity.rs"
  - "crates/audio_engine/tests/dsp_timeline.rs"
  - "apps/desktop-electron/src/renderer/viewModel.ts"
  - "apps/desktop-electron/src/renderer/App.tsx"
  - "apps/desktop-electron/tests/workspace.spec.ts"
autonomous: true
requirements:
  - P0-EDIT-02
user_setup: []
---

<objective>
Make baseline video/image/text/audio preview capability explicit so supported playback does not silently pass through FFmpeg artifact fallback or text/audio divergence.
</objective>

<context>
@.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-CONTEXT.md
@.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-RESEARCH.md
@crates/realtime_preview_runtime/src/capabilities.rs
@crates/realtime_preview_runtime/src/parity.rs
@crates/audio_engine/tests/dsp_timeline.rs
@apps/desktop-electron/src/renderer/viewModel.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.1-02-01: Add baseline preview capability tests</name>
  <files>crates/realtime_preview_runtime/tests/capability_matrix.rs, crates/testkit/tests/realtime_preview_parity.rs</files>
  <action>Add tests for a baseline timeline containing ordinary video/image, baseline text, and audio. Assert supported/degraded/unsupported outcomes are explicit and that FFmpeg artifact fallback is not reported as normal supported realtime playback.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime capability_matrix -- --nocapture</automated>
    <automated>cargo test -p testkit realtime_preview_parity -- --nocapture</automated>
  </verify>
  <done>Baseline support classification fails until capability decisions are tightened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-02-02: Tighten baseline capability and parity diagnostics</name>
  <files>crates/realtime_preview_runtime/src/capabilities.rs, crates/realtime_preview_runtime/src/diagnostics.rs, crates/realtime_preview_runtime/src/parity.rs</files>
  <action>Classify baseline video/image layers, baseline text, and audio sync readiness separately from artifact fallback. Keep advanced filters/effects/transitions/masks deferred, but make baseline unsupported states visible and serializable.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime capability_matrix -- --nocapture</automated>
    <automated>cargo test -p realtime_preview_runtime parity_diagnostics -- --nocapture</automated>
  </verify>
  <done>Runtime diagnostics can tell product UI when playback is truly supported, degraded, or blocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-02-03: Productize unsupported baseline playback states in desktop UI</name>
  <files>apps/desktop-electron/src/renderer/viewModel.ts, apps/desktop-electron/src/renderer/App.tsx, apps/desktop-electron/tests/workspace.spec.ts</files>
  <action>Replace debug/fallback success copy with product-level blocked/degraded playback messages. The default UI must not imply that FFmpeg preview artifacts are normal realtime playback for supported timelines.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "baseline preview capability"</automated>
  </verify>
  <done>Users see a clear reason when baseline playback is unavailable instead of a fake success state.</done>
</task>

</tasks>

<verification>
<automated>cargo test -p realtime_preview_runtime capability_matrix -- --nocapture</automated>
<automated>cargo test -p realtime_preview_runtime parity_diagnostics -- --nocapture</automated>
<automated>cargo test -p testkit realtime_preview_parity -- --nocapture</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "baseline preview capability"</automated>
</verification>

<success_criteria>
Baseline video/image/text/audio preview has an explicit support decision and UI failure mode; artifact fallback is no longer treated as production realtime playback success.
</success_criteria>

<output>
Create `.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-02-SUMMARY.md` when done.
</output>
