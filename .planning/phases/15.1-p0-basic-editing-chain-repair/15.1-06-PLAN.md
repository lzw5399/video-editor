---
phase: "15.1-p0-basic-editing-chain-repair"
plan: "06"
type: execute
wave: 4
depends_on:
  - "15.1-01"
  - "15.1-02"
  - "15.1-03"
  - "15.1-04"
  - "15.1-05"
files_modified:
  - "apps/desktop-electron/tests/real-workflow.spec.ts"
  - "apps/desktop-electron/tests/helpers/realWorkflow.ts"
  - "apps/desktop-electron/tests/product-user-journey.spec.ts"
  - "apps/desktop-electron/tests/helpers/userJourney.ts"
  - "apps/desktop-electron/src/main/realtimePreviewHost.ts"
  - "apps/desktop-electron/src/main/nativeBinding.ts"
  - "apps/desktop-electron/src/preload/index.ts"
  - "apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx"
  - "crates/bindings_node/src/realtime_preview_service.rs"
  - "crates/realtime_preview_runtime/src/**/*.rs"
  - "apps/desktop-electron/tests/workspace.spec.ts"
  - "scripts/phase15-1-source-guards.sh"
  - ".planning/phases/15.1-p0-basic-editing-chain-repair/15.1-VERIFICATION.md"
autonomous: true
requirements:
  - P0-EDIT-01
  - P0-EDIT-02
  - P0-EDIT-03
  - P0-EDIT-04
  - P0-EDIT-05
  - P0-EDIT-06
user_setup: []
---

<objective>
Close Phase 15.1 with a real user-chain gate that proves import, edit, realtime GPU/composited play, text, audio, trim, split, undo/redo, save, reopen, and export work together through production paths.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15.1-06-01: Add full no-mock GPU/composited user-chain E2E</name>
  <files>apps/desktop-electron/tests/product-user-journey.spec.ts, apps/desktop-electron/tests/helpers/userJourney.ts, apps/desktop-electron/tests/real-workflow.spec.ts, apps/desktop-electron/tests/helpers/realWorkflow.ts</files>
  <action>Extend or add a no-mock workflow using repo-owned moving video and audio fixtures. Assert vertical canvas adoption, realtime play control calls, no repeated `requestPreviewFrame` during playback, preview backend `gpu`, content evidence source `composited`, changing composited preview fingerprints/pixels during playback, bundled-font text, music audio, trim, split, undo/redo, save, reopen, export, and output validation. Decoded CPU frames, FFmpeg PNG artifacts, mock/offscreen textures, request-preview snapshots, and playhead movement alone must not satisfy the gate.</action>
  <verify>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/real-workflow.spec.ts</automated>
  </verify>
  <done>The end-to-end flow fails until a true GPU/composited preview path is connected and observable in the desktop UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-06-02: Connect production GPU/composited realtime preview</name>
  <files>apps/desktop-electron/src/main/realtimePreviewHost.ts, apps/desktop-electron/src/main/nativeBinding.ts, apps/desktop-electron/src/preload/index.ts, apps/desktop-electron/src/renderer/workspace/PreviewMonitor.tsx, crates/bindings_node/src/realtime_preview_service.rs, crates/realtime_preview_runtime/src/**/*.rs</files>
  <action>Wire the realtime preview host so imported video playback presents through the production GPU/composited preview pipeline instead of request-preview snapshots, FFmpeg artifacts, mock/offscreen fallback, or decoded CPU evidence. The renderer may request play/pause/seek and display compositor evidence, but must not own render graph construction, FFmpeg commands, GPU commands, cache keys, or timeline semantics. If the production GPU/composited path is unavailable, fail closed with a visible unsupported state rather than silently substituting a fallback.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
  </verify>
  <done>Dragging a video onto the timeline and pressing play changes the visible preview content through GPU/composited output evidence while the playhead advances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-06-03: Add source guards for P0 editing boundaries and no-fallback preview evidence</name>
  <files>scripts/phase15-1-source-guards.sh, package.json</files>
  <action>Add guards asserting renderer does not continuously call `requestPreviewFrame` from playback, does not construct render graphs/FFmpeg/cache keys/GPU commands, does not mutate draft tracks directly for multitrack operations, default text uses a bundled fontRef path, and product tests cannot accept mock/offscreen/artifact/decoded CPU evidence as realtime preview success.</action>
  <verify>
    <automated>bash scripts/phase15-1-source-guards.sh</automated>
    <automated>pnpm run test:no-product-fallback</automated>
  </verify>
  <done>Source guards catch the known failure modes directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15.1-06-04: Verify and close Phase 15.1</name>
  <files>.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>Run all Phase 15.1 verification commands, record results in `15.1-VERIFICATION.md`, update ROADMAP plan checkboxes and STATE only after gates pass.</action>
  <verify>
    <automated>cargo test -p realtime_preview_runtime -- --nocapture</automated>
    <automated>cargo test -p bindings_node realtime_preview -- --nocapture</automated>
    <automated>cargo test -p draft_commands -- --nocapture</automated>
    <automated>cargo test -p draft_model schema_exports -- --nocapture</automated>
    <automated>cargo test -p ffmpeg_compiler text -- --nocapture</automated>
    <automated>cargo test -p testkit realtime_preview_parity -- --nocapture</automated>
    <automated>pnpm --filter @video-editor/desktop build</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
    <automated>pnpm --filter @video-editor/desktop exec playwright test tests/real-workflow.spec.ts</automated>
    <automated>bash scripts/phase15-1-source-guards.sh</automated>
    <automated>pnpm run test:no-product-fallback</automated>
    <automated>cargo check --workspace --locked</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Phase 15.1 is verified and Phase 15.2 can start.</done>
</task>

</tasks>

<verification>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line</automated>
<automated>pnpm --filter @video-editor/desktop exec playwright test tests/real-workflow.spec.ts</automated>
<automated>bash scripts/phase15-1-source-guards.sh</automated>
<automated>pnpm run test:no-product-fallback</automated>
<automated>cargo check --workspace --locked</automated>
<automated>git diff --check</automated>
</verification>

<success_criteria>
The full user chain works through production paths, and there is executable proof that the Phase 15.1 P0 regressions are closed before scheduler work begins. Realtime preview success requires visible GPU/composited content changes during playback; mock, offscreen, FFmpeg artifact, decoded CPU, or request-preview snapshot evidence is explicitly insufficient.
</success_criteria>

<output>
Create `.planning/phases/15.1-p0-basic-editing-chain-repair/15.1-06-SUMMARY.md` and `15.1-VERIFICATION.md` when done.
</output>
