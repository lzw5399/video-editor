# Phase 15.1 Verification

Date: 2026-06-20

## Result

Phase 15.1 is verified. The full P0 editing chain passes focused Rust gates, desktop build/package gates, no-mock product E2E, no-mock dev and packaged real workflow, workspace regression, source guards, and no-product-fallback guards.

## Commands

| Gate | Result |
|------|--------|
| `cargo test -p realtime_preview_runtime -- --nocapture` | PASS |
| `cargo test -p bindings_node realtime_preview -- --nocapture` | PASS |
| `cargo test -p draft_commands -- --nocapture` | PASS |
| `cargo test -p draft_model schema_exports -- --nocapture` | PASS |
| `cargo test -p ffmpeg_compiler text -- --nocapture` | PASS |
| `cargo test -p testkit realtime_preview_parity -- --nocapture` | PASS |
| `pnpm --filter @video-editor/desktop build` | PASS |
| `pnpm --filter @video-editor/desktop run package:dir` | PASS |
| `pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts` | PASS, 48 tests |
| `pnpm --filter @video-editor/desktop exec playwright test tests/product-user-journey.spec.ts --reporter=line` | PASS, 1 test |
| `pnpm --filter @video-editor/desktop exec playwright test tests/real-workflow.spec.ts` | PASS, 2 tests |
| `bash scripts/phase15-1-source-guards.sh` | PASS |
| `pnpm run test:no-product-fallback` | PASS |
| `cargo check --workspace --locked` | PASS |
| `git diff --check -- . ':!reference'` | PASS |

## Product Evidence

- Product playback E2E imports a repo-owned moving video, adds it to the timeline, starts playback, and verifies:
  - playhead advances;
  - `requestPreviewFrame` is not used as the playback loop;
  - realtime backend reports `gpu`;
  - content evidence source is `composited`;
  - composited content digest changes while playback advances;
  - visible preview-region pixels change.
- Real workflow E2E runs both dev and packaged apps with no mock preview/export/runtime capability commands and verifies import, preview frame, preview segment, export, output dimensions, and audio presence.

## No-Fallback Evidence

- Product success cannot be satisfied by mock output, preview PNG loops, preview artifacts, FFmpeg artifacts, decoded CPU fingerprints, offscreen readback, or synthetic DOM/frame-token evidence.
- Unsupported production presenter paths fail closed with explicit unavailable diagnostics.
- The no-product-fallback guard is now part of the Phase 15.1 verification result.

## Remaining Scope

The verified production path is the baseline single-video native/composited playback path. Complex multi-track/effects/text/render-graph GPU composition remains later production compositor scope and must not be represented as complete until it has equivalent product E2E evidence.
