---
status: planned
task: Phase 17 template import UI audit follow-up
created_at: 2026-06-24
phase: 17-template-import-core-and-kaipai-offline-adapter-foundation
mode: inline-primary-checkout
scope:
  - apps/desktop-electron/src/renderer/App.tsx
  - apps/desktop-electron/src/renderer/viewModel.ts
  - apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx
  - apps/desktop-electron/src/renderer/styles.css
  - apps/desktop-electron/tests/template-import.spec.ts
  - .planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-UI-REVIEW.md
verification:
  - pnpm --filter @video-editor/desktop run build
  - pnpm --filter @video-editor/desktop package:dir
  - pnpm --filter @video-editor/desktop exec playwright test tests/template-import.spec.ts --reporter=line
  - pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "Chinese editor workspace opens|workspace panels switch" --reporter=line
  - pnpm run test:phase17-source-guards
  - pnpm run test:phase17
---

# Phase 17 Template Import UI Audit Follow-up

Execute inline in the primary checkout because UI audit fixes are already uncommitted. Keep the work limited to the Phase 17 offline template import panel, its desktop Electron tests, and UI review evidence. Do not touch Rust crates, schemas, generated bindings, render graph, preview, export, or provider adapter semantics.

## Tasks

1. Complete the template import panel UI/test follow-up.
   - Files: `apps/desktop-electron/src/renderer/App.tsx`, `apps/desktop-electron/src/renderer/viewModel.ts`, `apps/desktop-electron/src/renderer/workspace/FeaturePanel.tsx`, `apps/desktop-electron/src/renderer/styles.css`, `apps/desktop-electron/tests/template-import.spec.ts`.
   - Replace remaining old visible copy/selectors for "智能包装" with "模板导入".
   - Ensure adaptation report rows are not silently capped or ellipsized as the only access path; render all rows in a bounded scroll area and sort by one explicit severity order before lower-severity supported/approximated items.
   - Give `dropped`, `missingResource`, and `needsNativeEffect` distinct row/chip treatments and product-safe Chinese copy that explains the user-visible adaptation outcome without leaking provider-specific rendering semantics.
   - Extend tests to assert the new selector/copy, complete row visibility/count, severity order, distinct status treatments, and provider-neutral report copy.

2. Append evidence and run verification.
   - Files: `.planning/phases/17-template-import-core-and-kaipai-offline-adapter-foundation/17-UI-REVIEW.md`.
   - Add a concise "Follow-up Fix Evidence" section with changed files, screenshots if captured, and exact verification results.
   - Run:
     - `pnpm --filter @video-editor/desktop run build`
     - `pnpm --filter @video-editor/desktop package:dir`
     - `pnpm --filter @video-editor/desktop exec playwright test tests/template-import.spec.ts --reporter=line`
     - `pnpm --filter @video-editor/desktop exec playwright test tests/workspace.spec.ts -g "Chinese editor workspace opens|workspace panels switch" --reporter=line`
     - `pnpm run test:phase17-source-guards`
     - `pnpm run test:phase17`

## Done

- Product UI and tests use "模板导入" for the template import entry.
- Report rows expose every adaptation item, remain readable, and are severity-sorted.
- `dropped`, `missingResource`, and `needsNativeEffect` are visually distinct without encoding Kaipai/provider-specific rendering rules in the UI.
- `17-UI-REVIEW.md` records the fix evidence and verification outcomes.
